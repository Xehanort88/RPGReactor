# Changelog

## [Unreleased]

- **A skill or item can hit a random number of times.** Repeats has always been one fixed number. It now has a **Max Repeats** box directly under it: set 2 and 4 and each use rolls 2, 3 or 4 hits, each equally likely. Leave the two equal and the count is fixed, which is what every skill and item authored before now keeps. The maximum takes a row of its own rather than squeezing in beside Repeats, because the form's value columns are too narrow to hold two stepper boxes in one cell.

  The range is stored as an optional `repeatsMax`, written only while it is above `repeats` and deleted as soon as it is not, so a fixed count saves exactly as it did before and a plugin that reads `repeats` directly sees the low end rather than nothing. Raising Repeats to or past the maximum collapses the range rather than leaving a maximum below the minimum on disk.

  The roll lives in the runtime, in a new `Game_Action.prototype.itemRepeats` underneath `numRepeats`, so everything that wraps `numRepeats` -- Attack Times+, a boost multiplier, a plugin's bonus hits -- works on the rolled count rather than replacing it. It is rolled once per action and kept on the action, keyed by the skill or item: `numRepeats` is read several times for one action (laying out its targets, AI scoring, plugins mid-action), and a fresh roll on every read would let those readings disagree. Using an item from the menu read `numRepeats` in its loop condition, once per pass; it now reads it once.

  A notetag that rewrites `repeats` when the database loads, such as VisuStella BattleCore's `<Repeat Hits: x>`, moves only the low end: below the maximum the range survives, at or above it the count is fixed. The Max Repeats label and its hint are a machine-assisted first pass across all 17 locales and want a native-speaker review.

- **The behaviour forecast no longer assumes one way of counting turns.** Reactor's `Game_Battler.turnCount` returns `$gameTroop.turnCount() + 1`, so the first battle turn is 1 and a condition gated on turn 0 can never hold -- which is exactly what the panel reported. `VisuMZ_3_BattleAI`'s on-the-spot mode drops that `+ 1` on a non-TPB system, and turn 0 becomes reachable, so the same panel would have called a working action dead.

  Whether that branch is actually taken cannot be settled at edit time: CoreEngine resolves the battle system through `$gameSystem.getBattleSystem()`, and a plugin command can change it mid-battle. So rather than guess an offset and print a confident wrong number, on-the-spot mode widens the searched turn range to include 0 and the panel says the counting can sit a turn earlier than it reads. Widening can only ever withdraw a warning, never invent one, which is the direction that keeps the "never fires" list worth trusting; a test asserts that the widened sweep always reports a subset of the narrow one. The Turn box takes its floor from the same place, and clamps to it rather than offering a turn the game cannot reach.

  Nothing changes for a project that is not running Battle AI, or is running it with on-the-spot mode off, which is its default.

- **An enemy's Action Patterns table now says which rows can actually be chosen.** The Rating column is the most misread number on that screen, because a rating means nothing on its own. `Game_Enemy.selectAllActions` takes the highest rating among the actions valid *at that moment* and throws away everything below a window measured down from it, so a row is invisible whenever a better-rated row is valid beside it -- and comes back the moment that rival cannot pay its cost. Nothing on the screen said so. A collapsible **Behaviour forecast** under the table now does: a what-if row for the variables that can actually change this enemy's outcome, the resulting rating ceiling, the surviving actions with each one's share, and a list of the rows that can never win with the reason for each.

  The reasons are separated rather than lumped into one warning, because they need different fixes: a skill id pointing at nothing, an occasion that excludes battle, a cost higher than the enemy can ever hold, conditions that never hold together, and being outranked. The last carries the lowest ceiling seen while the row was actually usable, which is what catches the case no one finds by reading: a cheap-looking rating-2 action costing 50 TP is dead not because 2 is low, but because any enemy holding 50 TP can also afford the rating-5 action above it. On the enemies in one project here the panel finds ten unreachable rows across four enemies, three of them in a single boss, and one enemy whose only action is gated on turn 0 -- a turn that never comes, since battle turns start at 1.

  Which window is in force is read from the project's plugin manifest, not assumed. `VisuMZ_3_BattleAI` replaces `selectAllActions` outright: its classic style keeps `rating >= max - variance`, a different comparison and a different width from the engine's `rating > max - 3`, and its random, casual and gambit styles do not use ratings the same way at all. A project running Battle AI at the default variance of 0 keeps only the highest valid rating, which makes strictly more rows dead than the engine would -- so a panel modelling the engine unconditionally would have been confidently wrong there. Conditions that plugins add through their own notetags stay outside the model, and the panel says so rather than quietly folding them in.

  The analysis is a standalone module, `src/utils/EnemyActionForecast.js`, with the runtime rule it mirrors pinned by a test, so the numbers cannot drift away from `selectAllActions` unnoticed. `DatabaseManager.getPluginManifest()` is the manifest reader the panel and the party-size lookup now share; it caches on the file's timestamp, because a manifest runs to megabytes on a large project and this one is consulted on every enemy click. The 17 new interface strings are a machine-assisted first pass across all 17 locales and want a native-speaker review.


- **A multi-line comment reads as a comment, and a plugin command's arguments fold away.** Every line of a comment after the first is a 408 continuation, and 408 was the one continuation code missing from the command-name table -- so a four-line `<Forced BGM>` block showed one `Comment` row followed by three red `Unknown (408)` rows, each beside its own perfectly readable text. 657 argument lines had the matching gap one level down: no case in the description switch, so a line the plugin command editor had already formatted fell through to the JSON fallback and rendered as `["Round Count = 1"]`. Both now say what they are.

  Those argument rows also used to be shown twice. They rendered unconditionally as top-level rows, and the parent's own arrow rendered `parameters[3]` again underneath as nested key/value lines -- so expanding listed every argument a second time, under the plugin's internal keys rather than its labels, and collapsing removed only that second copy. The arrow is now the single control for them: the authored 657 rows are what it hides and shows, keeping the plugin's own argument names. A plugin command with arguments but no continuation rows still falls back to the nested rendering, so nothing authored earlier loses its display. On a real event this is the difference between 67 rows and 6.

  Troop battle pages and common events build their own rows rather than going through the map editor's list, so they had neither the arrow nor the folding, and troop pages were additionally the one list of the three still showing 655 Script continuation lines that the other two had long since folded into their parent's summary. All three now answer the same way about which rows exist, through one shared lookup rather than three scans. On the largest common event in a project here that is 360 rows down to 97; a battle page's plugin command folds the same way.

- **A fade no longer fights whatever it interrupts, and a music sequence can fade in.** Scheduling a gain ramp over a running one does not replace it: the Web Audio timeline keeps both, so the level jumps to the older ramp's target, waits out its end time, and only then follows the new one. Measured in Chromium with `OfflineAudioContext`, driving the interrupt from `ctx.suspend()` so what has already rendered stays immutable: a `fadeOut(2)` arriving half a second into a `fadeIn(2)` jumped from 0.25 to full volume, held there until the fade-in's end time, and then fell over the half second left of its own two seconds. It is reachable today, because `replayBgm` fades in -- leaving a menu or a battle and immediately triggering a transfer or a gameover runs `Scene_Base.fadeOutAll` straight into it. Both pairs of fades now clear the timeline first, through one pair of helpers rather than four copies of the guard; a fade-out holds the level it is audibly at, and a fade-in still forces zero because a gain node is created open.

  Underneath that, fades were sharing one `GainNode` with the authored volume and the ME duck, and three writers on one `AudioParam` cancel each other. Ducking to 0.25 half a second into a two-second fade-in was swallowed whole -- the same numbers as a fade-in nothing interrupted -- so an ME would not have ducked the bed at all, and a volume slider moved during the same fade was dragged back to full by the running ramp. Sources now feed a fade stage that feeds the volume stage (`source -> _fadeGainNode -> _gainNode -> _pannerNode -> master`). The fade stage carries a plain 0..1 envelope and fades ramp only that; the volume setter and the duck keep `_gainNode` to themselves. Plugins that splice nodes attach downstream of `_gainNode`, so the new stage above it leaves their attachment points untouched.

  With that separation in place, a sequence entry can fade in: **Fade-in (s)** sits beside the palette's existing Fade-out and on a plain track row, and a palette fades in every layer it starts, including one it redraws from its pool later. A palette had only ever faded *out*, so the bed died away smoothly and the next entry began at full level instantly. Absent or zero, which is what every sequence authored before now carries, changes nothing.

  A fade-in also decides whether the handover overlaps. A palette used to fade out and only then advance, so its tail and the next entry never sounded together. When the entry that follows names a fade-in, the palette now starts its tail and the next entry begins over the top of it; the outgoing layers keep playing from a retiring pool until their fade is done, and are released then rather than at the moment they were replaced. An entry naming no fade-in -- every sequence authored before there was one, and every silence, which has no such field -- keeps the sequential timing it has always had, so nothing already built shifts by a second.

- **A music sequence can lead with an intro, walk its pool in order, and hand over when a track ends.** Three additions on top of the crossfade. **Intro** on any entry plays it on the sequence's first pass only, so an opening statement can sit above a bed that then repeats without it; a sequence made entirely of intros replays one rather than falling silent. **Order** on a layer chooses between drawing at random, never the same track twice running, and walking the pool in order. And a layer whose track reaches its own end now hands over the same way a timed palette does, measured from the buffer's own clock so a slow decode delays the hand-over with the track instead of cutting it short -- which means **Duration 0** no longer only means "until the map changes": each track plays in full and crossfades into the next. A track shorter than its own fade-out is left alone, since handing over would mean beginning its fade before it was heard.

  A pool item carries its own **Volume**, trimming it against the rest of its pool. A layer's volume applies to every track it draws, so a pool assembled from two sources -- which rarely agree on level -- had no way to balance one against the other. The trim multiplies the layer's volume rather than replacing it, and the options slider still lands on top of both.

  A layer can be **Shuffled** as well as random or in order: it deals from a bag, so every track in the pool is heard before any of them comes round again, instead of each draw being independent and leaving one waiting its turn. The bag rides across a hand-over, or a cycling palette would deal a fresh one every time and never finish the one it was dealing. And a palette can **Move on after one track**: each layer plays a single draw and the sequence advances, which is what makes a random intro possible -- a palette of candidate openings, marked Intro and set to move on, plays exactly one of them and never returns. Its track running out crossfades into whatever follows, the same way a timed palette hands over.

  An intro survives a battle. The saved-BGM shape already carried which map's sequence to bring back; it now also carries whether that sequence has come round, so a battle -- which stops the sequence outright and restarts a fresh one on the way back -- no longer replays an opening the player has heard. The same flag rides through save files and through boarding a vehicle. Arriving on the map afresh carries no flag, which is what keeps an intro an intro on every visit rather than only ever once.

  The help under the checkbox was rewritten to say what these do, and what the track above the checkbox is for. It is not played while a sequence is on: it is how save files, battles and vehicles refer to the music on that map, and it plays only if the sequence is turned off. Layer controls moved onto their own line beneath the layer title, the way the palette's timings already sit, because four controls and a delete button do not fit the column. New strings are translated across all 17 non-English locales -- a machine-assisted first pass, worth a native review.

## [0.98.5] - 2026-09-07

0.98.5 brings battle choreography into the editor: build a battle arena from a map, position the party and enemies, and assemble attacks from editable steps. It also adds native lighting, map media surfaces, quests, expanded interface authoring, and a substantial MV/MZ compatibility and editor reliability pass.

[Download the binaries on itch.io](https://psychronic.itch.io/rpg-reactor). GitHub provides the source release. Existing battles retain their battlebacks and existing action behavior until you explicitly opt into the new presentation.

## Battle Rooms and visual Action Sequences

- **Use a map as a battle arena.** Troops can select Battle Room or a conventional battleback. Room Setup provides draggable formations, numeric placement, inherited map cameras, and per-troop camera overrides. The Troops preview displays the selected room and its formation live.
- **Author choreography in its own database section.** Action Sequences combines a step list, timeline, 2D/3D preview, movement, battler motions, sound, animations, camera cues, weapon presentation, and an impact cue. Add, duplicate, paste, drag to reorder, scrub, or play just one step.
- **Start with a working punch.** Unarmed Punch demonstrates running into range, striking, and returning home. Run to Target, Punch, and Return Home are reusable building blocks.
- **Assign actions where they belong.** Skills/items, weapon attacks, actor defaults, unarmed attacks, and enemy defaults have distinct assignments. Inherit and Use Existing Behavior preserve control over existing projects.
- **Customize motion and effects visually.** Model offsets, rotation and scale have sliders, precise values, arrows and rings. Animations have target placement/scale controls and optional completion waits. Sounds use the shared audio picker with independent wait settings.
- **Stage the camera.** Map camera inheritance supports isometric and third-person layouts; cinematic framing eases toward the acting battler, follows impact, and returns to the overview. Explicit camera cues take control when needed.
- **Set party capacity in System 1.** Max Battle Members determines the number of Battle Room party slots. Existing plugin limits remain in use until an explicit database override is set.
- **Improve 3D enemy integration.** Choose a 2D graphic or 3D model, see sharper thumbnails/previews, and keep battle markers and supported plugin UI aligned. Fixes cover stale battleback/battler requests, state-icon loading crashes, and inverted 3D ATB icons.

This is an initial presentation system. The PSYCHRONIC Battle Engine adapter preserves normal combat resolution. VE, YEP, VisuStella and LeTBS choreography still need dedicated adapters; their text sequences are not automatically imported. See [battle authoring and compatibility limits](https://github.com/Psychronic-Games/RPGReactor/blob/v0.98.5/docs/BATTLE-PRESENTATION.md).

## Lighting, models and media

- Place and edit native map lights with a visual Lighting tool: point/spot/beam fixtures, ambient controls, flicker, pulse, compound fixtures and twelve presets. Event commands can control lights, and model effects can carry them.
- Lights illuminate 3D surfaces and the room floor, with budgeted shadow atlases for static geometry and moving characters. The 2D editor also previews model lighting, animation, media and attached effects with corrected depth and compositing.
- **Media Surfaces has its own toolbar tool.** Place images or videos directly on the map, including walls and ceilings, or use event commands. Move, rotate, scale, warp corners, duplicate, and undo. Shift-resizing preserves proportions; newly selected media uses its source aspect ratio instead of a forced widescreen rectangle.
- Author model eyes, mouth and lip points with precise controls. Speak 3D Dialogue plays voice with audio-driven lip movement and an adjustable mouth interior. Text remains in the normal message system; teeth and tongues require authored geometry.
- Optimize existing models as well as imports, inspect rendering cost, preserve skinned animation and authored parts, and use distance levels. Model animation/effect copy-paste and playback controls are expanded.
- Isometric movement follows screen direction; 3D click navigation respects ground/elevation. Animated GIF/APNG playback and map/model preview cleanup are improved.

Rendering changes reduce redundant work, keep more effects on the same GPU, cache lighting/shadows, and move suitable preparation work to workers. Results depend on scene and hardware; sustained 60 FPS on the integrated-GPU test workload remains open. See the [performance report](https://github.com/Psychronic-Games/RPGReactor/blob/v0.98.5/docs/PERFORMANCE.md).

## Quests, interfaces, audio and creator tools

- **Quests database and runtime log:** categories, objectives, visibility/completion rules, event commands and save-backed progress. Import definitions from VisuStella, Yanfly and GS. Reactor uses its own quest data file to avoid collisions with plugin-owned files. An on-map tracker and automatic reward grants are not implemented yet.
- **Expanded UI authoring:** editable Actor Panel contents, state/buff icons, variable-driven and circular gauges, Main Menu formation/custom-scene support, layer multi-selection and improved inspector/script editing.
- **Map music sequences:** arrange tracks, silence and layered randomized palettes. Add sound variants and pitch variation, including separate MP/TP recovery sound slots.
- **Database improvements:** multiple elements for skills/items, state descriptions, trait help, passive-state editing, clearer plugin parameters, and more consistent pickers and layouts.
- **Playtest checkpoints** resume recent battle/map-transfer checkpoints from the title screen. **Forge Project Tools** supports project-supplied HTML tools with constrained save operations. **ReactorEvents** exposes a read-only battle notification feed for plugins.

## Compatibility, saving and polish

- Audit coverage across 11 existing 2D template projects, including fixes for fog/dust scrolling and opacity, vehicle visibility, picture-choice icons and lifecycle crashes, and plugin-specific battler facing.
- Additional MV compatibility for large plugin stacks, game-over/autosave fades, windows, animations, quest integrations, plugin-defined resolutions and battle behavior. Compatibility remains project/plugin dependent, not a blanket guarantee.
- BOM-aware map/JSON loading, safer writes in synced folders, preservation of unknown map fields, and fixes for sidecar copying, deletion, undo, failed writes and asynchronous project changes.
- **Event placement persists correctly.** OK commits newly created events; Save Project no longer overwrites a later 3D drag with a closed inspector's old elevation. Model changes stay in the event draft until Apply/OK.
- **Map tools hand off input consistently.** Media Surfaces, Events, Lighting, tiles and 3D-M release stale handlers and placement modes, with matching button highlights across map/renderer changes.
- More consistent database cards, tables, numeric controls and theme colors. New-system labels and help are translated across all 17 non-English locales, with live-language and RTL checks.

Validation at release preparation: **2,932 automated tests pass**, supplemented by native editor/game checks, the 2D project corpus, 18-language layout coverage and a 14-theme audit. These checks cover documented scenarios; they do not establish every plugin combination or a full playthrough of every project.

Thanks to the community contributors and everyone reporting reproducible issues. This release includes integrated contributions across animated media, battle notifications/previews, state descriptions, Forge tools, plugin parameters and trait help.

## [0.98.4] - 2026-08-31

### Fixed
- Project switch: opening a project over another no longer shows the first map black (3D view now disabled and re-framed across the switch; cross-project model preview cache cleared).
- Browser games launch scaled to their window (runtime `20260830.34`): stretch mode now defaults on everywhere, not just desktop/mobile builds; F3 still toggles 1:1.
- Web playtest console cleanup (runtime `20260830.33`): video-surface AbortErrors at scene switches silenced (lifecycle, not failure), onError no longer prints `undefined undefined`, and the 3D map effect preview warms the effects folder on web instead of erroring.
- Enemy AI (GitHub #33): Target State conditions work on plugin string scopes (`<Target: ...>`) — candidates group via probe Game_Action predicates; action ratings are 1-9 across all locales.
- Backdrop-close sweep: 110 `target === modal/overlay` close handlers excised across 102 files (brace-aware, listener shells kept); only UIManager's stateless confirm/alert/reload dialogs keep the gesture; guarded by `no-backdrop-close.test.cjs`.
- The event page's inline model preview centres skinned characters properly: their measured box can collapse at the feet (bind-pose vertices the skeleton hides), and subtracting that centre aimed the camera at the ankles — the declared extent places the middle when the box is clearly degenerate.
- `scanMessageLine` consumes the punctuation codes (. | ! > < ^ $ after a backslash) as zero-width; in-game parity proof via `fx-msgparity.cjs` (TextCodes injected into the live game: identical width and identical cut index against `Bitmap.measureTextWidth`).
- The message editor re-reads System.json on every open, so changing Main Font (or font size) in System 2 re-tunes the overflow measurement immediately — no editor restart.
- Message width model corrected against live game truth (`fx-msgwidth.cjs`: boxWidth 1272/padding 12/innerWidth 1248 on the 1280 Demo): `BOX_MARGIN 4`, `WINDOW_PADDING 12`, `NEW_LINE_X 4`, `FACE_MARGIN 164` replace the 4+8 model; `scanMessageLine` quotes single-token font families so `context.font` cannot reject the project font.
- `MessageBoxes.collectRun` walks backward to the run's first box and returns `startIndex`/`activeIndex`; `collectOne` means the box containing the index; the event list and troop editor splice at `run.startIndex` and the message dialog opens on `activeIndex`.
- The Show Text guide is now a per-line overflow marker instead of a single ruler column: each line is measured the way the game draws it — escape codes consumed (colours cost nothing, icons cost 36px, font-size steps move the pen, actor/party/currency codes measure as what they will actually say) — and only the characters that genuinely won't fit are highlighted, with the pixel excess in the tooltip. A ruler column could never be right: codes cost columns in the editor but nothing in game, and a proportional font gives every line a different budget.
- The Show Text guide line now measures against the project's own resolution (UI area width) and font size instead of MZ's stock 816px box — a 1280-wide game's dialogue no longer trips the guide with plenty of room left in the window. A plugin-configured message width still wins.
- The 3D Models editor's orbit centre gets the same guard: measured bind-pose bounds win over an under-declared extent.
- Event model preview: characters whose skinned exports declare a too-small extent centred on their feet — the measured bind-pose bounds now win when taller; and rotating counts as input, so the preview holds full frame rate through a ring drag instead of throttling to the idle rate.
- The Change Maximum dialog (all database sections and Types) keeps its rounded bottom corners (the footer carried square ones past the boundary) and no longer closes on a click beside it — Cancel and the cross close it, nothing else.
- Parked background scripts (runtime `20260830.26`): `Game_Map.updateInterpreter` moves an interpreter holding in a `BACKGROUND_WAIT_MODES` wait onto `$gameMap._reactorParked` (serialized with the map) when another event starts, gives the map a fresh `Game_Interpreter`, ticks parked runners each frame and unlocks their events on completion; the self-retrigger guard covers parked event ids.
- Preview-model clicks: `eventAt` box-tests `modelPreview` roots alongside the pickables; `highlight` returns early for them (a Group has no material and its scale is the model's size — the crash killed the outline/label updates); `previewEventModel` stamps a `pickBox`.
- Per-event 3D refresh: `rr-events-changed` routes to `refreshEvents` (never the world rebuild); `_buildOneEvent` carved out of `buildEvents`, `_eventIdentity` snapshots drive the diff — effects, animated models, sheet animations (now tagged `eventId`), pickables/billboards/labels all follow their event out. `UIManager`'s capture-phase Delete yields to an active props selection via `remove()` (which records undo).
- Self-retrigger guard (runtime `20260830.25`): `Game_Event.start` no-ops while the map interpreter is running this same event (touch/action re-arm during background waits kept `_starting` true, vetoing the `isEventRunning` exemption). Runtime revision now also readable in the F12 console: `RPG_REACTOR_RUNTIME_REVISION`.
- Background waits unified (runtime `20260830.23`): the `Game_Map.isEventRunning` exemption covers `reactorModelAnimation` as well as `reactorScopedWait`.
- Scoped Wait's switch/variable fields read as themed dropdowns: the chosen name inline (`0007: Torch Lit`) with a caret, opening the picker on click.
- Scoped Wait polish: `SwitchVariablePicker` buttons (label `0007: Name`, z-bumped over the dialog), one 120px-label grid for every row, picker section `Game Flow` translated across the section tables and the reviewed sections catalog (de/ru blocks were empty one-liners).
- Scoped Wait resume toggle (runtime `20260830.22`): "This event keeps moving" checkbox (default on) gates the invoking event's unlock; unchecked, it stands locked the MZ way while the wait holds.
- Scoped Wait switch/variable modes (runtime `20260830.21`): decision extracted to `Reactor3D.scopedWaitHolding` (all four modes; actions keeps the 60 s deadline, switch/variable hold indefinitely); dialog gains Switch id + ON/OFF and Variable id + operator + value rows, radios auto-select on focus.
- Scoped Wait (runtime `20260830.20`): `ScopedWait` plugin command ({target, mode: actions|duration, duration}), `WaitForModelAnimation` registered as an alias; waitMode `reactorScopedWait` decided by duration or `Reactor3D.scopedActionsBusy` (animation queue + `isMoveRouteForcing`); `Game_Map.isEventRunning` reports false while the only running interpreter sits in a scoped wait (unless an event is starting), and the invoking event is unlocked at wait start. Dialog gains Last action / Duration radios.
- Wait for 3D (runtime `20260830.19`): plugin command `WaitForModelAnimation` ({target: id|0|-1|'all'}) holds via waitMode `reactorModelWait` on `Reactor3D.modelAnimationsBusy` (queue entries or playing holder actions; the all-scope also scans the scene's instances; 60 s deadline). `WaitForModel3DEditor` dialog, picker entry, command name + Everything in 17 locales.
- Animation queue (runtime `20260830.18`): `_modelActions[characterKey]` is a queue — `playModelAnimation` pushes (empty name clears), the 3D holder and flat-map consumers start the next entry when the stage is free, a repeating action stops repeating when something waits behind it, and `modelAnimationWaiting` scans the queue.
- Flash scopes + animation wait (runtime `20260830.17`): `flashTimings[].scope` (2 screen / 3 hide) written by the timing modal and honoured in `Sprite_Animation.processFlashTimings`/`updateFlash` (`_reactorHideDuration`, checkEnd waits for it); `PlayModelAnimation` takes `wait`, holds via waitMode `reactorModelAnimation` decided by `Reactor3D.modelAnimationWaiting` (pending → playing → released; repeat releases on restart; 30 s deadline). Tests: `animation-flash-and-wait.test.cjs`.
- Turn sweep (runtime `20260830.16`): `eventModelSweepHits` takes the from/to yaws and samples the real footprint along the short arc (whole-disc caution only when no arc is given); `eventModelCanFace` and `eventModelWouldOverlapEvents` skip characters that fail `charactersOverlapVertically`.
- `MapEditor3D.teardown` recomputes the 2D renderer size (`applyViewportCrop` + `renderMap({preserveScroll})`, one frame deferred) instead of trusting the size saved at enable time; `updateMapZoom` only ever updated the label.
- Prop events write `_direction` directly at setup (runtime `20260830.15`): the synthetic page carries direction-fix, under which `setDirection` is a silent no-op — the stored facing now wins unconditionally.
- Event list 505/code-45 rendering goes through `SetMovementRouteEditor.parseRouteCommand` (step names, translated) with the script text as fallback; `_buttonDefs` places rise/descend/height in column 1, the three poses in column 2, Rotate… beside Script in column 3.
- Placement trigger override (runtime `20260830.14`): `Reactor3D.rulesForPlacement(rules, name, repeat)` clones the chosen rule as `trigger: 'action'` (+ placement repeat); applied at the game holder's pending-fire and in `MapEditor3D.animateModel` drivers. DB editor: `_keysHtml` renders for every pose trigger and `_workValues` strips keys only from the live slider stand-in (`triggerOverride`); `_healAllAnchorBindings` runs after `applySelection` so a fresh carve claims origin-anchored effects; `deletePart` converts anchored offsets back to the model frame before unbinding.
- Whole-model target (runtime `20260830.13`): `prepareModelInstance` stamps the anim-root's rest turn and `effectAnchorNode('')` resolves to it, so `effectAnchorWorld`/`effectAnchorQuaternion` track whole-model poses for unanchored effects (offsets still read in model space on unprepared objects). `renderPartList` opens with a built-in Whole model row selecting `''`.
- Anchor lookup + heal (runtime `20260830.12`): `Reactor3D.effectAnchorNode` resolves a part name via node name OR `userData.parts` (overlap-carved pieces answer to every owner); `effectAnchorWorld`/`effectAnchorQuaternion` use it, as do the editor's place/convert/marker paths (`_anchorNode`). `Database3DEditor._healAnchorBinding` rebinds an `anchor.part: ''` effect to the smallest piece whose box contains its point, on `selectEffect`. Functional test drives a pose through `applyModelAnimation` in a three.js vm: `effect-anchor-tracking.test.cjs`.
- `_placeEffectAnchor` binds `anchor.part` from the hit (carved part name, or dominant bone on a rig; origin only when the click lands on unowned geometry) and stores the offset in that part's frame; `syncWork`'s part change converts the offset between the old and new frames and refreshes the live video preview.
- Part-anchored effects (runtime `20260830.11`): `prepareModelInstance` stamps `__restQuaternion` on posed nodes; new `Reactor3D.effectAnchorQuaternion` returns the anchor part's pose delta, applied in `reactor_video_surfaces` placement, `MapEditor3D.updateEffectPlays` and `Database3DEditor._updateVideoPreview`. Part rename also renames `rawEffects[].anchor.part` (+ open `_effectWork`) and redraws the effect list/form. `syncPropRings` orients rings from the placed object's real rotation instead of the spec's bare yaw/pitch.
- Database3DEditor effect flow: `selectEffect` auto-starts `_playVideoPreview` for video effects (and stops it for others); the render gate's `animating` includes `_fxVideo`/active `_fxPreview`; `_pickPart` returns early in effect card mode so the card survives model clicks (parts stay reachable via their list and the card chooser). `DatabaseEditorUI` nav + title fallback renamed to `3D Models`. Tests: `db3d-effects-flow.test.cjs`.
- Lights flat + poses (runtime `20260830.10`): `syncLights` writes quads on ground axes (right = +x, up = north; facade lift is plain vertical now); `reactorFacePose(±1|0)`/`reactorRotate(deg)` on `Game_CharacterBase`, applied to models after facing (`rotateX(-pose·π/2)`, `rotateZ(-spin)`) and to sprites as feet-anchored `rotation` in both flat and stood-up form. Route steps `faceceiling/faceground/standup/rotate` share the code-45 codec rails; `_dlgRotate` dialog; five new phrases ×17 locales.
- Rave light shim (runtime `20260830.9`): `raveYaw` negates both the smooth flashlight angle and `facingYaw` (clockwise-from-south → the scene's anticlockwise aim, matching the nova shim's negation). New `lightSolidGrid` (per-map cache; on room maps a cell with no tile is wall at room height) + `lightBlockHeightAt`/`lightSegmentBlocked`/`clampConeReach`; `syncLights` skips a light whose cell the camera cannot see and shortens cones to `beamReach`; all behind `Reactor3D.LIGHT_OCCLUSION`. Tests: `light-occlusion.test.cjs`.
- Prop axis arrows: `RRAxisArrows3D` on the selected prop (created/disposed with the pose rings in `selectPropRings`, synced in `syncPropRings`), grabbed before rings on pointer-down; `dragPropAlongAxis` slides x/y fractionally and clamps z to `PROP_MAX_LIFT`, deferring the sidecar rerender to `finishPropDrag` like the rings. Card tab renamed Coordinates (`_tx`, 17 locales).
- `ModelGraphicPicker._placeFaceAt` snaps a face mark's horizontal direction to the nearest 45-degree step within a 15-degree tolerance (the mark is what `eventModelWorldYaw` turns by, so a slightly-off dot was a permanent tilt); its pointer-move work (ring pick, pose apply) is coalesced to one `requestAnimationFrame` per frame. `MapEditor3D.refreshEvents` rebuilds just the event group (effects stopped via `disposeEffectPlays`, materials disposed, `buildEvents` re-run — which draws start markers itself) and `EventManager.renderEvents` calls it when the 3D view is up, so a changed event model shows at once. Tests: `event-editing-3d.test.cjs`.
- **Vertical coordinate (runtime revision `20260830.8`).** `_reactorLift` is every character's height (was props-only), eased to `_reactorZTarget` in `updateMove` at `distancePerFrame` (`isMoving` includes it, so Wait for Completion holds); `reactorRise/reactorSetHeight` clamp to the room's ceiling (`verticalCeiling`, default 64); events read `reactor3d.eventZ[id]` in `setupEvents`; `charactersOverlapVertically` (heights from `characterHeightTiles`) filters `isCollidedWithEvents` and `isCollidedWithPlayerCharacters`; `standingPlaceFor` lifts billboards; flat maps lift only props. Editor: `EventEditor` position inputs write x/y and `setEventZ` (z counts as a model change for the commit); `MapEditor3D.placeEvent/previewEventModel` add `eventZAt`; `RRAxisArrows3D` (new util) gives the selected event X/Z tile-stepped and Y free drags; `syncGridLevel` draws the level plane; both passage overlays skip placements with z > 0.5. Route steps: `SetMovementRouteEditor.routeCommand/parseRouteCommand` (codec kind `route`); tests `vertical-position.test.cjs`.
- **Ghost at full rate.** `updateHover` keeps `propAt` at ≤30 Hz (cached `_hoverOverProp`) and places the ghost on every move (one `groundPointAt` raycast); `_placementSpec` is memoized on a field signature.
- **Ghost flicker (owner-reported).** `ModelPropsManager._pointerMove` returns before the hover-ghost logic when `mapEditor3D.isEnabled()`; `_hideGhost(also3D)` hides the 3D ghost only from deactivate; the 3D canvas's `pointerleave` hides its own.
- **3D-M transform card + per-axis stretch (runtime revision `20260830.7`).** `ModelPropsManager._renderCard/_cardApply/_syncCard`: range+number rows per tab (offset x/y ±4 tiles around the prop, z 0–8; rotate ±180°; size 0.1–20 with `stretch` sliders when not proportional); a drag pushes one undo and updates silently, `_syncCard` moves numbers without rebuilding. `stretch: [x,y,z]` normalized in `MapElevation.normalizeProp` (omitted when all 1), carried by `specOf`/`propModelSpec`/`_normalizeModelSpecNow`, applied in `RREventPreviewModels.instance`, `poseProp`, the play-time instance, the collision mask (per-axis fit, pose keys) and the footprints.
- **Undo for props on the map history.** `MapEditor.recordPropsState` pushes `{ kind: 'props', mapId, data }`; `undo/redo` restore through `ModelPropsManager.snapshotProps/restoreProps`; the tab's own Ctrl+Z defers to the toolbar handler so a step is not taken twice.
- **Placement ghosts.** `MapEditor3D.updatePlacementGhost/hidePlacementGhost` (faded material clones of the instance, keyed by spec + facing, moved with `placeProp`); `ModelPropsManager._showGhost/_hideGhost` (the thumbnail sprite at half alpha under the cursor).
- **`TransformModel3DEditor` + runtime `Reactor3D.transformModel/liveTransformAt/applyLiveTransform`.** Plugin command `RPGReactor`/`TransformModel3D` (args target, offsetX/Y/Z, yaw/pitch/roll, proportional, scaleX/Y/Z, duration, wait); state on `character._reactorTransform` (from/to/frame/duration, eased in-out, a new command starts from the eased state); applied after the pose in the model loop; `wait` uses `Game_Interpreter.wait`. Props are addressed as `PROP_EVENT_BASE + id`. Names in 17 locales (`RR_EVENT_COMMAND_NAMES` and the reviewed commands catalogue), phrase "Prop" added. Tests `transform-model-3d.test.cjs`.
- **`SelectThemingShim` labels follow programmatic `value`/`selectedIndex` (owner: 3D-M Facing showed Down).** Each wrapped `<select>` gets own accessors over the native setters that call `refreshLabel`; the MutationObserver still covers re-populated options. All 75 selects benefit; no per-panel change.
- **Event-mode exit re-activates `ModelPropsManager` when `tilesetPaletteViewer.currentLayer === 'M'`** (`main.js` toolbar toggle), the counterpart of the deactivate on entry.
- **`eventModelContains` for stationary off-grid characters (runtime revision `20260830.5`).** `contains(_x, _y) || contains(_realX, _realY)` is for a gliding event (the trailing tiles of a vehicle); a prop placed at (16.56, 25.2) with `isMoving()` pinned false was also tested from (17, 25), adding column 18 to a 16–17 footprint. Now: off the grid and not moving → the real position only. Diagnosed with `scratchpad/fx-prop2.mjs` (game `eventModelOccupies` tiles vs `blockedTilesFor` for one prop, same spec and yaw).
- **`buildModelCollisionMask` rewritten (owner: "only the outline, not every triangle").** A `Uint8Array` grid over the reach replaces the `Set` of cell keys; a mesh past `DENSE_TRIANGLES` (50K) is read from `DENSE_SAMPLES` (250K) evenly strided vertices with no triangle work; below that, a triangle inside one cell marks that cell and only larger ones walk edges and fill. Same mask API (`has`, `touches`, `blocksTile`, bounds; `cells.size` → `count`). Benchmark `scratchpad/bench-mask.cjs`: 3,056,400 triangles, 259 ms → 4 ms, identical bounds.
- **Passage overlay, part two.** `TilemapManager.modelPlacements(mapData)` lists props and model-bound events (the previewed page, `MapEditor3D.previewPageIndex`) for both overlays; `MapEditor3D.drawPassage/setPassageVisible/refreshPassage` draw `LineSegments` and footprint quads on the ground at cell elevation (redrawn on `rebuild` and in-place prop edits); `ProjectController.refreshTilesetSurfaces` re-points `tilemapManager.currentTileset` at the saved tileset and refreshes both views. `'Model size (tiles)'` → `'Model size'` in the picker, the event page and all locales.
- **Passage overlay (`TilemapManager.drawPassage`, `map-passage` toggle).** `TilemapManager.tilePassage(data, w, h, flags, x, y, roomFloor)` is the pure rule (top solid non-star tile decides; empty = 0x0f unless a room floor); marks are a `PIXI.Graphics` above the layers, model footprints filled from `Reactor3D.blockedTilesFor` per prop as templates arrive (`_drawModelPassage`, generation-guarded); `updateTiles` and `ModelPropsManager._changed` call `refreshPassage` (one redraw per frame). Test `passage-overlay.test.cjs`.
- **Room floor passability (runtime revision `20260830.4`).** `Reactor3D.installRoomFloorPassage` (from `installPropHooks`) wraps `Game_Map.checkPassage`: on a 3D map with `reactor3d.room`, a cell with no solid non-star tile passes. Diagnosed with `scratchpad/fx-canpass.mjs`, which maps `#` model / `m` map / `e` event blocking around each prop: the reactor's "buffer" was `m` on all-zero cells.
- **Props tool vs Event tool.** `main.js` drops `modelPropsManager` on entering event mode and leaves event mode when the 3D-M tab is picked. **Mask build**: `buildModelCollisionMask` keys cells by number (`keyOf`), `blockedTilesFor` caches per template and pose (`template.userData.__reactorMasks`).
- **Collision by tile coverage (runtime revision `20260830.3`).** `mask.blocksTile(x, z)`: the tile centred at (x, z) is blocked when any of the four quarter-cells around its centre is occupied or at least half its sixteen are; `eventModelContains` uses it (a box: `|local| <= raw`, half covered counts) instead of `touches` with `COLLISION_BODY_RADIUS` at tile centres, which padded every model by 0.34 tile. `buildModelCollisionMask` marks corners and edges a hair inside the triangle and gives a zero-area (vertical) face no interior scan: a box's wall at x = 1.5 marked the cell beyond it, so a 3×1 bench came out 3.25×1.25. Measured on the Demo: consoles 3×3 → 3×2 tiles. `blockedTilesFor(template, json, spec, direction, x, y)` lists a placed model's tiles by the same rule; `MapEditor3D.showPropFootprint` (with `selectPropRings`/`syncPropRings`) and `ModelPropsManager._drawFootprint` draw them. The panel keeps one Size (`scale` written as 1, an old scale shown folded in). `AnimationPreviewLayer` reads its pixels only until `visibleFrames` is learned (two plays); `EffekseerScene.shouldMeasure` drops to every 30 frames once `MEASURE_HISTORY` looks are in.
- **`Database3DEditor.saveRules` clears `RREventPreviewModels` and calls `refreshMap3DView` (owner report: on-demand trigger still Always on the map until restart).** `templateFor` caches the template with `userData.reactorSidecar`. `ModelPropsManager.chooseModel` keeps `fields.animation/effect` when the same model is picked again (the owner's consoles 3 and 4 came out with Effect none this way); `modelEffectNames` lists effects with their trigger.
- **`MapEditor3D.refreshProps(ids)` re-poses in place (owner: "transform instead of destroy and recreate").** `poseProp` applies size/scale (the same fit as `RREventPreviewModels.instance`), `applyEventModelPose` for facing and turn, and `placeProp` for lift and position to the existing instance; `propIdentity` (model file/texture, animation, repeat, effect) is stamped at build and a mismatch, or a prop that is new or gone, falls back to `buildProps`. Effect plays and animation drivers hold the same object, so they ride along; the per-step WebGL context churn is gone with the rebuild.
- **`AnimationPreviewLayer.dispose()` loses its WebGL context (owner report: white screen after editing a prop's Size/Scale).** `ModelPropsManager` calls `refreshProps` on every live edit → `disposeProps` → `disposeEffectPlays` → `layer.dispose()`, which released the Effekseer context and removed the canvas but left the WebGL context to the collector; Chromium's budget is 16 live contexts, and eviction took `MapEditor3D`'s (`THREE.WebGLRenderer: Context Lost` → `fail`: "WebGL 2 is unavailable"). Every other context owner in the editor already calls `WEBGL_lose_context.loseContext()` on dispose; the layer now does too, and drops `_drawFrame`.
- **In-scene Effekseer effects at full resolution (runtime revision `20260830.1`).** `Reactor3D.EffekseerScene` no longer draws every effect at `SCALE` 0.5 of the screen: `trackedRect` projects a cylinder around the anchor (`frameBox`) to a screen box, drawn 1:1 with the full-screen viewport and a scissor, copied out and mapped back by the quad's `rectMin`/`rectSize` uniforms (default whole-screen, so existing quads are unchanged). A box past `BUDGET` (0.25 of the screen, the old cost) is drawn at `sqrt(budget/area)`. The reach is learned: `measure` downscales the drawn box to 64 px every `MEASURE_EVERY` frames and turns the lit extent into a radius and a below/above range in tiles (`upY`, the camera up axis's world y, corrects a steep camera's foreshortening); the widest of the last `MEASURE_HISTORY` looks is kept, a picture touching the box's edge grows that side by half, never past the authored `effectRadius` (1.25 frames). The tracker lives on the holder per effect name, so an Always effect's restarts keep it. Screen-fraction tracking was tried first and drifted as the camera turned; a sphere wasted pixels on a tall beam. Measured on the Demo core in third person: 640×360 stretched → ~350×300 at 1:1, `render` 0.6–0.75 ms/frame, no clipped frames static or orbiting. The map editor's 3D view (`MapEditor3D._placeEffectQuad`) keeps its mechanism (the layer draws on its own loop, the quad takes the latest picture each render, whole-view mapping) and only grows the layer's canvas from a 512 square to the view's size (≤ `EFFECT_PIXELS`) through `AnimationPreviewLayer.setWorld({ rect, viewWidth, viewHeight })`; `previewActive` now counts a playing effect, since with video previews off the view idled at 10 renders/s and the effect refreshed only on a render (choppy). A synchronous `drawNow` variant and a per-effect box were tried in the editor and dropped. The frozen, screen-bound effect the owner saw was the **resize**: three allocates a canvas texture immutably at its first size (`texStorage2D` on WebGL 2), so once the window grew, uploads of the bigger layer canvas failed and the quad kept its last frame stretched over the screen (a 512 square never resized, so the old code never hit it). Both the editor and the game (`scratch` changes size with the box) now `texture.dispose()` when the source changes size so the next upload reallocates (runtime revision `20260830.2`).
- **Seamless loops (`visibleFrames`).** The runtime learns where each play's picture ended (`play.lastLit`, from the same measurement; `track.visibleFrames` keeps the longest seen since a play the camera looked away from ends early) and `updateAnchoredAnimations` starts a loop's next play at that frame with `entry.restarted`, the tail running underneath. `AnimationPreviewLayer` and the Animations page preview do the same from a 32-px alpha readback of their own canvas; the page's `setTimeout(play, 100)` is gone. Harness: `scratchpad/fx-verify.mjs` (look up first: the default third-person camera cannot see the core), `fx-editor.mjs`.
- **Autotile pick keeps the piece (user report, `scratchpad/Autotile/`).** `selectPickedAutotile` sets `tileId` on the palette position; `exactAutotileId` stamps it for Shift-paint in the pencil, rectangle and preview when it is of the selected kind. Test in `autotile-edge-and-pick.test.cjs`.
- **Conditional Branch in RPG Maker's four-tab layout (user reports).** `ConditionalBranchEditor` renders `TABS` (1: Switch/Variable/Self Switch/Timer, 2: Actor, 3: Enemy/Character/Vehicle, 4: Gold/Item/Weapon/Armor/Button/Script, Reactor: keyboard/mouse/wheel/pointer) as `.rr-cb-row` radio rows; `_conditionRow` disables every control of an unchosen condition, `_choiceRow` handles the indented Constant/Variable and actor/enemy sub-choices, `_checkRow` the Include Equipment boxes. Switch/variable fields reuse `.rr-variable-reference` (the `0001 Name ...` button → `SwitchVariablePicker`); database fields are `0001 Name` selects. Timer edits minutes and seconds into the single `timerSeconds`. `show()` takes `options.troop` (both `DatabaseTroopEditor` call sites pass `currentTroop`) so the Enemy dropdown reads `#1 Gnome`. Parse/build/advanced-input serialization is untouched; the 72 branch tests and a CDP drive of every tab in dark and light pass. Nine new phrases hand-written in all 17 locales.
- **`ColorFilter.uniforms` on PIXI 8 (GitHub #28, runtime revision `20260829.39`).** The v8 `ColorFilter` (a `ColorMatrixFilter`) defines `_defineLegacyUniforms`: a `uniforms` object whose `hue`/`brightness` accessors call the setters and whose `colorTone`/`blendColor` are Proxies over the backing arrays, rebuilding the matrix on element writes; `setColorTone`/`setBlendColor` copy in place (`_copyInto`) so the views stay bound. v5–7 untouched. Contributed patch, reviewed; `colorfilter-uniforms-v8.test.cjs`.
- **One depth buffer on model maps (runtime revision `20260829.45`).** `MapEditor3D.render` used three passes with `clearDepth()` between them (below / star tiles / layer ≥ 5 video surfaces), so anything in a later pass painted over the world regardless of position; on a map with models (`Reactor3D._hasEventModelsNow`, which now counts `reactor3d.props` too) it renders the single `world` pass instead, and the runtime's `setPass("world")` takes `_aboveBillboardsGroup` along when `scene.modelsInWorld` is set by `updateReactor3D`. The editor's Effekseer effect previews on the map are no longer a DOM overlay: `AnimationPreviewLayer.setWorld` draws from the view's camera at the world anchor (512-px canvas) and `MapEditor3D._placeEffectQuad` shows it on `Reactor3D.EffekseerScene.quadFor` (`flip` uniform on: a WebGL canvas reaches three top-down whatever `flipY` says). The effect plane is no longer a constant screen-depth quad but a 600-tile vertical plane stood on the anchor and turned to the camera (`standQuad`), so a tall beam's depth follows its height — from above, a tower's base is farther along the view axis than the mid-height anchor, and the flat plane drew the beam over it.
- **Anchored Effekseer effects in the 3D scene (runtime revision `20260829.44`).** `Reactor3D.EffekseerScene`: Effekseer cannot draw on three.js's WebGL 2 context (its programs are WebGL 1; the attempt blacked out the frame) and a second Effekseer context fights the first over the library's one global object table ("object does not belong to this context" every frame, massive lag), so the effect stays on the overlay context: during the update its hidden sprite's own handle is drawn from the scene camera (`setProjectionMatrix`/`setCameraMatrix` from the three camera, `setLocation` at `effectAnchorWorld`, one unit = span × scale / 26 tiles) into a scissored corner of the overlay canvas at `SCALE` (0.5), the corner is copied through a 2D scratch canvas into a screen-sized `ShaderMaterial` quad whose `gl_Position.z` is the anchor's clip depth (world pass only, `setPass`), and `Sprite_Animation.renderActive` clears the overlay as it always did. `Sprite_Animation.updateEffectGeometry` skips a `_reactorInScene` sprite so the anchor owns the handle. `Spriteset_Map.updateReactor3D` calls `render(viewport)` before the passes. MV sheet animations stay on the overlay with the facing rule.
- **Show Text multi-box + text codes (GitHub #29; contributed, reviewed).** New `utils/MessageBoxes.js` (`collectRun`/`buildCommands`/`splitLines`; boxes keep `source` and an `untouched` box writes back byte-for-byte), `utils/TextCodes.js` (catalogue: vanilla, MessageCore families gated on `status: true` in the plugin manifest, project `TextCodeActions`/`TextCodeReplace`), `utils/TextCodeMenu.js` (`attach`, `createReferencePanel`, `openPluginHelp`), `utils/Windowskin.js` (`\C[n]` sampled from Window.png through `RREncryptedAssets.readAssetBytesAsync`, so encrypted projects get real colours), `utils/ColorPickerModal.js`; `MessageCommandEditor` holds `boxes[]`/`activeIndex`; `EventCommandList`, `DatabaseCommonEventEditor`, `DatabaseTroopEditor` share the run reader. Review changes over the contribution: the duplicate `IconPickerModal.js` was dropped for `RRIconPicker.show(…, { zIndex })`, scripts wired in `index.html`, the bare `require('path')` guarded for the web build, and the re-split policy above. 24 strings hand-written in 17 locales.
- **Database text codes (GitHub #30, runtime revision `20260829.43`).** `TextCodes.forScope` scopes `help` and `battlelog` (wrap and pictures reach help only; alignment both; the message-only waits, `\CommonEvent`, `\Wait` neither) plus a `FORMAT_ARGS` placeholder tier (`skillMessage` %1/%2, `stateMessage` %1); `<linebreak>`/`<NoWordWrap>` are the spellings MessageCore matches. `database/DatabaseTextCodes.js` decorates `[data-rr-textcodes]` fields from `DatabaseEditorUI.showDatabaseDetail` (menu, shared collapsed panel per spec, a plain-Event→`change` bridge so an inserted code persists). Runtime (contributed addendum): `Window_BattleLog.setOutcomeItem`/`displaySkillOutcome` show skill `message3` on a hit and `message4` on a miss, named only on `BattleManager.invokeNormalAction`. Tests: `message-text-codes`, `database-text-codes`, `skill-outcome-messages`.
- **Grow: Max TP + random range; Change Parameter: Max TP + Random (GitHub #15, runtime revision `20260829.42`).** `Game_BattlerBase.PARAM_MAX_TP = 8` routes `addParam` into `_maxTpPlus` (read by `maxTp`, `|| 0` for old saves; `Game_Enemy.maxTp` adds it to an authored value); `Game_Action.effectGrowValue` draws between `value1` and `value2` when `value2` ≠ 0; `operateValue(op, type, operand, operandMax)` type 2 = Random, `command317` passes `params[6]`. Editor: Special tab param list has nine entries (Buff stays eight), `_growRangeRowHTML`/`_readGrowRange` keep the non-zero end in `value2`, `getEffectValue` fixes the `+-5` sign and shows `+3–8`; Change Parameter dialog Random radio with two ends (seventh parameter written only then); command list shows `{V}` for variables and `a–b` for ranges. `effect-grow-max-tp.test.cjs`.
- **Add State duration override (GitHub #16, runtime revision `20260829.41`).** Effect code 21 reads `value2` as turns (0 = the state's own, which is what MZ writes) and an optional `value3` as the range's upper end (written only when it differs); `Game_Action.effectStateTurns` rolls it, `applyEffectStateTurns` calls the new `Game_BattlerBase.setStateTurns` after `addState` for both normal and attack states. Editor: `_durationRowHTML` is a sibling of the Add State row (the i18n pass and radio lookups read `.effect-option > span`), boxes default to the chosen state's Min/Max Turns and grey until ticked, Auto-Remove None disables it with a note ("Not removed automatically", 17 locales); `durationSummary` appends ", 3–6 turns". `effect-add-state-duration.test.cjs`.
- **Enemy Max TP + Parameters/Drop layout (GitHub #31, runtime revision `20260829.40`).** `Game_Enemy.prototype.maxTp` reads `enemy.maxTp` (number or numeric string; anything else falls back to `Game_BattlerBase.prototype.maxTp` looked up at call time, so plugin overrides keep governing unset enemies). Editor: `parameterRows` (Max TP under Max MP, `field: 'maxTp'`, no param index — never a ninth `params` entry), `paramInputWidth` (`calc(Nch + 48px)`, 2–9 digits), two-row drop slots with `formatDropRate` readout (`data-rr-i18n-skip`, since `applyText` pins `.database-field-label` text), reveal with `display: 'flex'`, top-row flex 0.9/1.6/0.9. `enemy-max-tp.test.cjs` (12 tests, contributed).
- **Referenced by (GitHub #23).** Contributed and reviewed: `database/DatabaseReferenceFinder.js` scans the live database's structured fields (trait codes 13/14/32/35/43/44, effect codes 21/22/43/44, drops, troop pages, 30 event command codes with the 111 subtypes and 605 shop rows; variable-targeted commands skipped) and `DatabaseEditorUI.showReferencesModal` renders the groups with rows that `reveal()` the record in its list. Two phrases in 17 locales; `database-referenced-by.test.cjs` (18 tests).
- **Plugin command `showFilePicker` is folder-aware (GitHub #7).** `PluginCommandEditor.extensionsForDir(dir)` picks the extension list from the `@dir` root (`img`, `audio`, `movies`, `fonts`, `effects`; anything else lists all kinds).
- **User Lacks State / Target Lacks State conditions (GitHub #32, runtime revision `20260829.39`).** `meetsActionCondition` types 9 and 10 negate `meetsStateCondition` / `meetsTargetStateCondition` (so a plugin replacing either moves both polarities); catalog rows beside their positive twins, summaries, 17-locale reviewed strings; five tests in `enemy-action-conditions.test.cjs`.
- **Play Model Animation lists the target's actions.** The animation field is a dropdown of the action rules the chosen target's model declares (`PlayModelAnimationEditor.modelActionNames` reads `3d/<name>/model.json`; an event's model comes from the map sidecar or its `<r3d>` note, the player's from the starting party leader's actor binding in `Database.r3d.json`), refilled when the target changes. A target with no model, or a model without actions, lists every action in the project with a note saying so; a saved name the model no longer declares stays selectable and marked; Custom… keeps a free-text path.
- **Preview Event in the 3D view.** Choosing a preview page while the 3D view is on now rebuilds the 3D event layer, so model-bound previews (vehicles, monsters) appear at once instead of at the next project open; `setEventPreview` refuses an event without an id rather than writing an `undefined` key into the sidecar.
- **The 3D view is remembered per map.** `ProjectController.map3DViewRemembered/rememberMap3DView` keep a per-project `localStorage` record (`rrMap3DViewMaps:<project>`) of which maps were left in 3D; `refreshMap3DView` reconciles the viewport with the loaded map's memory on every load (a 3D map comes back in 3D after reopening, switching to a 2D map switches the view), the toolbar checkbox records the choice, and a crash last session keeps remembered maps in 2D until the box is ticked by hand.

### Added
- **Live message miniature.** The Show Text editor draws the box being edited right under the text field — windowskin, face, name box, colours and icons resolved, using the project's own font and resolution. It follows the caret: with more lines than one box holds, the box under the cursor is the one shown, updating as you type (`renderMiniPreview`, guarded in `message-text-codes.test.cjs`).
- **GLB import optimizer.** `RRGlbOptimizer` (`src/utils/GlbOptimizer.js`) + `UIManager.showModelOptimizeDialog`: importing a GLB in the Resource Manager offers Optimize (2K textures, uint16 skin weights, tangent drop, duplicate-vertex weld — in-place accessor substitution keeps animations/skins intact by construction; the tangent pass GC-remaps every reference), aggressive (adds mesh simplification via position-bucket clustering with UV tolerance and a normal-dot guard), or as-is; Cancel aborts. `validateModelBytes` still gates the output; guarded by `glb-optimizer.test.cjs`.
- **Effect scale is model-relative (runtime revision `20260829.38`).** Scale 1 = the animation's authored screen (`Graphics.height` px; 26 Effekseer units, or MV cell pixels) is the model's longest side: `Reactor3D.effectModelScale(object)` = `modelSpanTiles(object)` × tileHeight / screenHeight multiplies the axes of an anchored animation (MV sheets carry it through the stand-in's `_reactorExtra`). `AnimationPreviewLayer.setSpan(tiles)` projects Effekseer with `q = span / 8` and MV cells at `(size / 8) × span / screenHeight`; the DB passes the model as shown (1.6 tiles), the map editor the prop instance. Replaces `effectInstanceScale`. Demo Core effect 0.22 → 2.05 (same look).
- **Effect card sliders drive the running preview; overlay at display size; F4 is not Escape (runtime revision `20260829.37`).** `_updateTriggeredEffectPreview` re-points `_fxPreviewDef` at the working copy when the playing effect is selected (it kept the saved record, so `_updateEffectPreview` re-applied the saved offset/scale every frame and the sliders looked dead). `AnimationPreviewLayer` grows its canvases with the overlay (`size`, 128-px steps, `MAX_SIZE` 1024) so a large effect is no longer a 384-px thumbnail stretched over the model. The camera's keydown listener marks F3/F4 (and `fullscreenchange`) with `look.suppressMenuUntil`; `lockReleased` ignores a lock drop inside that window.
- **Context submenus stay on screen.** A submenu (Set Starting Position, Player Facing, Preview Event, Quick Event) opened near the bottom or right edge ran off the window and showed only its first entry; `EventManager.keepSubmenuOnScreen` shifts it up by its overhang (never above the top) and opens it to the left when it would overhang the right.
- **Map context menu fixed.** The Player Facing submenu called an undeclared `tt()` inside `showContextMenu`, so every right-click on the map or an event (2D and 3D) threw before the menu appeared; it now translates through `I18n.tText` and `player-start.test.cjs` guards the scope.
- **Player start facing + 3D start markers (runtime revision `20260829.36`).** `System.json.startDirection` (2/4/6/8, absent = 2; MZ ignores it) read by `Game_Player.setupForNewGame`; `EventManager.setPlayerStartDirection` saves it from the map menu's Player Facing submenu (`eventCtx.playerFacing`, 18 locales), `createStartingPositionMarker` takes a direction and draws `EventManager.arrowPoints`; `MapEditor3D.buildStartMarkers` (called from `buildEvents`, refreshed by `refreshStartMarkers` from `renderStartingPositions`) draws box + `textLabel` (+ `startArrow` for the player) for player/boat/ship/airship, not pickable.
- **Effect preview to scale, instance scaling, facing occlusion (runtime revision `20260829.35`).** `AnimationPreviewLayer` projects Effekseer with `q = screenHeight / 384` (the game's unit = screen height / 26 rule; `setScreenHeight`, default from the project's System), draws MV cells at 1:1 (was 0.85) and lets the overlay grow to 8192 px (the 1024 cap shrank effects on big models). Runtime `effectInstanceScale(object)` = object scale × span / `DB_PREVIEW_SPAN` (1.6) multiplies an anchored animation's axes (`MapEditor3D` does the same for prop effect plays). `effectFacesCamera` hides an anchored sprite whose anchor sits within `EFFECT_FACE_DEPTH` (20%) of a side face (never the underside) when that face's normal points away from the camera, with ±0.05 hysteresis; `occlude: false` opts out. Demo's Core effect retuned 0.58 → 0.22 to keep its look under the corrected preview.
- **Look up over the shoulder, Escape calls the menu (runtime revision `20260829.35`).** A negative third-person pitch no longer orbits under the figure: `place` keeps the eye at the focus height behind the figure and pitches the view up, `resolve` slides the distance in by up to `LOOK_UP_CLOSE` (75%) toward `MIN_THIRD_PERSON_DISTANCE`; the floor clamp is gone. `pointerlockchange` that loses the lock with the window focused is the browser-swallowed Escape: `lockReleased` sets `Scene_Map.menuCalling`.
- **Look up, unlocked mouse, head lean, floor clamp (runtime revision `20260829.34`).** `LOOK_PITCH.thirdPerson` is `[-25, 80]`; `mousemove` turns the look from client deltas when the pointer is not locked (`requestLook` asks for the lock on a key press or click); `Reactor3D.applyLookLean` pitches the head bone (else the body) by `Camera.lookLean()` (60% of the look past the default pitch, ±30°) after the animation pass, since a clip writes every bone each frame; `keepAboveFloor` keeps the third-person eye `FLOOR_CLEARANCE` (0.3) above the higher of the ground under the figure and under the eye by sliding in to `MIN_THIRD_PERSON_DISTANCE` (1.5) and then levelling the pitch. The game context supplies `groundAt`.
- **Mouse look + camera-relative WASD (runtime revision `20260829.31`).** The camera module keeps a `look` (yaw/pitch, seeded from the facing, driven by pointer-lock `mousemove` at 0.15°/px, pitch clamped per mode) that `resolve` uses for the player-relative modes; `held` keys (WASD/arrows) map through `relativeMove()` to a map direction or diagonal pair relative to the camera yaw, and the `moveByInput` wrapper walks it (`moveDiagonally`, falling back to either axis); first person turns the body to the look when still. Touch destinations are cleared in these modes. Listener install is guarded for sandboxes without `document.addEventListener`.
- **Collision follows the model's geometry by default (runtime revision `20260829.30`).** `readModelCollision` defaults to `mesh` (`collision: "box"` opts a model out); `modelCollisionMask` rasterises triangles below walking height at `COLLISION_SUBDIV` = 4 cells per tile (vertices, sampled edges, cell-centre fill) and answers `mask.touches(x, z, radius)` for a walking body of `COLLISION_BODY_RADIUS` 0.34 tiles, which `eventModelContains` uses instead of whole-tile tests; footprints are built under the loading fade in `preloadMapModels` (a three-million-triangle model takes ~300 ms once). Demo reactor: 117 blocked tiles as a round base vs 173 for its box; ~1 µs per check.
- **Video effect size is a fraction of the model (runtime revision `20260829.29`).** `video.width/height` are fractions of the model's longest side (`videoEffectFraction`, with values above 4 read as pixels ÷ 96 so saved effects do not explode); `videoEffectSize(effect, glbSize)` gives the plane in model units for the runtime's `anchor.size` and both editor previews.
- **Video effects are sized in model units (runtime revision `20260829.28`).** `video.width/height` are the model's own units, like the anchor offset; the runtime sends `anchor.size` with the surface and `updateThreePosition` scales the plane to units × the model's world scale over its tile-sized geometry, and both editors parent the preview plane to the model at that size, so the screen matches across the database, the map editor and the game.
- **3D database sidebar: folding sections, click-off deselect, rename without replay.** Parts / Animations / Effects headers fold their bodies (`toggleSection`); a click on a list's empty space deselects (effect → `_leaveEffectMode`, rule/part → `deselectPart`); the state-triggered effect preview is keyed by list index, so typing a new name no longer restarts a playing video.
- **Anchored effects scale with the world (runtime revision `20260829.27`).** `placeStandIn` gives the stand-in a `reactor3DScale` of (screen pixels one tile covers at the anchor) / tile size, which `Sprite_Animation.reactor3DScale` averages exactly as it does for characters, so an effect shrinks into the distance with its model instead of playing at flat pixel size; the model's yaw joins the effect's rotation. Both editors' overlays use the same rule (384 native pixels span eight tiles) and add the model's yaw.
- **Video effects, mesh collision, relative controls, editor playback (runtime revision `20260829.26`).** Effects gain `type: 'video'` + `video: {file,width,height,loop,audio,volume}`; `spawnVideoEffect/stopVideoEffect` drive `RPGReactorVideoSurfaces.manager.show/stop` with an `anchor` the surface descriptor now carries (`normalizeShowArgs`), and `updateThreePosition` places an anchored surface at `effectAnchorWorld` turned with the model (`Reactor3D.modelHolderFor`). `readModelCollision`/`modelCollisionMask` (triangles below `COLLISION_WALK_HEIGHT` rasterised into cells in the body frame, cached per model+pose; `_sidecarJson` keeps model.json for the synchronous footprint) feed `eventModelFootprint.mask` and `eventModelContains`. The camera module wraps `Game_Player.moveByInput` for third/first person. `playModelAnimation(character, name, { repeat })`; props carry `repeat`. The holder loop now runs effects outside the rules gate (models without rules never fired them) and `updateAnchoredAnimations` restarts loops after its pass with `MAX_ANCHORED_PER_MODEL` (the in-pass restart recursed through `spawnAnchoredAnimation`). Editor: `RREventPreviewModels.animate` (carve/rig + binding + rules) and `MapEditor3D.animateModels/playModelEffects/updateEffectPlays` run rules, Always effects and a prop's chosen effect in the 3D view (animation overlays via `RRAnimationPreviewLayer`, video planes); `propAt` picks by `Box3` with hover throttled; `ModelPropsManager` undo/redo snapshots, Repeat checkbox, every rule listed; `Database3DEditor` effect Type/video fields, `_playVideoPreview`, Transform-card collision checkbox, `mergeSidecar(..., collision)`.
- **Props panel via the model picker (runtime revision `20260829.23`).** `ModelPropsManager.openModelPicker` shows `ModelGraphicPicker`; the result's size/yaw/pitch/roll become the prop's (`fields` carry the pose; the selected prop is updated in place). `_modelChoices/_fillChoiceSelects` list the chosen model's action rules and effects; props gain `animation`/`effect` (sidecar + `Reactor3D.normalizeProp`), queued by `installPropHooks` after `setupEvents` through `playModelAnimation`/`playModelEffect`. The inline model list is gone.
- **Effect triggers, per-axis scale, global steppers (runtime revision `20260829.22`).** `Reactor3D.readScale/scaleAxes` accept a number or `[x, y, z]` for `transform.scale` and effect `scale` (free scale patches the anchored sprite's `updateEffectGeometry` to call `setScale` per axis after the stock pass; the preview layer scales the same way). Effects gain `trigger`; `updateTriggeredEffects` spawns a looping anchored animation while the state holds and `stopAnchoredAnimation` ends it (entries carry `triggered`). Editor: `_fxPreviewDef` is the live working effect so anchor placement/drag and sliders move the preview at once; `_scaleSlidersHtml/_bindScaleSliders` (Proportional checkbox ↔ X/Y/Z) shared by the effect and transform cards; Play-when select on the effect card; `_updateTriggeredEffectPreview` runs state-triggered effects from the sim. `utils/NumberSteppers.js` wraps every `input[type=number]` (MutationObserver for later ones; `data-no-stepper` opts out) and theme.css hides native spinners.
- **3D editor card rework (runtime revision `20260829.21`).** `utils/SearchSelect.js` (grouped, searchable dropdown; `RRSearchSelect.create`) replaces the card's native part select (`_mountCardChooser`/`_onCardChooserPick`: `__model`, part/bone names, `fx:<name>`). Card modes: `_renderEffectCard` (Offset/Rotate/Scale sliders in the model's native units and degrees, Loop, Place toggle, Play, Save) and `_renderTransformCard` (Transform | Animations tabs; base transform sliders, Clear, Save transform) beside the existing pose card; the card sits at `top:10px`. `Reactor3D.readModelTransform/applyModelTransform` wrap every instance's children on a `base-transform` group (all five runtime instance sites, the editor viewport, event previews); `mergeSidecar(..., transform)` writes model.json `transform`. Effects gain `rotate` (degrees) applied with `scale` on a copy of the animation record in `spawnAnchoredAnimation` and live in `RRAnimationPreviewLayer.setTransform`. Rules gain `repeat` (runtime restarts the action on its `until`; an empty Play Model Animation name stops it; the editor preview loops repeat and non-on-demand rules). The effect anchor marker drags in the camera plane (`fxdrag`). Sidebar headers use `.sidebar-header`.
- **Model effects (runtime revision `20260829.20`).** `Reactor3D.readModelEffects` reads `effects` in model.json (`{ name, animation, anchor: { part, offset }, scale, loop, se, flash }`); rule timelines accept `{ at, effect }`; `fireModelEffect` resolves named effects; `playModelEffect/takeModelEffects` queue by character; `spawnAnchoredAnimation` plays a database animation through `Spriteset_Base.createAnimationSprite` aimed at a hidden stand-in sprite that `updateAnchoredAnimations` moves to `effectAnchorWorld` (the anchor part's or model's frame plus offset) projected through the map camera each frame, looping when asked; flat maps fall back to the character. `PlayModelEffect` plugin command. Editor: `Database3DEditor` Effects list/form (name, animation via the animation picker, anchor part/bone, offset, Place on model = `fxanchor` tool click → local offset in the anchor's frame, scale, loop, SE via the audio picker, Play, Save), a pink anchor marker, `utils/AnimationPreviewLayer.js` (transparent MV/Effekseer overlay moved to the projected anchor), named effects in rule timelines with preview, `mergeSidecar(..., effects)`. `PlayModelEffectEditor` (dropdown of the target model's effects) wired like Play Model Animation. Animation picker: black default backdrop, no scrollbar; the palette's props tab reads 3D-M.
- **Model props (runtime revision `20260829.19`).** `RRMapElevation.props/propById/addProp/updateProp/removeProp` keep `reactor3d.props` (`{ id, name, ext, file, texture, x, y, z, yaw, pitch, roll, direction, size, scale, passable }`, position in tiles, fractional allowed, angles in degrees, `size` the model's longest side in tiles). `Reactor3D.installProps` (called from `DataManager.loadMapSidecar`) stands each prop in `$dataMap.events` at `PROP_EVENT_BASE + id` (10000+) as a model-bound event (`reactor3d.events[id][0]` gets the model spec) so rendering, facing, pose and footprint collision are the existing event-model machinery; `installPropHooks` (installed from `reactor_sprites.js`) sets the free `_realX/_realY`, `_reactorLift` and pins `isMoving` false so the position does not slide home; model instances add the lift to their ground height and flat-map sprites rise by it. Editor: `ModelPropsManager` (palette M tab panel, PIXI sprite layer drawn with the Preview Event thumbnails, tile-snapped drag, Delete, selection sync), `MapEditor3D.buildProps/refreshProps/propAt/groundPointAt/selectProp/pickPropRing/dragPropRing/dragPropTo` (free placement, ground drag, pose rings), `utils/PoseRings3D.js` (the ring gizmo shared out of the video-surface code), `TilesetPaletteViewer.tabIcon('model3d')`. Sixteen `props.*` strings hand-written in all 18 locales.
- **3D camera modes (runtime revision `20260829.18`).** `Reactor3D.Camera` (in `reactor_3d.js`, also `RPGReactorCamera3D`) holds `MODES` (fixed/topDown/isometric/thirdPerson/firstPerson with pitch/yaw/fov/distance/focus/lift), `normalizeState`, `mapDefault` (`reactor3d.camera` gains `mode`; older `{pitch,yaw,distance,fov}` sidecars still read as fixed with overrides), `normalizeArgs`, pure `resolve(state, context)`, `step` (ease-in-out over frames, shortest-arc yaw, `TURN_RATE` follow for player-relative modes), `place` (first person positions the camera at the eye; other modes go through `aimCamera`), and `update(spriteset)`, which `Spriteset_Map.updateReactor3DCamera` delegates to. `Game_Map.setup` resets the state to the map default unless `$gameSystem._reactorCamera3d` keeps a commanded one; `Game_Map.reactorCamera3D()/setReactorCamera3D(state, duration, keep)`; interpreter wait mode `reactorCamera3D`; `Reactor3D.characterHiddenByCamera` hides the party's sprites, billboards, and models in first person. Hooks and the `ChangeCamera3D` plugin command are installed from `reactor_sprites.js`, after the classes they wrap exist (`reactor_3d.js` loads before them).
- **Map Properties chrome.** Field labels are 12.5px semibold (`.map-props-label`), every number field (Width, Height, Encounter Steps, Room Height, camera Pitch/Yaw/Distance/FOV) and the Change 3D Camera dialog's numbers wear the themed `rr-number-stepper` from the Video Surface editor (blank camera fields step from the mode's placeholder value), and Browse buttons share the accent `map-props-btn primary` style with Choose and Add.
- **Change 3D Camera editor + Map Properties Default Camera.** `event/commands/Camera3DEditor.js` (mode, pitch/yaw/distance/FOV with mode placeholders, focus + event, duration, wait, keep; `Camera3DEditor.build` is the serializer) is wired into the picker's 3D section, `EventCommandList._reactorCommandEditor`, and the Common Event editor's insert/edit paths. `RRMapElevation.camera/setCamera` store the map default (dropped when it is the stock view); `map-3d-camera-*` controls read/write it through `saveMap3DSettings`. Twenty-eight `cam3d.*`/`mapProps.defaultCamera` strings and the command name hand-written in all 18 locales, including the Polish reviewed catalog.
- **Map room (runtime revision `20260829.17`).** `Reactor3D.roomFor`, `roomImageNames`, `clampRoomHeight` read `reactor3d.room` (`{ height, floor, walls, ceiling }`, height 1-512 tiles like the map size, default 4). `MapScene.build` calls `addRoom` after the parallax grounds: floor a layer step under the parallax (tiled at image scale), ceiling at `height` facing down, four `FrontSide` walls facing inward whose image height is the wall height and repeats along it by aspect. Meshes are tagged `userData.roomPiece`, `renderOrder -2`, `__reactorShaded`. `MapScene.clear` bumps a `_build` token so a bitmap that finishes loading after a rebuild cannot lay a piece into the new scene. `RRMapElevation` gains `room`, `setRoom` (dropped when all defaults), `setMode3D`, and keeps a sidecar that holds only a room. `MapEditor3D.loadParallaxes` fetches the room images through the same loader.
- **Map Properties 3D section and audio picker flow.** `map-3d-checkbox` mirrors `RRMapElevation.hasNote` and is stripped from/added back to the note on save (a typed `<3d>` still counts); `saveMap3DSettings` writes the sidecar through `TilemapManager.saveMapSidecar` for the loaded map or `RRMapElevation.save` for any other (`editMap` loads the sidecar for a map read from disk) and refreshes the 3D view when the room or switch changed. BGM/BGS keep `{name, volume, pitch, pan}` in `_mapAudio`, rendered as a track row plus levels line, and `openMapAudioPicker` opens `RRAudioPickerModal` with the levels cards; `populateAudioDropdown`, the preview transport, and the inline steppers are removed. `fillParallaxSelect` serves the parallax background and the three room images. Fourteen `mapProps.*` strings hand-written in all 18 locales.
- **GitHub #6, Resource Manager.** `ResourceManager` provides a locale-sorted searchable recursive catalog for all MZ image/audio categories, `effects/`, `movies/`, `fonts/`, `icon/`, and Reactor `3d/`. It retains logical and physical identities for plain, encrypted, case-sensitive, nested, and same-name multi-format assets. Image/audio/video/font previews use the shared resolver; `ModelPreview3D` reuses lazy `MapEditor3D` library acquisition plus `Reactor3D.readModelAsync`, template cloning, camera aiming, sibling textures, orbit/zoom, idle throttling, pako-backed archives, generation cancellation, and dedicated-context disposal. Ctrl/Cmd and Shift batch export preserves nested paths and decrypts selected files. Ordinary desktop imports validate names/extensions/signatures, collision approval, symlink-free ancestry, project identity, and the live lock before `RRWriteFileAtomicSync`. The deletion-disabled 3D category separately allows one validated model import into a newly named nested folder: GLB/OBJ/FBX/STL/USDZ/3MF/DXF geometry is staged as `source/<case-preserved file>` with empty `textures/`; `.blend` is refused with export guidance; embedded GLB dependencies, triangle accessors, source/staging identities, ownership, an exact destination reservation, and a per-destination lock are checked before publish. It never merges/replaces an existing folder and rolls back owned staging/empty parents on write, commit, or concurrent-destination failure. Web remains browse/preview/export-only.
- **Native Video Surface commands and live authoring.** `ShowVideoSurface`, `TransformVideoSurface`, and `StopVideoSurface` remain code-357 `RPGReactor` commands, isolated from stock 261 and `PSYCHRONIC_VideoOverlay`. Runtime PIXI handles 2D/all screen targets, projectively warps corners, leaves Z to the 3D view (the dragged position is the whole 2D placement), and culls in screen pixels; rectangular Three.js planes handle 3D map/event/player targets with Z/world-camera culling and ignore corners. Both retain generations/waits, anchors, autoplay retry, same-map time, and deterministic cleanup. `VideoSurfaceEditor` authors layer/opacity, audio/playback rate, loop/wait, scanlines, culling, position/size/rotation/scale/depth/Z, and corners. Preview PIXI warps projectively, Three.js stays rectangular and applies culling, and 3D-screen DOM reshapes only a bounding-box clip; no editor preview applies scanlines. Every preview moves directly and shares anchors, field synchronization, lifecycle cancellation, and exact source navigation. Pages reduce independently without page-condition/runtime-flow evaluation; preview media is muted/looping. Live map previews start on a placeholder and swap to the movie once a frame has decoded (PIXI's load restart aborted the early `play()`, leaving a transparent panel); edge handles resize along their normal while corners warp; authoring pans the surface into the part of the viewport the side panel does not cover, and the panel drags by its title. Changing the target converts X/Y so the surface stays where it is (screen pixels, map tiles, or anchor offsets), Event sits beside Target and is editable only for Selected Event (otherwise greyed, naming this event, Player, or None), other fields show only what the target uses, and the live panel can pop out into its own window for a second monitor. Saved surfaces are clickable on the map: a click opens the panel on their Show command (OK writes it back, Go to Event jumps to it in the event editor), and the event command list names Reactor commands by what they do instead of Plugin Command. 2D previews now stack among the editor's tile layers the way the game sorts them by Layer (below every tile when negative, between lower and upper tiles up to 3, above upper tiles at 4, above characters from 5) and re-stack live as the field changes; the panel's numeric fields use the themed step buttons. Map, event, and player surfaces now stand on their anchor in 2D as they already did in 3D (runtime revision `20260829.5`), the 3D drag keeps the grabbed point under the cursor, editor previews draw the authored scanlines on every backend at the same 1px-every-other-pixel pitch as PSYCHRONIC_VideoOverlay (0.5 matches the plugin's strength), typed values snap to their range instead of failing validation, and a Video box beside A1/Grid/3D hides saved previews. Preview Event (event right-click) draws a chosen page's graphic at its real size and position under the marker, remembered per event in the map's Reactor sidecar. In the 3D view, surfaces on layer 5 and up now render in the game's above-characters overlay pass, composited over star tiles and world geometry alike, instead of being depth-tested inside the star-tile pass. Depth now also pushes a surface toward the camera in 3D (tiles, positive = closer) while the 2D feet row stays the reference; in 2D it remains the within-layer sort key. Preview Event now works in the 3D view too (the previewed page replaces page 1's billboard), plays stepping animation at the game's cadence in both views, and previews 3D-model pages as the placed model in 3D and a front-facing render at footprint size in 2D. 3D-model events now work on maps that are not rendered in 3D: the runtime draws them as sprites, a front orthographic render of the model at footprint size that turns with the character and plays its animation rules (runtime revision `20260829.6`), which is exactly what the 2D Preview Event shows. The map note is now the only 3D switch (a sidecar can downgrade a `<3d>` map to 2D but never promotes a map without the tag), and the sidecar loads for every map on disk so a flat map still gets its event models and previews (runtime revision `20260829.8`); sprite-mode models use the scene's exact pose and animation driver, and the 2D video backend no longer aborts its own load (the video played in 3D but failed silently in 2D). Model sprites on flat maps (and their editor previews) are now rendered from the map's 55-degree pitch, the angle the 3D view defaults to, framed about the model's ground origin so turning never shifts it, with that origin on the tile centre: a car facing right shows roof and side, facing down shows hood and windshield, instead of an eye-level cutout lying on the floor (runtime revision `20260829.9`). Flat-map model sprites paint on the standalone renderer and never acquire the shared 3D viewport (its PIXI state reset was breaking 2D lighting plugins after a scene change and tripping three's texImage3D setup); three now starts on PIXI's context with the unpack flags cleared; culled character sprites keep their position current so plugin sprites they steer (lights) no longer freeze off-screen; the v5 Graphics API (beginFill, drawRect, ...) is provided by silent compat shims instead of PIXI's nagging ones (runtime revision `20260829.16`). The Demo's PSYCHRONIC_RaveLighting no longer re-attaches beam sprites a scene change destroyed. PSYCHRONIC_MenuManagerMZ (Demo and Star Shift Freelancers) gates its layout/font diagnostics behind a new Debug Logging parameter, off by default. New event priority Above Characters (Sorted by Y): the page draws on the above layer like Above Characters, but a character standing in front of it and overlapping it is lifted onto that layer for the frame and y-sorted over it, while one behind stays under it (runtime revision `20260829.16`). It fixes the classic MZ clip where a tall party walks into a console that has to sit above the tiles. The 3D view is unchanged for now: there the page is an ordinary depth-tested billboard, the same as Above Characters. In the 3D view, the surface being authored now carries the model picker's pose rings (yaw green, pitch red, roll blue) around it; dragging a ring writes the matching rotation field, so a video is turned to its angle in place instead of by typing. Turning the 3D view off while a Preview Event model was on the map no longer aborts the scene teardown (which left the 3D box stuck and any open video panel with it). The sidebar's Tileset Palette, Events, and Maps headers carry the accent strip the Plugin Manager and event editor sections use. Runtime revision `20260828.3` adds `reactor_video_surfaces.js` to the boot manifest across all ten bundled projects.
- **PNG-compatible modern image pipeline.** `RRAssetFiles` keeps PNG extensionless while preserving explicit JPG/JPEG, WebP, safe SVG, and GIF for converted battleback, parallax, enemy-detail, title, animation, event character/face, Change Actor Images, Change Vehicle Image, System 1 vehicle, Picture, Resource Manager, Reactor UI Picture, and runtime paths. Explicit names retry legacy `<name>.<ext>.png`. Import validates signatures and rejects SVG scripts, handlers, external references, animation, styling, and URLs. Runtime preserves formats/encrypted MIME and refreshes animated GIF textures. Tilesets, plugin fields, database actor/list thumbnails, Reactor UI character/face/party-face/System/Title/Icon sources, balloon sheets, and fixed system sheets such as IconSet remain PNG-oriented.
- **Trusted native codec overlays.** Exact NW.js release matching, checked-in archive hashes, single-binary content validation, separate `.nw-codec-cache` storage, and clean official-runtime acquisition when disabled prevent stale overlays from contaminating output. Full desktop packages default to the overlay and carry `rpg-reactor-codec.json`, `RPG_REACTOR_CODEC_NOTICE.txt`, the complete LGPL 2.1 text, immutable `nwjs-ffmpeg-prebuilt`/FFmpeg source revisions, hashes, and patent guidance; Minimal and Web packages remain codec-free.
- **RPG Catalyst community forum links.** `https://rpgcatalyst.com` appears as an `external-link` in the HTML Help submenu, projectless welcome screen, and About link group; the NW.js native Help menu has the same entry through `nw.Shell.openExternal()`. The proper product name is registered across all localization dictionaries, and shell coverage verifies all three HTML placements plus native-menu browser dispatch.
- **GitHub #27, multiple enemy action conditions.** Enemy action patterns now author an AND list through a checklist for Turn, HP, MP, TP, User State, Target State, Party Level, and Switch. Named parameters replace Param 1/2, range fields use exact clamped percentages, state/switch rows preserve orphan IDs, and summaries show every condition with accurate turn recurrence. Target State evaluates every living/dead candidate reachable by stock scopes 0-14 before target selection; single-target skills are gated but retain normal target choice. Runtime dispatch and all eight helpers live on `Game_Battler` for actor auto-battle compatibility, malformed list records fail closed, and empty lists remain Always. Legacy triples remain the fallback and mirror the first saved entry; unsupported plugin types, extension fields, duplicate records, and Cancel survive editor interaction. Four phrases are reviewed in all 17 non-English locales. Runtime revision `20260828.2` refreshes the synchronized bundle.
- **Animation capacity and stable timing layout.** The workload-safe Animations ceiling rises from 1,000 to 5,000 throughout database growth and event-command ID entry. SE/Flash timings now occupy an independent right-side scrolling column so long timing lists no longer stretch or reshape animation Properties and preview controls.
- **One album-art cache for every editor audio surface.** `RRAudioCoverArt.forFile()` centralizes extraction and Promise caching for the main Audio Player, `RRAudioPickerModal`, System 1 music, all 24 system-sound slots, event-command audio, and plugin audio parameters. Picker headers and selected rows request art eagerly while unselected rows remain lazy; embedded images and deterministic placeholders retain their existing formats.
- **GitHub #12, system-sound variant pools and pitch ranges.** All 24 `System.json` sound slots now open a dedicated draft modal: the stock primary plus any number of uniformly selected variants, each authored through the shared recursive audio picker with its own volume/pitch/pan, and an optional absolute random pitch range clamped to 50-150. Add/remove/reorder and nested picker Cancel remain local until the outer OK; unknown slot and variant keys survive confirmation. System 1 exposes the seven formerly hidden slots Miss through Use Skill and summarizes pool size/range in its table. `SoundManager` resolves and preloads every valid candidate, copies only when applying random pitch, and preserves exact stock behavior without consuming RNG when no extension is configured. The inline `variants` and `pitchRandom` keys deliberately leave the stock primary fields intact, so older Reactor and stock MZ play that primary; RPG Maker MZ's own editor can drop the optional Reactor keys when it rewrites `System.json`.
- **GitHub #10, editor-only database names.** Actors, Classes, Skills, Items, Weapons, Armors, Enemies, and States gain an optional Editor Name injected by the shared database detail hook. The id-keyed `data/Database.names.json` sidecar stays out of RPG Maker records and game deployments while participating in Cancel, Apply, dirty-state, list Undo, pruning, duplicate, and version-3 cross-project database clipboard payloads. Search always includes both names; Options chooses Editor Name first, Game Name first, or Game Name only, and typed plugin database references use the same labels. The sidecar is dormant when absent, preserves unknown top-level keys, refuses to overwrite malformed content, and saves before normal database files so a rejected sidecar cannot leave them partially written. Database dialog saves now route through `ProjectController.saveProject()` so Web IndexedDB writes are flushed before success.
- **User Interfaces editor UX redesign.** `DatabaseUserInterfaceEditor` keeps the searchable ARIA Use As combobox and pointer-derived Custom semantics in a 44-56px toolbar on wider layouts, using a compact 70px two-row toolbar at 1280x720, with Name, Presentation, Interface Settings, and Playtest. Background, initial focus, cancel, overlay visibility, transitions, duration, and Note now render and update through concise Inspector groups. At 1280x720 the Inspector starts as a closed contained drawer whose current state is Interface Settings; selecting a layer or pressing Interface Settings opens it. Detail panes wider than 1050px keep all three panels in-row, while one-column reflow begins at 620px. The bounded workspace always fits height in both dimensions. Layers retain Back -> Front ordinals, subtree reorder/reparent, keyboard movement, and explicit Game Reference imports, with shorter visible order labels and full accessible names.
- **Typed Lists, named contexts, actor bindings, and display-only overlays.** `LIST_SOURCES` now covers `party`, categorized `inventory`, actor `skills`, `actorParameters`, `actorEquipment`, `actorStates`, `options`, `saveSlots`, `variableRange`, and `literal`. Rows carry source-qualified keys plus typed identity/display fields, publish immediately under an authored `contextName`, survive refresh by key, and can store ID/value before their action. Text, `partyFace`, Gauge, and actor Lists share `partySlot` / `actorId` / `menuActor` / game-variable actor ID / named-context binding. `{actor.*}` spans identity, level/profile, resources, EXP, and parameters; `{context.*}` spans typed row details. `Window_ReactorUIList` remains a real `Window_Selectable`. Overlay records attach only to `Scene_Map`, reevaluate visibility, and deliberately expose no focus or activation path.
- **Gauge expansion and complete generated baselines.** Gauge kinds now include HP/MP/TP, level-relative EXP, MHP/MMP, ATK/DEF/MAT/MDF/AGI/LUK, and variable; maximums may be authored or variable-backed, values format as current/current-max/percent/hidden, and label, two bar colors, back color, and bar height are authorable. `RRStockInterfaces.KINDS` appends stable IDs 4-7 without renumbering the first three: 1 Title Screen, 2 Main Menu, 3 Game End, 4 Status, 5 Options, 6 Save, 7 Load. Existing interface files are unchanged; tracked Demo records match generation and all seven System binding fields remain absent by default. This completes and supersedes the initial three-baseline stage recorded below.
- **Functional Options and asynchronous Save/Load adapters.** `options` rows expose MZ's booleans and four volume symbols; left/right clamps by 20, confirmation wraps/toggles, the row refreshes, and direct or replacement Options interfaces persist `ConfigManager` on termination. `saveSlots` rows expose slot title/playtime/date/party metadata plus existing/enabled state. `saveSlot` and `loadSlot` deactivate input and reject duplicate activation while the Promise is pending, preserve stock before-save/map-reload/after-load sequencing, reactivate with a buzzer on failure, and transition only after success.
- **Seven post-plugin-safe replacement roles.** `ReactorUI.REPLACEMENTS` is exactly Title, Main Menu, Status, Game End, Options, Save, and Load. Records carry explicit `roles`; zero, missing/malformed, overlay, ID-mismatched, and role-mismatched selections route stock. `installSceneRouting()` wraps the latest plugin-provided `SceneManager.goto/push` again at boot and immediately before `SceneManager.run`, with recursion-safe stock bypass. Main Menu's actor context opens stock Skill/Equip or the configured Status role. Item, Skill, Equip, Shop, Formation, Name Input/message inputs, and Battle have no replacement role until dedicated workflow adapters exist.
- **Typography, nine-slice, control states, focus overrides, and transitions.** Text/Button/List typography adds font face, bold/italic, outline color/width, and letter spacing. Nine-slice is normalized off except for Picture/System images. Sparse focused, pressed, and disabled overrides inherit base appearance; directional Up/Down/Left/Right targets win when valid and otherwise fall back to geometric navigation. Interfaces support `none`, `fade`, and `slideLeft` open/close transitions with input locked until opening completes and one-shot close completion; overlay visibility supports fade only.
- **Capture is documented as a visual draft.** The draw log can generate recognized Box/Text/Image/Button/Gauge nodes or import a window bitmap as a Picture, but it cannot infer arbitrary plugin behavior, direct canvas operations, complete transactions, touch controls, or every subclass override. Shop and Battle capture remain visual references, not replacement adapters. Capture itself only updates the editor cache/reference; node imports remain unsaved database edits, while the explicit Picture fallback immediately copies its PNG into `img/pictures`.
- **Real Web and NW.js save smokes.** `tests/smoke` contains a dependency-free W3C WebDriver client. The Web smoke builds and serves the real browser editor, gates `RPGReactorHost.flush`, proves `ProjectController.saveProject()` awaits it, verifies the IndexedDB record, reloads, and checks restoration. The NW smoke launches the actual editor through its matching SDK ChromeDriver, loads a temporary mapless project, saves through `ProjectController`, and verifies the native file. They run outside `node --test` through `npm run smoke:web` / `smoke:nw`; CI downloads and hash-verifies the pinned 0.107.0 SDK before running both under Xvfb.
- **Validation.** The complete suite discovers **1,954 Node tests**, adding Resource Manager model validation/staging/reservation/rollback, modern image imports and runtime consumers, Video Surface serialization/scanning/backends/waits/lifecycle/live-authoring contracts, actor-preview disposal, codec hashes/provenance/legal packaging, and expanded localization coverage. All 1,954 pass with the new files tracked. Source syntax, `npm audit` (zero vulnerabilities), runtime-focused suites, and `git diff --check` pass. Existing real Chromium/NW.js persistence, NW.js layout, and read-only State/Animation screenshot results remain valid. Manual GIF playback, live 2D/3D Video Surface authoring/navigation, actor-preview performance, and final toolbar-icon checks remain open.
- **Historical interface milestones below.** The following capture-to-node and three-baseline entries record earlier stages from this cycle. Automatic capture import was superseded by the explicit Starting Layout/Add to Front actions above, and the original three generated records were extended to seven without renumbering them.
- **Capture → nodes, Gauge node.** Runtime: `ReactorUI.installCaptureHooks()` (from `beginCapture`, after plugins) wraps `Bitmap.prototype.drawText/blt/fillRect/gradientFillRect/clear/clearRect` into a per-bitmap `__rrDraws` log and, with the primitives suppressed underneath, the semantic draws: `Window_Base.drawTextEx` / `drawItemName` (text with codes), `drawCurrencyValue` (`\GOLD` + `\C[16]\G`), `Window_StatusBase.drawActorName/Class/Nickname/Level/Face/Character` (`\P[n]`, `\PCLASS[n]`, `\PLV[n]`, `partyFace` by slot), `Window_Command.drawItem` (a `button` element with the command symbol and enabled flag). `performCapture` writes `windows[i].elements` (window pixels) from `windowElements` — contents log through `elementsFromDraws` (text primitives merged per line into one run with `\C[n]`/`\I[n]` by `mergeTextRuns`; a lone run wider than its max width keeps the width and gets `fitText`; y re-expressed for a text node's line box), inner children and plugin-added child sprites through `spriteElements` (`Sprite_Gauge` → gauge element, `Sprite_Name` → text, image sprites → image by URL folder, drawn sprites → their log) — and `elements` for the scene's own sprites (title art, the drawn game title; window layer, spritesets, the background snapshot and `Sprite_Button` skipped). `imageSourceFromUrl` maps `img/<folder>/` to a node source; sub-frames of system sheets are dropped. Editor: `nodesFromCapture(data, {which, includeScene, nextId})` / `nodeFromElement` (pure, tested) build a Box per visible window (opacity 0 → `fill: none`, never a faded box) with children; `actionForSymbol` wires stock symbols; `addAllFromCapture` runs automatically when a capture lands in a record with no nodes, the list header has Add All, rows have Add (and Picture for windows with a contents PNG: copied to `img/pictures/Capture_<class>[_n].png`, Image node at the client area). Gauge node (`gauge` hp/mp/tp/variable, `index` party slot, `variableId`, `max`, `label`, `showLabel`, `showValue`; 128×24 default): runtime `Window_ReactorUINode.drawGauge` adds a `Sprite_ReactorUIGauge` (lazy subclass of `Sprite_Gauge`, sized to the node, bar = lower half, fonts scaled by height/24, variable kind reads `$gameVariables`) as an inner child with its opacity mirrored from `contentsOpacity`; the editor canvas draws it from the skin colours (19 back, 20/21 HP, 22/23 MP, 28/29 TP). Image sources gain `title1`/`title2`. `Window_ReactorUINode.convertEscapeCharacters` now runs `convertPartyCodes` first, so `\PCLASS[n]` draws as the class (it drew `[n]`: `currentText` converted, `drawTextEx` did not). Fourteen phrases added by hand to all 17 locales. Live rig `ui-capture-nodes.mjs` (session scratchpad): Demo menu → 48 nodes (8 buttons wired, `\C[6]`, `\PCLASS[n]`), title → 6, both booted as previews.

- **Stock interface baselines.** A project that has never authored an interface (no `data/UserInterfaces.json`) used to open the User Interfaces section on an empty list; it now opens on three records generated from its own settings by `src/utils/StockInterfaces.js` (`RRStockInterfaces.build(data)`, seeded in `DatabaseManager.loadProject`, written with the next save): Title Screen, Main Menu and Game End, laid out with the runtime's own window math (`Scene_Title` / `Scene_Menu` / `Scene_GameEnd` rects against `advanced.uiAreaWidth/Height`, `titleCommandWindow` offsets), buttons named from Terms > Commands and gated by System > Menu Commands, wired to the stock actions (New Game script, Continue → load enabled by `DataManager.isAnySavefileExists()`, Save enabled by `$gameSystem.isSaveEnabled()`, To Title, Cancel). Each record carries `stock: "<kind>"`. The game keeps its stock scenes until a record is called or set as the boot interface, so seeding changes nothing at play time. For the Main Menu's party area the runtime gains an image source `partyFace` (the face of the party member in slot `index`) and party codes resolved before the stock escape codes: `\GOLD`, `\PLV[n]`, `\PCLASS[n]`, `\PHP[n]`, `\PMHP[n]`, `\PMP[n]`, `\PMMP[n]`, `\PTP[n]` (n = party slot, 1-based; an empty slot reads as nothing); the editor previews both from System > Starting Party (`startingMember`, `startingStat`: class params at the initial level) and `\P[n]` now previews the same way. Two phrases in 17 locales. Verified live on Star Shift Rebellion (YEP terms with their icon codes, 1280×720). Tests: `stock-interfaces.test.cjs`.
- **Interface capture.** Runtime (`reactor_ui.js`): `ReactorUI.captureRequest()` reads `rrcapture=<scene>&rrcapturedir=<encoded dir>` off the launch line (`launchTokens()` unifies `location.search` and `nw.App.argv`, split on `&`); `Scene_Boot.start` hands a request to `beginCapture`: title goes straight to `Scene_Title`; everything else sets up a new game and starts `Scene_Map`, and once the map has run 30 frames `openCaptureScene` snaps the background and pushes the scene (battle sets up the first troop; shop gets the first six items as goods), which is the flow plugins assume (Star Shift Rebellion's `Irina_PerformanceUpgrade` dereferenced the missing map snapshot when the menu was booted directly). The per-frame `updateCapture` is hooked onto `SceneManager.updateMain` at boot, after every plugin and the MV layer have replaced what they replace (a load-time `updateScene` wrapper vanished under MV projects); it accepts MZ's `scene.isStarted()` or MV's `_sceneStarted`, forces `isGameActive` so an unfocused capture window still updates, waits until every window is open (or 180 frames; or captures as-is after 600 frames of nothing starting), wraps `SceneManager.catchException` so a plugin crash inside the scene is written as the capture's error and the game exits instead of the editor waiting two minutes, reads the screenshot by the screen frame rather than the stage's bounds (an off-screen 3D pass sprite made the battle capture's texture too big to allocate), then `performCapture` writes `capture.json` (scene, size, active plugins, windows: class, rect via tree-accumulated x/y, padding, opacities, openness, cursor rect, windowskin) plus `screen.png` (`renderer.extract.canvas` of the stage) and `window-N.png` per contents bitmap, and `SceneManager.exit()`. Editor: `PlaytestManager.captureScene` / `captureMode`; new `src/utils/EditorCache.js` (`RREditorCache.dir(leaf, projectPath, ...)`, same platform rule as the profiles); `DatabaseUserInterfaceEditor` gains the scene select + Capture button with status, `capture()` (saves, clears the folder, launches, polls for `capture.json` up to 120 s), `loadCapture` / `loadLastCapture` (localStorage per project key), the reference layer in `render()` (drawn under the nodes, opacity slider), `renderCaptureList` with a selected window outlined on the canvas, and `addBoxFromCapture`. `DatabaseEditorUI._selectEntryMarkup(type)` renders a first-run panel for an empty User Interfaces section ("No interfaces yet", what the section is for, Create Interface → the list's New, then the new record opened). Thirteen phrases in 17 locales. Verified live: the Demo's PSYCHRONIC menu (43 plugins, 6 windows) and Star Shift Rebellion's YEP menu (167 plugins, 4 windows) captured over their maps, through the launch line and the tab. Tests: `user-interface-capture.test.cjs`.
- **Custom user interfaces, phase 1.** `runtime/reactor_ui.js` (new, loads after `reactor_windows.js` and before `reactor_mv_compat.js` so the MV window wrapper sees its classes) reads the optional `data/UserInterfaces.json` through the same disk-probe path as the 3D sidecar — never through `DataManager._databaseFiles`, whose watchdog would retry a missing file forever. `Scene_ReactorUI` extends `Scene_MenuBase`; every node is a `Window_ReactorUINode` (a `Window_Base` that hides its frame and back unless the node uses the skin), laid out with anchored offsets against its parent's rectangle, so text uses `drawTextEx` and every escape code, and windows-restyling plugins apply. Focus moves by nearest-in-direction with wraparound, touch hover focuses and click activates, `\V[n]` text redraws when its value changes, and a resume stack restores the interface id when a pushed stock scene pops back (SceneManager rebuilds the scene from its class alone). `PluginManager.registerCommand("RPGReactor", "CallUserInterface")` backs the new Reactor-tab command; `test&rrui=N` on the launch line (the editor's Playtest Interface button) is a preview: `Scene_Boot.start` runs `DataManager.setupNewGame()` for the game objects escape codes and actions read, then `goto(Scene_ReactorUI)` with no title and no map; `ReactorUI._preview` paints a black `ScreenSprite` behind the interface whatever its background setting, and closing the root interface (close, closeAll, a common event, the title action) calls `ReactorUI.endPreview()` → `SceneManager.exit()` — the same exit battle test uses — so the preview never falls through into a playable game. Sub-interfaces and pushed stock scenes still pop back to the preview. Text and Button nodes gain `fitText`: `Window_ReactorUINode.applyFit` bisects a font scale (8 steps, floor `ReactorUI.MIN_FONT_SIZE` = 8 px) until `textSizeEx` of the (re-wrapped) label fits every authored dimension, and `resetFontSettings` applies the scale; the editor's `layoutText` runs the same search over `parseText(node, scale)` so the canvas matches. Editor: `DatabaseUserInterfaceEditor` (canvas at `uiAreaWidth × uiAreaHeight` with a Canvas 2D port of the window-skin drawing, node tree, property panel, undo/redo, drag/resize with 8 handles and snap), `CallUserInterfaceEditor`, `DatabaseManager` support that reads an absent file as `[null]` and writes no file for a project with no interfaces, and 62 new phrases hand-authored into all 17 locales. Verified live in NW.js on the Demo's sample interface: boot into it, keyboard path, hit-testing, push/pop of Scene_Save, close to the map, reopen through the 357 path, and the editor's drag, resize, add, undo, and dialogs, with no console errors. A refinement pass then swept the editor at 1280–2560 px, two light palettes, an empty record, and three authored kinds (title screen, yes/no dialog, HUD panel): the property panel became one label|field|label|field grid that collapses pairs when narrow, the workspace stacks below 1300 px of detail width and fits the viewport with panels scrolling inside, the canvas preview loads the project's main font through `FontFace` and uses MZ's line metrics, changing a node's parent or anchor keeps it in place on screen, condition scripts compile once, and cancel closes a button-less interface whose cancel action is Nothing. A third pass built a character sheet (nested columns, a wrapped bio, a label over a portrait, a 3×3 icon grid, a half-opacity group) and added what it needed: word wrap on Text nodes, Image nodes as parents, opacity that cascades to children, parents-first draw order kept on load and through ▲/▼ (which now move sibling subtrees), a same-row preference for directional wrap-around, and a ◐ marker in the tree for conditionally visible nodes.
- **The FPS counter names its renderer.** The F2 counter reads `144 FPS` on a shared baseline with "WebGL", "WebGPU", or "Canvas" centred on its own row beneath, as RPG Maker's meter did. The counter is a flex column that sizes to its content (min-width 90px) instead of absolute offsets in a fixed 90×40 box, so a three-digit frame rate no longer runs into the label. Every inline style now resets the properties MZ's stock `css/game.css` sets on `#fpsCounterBox/Label/Number` (absolute positioning, paddings, 30px heights, a 32px line-height, opacity 0.8) — projects created from MZ carry that sheet, and an inline declaration only beats an id selector for properties it actually sets, which is what had been stacking the text on itself. MV's `Graphics.showFps` shim shows the box as `flex`. The PIXI app initializes asynchronously, so the mode is read on the counter's own update tick rather than at creation; `Graphics.rendererModeName(renderer)` resolves it polymorphically (v8 `name`/`RendererType`, v5–v7 `RENDERER_TYPE` — the numeric values collide, so the string wins and the enum is taken from the loaded PIXI). Covered by `fps-counter-renderer-mode.test.cjs`.

### Fixed

- **Plugin Manager external links and scrollbars.** Parsed plugin URLs now carry the editor's `external-link` marker and `noreferrer`, so `UIManager` cancels NW.js navigation and dispatches the URL through `nw.Shell.openExternal()` to the user's default browser. Every Plugin Manager scroll surface, including the load-order list, details pane, help viewer, Add Plugin list, and nested structure/array editors, reuses `rr-accent-scrollbar`; the shared class now also declares standards-based theme colors and thin width alongside its Chromium gradient thumb.
- **Animation list and asset-picker polish.** The Animation database bypasses the generic 250-row append batches and renders its complete filtered index, so the native scrollbar reflects all 5,000 possible rows without thumb rebasing. Preview battleback triggers delegate to `showImagePicker` with recursive folders, thumbnails, search, alphabet rail, current selection, and `(None)`. Select Effect File keeps its transformed live Effekseer canvas in a wider right pane while replacing the flat list with `RRPickerIndex`, accent scrollbars, folders, Unicode search, keyboard navigation, explicit None, focus containment/restoration, mutation tracking, request-generation race guards, and complete pending/loaded effect cleanup. Pick SE delegates to `RRAudioPickerModal` with cover art, seek, transport, nested folders, and volume/pitch/pan; timing forms author and preserve pan in both MZ and sprite data. Duration replaces Chromium's native spinner with themed up/down controls. The shared image picker's Open in Folder action uses the secondary-button theme.
- **Futuristic tool icon set.** Undo/Redo, pencil/single, rectangle, circle, Fill, Shadow Pen, Eraser, Auto/Layer 1-4, Database, Plugins, Resource Manager, Audio, and Forge use dedicated high-contrast deep-blue/cyan SVGs with gold and magenta accents. Fill is a tilted bucket pouring paint; Shadow Pen has a cool-metal fountain nib visibly darkening the tile instead of sparking it; Undo/Redo are mirrored curved arrows. Web layouts retain horizontal toolbar scrolling; base desktop overflow behavior is unchanged.
- **Actor/model preview lifecycle.** `ModelGraphicPicker` generation-cancels superseded loads, disposes replaced templates, and avoids the HiDPI resize feedback loop. `Database3DBindings` deduplicates concurrent cold thumbnail renders and performs one close resynchronization. `ModelPreview3D` owns deterministic geometry/material/texture/object-URL/control/renderer/context cleanup. Rapid selection and project changes no longer leave stale blank previews, duplicate expensive renders, or leaked WebGL contexts.
- **GitHub #24, Effekseer preview and runtime parity.** `AnimationPickerModal` accepts either a project manager or direct project path, safely handles synchronous cached-load callbacks, coalesces repeated pending selections, uses an opaque canvas for correct additive output, and shares a persisted light/mid/dark backdrop with the event animation picker. Plugin `animation`/`animation[]` references and Show Battle Animation now open that live picker while preserving their string/positive-ID contracts and generic fallback. The Animations page blits its Canvas2D battleback, target, and flashes into WebGL before `captureBackground()`, uploads only when that scene changes, restores Effekseer's GL state, and applies the selected animation's offset, rotation, scale, and speed in the effect-file preview. Every editor, Forge, diagnostic, and runtime surface uses aspect-correct positive-Y projection and authored X rotation, avoiding the old depth-reversing 180-degree compensation. Runtime revision `20260828.1` refreshes existing same-version projects once while preserving plugins; `reactor_main.js` is copied last so a failed refresh cannot falsely mark a partial runtime current.
- **GitHub #25, Add State Normal Attack sentinel.** `DatabaseEffectEditor` prepends `dataId: 0` as Normal Attack for code 21 only, keeps it selected through render and interaction round trips, and leaves Remove State restricted to real state IDs. Summary text now calls zero Normal Attack only for Add State; a malformed Remove State/0 row reports honestly as State #0.
- **GitHub #26, complete image browse controls.** `DatabaseEditorUI.imageBrowser`, `browseImageFolder`, and `createImageBrowseButton` route bare image-name fields through the existing recursive, searchable preview picker without removing direct text entry. Show Picture, Change Battle Background, Change Parallax, Change Actor Images, and Change Vehicle Image cover eight fields; character and face sheets synchronize their chosen cell indices through the fields' existing input events. Map Properties and the Troop editor add Browse controls beside both retained battleback dropdowns, support clearing through the pinned `(None)` action, and accept files discovered after the dropdown was populated. Event-command calls lend the shared modal z-index 10007 and the next ordinary caller gets its base level back. Map parallax selection now passes the real file list with `allowNone` instead of inserting a translated sentinel and transparent preview asset. Five direct phrases are reviewed in all 17 non-English locales.
- **Local-reviewed localization architecture and routing audit.** `I18nReviewedTranslations.js` is a checked-in final-precedence layer over the legacy broad catalog and curated manager tables, with reviewed `text`, `keyed`, `commands`, and `sections` maps for every non-English locale. Corrections cover RPG-specific Party/Actor/MP terminology, build-worker wording, copied Greek/Turkish text, Thai orthography, and all 122 Polish event commands plus 22 sections. Newly localized paths include project/package diagnostics with placeholder-safe interpolation, map grid/anchor/range/parallax chrome, 3D rig status, unnamed-map and option-value fallbacks, picker search, project lock/playtest errors, Quick Events, and Tileset flags/3D tools. Animation timing uses a localized `None` label only for display rather than persisting it as an SE filename. The unused Microsoft-edge generator was deleted, leaving no online translation path. `i18n.test.cjs` now pins reviewed precedence, exact locale coverage, placeholders across all four catalogs, known Greek leakage, normalized Thai, complete Polish tables, and static source coverage; focused routing tests protect the changed UI.
- **Audio event-command player parity.** `AudioCommandEditor` now sends Play BGM/BGS/ME/SE directly to `RRAudioPickerModal` instead of rendering its older duplicated inline player. The shared current UI supplies cover art, recursive folders, Unicode ordering, thumbnails, seek/transport/loop controls, `(None)`, and live volume/pitch/pan while returning exactly `{name, volume, pitch, pan}` as the command's sole parameter. Troop insert/edit paths join map and Common Event paths through the same editor, with complete defaults for Play ME, fades, and Stop SE; plugin audio arguments loop BGM/BGS/ME previews like the base player. `AudioPickerModal` now reads native loop points, wraps per frame with modulo, preserves tail audition after a seek, honors the main player's Wine `htmlAudioOnly` policy, and closes a partially initialized Web Audio context before falling back.
- **State, trait-table, and database chrome polish.** `DatabaseStateEditor` now lays out Duration and Messages as controls-first grids, keeps dependent turn/damage/walking rows aligned, and documents `%1` as the Actor/Enemy Name token. Actor/Class/Weapon/Armor/State/Enemy trait headers use a two-cell row with Type spanning the hidden selection indicator instead of rendering an empty spur. The Database list scrollbar is palette-aware, Change Maximum suppresses Chromium's native number spinner while retaining keyboard stepping, selected-row contrast uses defined theme tokens, and recent System/audio/plugin modals preserve accessible names, focus containment/restoration, Escape/backdrop behavior, and narrow-width reflow.
- **GitHub #13, animation-preview fidelity.** MV sprite previews run their update loop at 60 Hz while advancing authored cells every four ticks, so target, screen, and hide flashes fire and decay at the same rate as the game. MZ/Effekseer previews consume target flash timings and keep updating through late timing/flash completion after the effect itself ends. Sprite cells map all four blend modes to Canvas composition, and head/center/feet/screen anchors now agree across rendering, hit testing, and dropped-cell coordinates. Frame-zero timings seed static views immediately; frame/timing edits redraw those views; sprite strips restore nearest-neighbor sampling after resize; playback resets its counters and flash state; Effekseer catch-up consumes every tick, balances `beginDraw`/`endDraw` with `try/finally`, and generation-guards delayed readiness retries. SE volume preserves an authored zero. Effekseer background capture/compositing and asynchronous browser effect loading remain separate follow-ups.
- **GitHub #18/#22, complete plugin schema and command-block handling.** `PluginAnnotations.js`, `PluginParamWidgets.js`, `PluginParamCodec.js`, and `PluginDataRefs.js` now provide one path for top-level parameters, nested structs, and plugin command arguments. This covers choices, all 19 database/System reference types and arrays, color values, authored Boolean labels, multiline values, recursive structs, malformed raw text, exact RPG Maker string/JSON encoding, and `img/...` file fields with recursive folders plus image/dimension previews. Web command dialogs receive the host filesystem, and encrypted Web previews decrypt asynchronously, including key recovery. `PluginCommandEditor` emits ordered readable 657 rows while retaining unknown rows under partial metadata; `EventCommandList`, Common Events, and Troops treat 357/657 and 355/655 as indivisible blocks for insertion, replacement, selection, clipboard operations, and direct continuation editing. Complex dialogs stack above command modals and Cancel never mutates the outer draft.
- **GitHub #21, escaped desktop asset URLs.** `EncryptedAssets.fileUrl` now uses `pathToFileURL` with explicit Windows handling only for drive and UNC paths, leaving native relative/root semantics native. Its fallback produces equivalent encoded URLs. Character, face, battler, tileset, database-icon, event-face, and IconSet consumers now use the shared resolver. Spaces, reserved URL characters, Unicode, POSIX double-slash paths, Windows drives, and UNC shares have regression coverage; encrypted `data:` URLs and WebHost's resolver are unchanged.
- **GitHub #20, recursive asset picker folders.** `RRPickerIndex.buildFolderTree` and `foldersLeadingTo` preserve complete relative values while producing arbitrary-depth, descendant-counted folder nodes. `createBrowser` filters before tree construction, opens all selected ancestors, renders folders before files, and gives folder headers focus, ARIA state, Enter/Space toggles, and Left/Right controls. `AudioPickerModal` uses the same tree without narrowing its playback lookup, keeps flat A-Z behavior, and exposes equivalent keyboard controls. Character, tileset, database image, title, model, and message-face callers enable folder mode.
- **GitHub #19, scaled window filter rectangles.** `Window._updateFilterArea` writes dimensions in world scale, and the PIXI 8 localization step divides both position and dimensions back into client-local coordinates using matrix-column magnitudes. Half-scale/plugin/stock-style rectangles, nonuniform reflected or rotated transforms, zero scale, stale transforms, and pre-v8 behavior are covered. All tracked runtime copies are synchronized.
- **Save success now means durable browser storage, and Reactor sidecars are atomic.** `ProjectController.saveAll` retains the old project/map/database dirty baselines until `RPGReactorHost.flush()` resolves; a rejected IndexedDB transaction restores those baselines, reports the storage error, and returns false. Desktop writes for `Database.r3d.json`, map/tileset 3D sidecars, `model.json`, and `model.rig.bin` route through `RRWriteFileAtomicSync`. Sidecar reads distinguish a missing file from malformed or unreadable content, so an edit cannot silently replace damaged authored data.

- **User Interfaces tab: fit, keys, capture scope (owner pass).** Layout: `.rr-ui-workspace` is `flex: 1 1 auto; min-height: 380px` with `grid-auto-rows: minmax(0, 1fr)` (the `clamp(420px, 100vh - 360px, 960px)` estimate overshot 1080p by 108 px); `fitCanvas` scales to the host's height as well as width when the workspace's `--rr-ui-fit` custom property reads `both` (the ≤1300 px stacked layout sets `width`); the Note textarea is a full-width row of General Settings; side columns are `minmax(170px, 200px)` / `minmax(320px, 360px)`. Keys: one `onKey` on the editor wrapper handles Delete/Backspace, Ctrl+D/Z/Y (arrows stay canvas-only) and `stopPropagation`s; tree rows and captured-window rows are `tabIndex -1` and take focus on mousedown, `renderTree`/`renderCaptureList` restore focus after rebuilding rows (a re-render dropped focus to the body, where the record list's document-level Delete cleared the record); `DatabaseEditorUI`'s list key handler ignores events whose target is inside `#database-detail`. Capture: the reference layer is per open view — `loadLastCapture` and the `rrui.lastCapture.*` localStorage key are gone, `clearReference()` + a Clear chip, the scene dropdown loads a cached capture on demand; `capture()` no longer reaches for a `saveProject` (it was undefined on the parent editor anyway) and appends "Unsaved changes are not in the capture." to the status when `projectController.hasUnsavedChanges()`; `addNode` returns the node, so `addBoxFromCapture` actually applies the window rect, `opacity` and `backOpacity → fillOpacity` (it returned early before, leaving a default box, and pushed a second undo step); `showUserInterfaceDetail` calls `detach()` first so window mousemove/mouseup listeners stop accumulating. Two phrases added to all 17 locales by hand.
- **GPU-side load.** `Window.clipWithMask` (default true, PIXI 8 only): `_createClientArea` keeps `filters = []` and a live `filterArea` for readers but clips through a `PIXI.Graphics` stencil rect child of `_clientArea`, rebuilt by `_updateClipMask` only when origin/inner size change; PIXI's `StencilMask` manages the mask's `includeInBuild`/`measurable` flags, and setting `renderable = false` by hand collects no geometry and clips everything out (found the hard way). Verified on Project3's item screen: pixel-equivalent to the AlphaFilter path, PIXI GPU time 2.12 → 1.96 ms/frame there (the filter passes are the bandwidth part, which weighs more on integrated GPUs). `Reactor3D.sheetTextureFor(bitmap)` (WeakMap) + `billboardView(holder, sheet)`: billboards share a sheet upload and set `repeat`/`offset` per frame (negative repeat mirrors); the canvas copy per frame change is gone. `MapScene.freezeStaticMeshes` after `build`. `Reactor3D.maxTextureSize` (2048) applied by `capBitmapSize` in the worker decode and `capImage` on the `TextureLoader`/`Image` paths (data-URI images bypass the worker; the Demo's Buick and Fleagus were 4096²). GPU timer-query harness `gpu-probe.mjs` (session scratchpad): on this RTX the 3D passes are ~0.5 ms and PIXI ~0.9 ms per frame; MSAA/scale are noise here, so their value is the weak-GPU ratio. Tests: `runtime-gpu-load.test.cjs`; the billboard flipY test now pins the sheet design.
- **Per-frame allocation churn (owner: "the destroy-and-recreate pattern").** `alloc-profile.mjs` (session scratchpad; CDP `HeapProfiler.startSampling`, aggregated by call frame and parent) ranked the sites. Fixes: `forceSinglePass: true` on the four flat double-sided transparent `MeshBasicMaterial`s (three's `renderObject` renders such materials twice and sets `needsUpdate` each pass → `getProgram` → `getParameters` allocations, 32 MB/8.5 s; the additive lights material deliberately keeps two passes, since single-pass halved every light and the frame diffed 3.6%); `modelSpecFromNote` memoised by note string, `normalizeModelSpec` by sidecar object (WeakMap), `hasEventModels` by map data (WeakMap); `projectToScreen` / `standScaleAt` scratch vectors; `applyModelAnimation` caches lowercased part and rule names; `Point`/`Rectangle.prototype.initialize` write fields directly on PIXI 8 (v5-7 keep `PIXISuper`); `Window._clampedCursorRect` returns a per-window scratch rectangle and `_updateFilterArea` applies through two per-window scratch points; `Tilemap.updateTransform` skips the repaint while both layers are hidden (a 3D map in their place) and repaints when they show. Second pass: `applyModelAnimation` draws every Vector3/Quaternion/Matrix4/Euler, action record, and per-mesh match record from `Reactor3D._animPool` (counters reset per call; nothing handed out outlives the call; `entry.accMatrix` persists per mesh) and filters clip rules once per rules array; `projectToScreen(camera, x, y, z, out)` fills a caller-owned record (`standScaleAt` and `Sprite_Character` pass theirs); `Sprite._refresh` on v8 moves the frame of a texture it owns and calls `texture.update()` (the texture is created `dynamic: true` so PIXI's Sprite listens and refreshes bounds; a fresh `PIXI.Texture` is only built on a source change), verified on Project3: same texture uid across frames, `getLocalBounds` follows the frame both ways; `PSYCHRONIC_EventCustomizerMZ.js` caches `$gameMap.isEventRunning()` per frame (it was `events().filter()` twice per event per frame). Walking 250 → 146 MB per 8.5 s, battle 151 → 110 MB; map frame pixel-identical. Tests: `runtime-frame-churn.test.cjs`.
- **Adaptive 3D resolution, overlay redraw discipline, idle preview throttles.** `reactor_3d.js`: `Reactor3D.renderScale` / `adaptiveResolution` / `minRenderScale`; `Viewport.setRenderScale` resizes the targets (`targetSize`) and bumps the generation so the pass sprites rebuild; `samplesForScale` (4 / 2 / 0 at 1 / 0.75 / 0.5); `_trackFrame` keeps an EMA of frame intervals per game frame and feeds `adaptScale` (pure: drop a quarter when the EMA exceeds 1.35× the 60 fps frame, climb when under 1.05× after 300 calm frames, 120-frame cooldown; an earlier draft measured the display period from the fastest interval seen and mistook a catch-up burst for 240 Hz). Battler targets follow the scale (`targetPixels`). `warmLoadedTemplates` no longer allocates a `THREE.Scene` per frame when nothing is pending. `PSYCHRONIC_RaveLighting.js`: `overlayScale` 0.5 (bitmap scaled, drawing under `setTransform`), `toneOverlaySignature` skips redraws when tone, light set, and light sprite positions are unchanged, `animatedRedrawInterval` 2 for flicker/fire/pulsate/flashlight/beam/phase. Editor: `Database3DEditor.shouldRenderPreview` and `MapEditor3D.shouldRender` (idle = 100 ms cadence); activity = easing, drag, load, edit state, ambient rules, recent input/pointer, camera or hover change, flight. Measured: Sword_Fleagus preview 29 renders per 3 s idle, full rate under drag. Tests in `reactor-3d-shared-context.test.cjs` and `db3d-preview-perf.test.cjs`.
- **Shared-context 3D map rendering.** `Reactor3D.Viewport` now initialises in shared mode when `Reactor3D.sharedContextAvailable()` (PIXI 8 with a WebGL2 context, `resetState`, and `THREE.WebGLRenderTarget`; `?r3dcopy` or `Reactor3D.useSharedContext = false` opt out; failures fall back to the canvas path via `_teardownShared`). three's `WebGLRenderer` is built on `Graphics._app.renderer.gl`; each pass (`below` / `above` / `lights`) renders into a `WebGLRenderTarget` (MSAA `renderTargetSamples`, `isXRRenderTarget = true` for the sRGB shader encode and canvas-style tone mapping, `texture.internalFormat = "RGBA8"` so the texture and the multisample renderbuffer match for the resolve blit). `passTexture(slot)` wraps the target through `Reactor3D.adoptGlTexture` (seeds `source._gpuData[renderer.uid]`, disarms `update`, no upload method) into a `PIXI.Texture` rotated `MIRROR_VERTICAL`; `render(slot)` brackets three with `resetState` on both sides and nulls PIXI's bound-texture cache. `Spriteset_Map.createReactor3DSprite` builds shared passes off `passTexture`, `updateReactor3DTexture` skips them, `destroyReactor3DSprite` never destroys a shared texture, and a viewport `generation` bump (resize) rebuilds the sprites. A/B harness `shared-ab.mjs` (session scratchpad): pixel-identical to the copy path, no GL console errors. Battlers: `Reactor3D.paintBattlerFrame(state, sprite)` replaces the two identical per-frame copy blocks in `updateEnemyModelSprite` / `updateActorModelSprite`; on a shared context `_paintBattlerShared` creates a `size×size` target per battler state, adopts it into the Bitmap's `baseTexture.source` (freeing the PIXI-made texture it replaces), re-asserts `MIRROR_VERTICAL` on the sprite texture each frame (MZ's `_refresh` rebuilds the texture on frame change), and `renderInto`s it; `releaseBattlerState` disposes the target on actor/enemy id change and from a `Sprite_Battler.prototype.destroy` wrapper at the end of `reactor_sprites.js`. Viewport gains `createTarget`, `targetHandle`, `renderInto`, `pixi`. Battle A/B: battlers upright, no `_battlerRenderer` context created. Tests: `reactor-3d-shared-context.test.cjs`; three pinning tests updated for the slot argument and `_initializeCanvas`.
- **Runtime 3D instance stalls (owner-reported, follow-on to the preview work).** `reactor_3d.js`: `cloneModelTemplate` detaches `userData.glbTextures` around `template.clone(true)` and reattaches it by reference (three's `Object3D.copy` JSON-copies `userData`; `Texture.toJSON` serialises the image), then calls the new `presetSkinnedBounds(object)`, which sets `frustumCulled = false` and `mesh.boundingSphere` from the shared geometry sphere on every skinned mesh (three r185 `projectObject` calls `SkinnedMesh.computeBoundingSphere`, a per-vertex bone pass, whenever the sphere is null and `sortObjects` is on, culling or not); `applyModelRig` presets the meshes it builds the same way. `pixi_compat.js` `PIXISuper` memoises `__rrEs6Class` per class so only the first call pays the throw-and-regex discovery. Templates synced. Harnesses `game-perf.mjs` (rAF gaps + longtasks + slow-call stacks by window: load / walk / battle) and `game-profile.mjs` (CDP `Profiler` sampling with self/total/caller-chain aggregation) in the session scratchpad. Tests: `reactor-3d-carved-parts.test.cjs` (clone never serialises textures; skinned instances carry preset bounds), `pixi-super-memo.test.cjs`. `Database3DEditor` shows `Loading model…` (17 keyed locales in `I18nManager.js`) via `_refreshHint` while `_loadingPreview`.
- **Database 3D preview stutter (owner-reported).** `Database3DEditor`: `_updateHover` is gated by `shouldRaycastHover` (pointer at rest ≥150 ms via `_pointerMovedAt`, no drag, no wheel/orbit input in 250 ms via `_lastInputAt`, and `_triangleCount ≤ HOVER_TRIANGLE_BUDGET` = 150k); the old interval throttle still fired a whole-mesh `Raycaster.intersectObject` on every pointer move. `_fillThumbnails` awaits `_whenPreviewIdle()` (no `_loadingPreview`, no drag, 600 ms since input) before each uncached render, passes `beforeBuild` to `_loadTemplate` so `Reactor3D.readModelAsync` (new optional 5th `options` argument, runtime + templates) holds the main-thread template build after the worker parse until idle, skips the +1.6 s re-render when `texturesDecoded(glbTextures)`, and reads/writes a per-machine PNG cache (`thumbnailCacheRoot`: `%LOCALAPPDATA%\RPGReactor\ModelThumbnails`, `~/Library/Application Support/RPGReactor/ModelThumbnails`, `$XDG_CACHE_HOME/rpg-reactor/model-thumbnails`; file name = sha1 of source path, size, mtime). `_drawPreview` wraps `_drawPreviewNow` to hold `_loadingPreview`. Harness `db3d-perf.mjs` (session scratchpad): rAF-gap + longtask probe with per-method attribution. Tests: `db3d-preview-perf.test.cjs`.
- **Plugin Manager icon and audio pickers (GitHub report).** New `src/utils/IconPicker.js` (`window.RRIconPicker`: `show`, `createField`, `imageUrl`, `iconSetPathFor`) is the database `showIconPicker` body moved out, with the asset-URL resolution from `applyListIcon`, an `onerror` path (closes, reports through `uiManager.showAlert`), a per-path sheet cache for previews (the picker reloads), and click clamping to the sheet. `DatabaseEditorUI.showIconPicker` delegates. `PluginManager` gains `createIconInput`, `createFileInput` (audio dirs: name + Browse... → `RRAudioPickerModal` with `levels: null`; other dirs: the file dropdown, keeping a saved name the folder no longer holds), `openAudioPicker`, `isAudioDir`, `audioFolderLabel`; branches in `createParameterInputWithDepth` (icon, file via the helper), `createStructFieldInput` (icon, file with `dir`), `showArrayElementEditor` (`icon[]`, `file[]` with `dir`), and `renderArrayStructureEditor`'s `+ Add Element` (audio `file[]` opens the picker; `(None)` adds nothing). `PluginCommandEditor` adds `case 'icon'` and routes audio `@dir` file args through `showAudioPicker` before the generic picker. Tests: `plugin-manager-pickers.test.cjs` (parser carries the types, dir spellings, source routing, the field and picker over a fake DOM, the missing-sheet path); verified live on a fixture plugin in NW.js.
- **New Project dialog, anvil Forge icon, list context menus (GitHub feedback).** `UIManager.showNewProjectDialog({ defaultName, hasTemplate, validate })` resolves `{ name, blank }` or `null`, in `rr-modal` chrome with a radio group for the Reactor One demo (disabled and deselected when no template folder is found) or a blank project; `validate(name)` returns an inline error and keeps the dialog open. `ProjectController.newProject` uses it in place of `prompt()`/`alert()` and passes `{ blank }` to `ProjectManager.createNewProject(targetPath, name, options)`, which then skips the template copy and writes the existing starter scaffold. `FORGE_ICONS.forge` and the toolbar button share a new anvil path. `setupMapTabs` binds `contextmenu` on `#maps-list` and `#quick-access-list` for targets outside a `[data-map-id]` row, routed through a new `showContextMenuItems(x, y, items)` that `showMapContextMenu` now delegates to (`showMapListContextMenu`: New Map at root + Paste Map; `showQuickAccessContextMenu`: toggle the open map). Eleven new phrases hand-authored into all 17 locales. Tests in `application-shortcuts.test.cjs` and `project-manager.test.cjs`; verified live in NW.js.
- **Encounter and battle transition shaders land where they aim.** `reactor_mv_compat.js`: `installVec2Compat` runs after the compat filter's `UniformGroup` is built and gives every scalar `vec2<f32>` uniform a get/set on `group.uniforms` that adopts the assigned value (`adoptVec2`: arrays and typed arrays gain `.x/.y` aliases onto `[0]/[1]`, `{x, y}` objects gain `[0]/[1]` and a `length`, a bare `{}` is seeded from the value it replaces, a number becomes `[n, n]`, `null` keeps the value, an expando written before adoption is folded into the slot). Why: v8's sync generator tests the group's *construction-time* value for `.x` to pick the point parser and caches the result per group signature and program; pixi-filters v5 classes call `super()` with no uniforms, so the test sees the seeded typed array and every later component write is invisible. `pixi_compat.js`: `PIXI.TexturePool.getOptimalTexture` is wrapped so a request matching `Graphics._canvas` (read lazily; the editor has none) gets an exactly sized texture under a negative pool key, with a stale-size pooled texture destroyed on canvas resize; every other request takes v8's native power-of-two path. Tests: two vec2 cases in `legacy-filter-apply-compat.test.cjs` (whose fake `UniformGroup` now normalises structures in place like v8) and `pixi-fullscreen-texture-pool-compat.test.cjs`.
- **Effekseer animations show again in VisuStella battles, and two more PIXI 8 gaps close from a user's debug report.** `pixi_compat.js` wraps `PIXI.Sprite.prototype.updateTransform` on v8 so the v5 no-args call (VisuStella CoreEngine's battler projection, `targetDistortionSpritePosition` in BattleCore) refreshes the local transform and returns instead of throwing on `opts.x`; the `(opts)` form passes through, and the wrap is Sprite-only on purpose (Container/Window/Tilemap plugin chains rely on the throw skipping their post-super work, and `Window.updateTransform` already branches on v8, so the old "removed wrap" note was stale). `Sprite_Animation.renderActive` reports the first `_doEffekseerDraw` throw once (`_effekseerDrawWarned`) instead of swallowing every frame. `reactor_mv_compat.js` `buildUniformStructures` seeds unset scalar array uniforms (`f32` → `Float32Array(N)`, `i32` → `Int32Array(N)`, lone `int` → 0) because `UniformGroup`'s `getDefaultUniformValue` returns the number 0 for `f32` whatever the size and nothing for `i32`, which reached `gl.uniform1fv` as a scalar and threw (DA_HeatDistortion's `sourceX/Y/R[10]` in Global mode). Tests: `pixi-sprite-update-transform-compat.test.cjs` and a seeding case in `legacy-filter-apply-compat.test.cjs`.
- **The Audio Player loops at LOOPSTART/LOOPLENGTH.** `AudioPlayer` drove looping through `HTMLMediaElement.loop`, which repeats the whole file; the tags only reached the game's `WebAudio`. New `src/utils/AudioLoopTags.js` ports the runtime's readers (OGG/FLAC vorbis comments, ID3v2 TXXX, WAV `smpl`) over a growing prefix read through `RREncryptedAssets.readAssetBytes` (a WAV whose `smpl` trails the data is read whole), returning `{start, end}` seconds. Each channel keeps `loopWanted`/`loopPoints`; `setChannelLoop` is the only place the element's `loop` flag is set, and it stays off while loop points exist. A per-frame `watchLoopPoints` wraps `currentTime` from the loop end back to the start keeping the overshoot, `ended` wraps a loop that runs to the file end, and `seeked` disarms the wrap past the end so the tail can be auditioned. Verified in NW.js against an ffmpeg-tagged fixture (wraps 2.99 → 1.03 s, never past the end) and by parsing the stock MZ RTP battle themes to their known points.
- **The Enemies note textarea keeps its dragged height.** `DatabaseEnemyEditor` gave the note `flex: 1` (basis `0%`) inside a stretched column, so the inline `height` the resize handle writes was ignored by the flex algorithm (measured: 400px drag, 199px result); `flex: 1 1 auto` takes it, as every other page's note already did.
- **Actor face icons show in the database list on Windows.** `DatabaseEditorUI.applyListIcon` built `encodeURI('file://' + path)` without converting backslashes, so Windows paths became `file:///E:/%5CGame%20Dev%5C…` — `ERR_INVALID_URL` on every actor row, whether or not the face file existed. Backslashes are now converted before encoding and `#` is escaped, matching the sibling builders in the file (`database-list-icon-windows-url.test.cjs`). Separately, 161 `console.log` progress lines across the tileset/animation/weapon/armor/system editors, EventManager, ProjectManager, and the database shell moved to `console.debug` (DevTools' Verbose level), and the runtime's `Reactor3D: 3D scene built.` report did the same.
- **Three Demo tracks no longer jump to the start when you seek.** The cover-art restoration pass remuxed audio with stream copy, and on Battle Of The Abyss, Darkstream Runner, and Pixel Party the remux wrote Ogg pages whose granule position carried a stray 2^32 samples: a four-minute song reported a duration of 24 hours 55 minutes, so seeking a minute ahead landed "past the end" and the player looped back to zero (or stopped with loop off), identically on desktop and web because the corruption is in the files. The three tracks are regenerated from their originals through the optimizer's single-pass encode (art embedded in the same pass, no copy remux), verified in a live browser: exact durations, and a seek to 60% keeps playing. The optimizer now reads the Ogg clock — last granule position over the Vorbis sample rate — of input and output on every re-encode and refuses a result whose duration drifts more than 2%, so this class of corruption can never ship silently again.
- **The web editor's 3D suite works: previews render, clips list, rigs bind.** Four layers deep: the 3D library loader's web branch checked `RPGReactorHost` while the host registers as `RPGReactorWebHost` — one word that kept three.js from ever loading in a browser, invisible to the suite because its test stubbed the same wrong name; the test now stubs the real global. Editor model reads (DB previews, embedded clips, the picker, rig weight binaries) go through a new async asset reader — synchronous on desktop, a fetch of the served file on web — because a synchronous XHR fallback deadlocks against the service worker (it reads the same IndexedDB store the page writes mid-transaction). The `3d/**/model.json` sidecars join the web build's preloaded files so synchronous sidecar reads keep working, and a `Buffer` polyfill covering the editor's exact API surface unblocks every binary writer: rig binaries, PNG and zip exports, the Forge generators. Verified in a served web build driven end to end: the Database 3D preview renders, Carol lists her 20 embedded clips, and Bind rig on both actors solves, writes model.json + model.rig.bin through the web filesystem, reads them back, and persists to browser storage — matching desktop behavior exactly.
- **Two more PIXI 8 compat gaps closed from live plugin reports.** Hendrix_Animation_Solution crashed calling `PIXI.utils.string2hex` for its bloom tint — the v5 color helpers (string2hex, hex2string, hex2rgb, rgb2hex) are reimplemented in the utils shim. And action combat could crash the whole GL pass ("reading 'addressModeU'") when a plugin destroyed a bitmap mid-animation on a non-Sprite renderable the SpritePipe guard doesn't see — GlTextureSystem now substitutes the empty texture for any dead source at bind, the last line of defense under every render pipe. A live audit of every `PIXI.*` path the Hendrix and Haven plugin packs touch (24 distinct) resolves clean against the shimmed runtime, and a scripted fresh playthrough of Haven — nine maps of intro, fast-forwarded dialogue, a battle fought and exited — runs with a silent console.
- **Loading a save from a map-based title no longer risks a soft lock.** Haven's title screen is itself a map scene; DataManager.loadGame replaces $gameMap instantly while the old scene still updates through its fade-out frames, so every plugin sprite keyed by map id (GALV layer graphics) read the wrong map's store and threw a TypeError per frame — a storm that stalled the scene transition and left the first battle soft-locked with no visible error. Spriteset_Map now stamps the map it was built for and holds still once $gameMap moves on; verified on a real Haven save: clean load, and a battle started through the engine's own Battle Processing command reaches Scene_Battle and plays turns with a silent console.
- **A quieter console.** PIXI's one-shot deprecation nags for the v5 Graphics API (beginFill, drawRect, endFill...) are burned off at compat load with warn muted — MZ plugins use that API forever, the shims work, and nobody can act on the nag — and the runtime's leftover Effekseer overlay/lifecycle and animation-anchor debug diagnostics are removed from the play path (the console-invoked helpers like $reactorAnimWatch stay).
- **PIXI 8's new `origin` accessor no longer hijacks engine bookkeeping.** Newer PIXI Containers claim `origin` as a live transform input; MZ semantics store scroll bookkeeping Points there (Tilemap, TilingSprite, Window, Weather — and MOG_Weather_EX, which warned "Setting both a pivot and origin" sixty times per map). Harmless while nothing rotates (origin cancels out of an identity transform) but wrong the moment a weather sprite spins with a large origin. The accessor joins `name` in the pre-boot deletion list, so assignments become plain instance properties again; verified against the Demo (plain origin, live scroll values, clean console) and a real Haven save (weather plugin active, zero warnings).
- **Benign window-chrome churn no longer warns every playtest.** The render guard's report for a live sprite whose texture source died distinguished nothing about the source — but a destroyed CANVAS source is the churn pattern plugins use for drawn window chrome (SelectableWindow destroys and recreates its gradient bitmap every geometry pass; the part sprites hold the dead canvas for one frame before reassignment). The skip covers that frame and the plugin heals itself, so the canvas case now reports on console.debug (visible at Verbose); a destroyed sprite, nulled GPU data, or a dead file-backed texture is a real leak and stays a warning.
- **The Database 3D panel behaves on first open.** On a fresh editor, `_readEmbeddedClips` ran before the 3D libraries had ever loaded — `Reactor3D` was still undefined, so a model's embedded clips came back empty and every clip rule looked unresolved until the model was visited a second time (the libraries load inside the preview draw; the clips now read after it). And on rigged models the Animations header packed a differently-styled Motions… chip beside Add and Delete into the fixed-width panel, pushing Delete past the dialog edge where it clipped; Motions… now sits on its own full-width row below the header, styled like every other button.
- **The web editor renders 3D.** Database model previews were black in the browser — the running game was fine — because editor previews read model bytes, sidecars, and rig binaries through synchronous `fs` calls the web build only honored for preloaded data files. The web filesystem shim now fetches any bundled file on demand through a synchronous request (cached after first read), which lights up the Database 3D section, the actor/item mini previews, and the model picker with no call-site changes.

## [0.98.3] - 2026-08-24

### Added

- **Database > 3D carves model regions into named parts and poses them with a direct-manipulation card.** The preview's tool strip holds Orbit, box Select, and Pivot; the right button or Ctrl always orbits. With Orbit active, hovering any poseable part boxes it and a stationary click opens the edit card — carved parts and reader-named parts (a GLB wheel) alike, picked by raycast through the part ancestry. Select drags a marquee and every triangle the rectangle touches joins the part — a vertex inside the box, the box poking into a big triangle, or an edge crossing all count, where centre-only testing let a visibly covered jaw plate slip away — through the model, so a jaw's far side comes with the near side, with Alt-drag removing (same touch test) and a floating count/Apply/Cancel bar; rules freeze while selecting so the mesh cannot slide out from under the marquee, selection always runs against the uncarved geometry so triangle indices count over the source, and Apply flows straight into posing the new part. The card is the whole editor and never leaves: it docks whenever the model has parts, its header carries a target dropdown (Whole model included) beside undo/redo, its × folds it to a small button, and a **Type** select switches the motion — **Pose** (Rotate / Offset / Scale tabs of color-coded X/Y/Z sliders), **Swing** / **Spin** / **Bob** (axis chips plus degree, speed, per-tile, and amount sliders), or an embedded **Clip** — every control driven on `input` so the model moves live through a synthetic always-on rule in the real engine, while every OTHER ambient animation stands still so nothing fights the hand (explicitly played actions still run; releasing the card resumes them). The numeric rule form on the right is gone — the panel keeps only the Parts and Animations lists, and clicking any animation opens it on the card. The card also carries a logarithmic **Duration** slider (0.1–10 s, the ease period in frames underneath), a **Play when** trigger select, an **At the end** control (Return to rest / **Stay posed**, the pose rule's `hold` flag), **Preview** (plays the pose once via a timed action rule — in-and-out, or in-and-hold for a held pose, the editing hold taking over seamlessly), double-click-to-reset per slider, and **Save as animation** / **Update animation**; an edited animation's saved copy is filtered out of playback so nothing plays twice, and a fresh card always defaults to On demand rather than inheriting the last rule's trigger. The card keeps state: working poses persist per part so reselecting resumes the sliders where they were left (a part with exactly one saved pose opens it for editing), each slider gesture stashes on its first tick and commits one undo step on release, Ctrl+Z / Ctrl+Y (and header arrows) walk a per-part undo/redo trail, and Escape closes the card ahead of the dialog. In the runtime, a held on-demand pose latches: the action eases it in over one period (`modelRuleDuration` is `period`, not `period×2`) and `binding.latch` keeps it there after the action expires — until another held pose claims the same part, which eases the previous one home; transient in-out poses overlay a held pose without releasing it, and the sim bar gains a Reset pose button that clears the latches. A part stores `{ name, pivot, meshes: { meshIndex: [[tri,count],…] } }` in `model.json` (foreign sidecar keys preserved); definitions may overlap, and the carve treats overlap as nesting — triangles are grouped by the exact set of parts claiming them, and each piece carries every claiming name as ancestry, most specific (fewest triangles) first with its own pivot, so a cannon selected inside a turret answers its own rules about its own hinge while riding every turret rule (the previous first-definition-wins partition silently carved such a nested part to zero triangles: no box, dead sliders — found on the real tank, not by the single-mesh tests). In playback, a mesh's contributions compose by that same ancestry rather than rule order — ancestors' turns apply before a child's own motion, so a fired cannon recoils along the turned barrel whether the fire rule sits before the turn in the array or rides at the end of the chain as the card's working copy (authoring-order composition slid the recoil along the model's original axis the moment the orders crossed); `Reactor3D.carveModelParts` splits those triangle groups into sibling meshes at instance time — attributes shared, remainder reindexed, never mutating the template — registered exactly like reader-named parts and carrying the source mesh's ancestry so old rules still match, which is how the same carve serves the editor preview and the running game (`syncCharacterModels` loads one sidecar for rules and parts, stat-ing it on disk first so a model without one logs nothing). Pivots are movable fulcrums for every part, named or carved: `pivots: { "<part>": [x,y,z] }` overrides in `model.json` (model space, converted to mesh-local by `Reactor3D.applyPivotOverrides` after the carve, in the editor and the game sync alike), authored from a Pivot row on the card (bounds presets: center and the six faces), by clicking the model with the Pivot tool, or by grabbing the axes gizmo itself and sliding it in the camera plane — the part re-hinges live on every change. The card's target dropdown retargets the animation being edited (values intact; a deleted part's animation shows its target marked `?` for re-aiming) while viewport clicks keep switching context, and a click on the canvas lets an open dropdown dismiss itself instead of preventDefault pinning the popup open. The new **pose** rule type eases to an authored rotation, translation, and per-axis scale (`resize`, applied about the part pivot; mesh base scale now round-trips through the animation compose) and back as its trigger holds and releases — action poses play in-and-out over `period×2×cycles`, with the blend persisted so a released trigger eases home instead of snapping. Renaming a part renames it in every rule that targets it; a part with no triangles yet says so on the card instead of silently ignoring the sliders. A precision-and-state pass hardened the flow: the marquee is occlusion-aware by default — every projected triangle carries per-corner view depth, a screen-space grid answers "does anything nearer cover this centroid" (`triangleDepthAt`, epsilon-tolerant so coplanar neighbours never self-occlude), and a **Through** toggle in the select bar restores far-side selection; each drag and Clear pushes a selection undo step (select-bar arrow or Ctrl+Z, Escape cancels the mode). Selection-highlight overlays are flagged `__reactorOverlay` and excluded from `carveTargetMeshes`, which had let a full-canvas box count more triangles than the model holds and could shift mesh numbering. Rule suppression no longer filters the playback array — blends and latches key by index, and the re-keying scrambled latched poses — suppressed rules are swapped for inert stand-ins in place; the sim bar's Play for the rule open on the card previews the card's current values instead of hitting a dead slot, and the card's Reset also clears latches so one button always means "model at rest". A fresh card no longer inherits the previous rule's trigger. A polish pass tuned the feel against a real multi-part tank: Preview zeroes its own blend slot and releases held copies of its pose first, so it visibly plays from rest even while the card holds the pose at full strength (a held pose previously had nowhere to go and Preview looked dead); opening an animation releases its latch — the card takes manual control and nothing pops back up on deselect; the card's second button is **Clear** (zero the sliders, undoable) with latch release living on Preview and the sim bar; a **＋** beside Save detaches from the rule being edited so a part with one animation can get a second; the pivot gizmo grew, renders through the mesh (it used to be buried inside the part), drags from any tool with a move cursor, and is anchored to the part's owning mesh — its position re-derives every frame through freshly updated matrices, where a one-shot placement read a stale (unscaled) transform right after a rebuild and otherwise froze at whatever pose the part held at that instant, which made the fulcrum appear to jump between reselects; Add part cancels a running selection session so a new part never inherits the old triangles; and the highlight boxes are refit every frame instead of hanging where a pose once stood. On-demand rules carry `effects: [{ at, se|animation|flash }]` in the sidecar — `at` a 0–1 fraction of the play. The sync loop fires each effect once as the action clock crosses its mark (`modelEffectsToFire`, pure and window-tested): SEs through `AudioManager`, database animations through `$gameTemp.requestAnimation` on the character (MV sheets and Effekseer both ride the stock pipeline), screen flashes through `$gameScreen`, and model flashes by tinting the instance's own materials — `cloneModelTemplate` now clones materials per instance so one tank flashes alone, rebuilding `userData.baseColor` as a real Colour because Material.clone's JSON pass degrades it to a hex number (which read as undefined channels and poisoned the flash; `updateModelFlash` also self-heals any degraded base). The card grows an Effects section on on-demand animations: per-row timing slider, ＋ Sound through the shared `RRAudioPickerModal`, ＋ Animation through `AnimationPickerModal`, ＋ Flash with an inline target/colour/strength/duration editor; the editor preview plays SEs via the project's audio files and shows model and background flashes at the authored moment (database animations play in game). The animation picker opened from the card now passes the section's own project shim — the global manager answered a different shape, `project` stayed null, and `startPlayback` bailed to a black canvas for sprite AND Effekseer animations alike; the picker's list also joins the `audio-scroll` accent-pill scrollbar treatment and its footer wears the header's `--color-bg-panel` (both improvements reach the Skills/Items/Weapons pages that share the modal). The Animations list highlight mirrors the card everywhere (editRule/select/deselect/＋/Add all sync `selectedRule`), every card-filling action clears `_cardCollapsed`, the panel's Add opens a neutral motionless animation (its old whole-model swing default rocked the entire model — invisibly, when the card was collapsed), the ＋ button reads "＋ New", and both latch-release paths (`_releaseLatches`, the sim bar's Reset pose) also cancel a matching in-flight `_sim.action`, because a hold whose action window was still open — Turn-Turret-Right runs 600 frames — re-latched on the next frame and quietly undid its own release. After a Preview of a return-to-rest pose the stage keeps the ending — the held working pose stands suppressed until the next slider touch, because it used to ease back in the moment the preview finished, which read as the cannon firing twice and "resting at the end instead of the beginning" (found on the owner's Fire Canon recoil; every edit path re-engages the pose via `_syncWorkRule`). Beyond the scripted flows, a seeded monkey harness (`scratchpad/db3d-monkey.mjs`) drives hundreds of random-order UI actions against invariants (no exceptions, card/state/sidecar consistency, finite transforms) on the plant and on the 35-part tank, and a directed scenario (`db3d-two-parts.mjs`) proves two parts' animations stay fully independent through play, resume, update, and delete. Verified over CDP in a live NW.js editor: docked card before any pick, occlusion counts (visible-only 3,428 vs 8,184 through on the 8,222-triangle plant), selection undo, carve on monster-plant, slider rotate/scale moving the carved mesh, ambient sway freezing while a different target is edited and resuming on release, preview timing, held poses latching and easing home, undo/redo/resume, sidecar write, sim-bar replay, and viewport picking. All new UI phrases ship curated in the 17 non-English locales.

- **Four rig templates, a preset motion library, and keyframed animations complete the in-editor rigging suite.** The Rig tool's bar gains a template picker: **Humanoid** (17 bones), **Quadruped** (18 — spine chain, tail, four three-bone legs), **Plant/Tree** (base–trunk–crown, wind-ready), and **Vehicle** (body plus four point-like wheel bones, so wheels rig by dropping a marker on each hub instead of marquee-carving through geometry). The weight solver's side gate grew a second axis with real thresholds — a quadruped's front legs stay off its rear legs, and a spine marker nudged slightly off centre can no longer gate the torso against half the body. Swing and bob rules take a **phase** offset, which is all a walk cycle is: the new **Motions…** button on a rigged model opens the **Preset Motions** library — Walk, Run, Breathe, Wave, Take a Bow, Nod, Shake Head, Sit, Overhead Strike for humanoids; Walk, Idle Sway, Pounce for quadrupeds; Wind Sway and Rustle for plants; Roll and Bounce for vehicles — and applying one drops plain, individually editable rules into the Animations list (reapplying replaces that preset's rules; hand-authored ones are untouched). Gestures ride the other new mechanic, **keyframes**: an on-demand pose can carry a timeline of captured slider stops the action interpolates through — rest, through each stop, back to rest — authored on the card with a per-stop time slider, capture, and delete, and multi-bone gestures compose by sharing one action name. The library also carries the action repertoire a game actually needs: **Jump** (real root motion through keyframes — crouch dip, launch, hang, land, all on the whole model while the limbs tuck), **Swim** and **Float** (a swimmer's whole mode: prone stroke cycle while moving, treading drift while idle — a model carries Walk or Swim on the moving trigger, not both), **Slash**, **Thrust**, and **Overhead Strike** for melee (torso coil driving the arm through the arc), and the held stances **Aim Rifle**, **Aim Pistol**, **Dual Wield**, and **Guard** — fire one and it stays until **Lower Arms** (or another held stance on the same bones) takes over, which is the existing latch mechanic doing exactly what it was built for. The sim bar shows one Play button per action name rather than one per rule, so a six-bone stance is one button. Verified live twice over: the Demo engineer walks with legs counter-swinging through 61° and a keyframed Wave that peaks at exactly its authored 140° and returns to rest; and a Meshy-exported 135 MB, 1.62-million-vertex character loads in 3 s, binds in 6 s, and walks in the preview at 185 fps on GPU skinning. All new text ships in the 17 non-English locales.

- **Size = largest dimension; camera easing.** The map sync's `span = max(extent.x, extent.z)` (footprint) became `max(x, y, z)` in BOTH the visual scale and the collision footprint (they must agree) — Fleagus (0.90 wide) and reduced Carol (0.66 wide), both 1.70 tall at size 2, rendered 3.8 vs 5.1 tiles tall under footprint normalization; both now stand exactly 2.00 (verified in-game as leader + follower). The car/bike/plant are wider than tall, so their sizing is untouched. Preview jitter: a frame-gap probe measured zero drops (worst 7ms at 596k tris) during orbit/zoom — the "jitter" was discrete stepping. Database3DEditor and ModelGraphicPicker gain `_viewGoal` + per-frame exponential easing (~70ms time constant, frame-rate independent); wheel steps at 1.15× per notch into the goal; the picker's face-framing also glides to its crop. Verified: goal jumps 4→1.73 on six notches while the view settles smoothly to it.

- **Binary weights sidecar (`model.rig.bin`).** Layout: u32 magic "RRWB" / version / meshCount, then per mesh u32 meshIndex + u32 vertexCount + count×4 indices + count×4 weights (the existing top-4 Uint8 quantization, no base64). `ModelRigger.encodeWeightsBinary/decodeWeightsBinary/buildRigBinary` (pure, dual-export); `Reactor3D.decodeRigWeightsBinary` is the parity-pinned runtime mirror. `bindRig` builds the binary form (JSON carries `weightsFile`, never bytes; the in-memory decode rides `customRig.weightsBin`, stripped by a JSON replacer on save); `saveRig` writes/deletes the bin beside model.json; `loadSidecar` and `loadModelSidecar` read it back and attach `weightsBin` (the pointer is a bare filename — path-like values refused); `readModelRig` accepts attached buffers or legacy base64 identically (parity-tested). Verified live end to end: bind → 7KB model.json + 242KB bin, disk-reload reads 2 meshes/17 bones back, and the running game skins the player through the binary path with all Walk rules live.

- **Editor previews on the worker, preview warm-up, folder glyph/state sync.** `Reactor3D.readModelAsync(buffer, ext, baseUrl, texture)` wraps the worker-or-sync dance once; `Database3DEditor._loadTemplate`, `ModelGraphicPicker._drawPreview`, and `EventPageEditor._renderModelPreview` all await it (each already had generation guards). `_rebuildInstance` runs `renderer.compile` + `initTexture` right after attaching the instance — shader compilation and texture upload were trickling through the first seconds of orbiting as one-frame hitches ("smooths after a time"). Folder-list fix: `renderModelList` resolved the auto-selection AFTER painting, so `selectModel`'s folder-open seed landed post-paint — the header drew ▸ while the state said open, making the first click a no-op close ("click twice" reports); the selection now resolves and seeds before the rows paint. Verified live: first paint shows the selection's folder expanded, single-click toggles both ways, `_parseWorker` live in the editor, reduced Carol's template loads through it and plays Walking.

- **Deploy dialogs rebuild on language change; full audio-quality range.** BuildManager and DistEditorManager stamp `_builtLanguage` in `setupModal` (now idempotent — it removes the previous modal element first) and `open()` rebuilds when the current language differs and no worker is running — the tt()-baked markup was frozen at construction while every phrase already sat translated in the deep catalog. The quality select lists 1–10 (descriptive anchors kept at 3/5/7/10, bare digits between — digits need no translation) and `DeploymentAssetPreferences` snaps to the full range instead of [3,5,7,10].

- **Map model preloading, GPU warm-up, worker-parsed GLB.** `collectMapModelSpecs` gathers a map's full model list (event sidecar pages + every actor's character slot, deduped by cache key; gated on `isMap3D`, not the loaded-runtime checks — cold boots haven't loaded THREE yet); `preloadMapModels` loads them all behind the loading fade; `Scene_Map` wrappers (END of reactor_scenes.js) gate `isReady` on the preload with an 8s fail-open timer. `warmLoadedTemplates` adds unwarmed cached templates to a throwaway scene for one `renderer.compile` pass + `initTexture` per texture — called at Scene_Map.start and from the sync loop for late arrivals (Set-guarded no-op steady-state). GLB parsing moves off-thread: `reactorSplitGlb` + `reactorDecodeGlbImages` live in reactor_3d.js, get `toString()`-assembled into a Blob-URL worker (the runtime stays one module — the architecture tests rejected a separate worker file, correctly; a Blob worker also serves the editor), transfer the file buffer in, and return parsed JSON + `ImageBitmap`s (decoded with glTF's unflipped/unpremultiplied orientation); `buildGlbTemplate`/`_loadGlbTexture` accept the bitmaps and wrap them directly as textures. Any worker failure transfers the buffer back to the synchronous parser; non-GLB formats never leave it. Verified live: the map gate holds while five models load, all five instances stand with objects one second into gameplay (previously 10–15s of visible pop-in), OBJ still loads, zero errors.

- **Event editor chrome pass.** The autonomous-movement Route… button joins `rr-btn-chip` (it painted itself in the panel background with no hover); the event Name input and Note textarea take the theme's surface/border tokens (the name field was a browser-default white box); the variable condition reads `Variable  [picker] ≥ [value]` — the ≥ moved between the name and the value where it reads naturally, the label's own ">=:" suffix dropped, and the value field is themed; the commands column's header wears `database-section-header` instead of blending into the background.

- **Head-on picker camera, stale-transform animation anchor, deferred thumbnails.** ModelGraphicPicker's default view is `{yaw: 0, pitch: 18}` — the 25-degree default meant "looks straight at me" authored a −25-ish yaw the map faithfully played back (found via a per-frame trace: direction resolved 2 everywhere, yaw constant −0.476 rad = the stored −27°; the owner's Fleagus binding was corrected). `Sprite_Animation.targetSpritePosition` falls back to `parent.worldTransform.apply(sprite.x, sprite.y)` when the target sprite is invisible or its own worldTransform yields non-finite points — v8 never updates transforms for sprites that skip the render pass, and a model event's hidden stand-in is exactly that, so animations anchored to wherever the transform froze (verified: anchor now returns the sprite's exact projected position beside the plant). `renderModelList` schedules `_fillThumbnails` after the paint and the fill yields a macrotask before each uncached render — first-time GLB load+clone+render chunks were swallowing folder-toggle clicks. `docs/demo-missing-se.md` lists the 120 stock SE names the animation database references that audio/se no longer holds.

- **General-panel mini model previews, in-list (None), stale-sprite skip.** `attachRow` gains `previewHost`/`thumbnail`: a cached render of the bound model under the icon column, live-measured to the icon preview's exact footprint for symmetry (weapon/armor/item editors pass `.db-general-grid > div`; the row itself moves into `.db-form` so it aligns with the fields); the shared `modelThumbnail(reactor3dEditor, spec)` helper now lives in Database3DBindings (exported — its first ship missed the api object and the throw blanked the whole weapon panel; thumbnail calls are wrapped so a provider failure can never take the detail down again). `RRPickerIndex.createBrowser` gains `leadingItem` — an action row pinned atop the file list — and `showImagePicker`'s (None) uses it instead of a row above the search box. The face and enemy-battler callbacks call `refreshListIcon`, so clearing a face updates the actor list immediately. Runtime: `Sprite_Character.updateBitmap` wrapper (end of reactor_sprites.js) skips the 2D sheet load when `characterModelSpec` answers — a 3D-bound actor's leftover characterName 404'd the map. Verified live: Short Sword's model beside its icon, row aligned, clean Demo boot with zero load errors, player model up.

- **Facing marks optional + 3D toggle beside the button.** The markless default in `applyEventModelPose` (yaw = dir8Yaw + authored) already equals the glTF export convention (front = +Z, toward the camera at rest) — now pinned by a contract test and surfaced in the picker: a `.model-face-status` line says marks are optional, and a **Clear marks** button (visible only with marks set) resets `selectedFaces` to the export default. `decorateSlot` moved the 3D checkbox from an absolute top corner into a flex row with the graphic's change button — no label padding tricks, titles single-line. Three new phrases ship in the 17 locales.

- **Actor-page 3D perf, (None) graphics, centred labels, trim fixes.** The slot thumbnail provider consults `reactor3dEditor._thumbs` (the 3D section's own data-URL cache) before rendering — cloning+rendering a large model per detail re-render was the typing/click lag — with a one-shot cache refresh at 1.8s for late-decoding embedded textures. `showImagePicker` gains `options.allowNone`: a (None) row above the file browser that calls back with `''` (character/face/SV/enemy battler pickers opt in), and those pickers' empty-folder alerts are gone — an empty `sv_actors` no longer blocks clearing a 3D-bound battler. `decorateSlot` pads the box label on both sides so it centres. `dist-editor-worker`'s model trim collects names from `Database.r3d.json` (event sidecars alone would strip bound actor/enemy models from web builds) and recurses into organization folders, trimming per model folder and pruning emptied parents — flat-walk deleted a whole nested folder over one unused member. Demo reorganized into `Actors/Enemies/Vehicles/Weapons`; `Map001.r3d.json` references remapped to the new paths.

- **Nested model folders.** `ModelGraphicPicker.listModels` walks `3d/` recursively (depth-capped, skipping `source`/`textures`), naming each model by its forward-slash path under `3d/`; the source-file match compares the path's last segment. `splitModelRef` accepts slashed segments (backslashes normalized) while refusing empty, `.`, and `..` segments, so notes and sidecars stay jailed inside `3d/`. Every URL and path builder already composed `3d/<name>/…` by concatenation, so nested names flow through loading, sidecars, thumbnails, bindings, and deployment unchanged. Both lists render folders as collapsible groups: the DB 3D Models list groups by first segment (open-state per folder, the selection's folder held open, searches expand matches, indented short names, thumbnails fill per visible row), and `RRPickerIndex.createBrowser` gains an opt-in `folders` mode — folder sections ahead of the letter sections, same toggle rules — used by the model picker (the shared item factory was refactored, other pickers unchanged). Verified live on the Demo's `Weapons/Sword_Fleagus`: collapsed by default, toggles open in both lists, selects, and renders.

- **Per-slot actor 3D (character / face / battler) and gait-specific triggers.** `Database.r3d.json` actor entries become `{ character, face, battler }` slots (a flat legacy entry reads as its character slot and migrates on the next slot write; `Database3DBindings.get/set` take an optional slot, `Reactor3D.actorSlotSpec(id, slot)` reads them, `databaseModelSpec('actors', id)` stays the character slot for the map). `decorateSlot` mounts a corner 3D checkbox on each of the actor page's graphic boxes: bound, the box retitles, swaps its 2D canvas for a 140px model thumbnail (rendered twice — a first render can precede embedded-texture decode and bake a silhouette), and its button opens the model picker in capture phase; unbound, the original 2D flow is untouched. The face slot's picker adds `{framing: true}` — Zoom and Height sliders stored as `spec.view`, with the preview camera steering to the exact crop. Runtime: `actorFaceState` renders a lit 144×144 portrait (camera-side key light; repaints at 0/0.7/2.5s for texture decode; windows that drew too early are refreshed via a waiters list), `Window_StatusBase.drawActorFace` wrapper at the END of reactor_windows.js; `updateActorModelSprite` + `Sprite_Actor` update/updateBitmap/updateFrame wrappers at the END of reactor_sprites.js render the battler slot into `_mainSprite` (full-bitmap frame, stock motion cells bypassed, no SV sheet needed on disk; `playActorBattlerAnimation` queues actions). Triggers: `walking` and `dashing` join the rule/clip trigger set — `Reactor3D.moveTriggerActive` grades them, clip pick prefers the most specific gait then falls back to `moving`, spin gain and pose blends share the gate, the map sync passes `character.isDashing()` (followers mirror the party leader), and the sim bar gains a Dash toggle. Picker fixes: skinned models measured as an empty Box3 so animated GLBs were never centred (feet-at-origin left them towering off-frame) — centring now uses the template's measured size; `aimCamera` adds +0.5 to x/z (cell centres), so targets pass −0.5 to aim the true origin (picker and DB thumbnails both). Verified live end to end: three checkboxes and retitled bound boxes, face-framing sliders, Carol's lit 3D portrait in the pause menu, the engineer standing as the actor's battler in battle under the full plugin stack, and Fleagus dead-centre in the picker. New phrases ship curated in the 17 locales.

- **Real-time animation clock, rig-mode orbit, clip speed.** The Database 3D preview's frame counter incremented per rAF tick — a 120Hz display doubled every rule and clip; it now derives frames from `performance.now()` and `applyModelAnimation` runs once per animation frame (spin and walk-distance gains accumulate per call). The runtime's clip mixer steps by the caller's frame delta instead of a fixed 1/60 per call (`binding.clipFrame`), so any call rate plays clips at true speed. Rig mode adds `'rig'` to the orbit predicate — presses that miss every marker orbit; marker hits still drag. Clip rules carry a `rate` field (0.25–3, default 1, omitted when 1): `clipAction.timeScale`, duration scaled in `modelRuleDuration`, a **Speed** percent slider on the card, wired through defaultWork/snapshot/editRule/savePose. And `renderEditCard`'s parts gate now counts embedded clips — a clip-only GLB (no parts, no rig) hid the card entirely, which is why adopted clips seemed to have no settings. Verified live: rig-drag orbits 38°, clock 59.9fps, saved rate 2 halves the rule duration (62→31 frames), same-frame mixer step is exactly 0.

- **Embedded clips surface in the Animations panel, and skinned GLB clips play undistorted.** `renderRuleList` appends an **Embedded clips** section for a clip-bearing GLB: per clip, ▶ plays it through the sim's `__preview` action with a transient clip rule (no authoring needed), and ＋ adopts it as a saved `{name, type:'clip', clip, trigger:'action'}` rule — named after the clip so `playModelAnimation`/`playBattlerAnimation` and the sim bar pick it up; an adopted clip shows ✓ and its row disables. Underneath, `buildAnimatedGlbTemplate` now binds skinned meshes with an **identity bindMatrix**: three applies `boneWorld · IBM · bindMatrix` per vertex and glTF's inverse binds already map mesh space to joint space, so binding with the mesh node's world mixed the armature transform in twice — harmless-looking at rest (a uniform shrink the camera framing absorbed: the reduced Captain Everson measured 1.7cm), catastrophic in motion (bones correct, skinning shredded — 72/72 tracks bound and sane while the render exploded). Verified live on her six Meshy clips (raw mixer and rule-engine paths both clean) and pinned by a synthetic cm-armature skinning test that fails 66× out of range under the old bind.

- **Database 3D bindings: actors, enemies, weapons, armor, and items take a model.** A shared `Database3DBindings` helper owns `data/Database.r3d.json` ({ actors/enemies/weapons/armors/items: { id: spec } }, spec identical to a map sidecar's event entry; clearing the last binding deletes the file) and renders the 3D Model row — checkbox, bound-model name, Change Model — on all five detail pages through `ModelGraphicPicker` (handed a shim carrying mapEditor3D so the preview can load three.js — the database editors' bare project shim left it black); the picker's cancel path never calls back, so the row watches for the modal leaving the DOM and resyncs from the sidecar. The runtime loads the sidecar once (disk-stat first, so its absence never logs), `characterModelSpec` answers for `Game_Player` (party leader's actor binding) and `Game_Follower` (their own actor's), the model sync includes the player and visible followers (instance keys `p` and `f<index>`), and `normalizeModelSpec` is the single raw-entry-to-validated-spec path shared with event models. In battle, `Sprite_Enemy.update` hands bound enemies to `updateEnemyModelSprite`: an offscreen shared WebGL renderer draws the posed, lit model into the sprite's bitmap every frame, ambient rules play on the battler's own clock, `playBattlerAnimation(enemyId, name)` queues named actions, and an enemy transform rebuilds the state — the wrapper is installed after the class body, since `Sprite_Enemy`'s prototype is replaced wholesale at definition and an early wrapper dies with it. Rig markers carry canvas-sprite name labels (center yellow, left blue, right red) that ride marker drags with their mirrored twins; hover raycasts skip entirely during orbit/marker drags and on a stationary pointer, and the raycast interval scales with the model's triangle count — a 3.1M-triangle Meshy character orbits at ~6 ms a frame where it previously hitched. Verified end to end over CDP: the row on all five pages, picker open/cancel/resync, binding round-trip showing the model name, the rigged engineer walking as the player with 17 bones and Walk rules on the map, both goblins of troop 1 rendering as ready 3D battlers at 60 fps under the Demo's full 44-plugin battle stack, and marker labels on screen. A bound enemy needs no battler art on disk: the stock image load is skipped and a placeholder bitmap stands in until the model render takes over (plugins read the bitmap for state-icon placement). New phrases (35 marker labels and Change Model) ship curated in the 17 non-English locales.

- **Static humanoid models rig inside the editor — no external tools.** Database > 3D grows a **Rig** tool: thirteen draggable joint markers (mirrored left/right, color-coded, with the derived 17-bone skeleton drawn live between them) fit the standard humanoid to any static model, and **Bind rig** solves skin weights right there — distance-to-bone falloff with a side gate that keeps a left thigh off the right leg, seam welding so UV-split duplicate vertices can never crack open when posed, and Laplacian smoothing over the mesh's own edges. The solve is fast enough to feel instant (218 ms for the 30,940-vertex Demo engineer) and the model reappears skinned on the spot. Every bone then **is a part on the pose card**: click the model and the dominant bone under the cursor is picked from its skin weights, the card's Pose/Swing/Spin/Bob sliders bend it live about its own joint, parent bones carry children exactly as a skeleton should (the same ancestry composition the tank's turret uses — a bone's local rotation simply *is* forward kinematics), and held poses, timed effects, previews, and undo all work unchanged because bones ride the existing rule engine rather than a new one. The rig persists in the model's own `model.json` — skeleton plus quantized weights, about 330 KB for the engineer — and the game performs the identical bind at load: verified end-to-end in a live run where the editor bound the engineer, saved a Raise-Arm animation, and the running Demo loaded the model skinned, played the animation through `Play Model Animation`'s pipeline (a shoulder vertex measurably swinging 11.5 model units), and eased back to exactly rest. A model carries a rig **or** carved parts, never both — both count triangles over the uncarved meshes — and the editor says so instead of letting them scramble each other. All new UI text ships in the 17 non-English locales.

- **MP3, WAV, FLAC, and M4A are first-class audio formats alongside OGG.** Project data stays extensionless exactly as MZ stores it; the actual format resolves at load time. In the runtime, `WebAudio._startLoading` resolves the on-disk extension up front through `Utils.resolveAudioExtension` (NW.js, alongside the existing case resolution — so plugins constructing `WebAudio` directly inherit it), while web deploys walk the other extensions in `_onError` only when a request died with zero bytes received, then restore the original URL for honest error reporting. `_readLoopComments` dispatches on container magic: OGG unchanged, WAV reads the `fmt ` sample rate and first `smpl` sampler loop, MP3 reads ID3v2.3/2.4 `TXXX` `LOOPSTART`/`LOOPLENGTH` frames with the sample rate from the first MPEG frame header, FLAC reads STREAMINFO and its vorbis comments — all bounds-checked because streamed loads parse a growing prefix, and a truncated-prefix `decodeAudioData` failure no longer counts as a load error (generation-guarded; WAV prefixes genuinely fail where OGG's tolerate). The Vorbis fallback decoder is now gated to `.ogg` URLs. In the editor, every audio surface (Audio Player, audio commands, System sounds, Animation SE, movement-route SE) lists all formats through a shared `RRAssetFiles.AUDIO_EXTENSIONS`, ranked so OGG shadows a same-named twin (MV's `.ogg`+`.m4a` pairs keep listing once); `AudioCoverArt` additionally extracts MP3 `APIC` frames (front cover preferred) and native FLAC `PICTURE` blocks; encrypted variants (`.mp3_`, `.wav_`, `.flac_`) decrypt everywhere the OGG ones did. `audioFileExt()` still answers `.ogg` for plugins that ask. Verified in a live NW.js boot: all four formats resolve from a plain `.ogg` request, decode, and report correct loop points and sample rates.

- **Every audio picker is the Audio Player's interface, served by one shared modal.** New `RRAudioPickerModal` (src/utils/AudioPickerModal.js) carries the player's own classes: a cover-art header (`track-art-large`) naming the selected track, the fine-step seek row, play/pause/stop/loop `audio-player-button`s beside optional `audio-inline-control` volume/pitch/pan cards, and the alphabet rail with letter-section headers and 26px cover thumbnails per row — lazy-loaded through the same IntersectionObserver pattern and the Audio Player's shared cover cache and locale-aware track ordering (`window.reactor.audioPlayer`, with local fallbacks). Clicking a row selects and previews it, double-click confirms. Four surfaces route through it: the System 1 slots (title/battle/vehicle BGM, ME slots, system SE list — level cards shown, replacing the per-row play buttons and bare slider grid), the audio event commands' Browse picker (cards hidden since the command dialog owns those fields; previews at the command's current levels, and picking (None) now clears the command), the movement route's Play SE dialog (fully replaced — its dropdown-plus-sliders dialog is gone), and Map Properties' autoplay BGM/BGS via a new "…" button beside each dropdown that writes name and levels back into the form. The single `releaseAudio` path closes the preview AudioContext on every exit, keeping all four surfaces under Chromium's per-page context cap. Shared styling: the `.audio-player-info` card is panel-black behind a `--color-accent` border, and `.audio-scroll` lists get an accent-gradient scrollbar pill on a `--color-bg-deep` channel — the Audio Player itself inherits both. The Demo's 65 Psychronic BGM tracks carry embedded `METADATA_BLOCK_PICTURE` covers (art sourced from the MP3 masters; oversized art recompressed to 512px so extraction stays within the prefix-read ceiling).

- **The Audio Player's BGM/BGS/ME/SE tabs read as tabs.** Dormant tabs were the modal background with no outline; they now sit as bordered near-black chips (`--color-bg-panel` + `--color-border`), hover in the accent tint, and keep the accent highlight when active — all from theme variables, so every palette in both modes gets its own version.

- **The Audio Player shows album art and packs its controls into one row.** New `AudioCoverArt` util parses the OGG Vorbis/Opus comment header for embedded pictures (`METADATA_BLOCK_PICTURE` FLAC blocks and the legacy `COVERART`/`COVERARTMIME` pair), reading only a growing prefix of the file — `RREncryptedAssets.readAssetBytes` serves those prefixes for plain, encrypted, and case-mismatched files, since only the first 16 bytes are XORed. The header shows a 56px cover beside the track name and every track row a 26px thumbnail, loaded lazily via IntersectionObserver as rows scroll into view and cached per path; tracks without art get a deterministic name-hued placeholder tile. Volume/pitch/pan moved out of their grid card into the playback-button row as inline slider groups, and the seek sliders (Audio Player and both AudioCommandEditor previews) step at 0.1% instead of the default 1% snap.

### Changed

- **PixiJS updated to 8.20.0** (from 8.14.0), in the editor's dependencies and the bundled runtime (`runtime/libs/pixi.js`, synced to every template project). The internals the compat layer patches (`_gpuData`, `_activeBlendMode`, the back-buffer filter path, TexturePool) are unchanged in 8.20. Verified live: the Demo boots to title and renders a 3D map cleanly, and Star Shift Rebellion's plugin-heavy map title renders through the MV compat layer — both with zero console errors — and the full editor test suite passes.

### Fixed

- **Event clipboard and undo carry the 3D model sidecar.** `EventManager` copy/cut attach `map.reactor3d.events[id]` to the clipboard payload (`ReactorClipboard` included, so cross-window paste works), paste writes the entry under the new event's id, delete purges the entry (`getNextEventId` reuses freed ids — a stale entry would attach its model to a future unrelated event), and `saveState`/`undo`/`redo` snapshot and restore `{ events, models }` together (old bare-array stack entries still restore). Covered by a behavioral test driving the real class through copy → paste → delete → undo → redo → cut → paste.

- **Light themes pass a real contrast audit.** A WCAG sweep of every text-on-background pairing across all seven palettes found the light modes leaning on accent tones picked for dark backgrounds — the default light theme's gold `--color-accent-hover` sat at 2:1 as text on white (track names, letter headers, link-colored labels were the "hard to read" spots), and several palettes put white button text on accents that only reach ~3:1 (creamsicle's orange was worst at 2.85). Light-block accents are darkened until accent-as-text and white-on-accent both clear 4.5:1 (`--color-accent`, `--color-accent-hover`, and dim placeholder text, per palette), with dark themes untouched. The toolbar's pixel-art icons — bright cyan drawn for dark button faces — get a light-theme-scoped `filter` pulling them toward their outline color so they read as ink instead of neon on cream. The About dialog also now credits Three.js alongside NW.js and PixiJS v8, in all 18 locales.

- **The map grid no longer loses lines as the view zooms out.** The grid is drawn in map space inside the zoomed container, so its width-1 stroke shrank with the view — at 50% zoom every line was half a device pixel wide and rasterized away (measured live: 0 of 50 expected lines at 50% zoom, 2 at 25%). The stroke now uses PIXI 8's `pixelLine`, which stays exactly one device pixel through any transform: the full line count renders at every zoom level, and the grid also stops fattening past 100%.

- **The tileset palette empties stale slots when the map's tileset changes.** `TilesetPaletteViewer.loadTilesetImages` skipped empty `tilesetNames` entries without clearing the per-slot texture cache, so creating or opening a map whose tileset leaves a slot blank (no A3, no C, ...) kept rendering the previous tileset's sheet in that slot. `loadTilesetForMap` now resets the cache and cached layer canvas on every load, clears the selection only when the tileset actually changed (map switches sharing a tileset keep the pick), and carries a load token so a slow superseded load can neither commit its sheets nor repaint the palette after a newer switch.

- **Asset filename casing resolves before the request instead of in the error handler.** The 0.98.2 recovery lived in `Bitmap._onError`/`WebAudio._onError`, which plugins may replace without calling the original — CGMZ_Fallback does exactly that, so on case-sensitive filesystems a Windows-authored case mismatch (`npc_female2` vs `NPC_female2.png_`) became the fallback placeholder forever plus perpetual `ERR_FILE_NOT_FOUND` console spam. `Utils.resolveFileCase` now corrects the URL at every request point (plain bitmap, encrypted bitmap, audio) with a single `existsSync` gate keeping the exact-match fast path to one stat; the `_onError` retry remains as a backstop. Verified live in Haven: the three mismatched NPC sheets load their real 624×576 art with zero console errors.

- **A `<3d>` map with no painted sidecar no longer logs a failed request on every entry.** `DataManager.loadMapSidecar` asked the network for `Map###.r3d.json` and treated the 404 as the normal "render flat" answer — correct behavior, but the browser logs `ERR_FILE_NOT_FOUND` at a level the page cannot suppress, so testing Star Shift Rebellion's Map596 spammed the console on each visit. Under NW.js the loader now stats the sidecar on disk first and skips the request entirely when the file is absent; a sidecar that exists is requested and parsed exactly as before (verified live against SSR's Map596/Map612 pair).

- **Class parameter curves author and display the engine's real 999-level domain.** Reactor's level cap is 999, but the Classes graphs plotted only the stored MZ levels 1–99 and the Generate Curve modal stopped at a "Level 99 value" slider. Both graphs now plot the exact series the game computes at every level 1–999 — stored values where the array has them, the runtime's linear extrapolation at the final slope beyond, drawn dashed and dimmer past a faint divider so projected growth reads as projected. The modal's second slider authors the **Level 999 value** directly (label curated in all 17 locales), the readout reports Lv1/Lv99/Lv500/Lv999, and Apply writes every level per-level, so what the curve shows at 999 is authored rather than an accident of the slope at 99; HP/MP bounds rise to 99,999 and other stats to 9,999 to fit the taller domain. Legacy 100-entry arrays still open (the cap value seeds from the extrapolation, the shape infers from whatever domain is stored) and still play identically — the runtime reads exact stored values first and extrapolates only past the array, so no runtime change was needed. Verified live over CDP: the mini graphs ink the full width, and Apply commits a 1000-entry array whose Lv999 matches the modal.

- **The Classes dialogs wear the editor's standard modal chrome.** The Learnable Skill editor was a bare padded box with an inline heading, the Trait editor rolled its own header and footer with a hard-coded dark tab bar (`#252525`, wrong in light themes), and their footers mixed three button styles between them. All four dialogs on the Classes screen — Learnable Skill, Trait, Generate Curve, and EXP Curve — now use `rr-modal-header`/`rr-modal-body`/`rr-modal-footer` with a secondary Cancel and a primary OK/Apply, the tab bars sit on `--color-bg-panel`, and the Trait editor's title says Add or Edit to match the action. Trait rows across all six tabs share one grid (radio | label | control | prefix | value | unit), so every select, number, and % sign lands on the same vertical lines — including through the dropdown shim wrapper, which had collapsed selects to their shortest option. Verified live: all rows compute the same six grid columns on every tab.

- **Trait sections' Add/Edit/Copy/Paste/Delete are the shared chip buttons.** Classes, Actors, Weapons, Armors, States, and Enemies all styled those five buttons inline with JS hover handlers, visibly mismatching the Learnable Skills, Effects, and Action Patterns strips beside them. All six editors now use `rr-btn-chip` with CSS-driven hover and disabled states, and the hand-rolled hover/enable juggling is gone.

- **An app-wide modal and button consistency pass.** Every OK/Save/Apply confirm button that was styled inline with accent colors — 65 event-command dialogs, the plugin manager, the audio picker, the animation picker, the battle-test dialog, System pages, the movement-route editor, and the Troop and Common Event command dialogs' shared button factory — is now the one `rr-button-primary` class, with the leftover JS hover handlers (which fought the class's own hover) removed. The Skill/Item **Effect editor** was rebuilt on the same modal chrome and shared grid rows as the Trait editor (its hard-coded `#252525` tab bar is a theme token now), and the **enemy Action Pattern editor** gained the standard header and footer bars in place of a bare padded box.

- **Every event command dialog and the remaining database dialogs wear the standard modal chrome.** All ~85 event-command editors carried hand-rolled copies of the header, title, close button, and footer as inline styles; they now use the `rr-modal` chrome classes (84 titles, 81 close buttons, and 141 header/footer bars converted by pattern, plus the Event Command and Animation pickers and the map Find Event dialog). On the Troops page, the enemy picker, the page Conditions dialog, the raw-parameter fallback dialog (Common Events' twin included), and the Battle Test configuration all moved onto the same chrome with close buttons where they were missing, the sidebar's Battle Test and Members/page-toolbar buttons became the shared secondary and chip classes, and the Animation editor's SE picker, Effekseer effect picker, and cell context menu dropped their one-off chrome — including a hard-coded `#3a3a3a` hover that ignored light themes. The Deploy Game and Deploy Editor wizards joined too: standard header bars, and their Start/Cancel Build buttons moved out of the log column into a proper footer bar with the primary/secondary styles.

- **Deploy Game compresses every audio format, opt-in per deploy.** The "Re-encode OGG audio" option is now **Compress audio (lossy)**: one checkbox and one quality choice covering everything the engine plays — OGG re-encodes through libvorbis at the chosen quality as before, MP3 through LAME VBR and M4A through AAC at bitrates mapped from the same scale, and WAV and FLAC convert to OGG Vorbis, the format the runtime already prefers when several carry one name. Loop points survive every path: OGG/FLAC vorbis comments and MP3 `TXXX` tags carry through the re-encode and are verified afterwards, and a WAV's `smpl` sampler loop is written into the produced OGG as `LOOPSTART`/`LOOPLENGTH` using exactly the runtime's semantics — any file whose loop tags fail verification, or whose result isn't smaller, ships untouched, and a WAV/FLAC shadowed by a same-named OGG is left alone. The encoder checks confirm libvorbis, libmp3lame, and aac in the verified FFmpeg build only for the formats the project actually contains. Because the compression is lossy, the checkbox now starts unchecked on every deploy instead of restoring a saved preference (the quality choice is still remembered), and the settings note spells out the per-format behavior in all 17 locales. The deploy wizards' scroll areas also join the accent-pill scrollbar treatment with a proper gutter, so options no longer sit flush against a near-invisible scrollbar.

- **Modals and database pages behave at small window sizes.** `rr-modal` is globally capped at the viewport (`max-width: calc(100vw - 16px)`), 65 event-command dialogs cap their fixed pixel widths with `min(...)` plus a `max-height` and a scrolling body, and the Actors page's section pairs and the Classes page's columns moved from inline grid styles into CSS — so the existing `database-detail` container query can collapse them to one column when the pane narrows, verified live (2 columns wide, 1 at 700px). The trait/effect modal grid also tightens its columns below 540px.

- **No more awkward voids in paired database cards.** On the Actors page, General Settings ended mid-card while the Images card ran long, and the Traits card floated its buttons above dead space. The card-fill chain (section → content → form) now covers the Actors pairs: the Profile textarea absorbs the leftover height exactly as Description does on Skills — a grow-row alignment fix (`align-items: end` from the shared row rule was pinning the field to the bottom of its grown row) applies to every filled card — and list-card action strips (traits, learnable skills, effects, action patterns) pin to the bottom edge of a stretched card so a short list reads as free list area. In the Trait editor, rows without a dropdown (Attack Speed, Attack Times+, Action Times+) place their number where the dropdown column sits instead of leaving the middle of the row empty.

- **Legacy pixi-filters (v5 era) run on the PIXI 8 runtime.** Plugins bundling the old filter pack (Kawase blur, Godray, DropShadow, MultiColorReplace, SimpleLightmap, ...) override `Filter.apply` with multi-pass bodies that call v5 FilterSystem APIs, which black-screened Haven/Project3 the first time a scene enabled one. The mv_compat bridge now covers the whole surface: `_frame`/`filterFrame` aliases on textures (assignable, matching v5), `getFilterTexture`/`returnFilterTexture` mapped onto the v8 TexturePool and sized from the running filter pass, array uniforms with literal or `const int`/`#define` sizes carried into the uniform group, extra `sampler2D` uniforms routed to shader resources through non-enumerable accessors (a texture key the uniform-sync generator can see but not type crashes it), a rename for shaders that use `finalColor` as their own local before PIXI 8 claimed that name, and v5 `(callback, scope)` ObservablePoint construction. Verified in the live NW.js runtime: all 41 filter classes in Project3's bundled plugin construct and render with no errors.
- **Web builds no longer crash on a 3D-bound actor's retired sprite sheet.** `Database.r3d.json` reads synchronously on NW.js but is an async fetch in a browser, so the first web frames didn't yet know an actor was 3D and requested the 2D walking sheet the project no longer ships — "Failed to load img/characters/...". `Scene_Boot` now waits for the sidecar answer (the fetch always settles with success or failure, so boot cannot hang), and a player or follower sprite never commits to a sheet while the answer is pending — it retries the next frame instead. Verified by booting the Demo with the sidecar artificially delayed two seconds: boot held, the stale sheet was never requested, and both bound actors reached the map as models.
- **Deploy audio compression keeps embedded cover art.** FFmpeg's demuxers surface embedded art (an Ogg `METADATA_BLOCK_PICTURE` comment, an ID3 `APIC` frame, a FLAC `PICTURE` block) as an attached picture stream and delete the tag it came from, so every audio re-encode shipped artless music while faithfully preserving loop tags. The optimizer now extracts the picture before encoding and writes it back — rebuilt as a `METADATA_BLOCK_PICTURE` comment for Ogg outputs (through an ffmetadata file, never argv, which Windows caps at 32K characters), or as a copied attached-picture stream for MP3 and M4A. A failed with-art encode retries without art rather than failing the deploy. The Demo soundtrack's art, dropped by the 0.98.3 size pass, was restored losslessly from the pre-compression files (66 of 68 tracks; the other two never had art).

- **The MV-compat stale-event guard no longer eats plugin-spawned events.** The guard drops save-restored events whose map data no longer exists — but it judged staleness by looking the event's id up in `$dataMap` directly, and action/spawner plugins (Hendrix Action Engine item drops, template events, DotMoveSystem companions) legitimately add runtime events at ids beyond the authored map. Every `Game_Map.refresh` deleted them and the console spammed "dropped N stale event(s)" while drops vanished off the ground. The guard now asks the event itself (`event()`, which dynamic-event classes override to serve their own data): an event whose own data comes back empty or throws is stale; one serving plugin data is alive. Verified in the Hendrix Action Combat project: a full boot–play–transfer run that previously warned continuously now runs silent with the spawned events intact.
- **Tilemap teardown no longer warns about a destroyed texture still bound to a shader.** PIXI 8's mesh pipe keeps the last-drawn texture in its shared shader after the final draw, so destroying the tile atlas on map transfer logged "[BindGroup] a 'textureSource' was destroyed while still bound to a shader" every time. The atlas release now points those shader slots at the empty texture before destroying the source — guarded so a renderer without that private shape simply keeps the harmless warning.

- **Render-guard warnings now say which resource died.** The SpritePipe guard that skips destroyed/orphan sprites (instead of letting PIXI 8 crash the render pass to a black screen) warned with the sprite's class and parent chain but not the cause. It now names the exact reason — destroyed, `_gpuData` nulled by a destroy, or a destroyed texture source — and every Bitmap's GPU texture source is labeled with its file path (or canvas size), so "texture source destroyed [img/system/Window.png]" points straight at the leaked resource. Verified live in the Haven test project: labels resolve to real asset paths on every scene sprite.
- **Album art shows in the web editor too.** The cover-art reader sniffs a small prefix of the audio file through synchronous byte access, which a browser does not have — on itch.io it silently returned nothing and every track wore its placeholder tile. In web mode the same prefix walk now runs over `fetch` with a Range header, streaming and cancelling at the read cap when the host ignores ranges, so a 13KB cover never downloads a 5MB track. Both call sites already resolve() the reader's return value, so the promise the web path returns is adopted without any UI changes; pinned by a test that serves a real Demo track through a Range-ignoring fake server and asserts the extraction and the byte cap.

## [0.98.2] - 2026-08-16

### Added

- **3D models animate through authored rules, and the Database has a 3D section to author them.** A model folder carries `model.json` naming its animation rules — spin (wheels roll with distance travelled and hold their angle at rest), swing (a hinge about a named part's own pivot), and bob — triggered always, at idle, while moving, or on demand by name. The readers preserve named-part ancestry and pivots (GLB before its hierarchy is baked flat; OBJ by splitting named groups into their own meshes), so one rule like `DEF-Wheel` turns all four wheels of a car about their axles. Database → 3D lists every model folder in the project, discovers its part names from the mesh — scene-graph plumbing (root/rig/scene wrappers that grab every mesh at once) and exporters' anonymous "Object" bucket are kept out of the list, and numbered siblings collapse into one group option (DEF-Wheel drives all four wheels) — edits the rule list with a live animated preview (Walk simulation, a Play button per action), and writes each model's own sidecar on every change, no hand-written JSON. Selecting a rule draws a box around exactly the meshes it drives with a live affected-part count, edits restart accumulated spin from rest, and a GLB's embedded clip count is surfaced. **Embedded clips play.** A GLB that carries animations keeps its node hierarchy live instead of being baked flat: joints stay real objects, skinned meshes ride a GPU skeleton, every instance rebinds to its own cloned bones, and each embedded clip is parsed into a playable animation. A "Clip" rule type in the 3D section plays a chosen clip through the same trigger system — always, at idle, while moving, or on demand from the Play Model Animation command — with crossfades between states; one-shot actions return to the ambient clip when done. A model whose clips are static poses (common in ripped character models — sas blue's 394 are an eye-test pose library) gets a pose system from the same rules. Sizing an animated model measures a CPU-skinned sample of its rest pose, the only size matching what renders — bone positions undersized a Source-style rig to a speck. The model list in the 3D section now uses the standard database list styling with a search box. A new Play Model Animation event command (under a fourth Reactor tab in the command picker, where Reactor-only commands will grow) triggers an action on this event, the player, or any event; it is stored as an MZ plugin command for "RPGReactor", so the data stays loadable everywhere and a stock MZ runtime simply ignores it. In the Demo, the Buick's wheels roll as it drives, and the monster-plant sways, pecks at idle, and bites on command.

- **Event Image can author a 3D model into the map sidecar.** Checking 3D opens `ModelGraphicPicker`: folder names under `3d/<folder>/source`, a WebGL orbit preview with the same rotation gizmo as the Animation Generator, size in tiles, and four placeable facing dots rather than snap-yaw buttons. Apply writes `map.reactor3d.events[id][page]`; the Event editor preview fills the leftover left column and turns with the page's Down/Left/Right/Up. Runtime `characterModelSpec` prefers that sidecar over `<r3d>` notes. Models occupy their ground AABB, do not block themselves, refuse a turn that would hit the player, and draw other characters as upright billboards so they pass in front or behind the mesh. A model collides as its true rotated rectangle at the yaw the mesh actually stands at — the authored spec rotation plus the front-mark aim, not the facing alone — so a character walks right up to an angled vehicle's body instead of being stopped tiles away at a bounding-box corner, and a model posed with an authored yaw no longer collides crosswise to its visible body. A gliding model keeps its trailing tiles occupied, a step that implies a turn is collision-tested in both body orientations against the player and other solid events, the mesh eases through turns at `MODEL_TURN_SPEED` instead of pivoting instantly — and a turn must clear the disc the body's corners sweep, in both swing directions, with the footprint covering the arc while the easing plays out — and character billboards draw right side up (three's default `flipY`, not the glTF convention). Leaned billboards — character sprites and tile cut-outs alike — write the depth of a vertical twin at their anchor (`straightenBillboardDepth`), so front/behind against real meshes settles per pixel by ground row instead of the lean tipping a sprite's head into the model behind it. On such maps the star-flagged tiles render inside that same depth buffer rather than as a flat composite pass, so a character in front of a tall structure occludes its top and one behind it is hidden; a stationary event on a facade cell — any priority — snaps to that wall's plane and is pulled just ahead of the coplanar wall art, so a console screen shows whole over its machine while a genuinely nearer character still occludes it by real depth, in front winning and behind hiding exactly as the camera sees it. A walking character standing on a facade's own footprint — pressed against a console, crossing a machine's apron rows — has their depth pushed just in front of that wall's plane, since the art on their cell draws under them in 2D; off the footprint, genuinely behind still means hidden. Character billboards also take the tile cut-outs' half-cell step towards the camera, and an above-characters event whose cell was stood into a facade anchors on that wall's recorded plane (`facadeAt`), lifted along the same leaning axis its quads use — so an event's art keeps the alignment it was authored with over tile art from every camera position instead of drifting off it towards the screen edges.

- **Looking around the model picker no longer edits the pose, and posing happens through rings around the model.** A plain drag used to rotate the model itself about camera-relative axes, so inspecting a model quietly baked pitch and roll into its authored angles and it stood crooked in game — which read as the face marks depending on the viewing angle; worse, once any roll existed a straight drag bled into all three angles at once and the model fought the hand. Left-drag orbits now, and the pose controls are three colour-coded rings wrapped around the model: grab a ring and the model follows the pointer around that one axis, the others untouched. The rings nest like a gimbal so each always matches the axis it will change, arcs passing behind the model hide behind it (a faint twin keeps the circle traceable), and they whisper until touched: dim at rest, brighter under the pointer, full only while held — when the other two fall away so the turn reads clearly. Placing a Front/Back/Left/Right dot hides the rings entirely, since the dots share the same colour palette — and an armed dot lands on a stationary left click again (making plain drags orbit had silently killed placement, since the old gate required the drag state that no longer exists). A near miss lands too: sparse meshes — foliage, struts — let the ray slip between triangles at spots the eye reads as on the model, and swallowing that click kept the mode armed with the rings away, so placement looked refused; the ray's entry into the model's bounds stands in, and a click past the bounds entirely — out where the rings circle — still lands on the model's side in the clicked direction. An armed canvas click is never swallowed — the dot lands the moment the button goes down, on the pressed pixel, so hand wobble can no longer turn a placement into an orbit. A drag that slips past the modal's edge also no longer cancels the picker outright (a browser click targets the common ancestor of press and release, so escaping the preview "clicked" the backdrop and threw away the dots); closing by backdrop needs the press and the release both out there. The floating gizmo ball and controls strip no longer stand between the pointer and the scene: the strip's transparent gaps pass clicks through, and while a side is armed the gizmo ball goes inert and dim so a dot click lands on the model behind it — in a small window those widgets sit right over the model's base, and they were catching the very clicks that looked like placement refusing to work. A grab also lands on the ring the eye sees: rings are picked by screen distance to the drawn line, tie-broken at crossings by whichever ring is drawn on top, where fat invisible grab tubes previously sent a third of direct clicks to a ring visibly away from the pointer. Ctrl-/right-drag keeps a turntable nudge (horizontal turns, vertical tips — never a camera-axis tumble), the X/Y/Z fields still take typed angles, and new Upright / Reset buttons rescue a drifted pose — Upright keeps the turn and stands the model up.
- **OBJ models render with their textures.** The OBJ reader now keeps texture coordinates (welding position/uv corners into one attribute), the picker names a colour map from the model's `textures/` folder — preferring the colour pass over normal/emissive companions — and the sidecar carries that choice to the runtime, so a plant that shipped as OBJ + JPG stands textured in the picker, the event editor's preview (which also derives the map for specs saved before the sidecar carried one), and the game. STL/FBX/DXF/3MF/USDZ gain the same texture path where their readers produce UVs.
- **Model files keep whatever extension case they shipped with.** Exporters write `Plant_001.OBJ`; the picker's lowercase-only listing rule hid such files and the folder read as empty, so OBJ models seemed unsupported. The model picker now lists any-case extensions and the sidecar records the real filename; a note-based spec with no extension also probes upper-case variants at load.
- **A 3D object spanning both palette halves selects and extends as one.** A B–G sheet is sixteen columns shown as two stacked eight-column halves, so a prop crossing column eight — a full-sheet reactor — is authored as two drags. Ctrl and Cmd now arm the extend gesture alongside Shift, the merged object's selection is stored in sheet coordinates and drawn as both display pieces, and after an extend the whole object stays highlighted instead of only the piece just added.
- **Arrow keys step through asset picker file lists.** In the character graphic picker — and every picker built on the shared file browser — Up/Down move the selection one file at a time with the preview following, scrolling the list only when the selection reaches its edge, instead of scrolling the pane out from under the reader. Home/End jump to the ends, and the keyboard is armed the moment the picker opens.
- **The tileset 3D preview turns when dragged and fits its pane.** Standing art is drawn as an upright plane that shears with the turning ground line, so a drag visibly swings the object instead of only sliding a parallelogram beneath a fixed picture; near edge-on it holds a readable width, as the in-game billboard does. The scale now answers to the pane's width, the ground's depth, and the slab's height at once — a fixed ten-pixel floor previously let a sixteen-row object tower off the top of the pane and read as off-centre.
- **The flag Key stays on screen while a tall sheet scrolls.** The Key column — and the passability brushes and 3D tool palette it hosts — is sticky within the sheet's scroll pane, so painting the bottom rows of a 32-row F sheet no longer means scrolling back up to switch marks.
- **The tileset passability Key doubles as a paint palette.** Clicking O, X, or ★ in the Key arms that mark: a click on the sheet writes it directly and a drag paints every tile the pointer crosses, with autotiles still mirroring across their 48 shapes. Clicking the armed mark again returns to the classic click-to-cycle. Re-crossing a tile mid-drag is idempotent, so a wobbly sweep cannot flip anything back.

### Fixed

- **Tiles no longer show crawling horizontal seams when the camera pans zoomed or at fractional positions.** The PIXI 8 tile mesh inset every tile's texture rect by half a texel, compressing 48 texel rows into a 47-row span — invisible at integer alignment, but the moment a camera plugin panned the map at fractional screen positions or any zoom was in play, one texel row per tile duplicated or dropped, and its position crawled with the pan ("warbly" seams). The inset is now a whisker (1/128 texel), just enough to keep a quad edge from sampling the neighbouring atlas row: at 2× zoom every texel row now doubles exactly, and integer-aligned rendering is pixel-identical to before.

- **A menu-background snapshot survives the next snap for plugin windows still showing it, and setting `blur` on a filter is quiet again.** MZ destroys the previous screen snapshot the moment a new one is taken; menu-background plugins cache it into their own window sprites (a game's save screen hit this), and under PIXI 8 the destroyed texture drops that sprite from the render with a destroyed/orphan console warning. Retired snapshots now pass through a three-deep trail before dying, so a cached background keeps drawing and memory stays bounded. PIXI 8 also kept `BlurFilter.blur` only as a deprecation wrapper that warns on use — VisuStella's picture effects set it every frame — so the compat layer replaces the accessor with a silent passthrough to `strength`.

- **The Web editor bundle fits browser storefronts' 1000-file upload cap again.** The zip had grown past it, and the bulk was library stock the bundled project never touches: battle-hud face skins for actors the project does not define, source .psd files, and a hundred unreferenced stock enemy battlers. The web packager now trims exactly those — the audio library, fog pack, and everything the sandbox browses stay — dropping the archive from over a thousand files to roughly 700. Extracted size comes down the same way: 3D model folders no map places (a 77MB rip among them) leave the web bundle, and the Character Generator's bulk-imported looseleaf style pack — 76MB of generated part data — stays out of the web editor bundle, which shrinks from 87MB to 11MB and parses at boot accordingly; the compact psychronic style ships, and the soundtrack is untouched. The extracted bundle lands near 426MB. Counts and sizes are a storefront's rule, not the build's: packaging always completes, reports its file count and extracted size, and warns past 1000 files or 500MB. Shipped templates also exclude any live project lock or playtest saves. The web editor also loads again at all: the desktop runtime-refresh check probed project files with synchronous reads the browser host reserves for preloaded files, and the uncaught throw froze the web editor on its splash screen — the web bundle bakes its runtime in at packaging time, so the check now stands down in the browser. And because browser-saved edits shadow the bundled project, a returning player kept seeing the OLD Reactor One after an update — its new content missing or broken; the bundle is stamped now, and when the stamp changes under stored edits the editor offers to load the new version up front instead of silently serving stale files.

- **A native-Windows GPU crash on the 3D map toggle now diagnoses and heals itself.** Checking 3D could take the whole editor down instantly on Windows proper (while fine under Wine/Proton) — a GPU-process death no error handler can see. Every activation stage is now breadcrumbed synchronously; a leftover breadcrumb at the next launch means 3D killed the last session, so the editor says so, records which rendering strategy was in flight, and the next attempt takes the other one — the PIXI-shared WebGL context and a dedicated context each dodge a different class of Windows driver crash. If both strategies have crashed on a machine, 3D refuses to arm rather than crash again. The breadcrumbs then caught the actual culprit on the first native report: the editor injected three.js as inline script text, which parses synchronously on the main thread — and V8's recursive parser overflows the ~1MB Windows main-thread stack on a 2MB bundle (STATUS_ACCESS_VIOLATION) while Linux's 8MB stacks never noticed. Libraries now load through Blob URLs with external-script semantics, compiled off the main thread; a library-stage crash also no longer blames a context strategy, since no context existed yet.

- **Effekseer effects no longer draw black squares and slabs — every blend mode lands on the real scene.** Effects drew on a transparent overlay canvas, and each blend mode broke its own way: additive layers wrote alpha into empty pixels so black-backed textures composited as opaque black squares, multiply and darkening layers darkened nothing into solid black, and distortion panes had no background to sample. While effects play, the overlay now carries a copy of the whole rendered scene and the effects draw ON that copy — the same shared-framebuffer semantics the stock runtime has; the copy is pixel-identical to what it covers, so it is invisible in itself. Distortion panes capture their backdrop from that copy, aligned to each effect's own viewport. Verified by playing all 120 Demo animations and grading screenshots for black growth: the black-square class is gone, and what remains dark is darkness effects doing their job.

- **Editing an event no longer strips its model's texture.** Opening the event editor rebuilt the model spec for its pending pages and dropped the `texture` field, so pressing OK after any change — movement, a switch, anything — rewrote the sidecar without the colour map and the model stood gray in game.

- **A plugin that replaces `Tilemap.prototype.update` no longer blanks the tile layer.** MultiTweaks' "Tilemap animation speed" option installs a copy of the stock MZ update body; PIXI v5 tolerated that because it invoked `updateTransform` itself during render, but the PIXI 8 runtime runs repaint from its update tail, so the replacement left `_addAllSpots` unreachable — a black map with sprites, windows, and a clean console. `Tilemap.initialize` now installs a v8 `onRender` fallback that runs the shared `_prepareV8Frame` when the frame reaches render unprepared; a frame stamp keeps preparation single-shot so the Project3 double-preparation seams cannot return.
- **Encrypted RPG Maker projects display their images and audio in the editor.** Deployed MV/MZ games ship `.png_`/`.ogg_` (or `.rpgmvp`/`.rpgmvo`) files, so an imported game opened as an empty checkerboard, the tileset palette threw drawing a 0x0 canvas, and character previews stayed blank. The desktop asset resolver now decrypts those files on the fly with the key from `System.json` — recovering the key from the constant PNG header when the entry is missing — and asset listings present encrypted files under their plain names. Filename lookups also fall back to a case-insensitive match, since Windows-authored projects freely reference `bell3` for `Bell3` and a Linux filesystem does not. The runtime gained the same case correction: a Bitmap or WebAudio load that 404s retries once with the on-disk casing before reporting an error.
- **The fading splash screen no longer swallows the first clicks on the map.** It kept intercepting pointer events while transparent until its display switched off; it now lets clicks through as soon as the fade begins.
- **Opening an existing Reactor project updates the runtime that Playtest and deployment actually execute.** Editor releases update the application, not the `js/reactor_*.js` files already copied into every project. That allowed the 0.98.1 editor to open and playtest a project still carrying 0.98.0's shader bridge: its translator removed the `filterArea` declaration and rewrote `.xy`, but left `.zw` references undeclared, producing the unchanged `Could not initialize shader` and invalid-program flood despite the fix being present in the editor's canonical runtime. `reactor_main.js` now carries the engine version. Before game data is loaded, an older Reactor project receives the current canonical runtime recursively, preserves `reactor_plugins.js` and unrelated plugin files, updates its Reactor metadata, and is not rewritten again on later opens at the same version. Raw RPG Maker projects remain untouched until the explicit Install Reactor Runtime action.
- **Learnable Skills, Effects, and Enemy Action Patterns are directly editable.** The Class learnings table was display-only, while Skill/Item Effects and Enemy Actions put Add behind an undiscoverable context menu. Each now has visible Add/Edit/Delete controls, row selection, double-click editing, and Enter/Delete handling. Class learning edits author level, skill, and note; enemy Add uses a transaction-safe draft instead of silently inserting Attack, and HP/MP action conditions are presented as percentages while retaining the runtime's fractional data.
- **Extended face sheets are selectable throughout the editor.** Four columns and 144px cells remain RPG Maker-compatible, but the row count now comes from the loaded image rather than a hard-coded two. Actor list/detail previews, the Actor face picker, both Show Text face pickers, message preview, and Change Actor Images can preserve and choose indexes above 7.
- **The Sprite-based animation type can author a new MV animation.** Format detection now follows the runtime and keys off the `frames` array rather than requiring Animation 1 to already have a filename. Switching from Effekseer creates one empty frame, leaves both sheet pickers usable, persists the conversion, maps MZ `color`/`duration` flashes to valid MV combined timings and back, preserves imported `pattern: -1` cells, and refuses a seventeenth cell that the MV runtime cannot draw. New records remain MZ-format until Sprite-based is selected.
- **Troop Plugin Commands use `PluginCommandEditor`.** Insertion and editing of both MV code 356 and MZ code 357 now expose the same plugin, command, annotation, and argument controls available on map and Common Event pages instead of raw JSON.
- **Map event creation exposes the interactions that were already intended.** Event-mode double-click accepts the browser's click count and a 500ms fallback instead of requiring two presses inside 300ms. Enter creates on the selected empty cell or edits its event; context-menu rows show shortcuts; Ctrl/Cmd+F opens event search; and event copy, cut, paste, undo, and redo support Command on macOS. The Conditional Branch selector groups the complete stock MZ set separately from Reactor Advanced input conditions while preserving all existing command shapes.
- **Quick Event Creation builds the four stock MZ conveniences from an empty Event-mode cell.** Transfer opens Reactor's visual map/location picker and authors direction and fade. Door and Treasure use the project character picker, automatically suggest matching project assets and sound effects when available, and generate stock movement-route animation. Treasure can award gold, an item, a weapon, or armor before enabling an opened-chest self-switch page. Inn authors the offer choices, gold condition and deduction, fade/wait/recover sequence, and insufficient-funds branch at the chosen project currency price. The modal owns only a draft: Cancel or Escape performs no map mutation, commit revalidates the map and cell, and the complete event is one Undo step.
- **Simplified Chinese no longer lets generated guesses override reviewed terminology.** The deep catalog now fills only missing phrases. Reviewed forms restore terms including `职业`, `会心`, `注释`, `公共事件`, HP/MP commands, quantity, and Close; Turkish text accidentally copied into Chinese toolbar keys is removed; and `zh_CN`, `zh-CN`, `zh-SG`, `zh-TW`, `zh-HK`, and `zh-MO` aliases resolve to the matching script locale.
- **Database layout responds to the detail pane, not the outer window.** Container queries collapse generic section grids and form rows when navigation and the record list leave too little room. Translated labels wrap instead of ellipsizing, and the specialized Actor, Class, and Enemy compositions now reflow too; Class parameter curves use intrinsic columns rather than forcing four narrow graphs into half the workspace.
- **One-line plugin annotations no longer glue `@text` and `@desc` into the parameter name.** MZ3D writes `@param spacer|graphics @text<bidi>@desc ===` on a single line so RPG Maker can treat the bidi marks as a blank label. Reactor took everything after `@param` as the name, so the Plugin Manager showed `spacer|graphics @text @desc` and could not match the saved `spacer|graphics` key. The header, struct, and default parsers now split every known annotation on a line and strip format characters from values. Those spacer rows render as a rule, not as an empty field labelled with the internal `spacer|graphics` name. Section titles such as Map Settings that only exist to parent other parameters no longer show an empty box either.
- **MZ3D keeps imported character models and hides the 2D sheet from `pixi_compat`.** Cropping GLB materials to a walk frame smashed the authored UVs into a solid blob. `installMz3dCompat` only hides the 2D map sprites MZ3D already turned off via `_tilemap.visible`.

## [0.98.1] - 2026-08-11

### Fixed

- **The Web editor can load the map's 3D viewport dependencies.** The checkbox was not disabled; activation failed and immediately unticked it because `MapEditor3D.ensureLibraries()` only knew the desktop path: locate `runtime/` through `process.cwd()`, synchronously read `libs/three.js` and `reactor_3d.js`, and inject their source. WebHost has no Node `process`, and immutable project scripts are hosted assets rather than synchronous virtual-filesystem contents. The Web branch now resolves `project/js/libs/three.js` and `project/js/reactor_3d.js` through `host.assetUrl()`, loads them lazily and in order as classic scripts, shares an in-flight request across rapid toggles, and clears a failed request so it can be retried. Desktop retains its existing filesystem loader. Distribution coverage opens a fresh Web ZIP and verifies both files byte-for-byte against `runtime/` while confirming the outer editor page does not eagerly load three.js.

- **A saved 3D preference cannot make every later project open repeat a crash.** 0.98.0 wrote `map3DView: true` to the global `rr-settings` record before attempting library loading, geometry construction, WebGL2 context creation, shader compilation, or the first render. Deleting the original project therefore changed nothing: every project uses the same NW.js profile preference. Clean activation failure changed only the in-memory field, and a native Chromium/driver exit could not run rollback at all. A new process now migrates saved 3D state to false before the last project can auto-open. Each manual activation durably records false before graphics work and records true only after a complete build and initial render, so even a native exit leaves the next launch in 2D. 3D remains active across maps and projects in the current process, but every fresh process requires an explicit opt-in.

- **3D viewport activation and teardown are transactional.** Concurrent enable requests share one activation instead of creating multiple canvases and orphaning all but the last renderer. Disabling records the desired state and invalidates a pending activation even while scripts or images are loading. Renderer construction, the first rebuild, and the first render are contained; any exception resolves activation as false, restores the PIXI canvas, clears durable state, and reports the reason rather than becoming an unhandled rejection. WebGL2 is checked explicitly because the bundled three.js no longer supports WebGL1, and pixel ratio is capped at 2 to bound framebuffer allocation.

- **The 3D editor no longer creates a second WebGL2 context beside PIXI's.** Checking 3D on the bundled 50x50 Reactor One map could terminate the Windows NW.js process, including when the Windows build ran through Wine, while native Linux remained stable. That platform split follows Chromium's Windows ANGLE/D3D context path rather than map geometry. PIXI 8 explicitly supports sharing its context with Three.js through renderer-state resets, so 3D now pauses PIXI's ticker, renders Three into PIXI's existing canvas and WebGL2 context, and places a context-free overlay above it for orbiting, painting, and event input. WebGL1 is refused without probing or allocating another context.

- **3D renderers and stale map work are released deterministically.** Teardown disposes Three-owned scene and renderer resources without calling `forceContextLoss()` on the shared PIXI context, resets PIXI's cached GL state, restores its dimensions, redraws 2D, and resumes a ticker that was previously running. Project close and database-load failure tear down 3D before destroying the map renderer. Rebuild generations prevent an older map's delayed sheet, parallax, or character loads from committing after teardown or replacing a newer scene. A render exception stops the animation loop and fails back to 2D instead of scheduling another throwing frame at the display refresh rate.

- **The 3D editor preview refuses allocations that can terminate Chromium before JavaScript can catch an error.** The 2D editor supports 512x512 through chunked residency, while 3D still constructs the complete scene at once in ordinary arrays before copying it into GPU buffers. 0.98.1 limits preview construction to 40,000 cells and 400,000 estimated source quads, preserving the measured 200x200 production map while keeping larger or unusually dense maps in 2D with a clear status. Runtime map data and the game's own 3D behavior are unchanged.

- **MV-era PIXI filters no longer produce invalid PIXI 8 shaders during battle snapshots.** The v8 bridge replaced the legacy `filterArea` declaration and its `.xy` size reads with `uInputSize`, but left `.zw` origin reads untouched. Haven's Pixelate filter consequently compiled with an undeclared `filterArea`; the battle-background `Bitmap.snap()` path exposed the lazy compilation failure and then emitted `useProgram: program not valid` every frame. The translator now maps the logical origin to `uOutputFrame.xy`, maps legacy `filterClamp` to `uInputClamp`, handles precision-qualified declarations, and supports whole-vector or component reads. PIXI's six filter-global uniforms are excluded from the plugin `UniformGroup`, preventing zero-valued custom defaults from overwriting the real filter frame, input size, clamp, or output texture. The canonical runtime was synchronized into all local project templates; a real Project3 NW.js render of its bundled `PixelateFilter` completes with no shader messages and `glError: 0`.

- **PIXI 8 tilemap meshes no longer expose a blank, partial, or folded object seam while the camera pans.** `Spriteset_Map.update()` assigned the tilemap origin after `Tilemap.update()` had already synchronized its transform and mesh, so that update processed the previous camera position. The render transform could then detect the current position, repaint by clearing the visible geometry, and leave its newly generated commands dirty until the following frame. The current origin now reaches the tilemap before the child update cascade, and `_addAllSpots()` immediately synchronizes every lower, upper, or plugin-added tile layer before returning.

  One mixed phase remained: the PIXI 8 compatibility bridge also ran the complete plugin-wrapped `Tilemap.updateTransform()` chain from `onRender`, two or three times in a measured Project3 frame. `TF_Billboard` splits tall objects across 19 independently positioned and sorted row layers, so rerunning their positioning while PIXI constructed render groups could briefly fold adjoining rows even though every mesh was complete. Tilemap preparation now runs exactly once from `Tilemap.update()`; Window and TilingSprite retain the generic render hook they still require. A real Project3 mesh-backend smoke test completed a 360-frame diagonal out-and-back pan across 23 mesh layers with one preparation per frame, no hidden or dirty layer, and no fallback.

## [0.98.0] - 2026-08-10

### Added

- **An event can decline to join the object painted over its cell**, with `<3d ground>` in its note — or `<no 3d object>`, which says the same thing the other way round. Painting a group on the map takes everything standing on those cells and makes it one object, which is exactly right for the things that *are* the building: an animated sign, a lit window, a swinging shop door. They ride the building's plane and so hold still against it as the camera comes round, which is the whole reason grouping exists. It is wrong for anything merely passing through — a character walking behind a shop crosses its cells, and the moment they stop walking they are pinned to the shopfront and carried up it, because "is this part of the building?" and "is this standing on the building's square?" are the same question to everything except the author. A walking townsperson was already excluded by having left home; this is for the ones that stand still somewhere a building was painted. Honoured in the running game, in the pass that bakes events into the map, and in the editor's preview, so none of the three can disagree.

- **A parallax is chosen by looking at it.** Map Properties offered a dropdown of filenames, which is a poor description of a picture — parallax names run to things like `!Aeheri-Leader-Room`, enough to tell two apart only if you already know both. A **Browse…** button opens the editor's own image picker: the whole folder, searchable, with the current choice highlighted and scrolled to, and a full-size preview of whatever is selected. `(None)` is an entry in that list rather than a separate button, because clearing the parallax is the same kind of choice as picking one.

  The dropdown stays, since it is the fastest way back to a name you already know, and a thumbnail of the current parallax now sits under it with the image's pixel dimensions — which answers "is this the one?" without opening anything. Clicking the thumbnail opens the picker too. A name whose file has since gone hides the thumbnail rather than showing a broken image.

  The picker itself moved above the other dialogs. It sat at a z-index of 1001 and everything that had opened it until now happened to sit below that; the map properties dialog is at 10000, so the picker opened *behind* the dialog that asked for it — plainly triggered, with no way to tell.

### Changed

- **The language globe is a global control at the far right of the menu bar.** Its short code (`EN`, `JA`, `ES` and so on) shows which locale is active even when the translated menu labels are unfamiliar, updates immediately when the language changes in Options, and yields the code but not the globe on a narrow Web layout.

### Fixed

- **3D water moves in-game, mountain ranges keep their coverage, and mixed standing/flat objects meet cleanly without duplicate support meshes.** The geometry already carried animated A1 UVs and the editor advanced them, but the runtime render loop never passed its tilemap animation frame into the 3D scene. Runtime water and waterfalls now use the same clock as the 2D tilemap, while the editor uses elapsed time so 60, 120, and 144 Hz displays all advance at the intended rate.

  A repeating multi-cell foliage stand-in is one picture at its authored footprint rather than one complete object per constituent tile. Every source quadrant retains its physical map plane, and an incomplete stamp reconstructs the absent quadrants so a ragged fill edge cannot turn the lone mountain variant into half a mountain. A separate underlay at 0.6 opacity fills transparent silhouette gaps without contributing opaque depth. Declared structures preserve one composition frame and pivot across source layers and incomplete rows; missing cells can be reconstructed from that frame, standing rows and flat apron rows keep their roles, and the `edgeHinged` path prevents a declared facade from being shifted half a cell into its own footing.

  The lower and starred passes contain only their authored pieces: `_supportMeshes` and the runtime-only lower copy are gone. Fully opaque texels draw colour and write depth together in an exact-alpha core (`alphaTest = 1.0`); alpha from 1 through 254 stays in the depthless blended pass. This gives the depth buffer stable ownership without cutting soft edges or punching colourless holes into flat shadows. Source map layer remains the coplanar tie-breaker, and the editor renders lower geometry with events before clearing depth and drawing the starred pass, matching the runtime's lower/character/upper sequence.

- **The running game now batches its visible map into native PIXI 8 meshes.** The v8 compatibility path had restored tile rendering with one pooled Sprite per tile rectangle, which was correct but made every scroll repaint detach and reattach thousands of objects. Each physical `Tilemap.Layer` now emits its ordered commands into one typed geometry over a shared 4096×3072 nearest-neighbour atlas; shadows use a reserved atlas texel, animated autotiles rebuild with the same cadence as RPG Maker, and plugin-added tile layers receive both the tileset and the per-frame synchronization pass. Plugin children that are not tile layers survive clears and bitmap changes untouched, and Ultra Mode 7 still owns tile visibility while active.

  This is compatibility-safe rather than mesh-or-nothing. A layer automatically returns to the pooled Sprite path if atlas creation, GPU limits or geometry updates fail, and `window.$reactorTilemapBackend = "sprites"` forces that path for comparison (`"mesh"` requests the new path and `"auto"` is the default). `window.$reactorTilemapStats` reports builds, vertices, allocations, pool hits and fallback reasons. On a live WebGL run of Star Shift Rebellion map 596, a forced 180-frame scroll held mesh p99 at 5.7ms with 15.1ms total geometry-build work; the sprite path reached 22.2ms p99 and performed 222,234 pool operations. Both paths rendered with `glError: 0`.

- **Map dimensions are bounded before they can become an allocation.** The Map Properties fields advertised 999 as their maximum but never enforced even that: typing 10,000×10,000 proceeded to a 600-million-entry tile array, 5,000×5,000 could mutate resize state before failing to save, and 500×500 eventually asked PIXI to cache a 24,000px-wide layer. PIXI rounds cache dimensions to powers of two, so that became a 32,768² RGBA framebuffer, four GiB for one of as many as eight static layers, and WebGL discarded its contexts rather than throwing a useful JavaScript exception.

  Reactor now supports 1×1 through **512×512**, four times MZ's maximum map area. Larger maps use the viewport culler as an actual residency boundary instead of lazily materializing every offscreen tile a few seconds after load. The resident rectangle is aligned to 32-tile chunks with a one-chunk halo: ordinary movement consumes geometry already prepared around the screen, crossing its edge builds only the entering chunks and destroys leaving chunks as units, and unchanged meshes survive. Zoom and pan prepare destination coverage before changing the transform, so a frame cannot expose a missing chunk.

  Static tiles, autotile quadrants and animated A1 water are emitted as typed vertex, UV and index buffers grouped by source sheet and chunk, then drawn through PIXI 8's native WebGL mesh pipeline. A1 animation changes only mesh UV buffers every 30 frames, and painting rebuilds only affected chunks. This replaces hundreds of thousands of JavaScript display objects with a few hundred GPU meshes without allocating map-sized framebuffer caches. It also removes the visible-cell zoom restriction: minimum zoom is again the integer-pixel scale that fits the complete map. On Star Shift Rebellion's exact 200×200 map 596 data, which would expand to an estimated 312,141 tile sprites, true full-map zoom now builds in 147ms and leaves 461 display objects (219 meshes and 48 shadow sprites); twelve complete A1 updates average 2.05ms each and returning to a smaller resident window takes 5.6ms. A headless WebGL render with the project's real tileset PNGs completed at 0.0625 scale with all 40,000 cells resident, no context loss and no GL error; the previous all-sprite path did not complete the same full-map check within 15 seconds. Tile-layer caches independently reproduce PIXI's power-of-two allocation, query the active GPU limits, and decline anything over a conservative byte budget both when first cached and when painting expands a sparse cache.

  Typed sizes above 512, fractions and nonpositive values are rejected before new-map allocation, resize analysis or project mutation. Pasted maps and loaded files must have valid integer dimensions and exactly six complete data planes; imported files over 64 MiB are declined before JSON parsing. Full-map canvas previews and exports have their own pixel budget, and large-map undo depth scales down to keep whole-map snapshots within a bounded memory allowance.

- **Map JSON keeps its tile data inline, as RPG Maker does.** Ordinary saves expanded every tile ID onto its own indented line, paste compressed the entire map onto one line, and resize-reference updates used a third layout. The formats parse to the same object, so games were not functionally wrong, but the expanded tile plane roughly doubled real map files and added avoidable serialization, disk and parse work precisely where large maps already cost most. Every `Map###.json` writer now uses one hybrid layout: readable two-space properties and events, with only the top-level `data` array compact. The bundled Demo map falls from 145,984 to 70,981 bytes without changing one parsed value.

- **Event mode now outlines the cell under the pointer before it is clicked.** Empty ground and cells holding events use the editor's golden-yellow selection outline, so both creating and selecting have a visible target. Moving onto the cell already selected leaves its stronger persistent mark alone, leaving the map hides only the hover, and the first click on empty ground no longer clears its own selection while waiting for the double-click that opens a new event.

  The arrow keys move the persistent target one cell at a time and stop at the map edges. Crossing an occupied cell selects its event; crossing empty ground keeps the cell itself selected, so Ctrl+V puts a copied event exactly where the outline says it will and selects the new event afterwards. A map switch or Escape clears that target, and no target means no paste rather than an invisible fallback to `(0, 0)`.

- **Map shadows are composited where RPG Maker puts them.** The editor placed the shadow plane immediately after map layer 0, so an authored base tile on layer 1 could cover a shadow that RPG Maker draws above both base planes. Shadows now follow layers 0 and 1 and precede decoration layers 2 and 3, including in chunked large-map rendering.

- **PIXI 8 filter lists remain mutable through the legacy plugin API.** PIXI 8 freezes the array returned by `Container.filters`, so the common plugin sequence `this.filters = this.filters || []; this.filters.push(filter)` threw before an encounter transition could start. Reactor returns a cached mutable proxy and commits every push, splice, index assignment, deletion, or length change through PIXI's native setter, preserving PIXI 8's internal representation and its filter-effect attach/detach bookkeeping.

- **PIXI v4 geometry `copy` methods are restored with their original directions.** Point, ObservablePoint, and Rectangle copy their argument into themselves through `copyFrom`; Matrix retains the legacy opposite direction through `copyTo`. Existing native implementations are left untouched. This restores MV-era callers such as MVNovaLighting without silently reversing Matrix copies.

- **MV window padding follows plugin overrides again.** MZ's `updatePadding()` reads `$gameSystem.windowPadding()` directly, bypassing the `standardPadding()` method MV plugins replace. The MV semantics layer restores the original call so a zero-padding autosave window keeps its complete client area while ordinary windows still use the game system value.

- **MV font families declared in `fonts/gamefont.css` load under Reactor.** MV relied on a stylesheet to register names such as `GameFont`, while the modern runtime uses `FontManager` and previously registered no legacy family. Reactor reads valid local `@font-face` declarations and loads them through `FontManager`; browser-only play falls back to linking the stylesheet. When an imported MV project carries MZ's default font filenames but does not ship them, its declared game font is used instead.

- **A `ParticleContainer` draws the sprites put into it again.** v8 kept the name and changed what it renders: only `particleChildren`, which are `Particle` objects handed to `addParticle()`. Ordinary children are ignored outright, so a plugin written against the old API adds a hundred sprites, gets no error and no warning, and sees nothing on screen — the weather, the particles or the damage numbers simply never appear. It is now an ordinary container, which draws them. Nothing is given up: v8's batching needs `Particle` instances that no MZ-era plugin constructs, so those call sites could never have reached it. v8's own class remains available as `PIXI.__v8ParticleContainer`, and the MV spelling `PIXI.particles.ParticleContainer` points at the same place for MZ projects, where the MV compatibility layer is switched off.

- **A tile layer that a plugin added to the tilemap can draw again.** Through PIXI v5, v6 and v7 the tile textures belonged to the shared renderer plugin — one atlas, uploaded once, addressed by set number — so a layer drew from it whether or not it had ever been handed the tileset. That is what made it reasonable for plugins to add tile layers of their own, which billboard and overpass plugins have done for years. v8 has no shared tile renderer: each layer builds its tiles out of its own image list, and a layer that was never given one drops every tile it is asked to draw, with no texture to make and nothing to report.

  TF_Billboard moves every ☆ tile that *also* carries passage flags onto a layer of its own, so it can stand it up as a billboard. On a wooded map those are the trees, and each one lost exactly the tiles that plugin had taken — solid holes in the shape of the tiles themselves, a clean console, and an editor that looked perfectly correct, because the editor runs no plugins at all. The tileset now reaches every tile layer on the tilemap rather than only the two it built itself.

- **A tileset image that has not decoded is never made into a texture source.** The source is cached on the image element and lives as long as the session, so one built from an image measuring nothing stays 1×1 for good: every tile drawn from that sheet afterwards samples a single pixel and arrives as one flat colour, and the sheet finishing loading invalidates nothing. Painting runs every frame, including the frames before the tileset has arrived, so this was reachable in ordinary play — it turned a forest into plain green squares. Such a tile is now skipped and drawn on the repaint that follows the tileset actually being ready. Every window puts its contents inside a client area carrying an alpha filter, and hands that filter a rectangle so a scrolling list is clipped to the inside of the window instead of spilling over the border. Through PIXI v5, v6 and v7 the rectangle was screen-space and used exactly as given; v8 reads it as *local* to the container and applies the container's world transform itself. Handed the screen-space rectangle the engine has always documented, v8 transforms it a second time, and the region it captures lands about twice as far from the origin as the window — off the edge of the screen entirely for any window past the middle. The filter then resolves to nothing and everything inside the client area vanishes: the text, the cursor, the scroll arrows. The panel and its border are not in the client area, so they keep drawing, and the window renders as a correct, empty box with no error and nothing in the display tree measuring wrong.

  Reactor's own copy of that method had been changed to write the local rectangle v8 wants, which is why this was only ever seen under plugins — windows are a favourite thing to reimplement, and a plugin's version was written against the documented screen-space rectangle years before v8 existed. VisuStella's Core Engine has one, so a title screen built on its event title scene showed its frame and none of its six commands. Obfuscated releases cannot be edited in any case. The rectangle is now written in world space on every version, as it always was, and converted for v8 in a separate step afterwards — so whichever version ran last, ours or a plugin's, ends up clipped to the right place.

- **No plugin's `WindowLayer.render` runs on PIXI v8, and none of them throws either.** That method masks each window with raw GL stencil calls, flushing a global batcher between them; v8 has no global batcher — each render pipe defers its own — so the calls never bracket the draws they were written for, and the stencil test is simply left switched on across everything drawn afterwards. Reactor's own copy returns early on v8 for that reason. A plugin that replaces the method does not know to, and VisuStella's Core Engine does, so it failed on the first missing call with a `forceStencil is not a function` on every frame. Shimming that call so it could run to completion was worse than the error: the whole masking dance then took effect and every window it was meant to reveal was rejected. The rule is now the class rather than the call — nothing deriving from `WindowLayer` has its replacement `render` bridged to v8 — and the missing framebuffer calls are stubs for the plugins that reach for them elsewhere.

- **A bitmap whose canvas has been freed is no longer uploaded to the GPU.** MZ releases a bitmap's canvas once it has an image to draw from, and a plugin calling `update()` afterwards asked v8 to send pixels it no longer holds: `texSubImage2D` received no canvas, WebGL raised `INVALID_VALUE`, and it repeated every frame for the life of the map. A source with no picture — no resource, or a detached canvas measuring zero — now declines the upload instead.

- **A plugin whose annotations were stripped out shows its saved parameters, in sections.** A plugin's parameter schema lives entirely in the `/*:` comment block at the top of its file, and obfuscated releases ship without it — of the forty-six VisuStella plugins in one protected distribution, not one still has an `@param`, an `@plugindesc`, or the comment block itself. There is nothing left to parse, here or in RPG Maker's own Plugin Manager, so the plugins carrying the most configuration are exactly the ones that appeared to have none.

  The values survive in `plugins.js`, and their shape says most of what the annotation would have: an object is a struct, a list of objects is a list of them, `true` is a checkbox, a number is a number, and a string that was stored JSON-encoded has to be written back that way. Read like that, one project's plugins produce 198 nested editors from 306 recovered struct definitions, so the settings open as the sections their author laid out rather than as one wall of JSON. VisuStella also writes each parameter's type into its own key — `QoL:struct`, `ActorHPColor:func` — for its own loader rather than for anyone reading the list; the suffix is dropped from the label.

  Reading a shape is a reading, and can be a misreading: an `:eval` parameter holding the source text `[255, 255, 0, 160]` looks exactly like a list and is an expression, and RPG Maker writes its own `__collapsed` bookkeeping into a struct as a bare array where every real field is a string. So each reading is checked by performing it — a parameter is offered as structured only when saving it reproduces the stored text byte for byte, and anything else falls back to a text box, which is what it would have had anyway. Across that project, 198 parameters round-trip exactly and 23 decline. What cannot be recovered at all is naming: which field is a colour, a switch, a file. Those stay plain text, and merely offer no picker.

- **A parameter declared `multiline_string` gets a text area.** RPG Maker 1.5's multi-line text type was rendered as a single-line box, which is a poor way to edit a paragraph. Unlike `note`, which is stored JSON-encoded, this one is an ordinary string and is written back exactly as typed.

- **Object designations can be painted again after switching project or map.** The 3D object overlay is built around the map's tilemap, and that is rebuilt per project — but the editor was handed the new one only when the Objects tab was *clicked*, and a tab that is already open is never clicked. Open a project with that tab selected, which is where the last session left it, and the editor was still holding the manager built for the previous project, or none at all. The paint path checks for one and returns quietly when it is missing, so a click produced no mark, no error and no clue it had been received. Every surface the editor paints through is now bound in one place, called both when the surfaces are rebuilt and when the editor arrives, so neither end has to remember the other.

  The Regions and Objects tabs also do their setting up when they are *selected* — building their panel, making their overlay layer, showing it — and nothing selects a tab on a project switch. An already-open tab is now re-opened, so the rebuilt surfaces arrive with a panel and a layer to draw on rather than neither.

- **An event can say which layer its animations play on**, with `<animation z: 6>` in its note, or `<animation over>` for the layer RPG Maker uses. Where an animation belongs is a question about the scene rather than about the engine — a spell cast over a character belongs in front of the furniture, a glow on a console set into a table belongs behind the table's own top — and one map will want both, so it is written on the event the animation is played on, which is the only thing that knows. The layers are the tilemap's own: 0 the ground, 3 the characters, 4 the tiles drawn over characters (the star-flagged pass, in 3D), 8 where an animation goes by default; a half above a layer sits clear of everything on it. Read in 2D as well as 3D, which had no way to place an animation below anything at all. Saying nothing keeps exactly the behaviour that was there before.

- **A tile samples inside its own square of the sheet at every zoom.** The half-texel inset added earlier fixes the boundary case — it moves the sample off the fence between two tiles — and cannot fix the far case: zoomed out, one screen pixel covers many texels and the sample can land in the next tile along. The inset would have to grow with the zoom, and the zoom is not a constant. So every quad now carries the rectangle it is entitled to and the fragment shader clamps to it, which is correct at any distance by construction. Mipmaps would have made this worse rather than better: a mip level of an unpadded sheet is built by averaging *across* tile boundaries, so the bleeding would be baked into the texture instead of merely sampled from it.

- **The 3D camera looks at the middle of the view.** It was given the exact centre point of the display, and the aiming code adds half a tile to whatever it receives — to turn the corner of a character's cell into the middle of it — so the camera looked half a cell past the middle and everything sat slightly left of where it belonged, by a margin just small enough to doubt.

- **An animation takes its place on the frame it appears.** The tilemap sorts its children in its own update and MZ creates animation sprites after the spriteset has updated its children, so a sprite added this frame stayed wherever it was appended — last, which means in front of everything — until the frame after. One frame is enough to see, and a looping animation restarts every few frames: three of them on a table flickered continuously between in front of it and behind it, and no amount of getting the layer right could settle it.

- **An event that is part of a 3D object sits on it, not half a tile above it.** The shader steps every cut-out half a cell towards the camera, because a thing standing on a cell fills it front to back and what the eye reads as "where it stands" is the near edge of that circle rather than its centre. Sprites were placed without that step, so an event pinned to a painted object was half a cell further from the camera than the art it belongs to — and under a pitched camera, further away reads as higher up the screen. A sign floated above its own board and took its animation with it. Only things standing on art take the step; anything on the ground is where it always was.

- **A parallax the 3D ground was built from is no longer also pasted flat over it.** A parallax plugin adds a layer per parallax to the tilemap, and the 3D ground sits in the tilemap too, sorted above them so a backdrop stays a backdrop. That is right for a starfield and wrong for the layer the ground was made from: the same picture was then on screen twice, once laid on the world and once pasted flat across it. Only the layers the ground actually took are put away — the map-pinned ones — so anything that scrolls or loops keeps drawing exactly as it did.

- **A new event can be made in the 3D view.** Right-clicking stopped at the event cube: a click that hit nothing returned, so the only cell you could open a menu on was one that already had an event on it — and *New Event…* lives on the menu for a cell that does not. Existing events could be edited and none could be created, which reads as the tool half working rather than as a case nobody had written. Bare ground now gets the same menu the 2D map gives it, on the cell the hover outline is pointing at, routed through the same calls so the cell is selected and the events panel follows. It still refuses outside event mode, and still stands down at the end of a right-drag pan.

- **One click of the shadow pen paints.** It did not: the pen paints a *quadrant* of a cell and worked out which one from the last position `pointermove` had recorded — a field the press handler never set. A press with no move before it therefore had nothing to read, and the pen took a missing position as "cannot tell which quarter" and declined. Dragging worked perfectly, because the drag's first move filled the field in, which makes this a strange thing to report and an easy one to dismiss as a slip of the hand. The press records where it landed now, which is the position that was always meant.

- **The editor's 3D preview draws the map's parallax.** The runtime asks ImageManager for it and there is no game running in the editor, so a parallax-mapped map previewed as its bare tile layers — which on a parallax map is very close to nothing at all. The editor loads them off disk instead. More than one is honoured: the map's own parallax and any `<MultiParallax>` block in its note, stacked in the author's `z` order, each map-pinned layer treated as ground and anything that scrolls or loops left alone as the backdrop it is. Layers added by a plugin *command* cannot appear — they do not exist until an event runs, so there is nothing for the editor to read.

- **A plugin that renders somewhere else and hands the picture to PIXI as a canvas now shows it.** This is the whole of MZ3D, which draws the world in babylon.js and passes the result through as a texture; under Reactor it drew nothing, and the map was the parallax sky it had been laid over. Two v5-era behaviours were quietly gone in PIXI 8, and neither leaves a mark.

  `Texture.update()` used to mean "push this to the GPU" — it called the resource's own update, which is where a canvas's *current* dimensions are read back off the element. In v8 it only recomputes UVs; the source has to be updated separately, and v8's own documentation says so. A plugin that builds its texture before sizing its canvas — which is the natural order, since the canvas is sized to the game and the game is not up yet — is therefore pinned to whatever the canvas measured at the time, and for a bare `document.createElement` that is the HTML default of 300×150. Reactor restores the v5 meaning: updating a texture updates its source, so the size catches up and the pixels arrive.

  The second half is that a Sprite used to track its texture, and in v8 it only subscribes when the texture is flagged `dynamic` — a v8 concept no MZ-era plugin knows to set. So the picture would have arrived correctly and been drawn into a 300×150 corner of the screen. A texture built from a canvas is now flagged, because a canvas handed to `Texture.from` is one the caller means to draw into. Textures from anything else keep v8's default, and Reactor's own `Bitmap` does not go through this path at all.

  Both are applied to the textures Reactor hands out rather than to every texture, which is not fastidiousness. PIXI's own Texture listens for its source's resize with `update()`, and a canvas source emits that *before* it has resized the element — so a blanket version reads a stale canvas mid-resize and puts the source straight back. The renderer's own view is a texture over a canvas, so the effect of getting that wrong is that `Graphics.resize` silently does nothing and the game draws into the bottom-left corner of its own canvas, with every measurement in the page still reading correctly.

- **A 3D map lit by PSYCHRONIC_RaveLighting is no longer hidden behind its own darkness.** A lighting plugin draws in two parts, and only one of them is the lights. Reactor was putting away `_lightContainer`, which holds the glow sprites, and leaving `_toneSprite` — a full-screen bitmap filled with the screen tone, with light-shaped holes punched through it. On a night or interior map that tone is `[-255,-255,-255]`, so the sprite is opaque black across the whole screen, over a 3D world that had already been lit for real.

  It also has to be told more than once. The plugin rewrites `_lightContainer.visible` from the options setting on *every* frame of its own `Spriteset_Map.update`, so a one-shot `visible = false` was undone before it was ever drawn. Reactor takes `renderable` instead and re-applies it each frame: `visible` is the plugin's, and writing it back would overrule a player who turned lighting effects off. Walking from one lit 3D map to another never crosses the on/off boundary either, so the second map's freshly built overlay used to arrive un-suppressed.

- **RaveLighting's lights are found where they are kept.** They were read out of the spriteset's `_lightContainer`, which holds additive glow sprites the plugin builds *from* the real configs — pooled, created lazily, left invisible when unused, and not built at all on a map whose overlay pass returns early. So a map with five lit braziers presented an empty container and Reactor put no lights in the scene at all. A light belongs to a *character*, on `_lights`, and that is now the only place it is looked for — the player and its followers included, each shape reading its reach from its own field, since a pulsating light records where it is rather than how far it gets.

- **The audio player's track list fills the dialog.** It was a flat 475px — the height of the A-Z strip standing beside it, 26 tabs at 17px plus padding — so the list was sized to fit the alphabet rather than the window, and everything below it was an empty grey band that could have been holding twice as many track names. The dialog is a column now and the list is the part that grows.

- **Verified candidate artifacts attach to the source release without replacing its notes.** The tag workflow normally creates the release body from the root changelog first. Artifact publication now detects that release, updates its title, and uploads all verified manifests and archives with `--clobber`; if source publication was interrupted, it can still recover by creating the release. The release workflow continues to verify all four manifests against the tag commit and sends the same bytes to GitHub and, when requested, itch.io.

- **The Web editor no longer begins with avoidable missing-file requests.** The editor page now declares its application icon, and the bundled MZ starter explicitly sets `$reactorMvCompat = false`, preventing the runtime from probing for an MV project marker or requesting MV-only `fonts/gamefont.css`. A served archive was verified through service-worker control, IndexedDB persistence across reload, and in-page Playtest reaching `Scene_Title`.

## [0.97.0] - 2026-08-03

### Added

- **3D objects are grouped on the map, not only in the tileset.** A tileset says what a *tile* is, and that is all it can say: an autotile id is a corner arrangement shared by forty-eight shapes, so three shops built from one wall kind are the same tile as each other and nothing in the tileset can tell them apart. Declaring a rectangle of a sheet does not help either — an autotile has no place in a drawing to declare. Which cells make up one building is a fact about a *placement*, so it is now said on the map, in a palette tab beside Regions and painted the same way. Cells sharing a number are one object however they are arranged, so a building hacked together from bits of three others is one thing and says so.

  It is per layer, because a tree on B standing over a wall on A is not part of the building; `Auto` paints every layer holding a tile, which is what takes a wall and the signage hung on it in one gesture. A **Footing** brush marks the cells that are the ground an object stands on rather than a course of its height — standing a drawing up turns its rows into courses, so a building painted across seven rows is seven tiles tall and plants itself on its southernmost row unless it is told which rows are the pavement.

  Every tool routes it: pencil, eraser, rectangle, circle and fill, with fill flooding by object number so a structure is grouped in one click. The eraser takes the grouping rather than the tiles under it. It lives in `Map###.r3d.json` beside the map, never in `Map###.json`, so the map stays ordinary RPG Maker data — which matters more here than most places, because MZ's six planes are full and there is no seventh to take.

- **A page set to Custom movement can say what the route is.** Choosing Custom was half of it: the type could be set and there was no way to author the route, so the page kept whatever list it already had and a new event kept an empty one — Custom meant "stand still" and could not be made to mean anything else. A **Route…** button sits beside the dropdown, enabled only for Custom, and opens the same dialog the Set Movement Route command uses, because an autonomous custom route *is* a move route built from the same forty-five commands. Two things the page has already answered are left out of it: the character, since it is always this event, and Wait for Completion, since nothing is waiting on it.

- **Flying the editor's 3D camera.** WASD moves, Q and E change height, Shift hurries. Movement is timed rather than counted, so the same press covers the same ground on any machine, and its speed is judged against whatever is in front of the camera when the flight starts. Orbiting is how you inspect a thing; this is how you inspect a place.

- **A hover outline in the 3D view.** On a flat canvas the cursor is already on the map and where a click lands needs no explaining; through a perspective camera the tile under the pointer is the answer to a raycast, which is not something you can eyeball. The cell is outlined as you move, scaled to whatever brush is in hand, so a three-by-three stamp outlines all nine cells rather than one.

- **Events can be dragged in the 3D view**, as they can on the 2D canvas, with the same refusals: not off the map, and not onto a cell another event already holds. Undo is captured on the first cell actually crossed rather than on the press, so clicking an event to select it does not push a snapshot of the whole event list and wipe the redo stack.

- **Events are drawn in a box in 3D.** An event drawn from a character sheet is a picture standing on the map and nothing about it says it is an event — it reads as a painted tree until you happen to click it, which is why the 2D canvas borders every event. Edges only: a filled box would have to be transparent, and transparent boxes are what sliced the name labels.

- **Escape lets go of whatever is held** — a lifted map stamp first, then a selected event, then the tile palette. Every selection was sticky and choosing something else was the only way out; carrying a stamp is easy to do by accident and it keeps painting on the next click. It stands down while a dialog or a text field is up, so closing a dialog does not silently drop your brush.

- **Clear, in the tileset's 3D Shape editor.** Every other setting there could be changed and none could be removed: Auto reset a tile's class and left its stand-in, its roof pairing and its object membership behind, with no way back short of hand-editing the sidecar. Clear forgets all four for every tile dragged over, works on autotiles — anything that can be classified has to be un-classifiable — and reports honestly, since the accessors return derived facts as well as authored ones and asking them naively marked every tile in the sheet as carrying information. It replaces Remove, which undeclared an object and kept the classes; the two overlapped almost entirely outside one narrow case and did not earn a second destructive button.

### Removed

- **The height brush.** Massing painted per cell was the wrong half of the problem: nothing on a real 3D map uses it, because the massing all comes from the tileset's 3D classes. Its toolbar button, level, action and width controls, undo integration and the 3D view's paint path are gone, along with eleven tests covering them. `MapElevation` itself stays — the runtime still reads elevation and the `<3d>` note from the sidecar — so a map that carries heights is unaffected; nothing writes them now.

### Fixed

- **3D grouping survived a save and not a reopen.** The sidecar was written correctly and nothing read it back: it was attached to the map only when the *3D view* opened, which held while everything in the file was something only that view could show. Grouping is painted on the 2D canvas, so the file has to be in hand whenever the map is. The visible half was an empty overlay; the silent half was worse, because painting again over an unread sidecar and saving writes one built from nothing and takes everything else in the file with it. It is now read as part of loading the map, and a corrupt one is logged and skipped rather than stopping the map from opening.

- **A grouped building counted as no change at all.** The dirty check compares what goes into `Map###.json`, which deliberately excludes the sidecar, so a map whose buildings had just been grouped read as untouched — closing the project never asked, and the work went without a prompt. The check now asks the sidecar too.

- **The Role tool changed nothing that lasted.** It cycled roles in memory and returned without saving, while every other tool in the 3D Shape editor saves — so laying an object's ground rows flat reached neither the file nor the open 3D map, and reopening the project stood them back up. It read as the tool doing nothing at all, which is a poor way to discover that roles are how a seven-row gateway is told which of its rows are the pavement.

- **Fifteen strings in the movement route dialog, and the whole 3D tool palette, were never translated.** The palette's tool names and hints — Select, Flat, Upright, Scenery, Foliage, Panel, Auto, Clear, Object, Role, Roof and every description beside them — were bare literals that went through no translation helper at all, so that palette shipped in English on every locale and the localization gate could not see it. They are routed now, translated into all seventeen non-English locales, and covered by the gate, which follows indirect helpers as well as direct calls.

- **The Region palette could not be scrolled, and neither could the new one.** A flex child defaults to `min-height: auto`, so it grows to fit its content instead of scrolling it: thirty-two rows of swatches in a panel a few hundred pixels tall with no way to reach the bottom. The tileset preview beside it has always said `min-height: 0` for exactly this reason.

- **Both palettes now sit in their panel the way the A-G tabs do.** The tab reveals the panel with `display: flex`, which is a row, so its one child was sized to its content and stopped short of the right edge — a bare strip beside the scrollbar that no amount of styling the canvas could close. They also draw at the project's tile size now, like every tile beside them, rather than at a fixed 48.

- **A region number was drawn at a fixed 18px into a canvas the size of a tile.** At 48 that fits; at 24 it fills the cell edge to edge, and at 16 it is not a number but a smear wider than the cell and taller. Four places drew one — both overlays and both cursor previews — and all four now size the glyph and its outline from the tile. The region overlay's texture cache was also keyed by number alone, so opening a 24-pixel project after a 48-pixel one handed it the previous project's textures.

- **The 3D view kept showing the previous project's 3D checkbox state**, and switching between the Region and 3D object tabs left both overlays drawn at once — two sets of coloured numbered squares over one map, answering different questions, impossible to tell apart.

- **Turning while flying threw the camera around an arc the width of the view.** `orbit` circles the point the camera is aimed at, which is right when you are studying a building and wrong when you are moving; turning now pivots in place. Turns are also banked and spent once a frame, because pointer moves arrive several to a frame and each one moved the camera, so a smooth drag came out as a burst of steps.

- **Orbiting pivoted about a point that had nothing to do with what was on screen.** The camera always looks straight at its target, so the pivot is at the centre of the view — but only as a *direction*. How far along it the pivot sits is the view distance, and nothing kept that honest: framing sets it to about the map's width, and flying then moves the camera without touching it. After a flight the pivot could be hundreds of tiles past everything visible, so a drag swung the whole map about a point out in the distance. A gesture now seats the pivot on whatever the centre of the screen is actually looking at, without moving the camera — the hit lies on its own view ray, so only the distance along that ray is corrected.

- **Cut-outs were built one cell wide however big their art was.** A stand-in names a rectangle of the sheet — a mountain drawn across a 2x2 block — and the span was read as an aspect ratio with its size thrown away, so that mountain was built at the same world width as a single tile. The picture came out squeezed to half its width, and the quad no longer covered the cell it stood on, so bare ground showed between one cut-out and the next: rectangular bites out of a range's silhouette, with the straight vertical sides a billboard quad has. A cut-out is now as wide as its art and as tall in proportion, it is never narrower than the cell it stands on, and its scatter is clamped to the overhang it can afford so a big one still breaks up the grid. On Infernis Prime, 7,515 of 8,550 foliage cells failed to cover their own cell and 3,821 adjacent pairs had bare ground between them, up to two thirds of a tile; both are now zero. A 1x1 stand-in multiplies by one, so ordinary foliage keeps its look apart from that size floor.

- **Star-flagged art wins where it stands in the same place as art that is not.** A column's capital carries the star flag and a plate resting on the same cell does not; both are cut-outs at one anchor, so they are exactly coplanar, and at equal depth whichever is drawn first wins and the other is rejected. Which of them that was came down to the order the meshes happened to be built in, so a capital could end up behind the plate leaning against it — the opposite of what the flag means, which in 2D puts the tile over every ordinary tile in the cell. The starred pass now carries a deeper depth bias, which decides ties between surfaces at the same depth and changes nothing about real occlusion: a column still goes behind a wall a tile in front of it. The runtime draws its two passes separately over a cleared canvas and never saw this; the editor draws the scene in one go, which is where it showed.

- **Standing art turns about its own axis, and stays seated where 2D draws it.** A cut-out's anchor is the point it pivots on, and it centred on the cell across the object's columns but sat on the *southern edge* of its bottom row along the other axis — so the two axes disagreed by half a tile. RPG Maker plants a sprite's feet on the bottom edge of its cell, `screenY` being `scrolledY * tileHeight + tileHeight`, and that had been read as a world position when it is a screen convention. Half a tile of pure depth is invisible head-on, which is why it looked right, and it swings into view as the camera comes round: a column standing in the middle of a three-by-three pool ended up planted on the pool's southern lip. Props, characters and the position plugins are told all take the middle of the cell now — they have to agree, or a character walking up to a column would not meet it.

  Centring the anchor alone then read half a square too far back from the front, because a column standing on a cell fills it front to back and what the eye takes for "where it stands" is the near edge of its base, not the middle. A cut-out has no depth to spread across, and which edge is near changes as the camera comes round, so no fixed world position satisfies both — the southern edge is seated head-on and adrift once turned, the centre is the reverse. The quad is offset half a cell towards the camera instead: the pivot stays the cell's middle, so the object turns about itself, while the visible base sits on the near edge from every angle.

- **Two standing objects can share a cell, as they do in 2D.** A cell has four tile planes and more than one of them can be standing art — a plate set down on a column occupies the same square as the column, and the 2D tilemap simply draws one over the other. The 3D builder asked for *the* upright tile of a cell, was handed the topmost, and threw the rest away: the plate was drawn and the column it stood on vanished, while 2D looked perfectly ordinary. Every standing tile in a cell is built now, lowest plane first so they layer the way the tilemap layers them. The questions that genuinely want one tile — which object a cell belongs to, whether two cells are one picture — still take the topmost, so nothing about grouping changes.

- **A cell can belong to more than one declared object.** A plate set down on a column shares both of the column's cells, and asking the cell's *topmost* tile which object it belongs to read the plate as the whole story — so the capital and the shaft joined the plate's 1x1 instances instead of each other. That made them two objects with two anchors, and a cut-out turns about its own anchor, so the column came apart and its halves swung independently as the camera moved. Each tile answers for itself now: the column's two cells find the same origin and are one object, the plates are their own, and the pieces of a column turn together.

  The star flag goes back to being read per tile, as the 2D tilemap reads it. Answering for the whole object was tried while a column was disappearing, on the theory that its halves were split between the passes; the cause was elsewhere — a cell only ever yielding one standing tile — and the object-wide answer had a cost of its own, putting a column's unflagged shaft in the pass over the characters where it covered a plate standing in front of it.

- **Painting a 3D class in the tileset editor takes effect, and is saved.** The Roof, Clear and Object tools each wrote the classification file when they were used; painting Flat, Upright, Scenery, Foliage or Panel did not. So the change lived only in the editor's in-memory store — not on disk, and not announced — which is why it appeared on the map only after the 3D view was switched off and on, that being what rebuilds from the store. Both the drag tool and the click-to-cycle path save now, once per gesture rather than once per cell.

- The 3D render loop lets go of its frame handle when it stops. `startLoop` refuses to start a second loop by checking that handle, so a tick that bailed while still holding a stale one meant the loop could never be started again — leaving a canvas that drew only when the pointer moved over it, those handlers rendering directly, and otherwise showed nothing.

- **Changes made in the Database reach the open map.** Passability, the star flag and the 3D classification were all edited in place and repainted only the Database's own canvas: the map editor holds the tileset object it captured when the map opened, so a flag change did not take effect until the project was closed and reopened, and a 3D class change did not until the map was next painted. Flag edits now announce themselves — `ProjectController`'s listener was already written expecting exactly that, its comment saying "on every flag edit as well as on an image assignment", and it was simply never sent — and the 3D classification gets an announcement of its own, `rr-tileset-3d-saved`, which the 3D viewport follows. Separate events because they cost different things: a stroke of the Shape tool would otherwise re-diff every sheet and refresh the database list per cell.

- **A project opened with the 3D box ticked comes up in 3D.** The view is a preference rather than a property of the map, and the viewport is torn down when a project closes — so opening another one left a ticked box over a 2D canvas, and only unticking and reticking put the two back in agreement. The controller now reconciles what is showing against what the preference asks for whenever a map comes up with the viewport off.

- **Which layer a tile lands on no longer depends on the tab it came from.** B started at z1, C at z2, D onwards at z3, and the search for a free slot then ran *upwards* from there — so the same object dropped from two tabs sat at two different depths, and a B tile went underneath a C tile for no reason but their names. RPG Maker's own maps settle it: across the bundled projects, of 256,366 cells carrying exactly one B–G tile, **90.8% have it on z3** and 8.9% on z2; of the 45,075 carrying two, **99.8% use z2 and z3 together**. B, C, D and E each sit in those proportions — the sheet makes no difference at all. Every picture sheet now starts at the top of the stack. Which *way* a cell fills is not something those counts can answer — two tiles end up at z2 and z3 whichever order they were laid down in — and it is settled instead by what the map draws: z3 covers z2, so painting one thing over another has to put the new one *above*, or the tile just placed vanishes behind the tile placed before it. So the top slot is always taken and the stack moves down beneath it, which is also what leaves that z2+z3 pairing, older underneath. The terrain slot is preserved however deep the pile gets, and putting something *below* what is already there is what choosing a layer by hand is for. The A tabs keep the ground slot.

  One exception, and RPG Maker has it too: an **overhang keeps its place**. The star flag means "draws above characters" — a canopy, an archway, the top of a doorway — and a plain tile painted into a cell holding one of those slides underneath rather than shoving it down, because the overhang is the thing meant to be in front. The authored maps bear it out: of the 14,066 stacked pairs where exactly one of the two is starred, the starred one is the upper of them 84% of the time. Two overhangs, or an overhang painted over a plain tile, follow the ordinary rule. Coverage in `picture-sheet-layers.test.cjs`.

- **A sheet assigned in the Database is usable straight away.** Assigning one to a slot updated the tileset editor's own thumbnails and nothing else: the map canvas and the tile palette each captured the tileset when the map opened, so neither heard about it until the Save button beside the name was pressed. A sheet added to E, F or G was therefore not selectable in its tab, and a tile placed from one drew nothing at all — the sheet it addresses had never been loaded, which reads as the tile vanishing. Assigning now announces itself the same way saving does. It only announces: writing `Tilesets.json` stays with the Database modal's own OK, so Cancel can still put everything back.

- **Walls cast their shadow, and take it away again.** RPG Maker fills the left half of the cell immediately east of a wall — quadrant bits 0x01 and 0x04 — and clears it when the wall goes. Reactor drew none: the only thing that wrote the shadow plane was the shadow pen, so a wall placed in the editor had no shadow at all, and one arriving inside a pasted stamp brought whatever had been lifted with it whether or not a wall was left to cast it. The rule is read off the authored maps rather than guessed: of 39,104 shadow cells across the bundled projects, 85.6% carry exactly that pattern and much the commonest thing beside a shadow is a wall immediately west.

  It runs on every path that lays a tile down or takes one away — the brush, the rectangle and circle area tools, bucket fill, the eraser and a pasted stamp — and keys off walls *coming and going* rather than off cells being touched — filling a floor beneath a wall that was already standing must not conjure a shadow the author never asked for. Only the two quadrants a wall casts are ever written, so a shadow put in the other half of the same cell with the pen survives a wall being built or removed beside it. A wall casts nothing onto another wall, roofs and ground autotiles cast nothing at all, and a wall on the map's eastern edge casts nothing off it. Coverage in `wall-auto-shadow.test.cjs`.

- **A pasted piece of map rebuilds its autotiles where it lands.** A stamp lifted with a right-drag carries the tile ids it was copied from, and an autotile id is a corner arrangement rather than a picture — so a stretch taken out of the middle of a wall arrived carrying middle-of-wall pieces, with no ends, and read as a wall with a slice cut out of it. The shapes are recalculated on paste now, as RPG Maker does, so the copy comes down as a finished wall of its own; the border is rebuilt too, so pasting against an existing wall joins it rather than leaving both with an end facing the other. **Shift** asks for the ids verbatim, which is what Shift already means when painting a single autotile from the palette — and it now claims the gesture while a stamp is held, where it was previously read as a map pan because the palette selection is cleared when a stamp is picked up. Only A1–A4 are rebuilt: A5 is not an autotile and B–G are pictures, whose ids mean a position on a sheet. Coverage in `map-stamp-autotiles.test.cjs`.

- **Auto-layer ground autotiles replace the ground they are painted over.** Sand painted across water was treated as an overlay merely because it was a different autotile kind: it went onto map layer 2, left the water underneath, and calculated its shape in a layer with none of the surrounding ground. It now stays on map layer 1, replaces the water, and reconnects every painted cell and neighbor there. Decorations still use the second A slot, and distinct A1 water kinds retain their deep-water stacking behavior.

  A tile also **joins the slot its own terrain already occupies beside it** before the fallback is considered. That keeps a repaired upper-layer road on the layer occupied by the rest of its run; moving it there now removes the stale lower copy instead of hiding a duplicate beneath the repair. A lower-layer decoration beside the cursor cannot pull a new decoration down through the floor at its destination.

- **The front of a mountain range stood a tile clear of the ground in 3D.** A connected region of upright cells shares one footing — its southernmost row — so that a wall does not tear in depth when its base is uneven. That footing was deciding *height* as well: a column's art was indexed from it, so a column whose lowest painted cell is north of the footing was drawn that many courses up with nothing beneath it. A range's southern edge steps back in ones and twos, so its whole front row hung. On Infernis Prime 166 of 616 columns hung; across that project's 906 maps, 10,207, some of them twenty-one tiles up.

  The two questions are separated now. The footing still sets the depth, which is what it exists for; height is asked separately, and a column only hangs where something is holding it up — an archway's panel has posts either side, so the rows beneath it are spanned by the same region left and right, while a range's edge has open ground on one side and sits down. Bridging counts only while it is unbroken, so a panel on posts standing on a slope is not read as floating. Across those 906 maps the run count and the number of courses drawn are both unchanged to the unit — no art is dropped or invented, it is only lowered — and hanging columns fall to 2,435, which are the genuine openings.

  The bridging test is reach along a row, so a connected spur of the same region further along reads as support. `reactor-3d-footing-lift.test.cjs` pins that limit as well as the fix: tracing what actually carries a load is the structure question the design notes record as unsolved, and this failure is one column of one shape rather than a whole range's front.

- **Panning past the edge of the map, then jittering back.** Holding the scroll wheel and dragging wrote the container position straight out and left the clamping to `updateScrollbars`, which is throttled to one call a frame — so the view was drawn genuinely outside the map and yanked back a frame later, every frame, for as long as the drag kept pulling. The view is held inside the map as it moves now, so a pan stops at the edge and stays there. The drag anchor is re-read from the clamped position each move as well: clamping alone banks the overshoot in the anchor, and pulling back then does nothing until it has been unwound, which reads as the pan sticking. `panBounds`/`clampContainerToMap` are the single answer to how far the view may travel — the same two `Math.max(min, Math.min(0, ...))` lines had been written out four times, for wheel zoom, the scrollbar thumbs and the scrollbar refresh, and not at all for the pan. Coverage in `map-pan-bounds.test.cjs`, which drives the real pointer handlers.

- **Shift could not extend a 3D object onto a second piece.** It only ever extended an object declared earlier in the same session, so selecting an existing object and shift-dragging the rest of it started a fresh object instead — which is the entire gesture, for a prop that crosses the seam where a B–G sheet's two halves are stacked. It now joins whatever is selected, read before the drag reselects, and a single shift-click onto an existing object is no longer refused.

- **An object anchored at a sheet's first tile could never be extended.** The merge asked `tileAtCell` for the origin and treated a falsy answer as failure — but the top-left tile of sheet B *is* id 0. The edge is checked by bounds now. The same call reported three different failures as one message, so a mismatch that was nothing to do with sheets said "that is on a different sheet"; each says which it is.

- **Dragging off the edge of a tileset claimed rows that were not on screen.** The drag never clamped, so it kept counting past the canvas and declared a rectangle over tiles the author could not see until they scrolled to them.

- The editor assumed 48-pixel tiles. `System.json` carries `tileSize` — MZ offers 48, 32, 24 and 16, and `DatabaseSystem2Editor` already exposed it — and `Game_Map.tileWidth` in the runtime reads it, but `TilemapManager`, `TilesetPaletteViewer` and `DatabaseTilesetEditor` all measured in literal 48s. A 32-pixel project's sheets are two thirds of the standard layout (512x384 where MZ would be 768x576), so every sample landed 1.5 tiles along and drifted, drawing a mosaic of unrelated art. `src/utils/TileMetrics.js` is the single source: `tileSizeOf` validates against MZ's four sizes and falls back to 48, `sheetScaleOf` converts a 48-based measurement. `TilemapManager.TILE_SIZE/WIDTH/HEIGHT` and the palette's `tileSize` are read from it and refreshed by `refreshTileMetrics()` once the database has loaded, since both surfaces are constructed before it. The 48s in tile-id arithmetic are untouched: there 48 means shapes per autotile kind, a property of the format rather than of the art, and a test asserts both that the pixel assumptions are gone and that the format constant survives. `TilesetPaletteViewer.prototype.tileSize` carries the default so an instance built with `Object.create` — which the transparency tests do, to avoid the DOM — still measures in whole tiles. `MapEditor`'s palette sampling, `ProjectController`'s map pixel extents, `MapEditor3D`'s character frames and raycast quadrant offsets, and `Reactor3D.MapScene.build` (via `Reactor3D.currentTileSize`, which reads `$dataSystem` the way `Game_Map.tileWidth` does) were converted in the same pass. `DatabaseSystem2Editor` calls `window.rpgReactorTileSizeChanged`, which `main.js` binds to re-read every surface and reload the open map, so the setting takes effect where it is made. Coverage in `tile-size.test.cjs`, including a sweep over all six renderers that fails on any bare pixel 48 while allowing stated `|| 48` fallbacks and the format's `% 48` / `2048 + kind * 48` arithmetic — the sweep is what caught the three sites in the tileset editor's autotile palette that a by-hand list had missed.

- The tileset editor's overlay marks were measured for a 48-pixel cell and did not fit a smaller one. The passage arrows sit a fixed 8 pixels in from each edge, so on a 16-pixel tile the left and right markers landed on the same point and all four directions read as one blob; a ladder icon 16 pixels tall starting 4 down ran into the tile beneath it; and the 3D class marks are sized as `tileSize - 26`, which at 16 and 24 is negative — and `fillRect` with a negative width draws *backwards*, into the neighbouring tile, rather than clipping away. `markScale()` brings the marks in with the cell below 32 and is capped at 1, so 48 and 32 are untouched: the marks are deliberately not shrunk down to 32, since one nobody can read is worse than one that is slightly large. Coverage in `tile-size-overlays.test.cjs`, which drives the real overlay through a recording canvas at all four sizes and holds 24 and 16 to the proportions 32 already has.

- Tile-size support is now checked by running the renderer rather than by reading it. `tile-size.test.cjs` sweeps the source for bare pixel 48s, which cannot tell a right formula from a wrong one built out of `TILE_WIDTH`. `tile-size-rendering.test.cjs` loads `TilemapManager` itself and renders every autotile kind and shape, every A1 animation frame, A5 and all six B–G sheets at 48, 32, 24 and 16, asserting that each rectangle is cut on the grid it belongs to, fills its cell exactly, stays inside its sheet — and, the load-bearing one, that the rectangle read at a smaller size is the rectangle read at 48 scaled by the same ratio the sheets themselves are. So a smaller project reads the same cells of the same layout, which is the whole claim.

- `EditorInstanceBroker`'s constructor threw in the web build, taking the editor down at boot with `Node module "os" is unavailable in RPG Reactor Web`. `WebHost` shims `fs`, `path` and `url` and throws for the rest, and the broker resolved `os` and `child_process` eagerly in its field initialisers — `start()` already returned false when any of them was missing, but construction never reached it. An `optionalModule` helper swallows the throw and yields null, so the broker declines on web exactly as it was written to. Coverage in `editor-instance-broker.test.cjs`, which loads the source against a require shim that behaves like the web host.

## [0.96.0] - 2026-07-27

Cycle overview: [RPG Reactor 0.96.0: A Deep Correctness Audit](../docs/devlogs/2026-07-25-rpg-reactor-0.96.0.md).

0.95.1 was an internal development version and was never published; its changes ship in 0.96.0.

### Added

- `runtime/reactor_3d.js`: the HD-2D renderer. A map opts in with `<3d>` in its note; `Map###.r3d.json` beside it carries elevation and camera, so no 2D project gains a file and a 3D map's `Map###.json` stays valid RPG Maker data describing its 2D footprint. The grid stays authoritative — `Game_Map`, `Game_Character`, passability, regions and the interpreter are untouched, and nothing in the 3D view feeds back into game logic. three.js (vendored to `runtime/libs/three.js` by `build-scripts/vendor-three.js`, absent from `scriptUrls`) loads on first entry to a 3D map, so a project with no 3D maps never fetches it. Compositing is by stacked canvases, the arrangement the runtime already ships for Effekseer: the 3D canvas takes z-index 0 under the PIXI canvas, so windows, pictures, weather and plugin sprites draw exactly as in 2D. `Reactor3D.Geometry` is pure — no THREE, no DOM — and produces one merged buffer per tileset sheet, which holds a 200x200 map to a handful of draw calls; autotile cells emit four quadrant quads picked from the same shape tables the 2D renderer uses. Upright tiles are collapsed into north-south runs so a building becomes one facade rather than a stack of per-tile panels. Coverage in `reactor-3d-foundation.test.cjs` and `reactor-3d-geometry.test.cjs`.

- Per-tile 3D classification, `Tilesets.r3d.json`. No heuristic over 2D flags separates buildings from terrain — on a real city map, flag-guessing produced facades fifty-one tiles tall — so it is authored, per tileset rather than per map since a tile is the same kind of thing wherever it is painted. `src/utils/Tileset3DClass.js` holds the store; `Reactor3D.CLASS_*` mirrors its values and a test pins the two together, as the editor cannot load the runtime module. `AUTO` is the *absence* of an entry, so clearing a tile deletes it and an emptied tileset drops out entirely: a fully cleared file equals a fresh one instead of accumulating husks. `keyFor`/`Reactor3D.classKey` fold autotile ids to their kind base, so one entry covers all 48 shapes — the difference between a few hundred lines and tens of thousands for a single tileset. Unclassified tiles fall through to the flag heuristic, capped by `AUTO_MAX_FACADE`, so an untouched map still shows something recognisable. The database loads and saves it either side of `Tilesets.json` and reports it through `getDirtyKeys`, since it sits outside `dataFiles` and would otherwise be lost on close without a prompt; the file is only written when non-empty. Authoring is a `tile3d` mode in the tileset editor that draws instead of the flag markers rather than alongside them, a tile already carrying up to seven glyphs. Coverage in `tileset-3d-class.test.cjs` and `tileset-3d-authoring.test.cjs`.

- The build preflights now require `reactor_3d.js`. It is in `scriptUrls`, so a project without it hangs at load waiting for a script that 404s — but `validateProjectRuntime` in `build.js`, `build-worker.js` and `dist-editor-worker.js` never listed it, so such a project passed validation and failed later. A test now derives the expected list from the manifest itself, so a future addition to `scriptUrls` cannot be missed the same way. `libs/three.js` stays out of both, deliberately: `Reactor3D.ensureLoaded` fetches it on demand and a project with no 3D maps should not pay for it.

- Painting in the 3D viewport, through `MapEditor` rather than beside it. `tileAt` raycasts the merged ground meshes and floors the world position into map coordinates — the map is one unit per tile, so a hit on a mesh shared by thousands of tiles still names one — and hands `MapEditor.beginEditState`/`paintTile`/`resetDrawingState` the same arguments the 2D canvas does, including a synthetic `lastMousePos` in tile-pixel space for the quadrant-sensitive tools. A left drag paints when `tilesetPaletteViewer.selectedTiles` is non-empty or a stamp/shadow pen is active, and orbits otherwise; Ctrl forces orbit. `resetDrawingState` commits the undo entry and fires `rr-map-edited`, which is what rebuilds the geometry, so a stroke needs no special path back. The cursor's tile is reported through `updateMapCoordinates` as in 2D.

- `applyAtmosphere` gives the scene a background and `THREE.Fog` scaled to the map's longest side (fading from 0.9x to 2.2x), taken from `--color-bg-base` — not `--color-bg-deep`, which is pure black on the dark themes and gives fog nothing to fade into. `MeshBasicMaterial` respects scene fog, so this needs no material change, and the runtime sets no fog of its own.

- Facade footprints get an apron. Every cell of a run is added to `consumed`, and those cells typically hold only the building's own art, so filtering out upright tiles left them with no ground quad: a fifteen-tile facade rendered a fifteen-cell hole to the sky. `nearestGround` walks south from the run — the direction it faces, so the first floor found is the street in front of it — then north, then along its row, and the result backfills every cell of the footprint. It takes `stack[0]`, the bottom of the layer stack: taking the top returned whatever decoration was drawn over the floor, which alpha-tests away to nothing and leaves the hole it was meant to close. On Moletown this drops the cells rendering as sky from 1,748 to 38, and those 38 are cells whose artwork is genuinely transparent — the 2D editor shows its own background there too.

- The 3D wheel handler calls `stopPropagation`. The 3D canvas is a child of `#canvas-container`, which carries `TilemapManager`'s wheel-zoom listener, so every scroll zoomed the 3D camera *and* the hidden 2D view, re-cropped its canvas, updated its scrollbars and overwrote the zoom readout with the 2D scale. `updateZoomReadout` now reports camera distance against `LABEL_REFERENCE` as a percentage while 3D is on, and teardown calls back into `updateMapZoom` to restore the 2D figure.

- Facade depth is a crossing plane, not a box. `uprightDepth` used to add two side faces and a cap; for alpha-tested cut-out art both are wrong — a side stretches one texel column across the depth, and the cap draws the tile horizontally over the full cell, which gave every spire and jagged roof a flat shelf the size of its footprint regardless of silhouette. The depth quad now runs at right angles through the cell centre, so both planes are ordinary billboards. Culled where either neighbouring column continues the run, so a wide building is still one flat wall. Two quads per level instead of four.

- Wall faces take the autotile's quadrants. `faceQuads` subdivides any face — cliff, rim or facade — by bilinear interpolation of its corners when the tile is an autotile, so the same four quadrants land on a vertical surface as on the ground. Walls previously used `sheetRectFor`, the block's whole top-left tile, which patched raised terrain with corner fragments of unrelated artwork.

- `Reactor3D.CLASS_SCENERY` (3) and `Geometry.build`'s `isScenery`. Upright collapses a run of cells into one facade as tall as the run, which is correct for a building drawn as a single perspective prop — Moletown's towers reach fifty-one tiles — and wrong for terrain, which is one tile repeated over an area. Classifying the Infernis world tileset's A2 terrain as upright produced 391 runs over eight tiles and a longest of 58; as scenery it produces 690 facades capped at 16 (the towns and towers, which really are pictures) and 7,979 single standing cells. Scenery raises its cell through `surfaceAt` rather than emitting geometry: a billboard per cell was tried first and turned a mountain range into rows of cardboard with daylight between them, because terrain covers an area and a picture does not. Raised ground takes its cliff faces from the existing wall code. `uprightPredicate` now returns false for *any* explicit class other than upright rather than falling through to the guess, which had let a scenery tile both stand on its cell and weld into a facade behind itself. Coverage in `reactor-3d-geometry.test.cjs`; the editor cycle is Auto -> Flat -> Upright -> Scenery -> Auto.

- The autotile shape tables ship inside `reactor_3d.js`. `Geometry.autotileTables` read them off the global `Tilemap` and returned `null` when there wasn't one — the situation the editor viewport and the offline preview renderer are always in, since both load the module alone. `autotileQuads` then returned null and `build` fell back to a whole-tile blit, stamping each autotile block's bordered top-left corner over every cell: on a world map, 63,620 shape-0 interior tiles that should have been seamless rendered as a grid of squares. A running game's `Tilemap` still wins, so a plugin that replaces the tables governs the 3D view too. The embedded copy is extracted from and pinned against `reactor_core.js` by test. The old test asserting the whole-tile fallback was pinning this bug as the contract; it now pins the four-quadrant path, and the fallback is pinned separately for a caller that supplies an incomplete table.

- `MapEditor3D.loadClassification` reads `Tilesets.r3d.json` off disk. The runtime fetches it by XHR relative to the running game, so in the editor `Reactor3D._classification` stayed null: every tile fell back to the flag guess, the facade cap then rejected anything over eight tiles, and `topTileAt`'s fallback laid the rejected wall art flat as ground texture. A classified city rendered as building facades painted on the floor.

- `Geometry.groundStackAt` replaces the single `topTileAt` call for ground, so all four planes of a cell are drawn bottom-up, each lifted `LAYER_LIFT` (0.001 tiles) above the last to keep the author's order without z-fighting. On Moletown 260 cells were dropping a layer. Wall skirts take the topmost ground tile's art. A facade's footprint now also draws the floor under the building — skipping it left a hole visible from above, since a facade is one plane at the run's southern end — and gets no wall skirt of its own, which would z-fight with the facade.

- `opts.uprightDepth` (default 0.5 tiles) extrudes facades into shallow boxes with a capped top. A single plane is paper: a chimney or a mast disappeared to a line when the camera came round. Side faces are culled where the adjoining column carries a run to the same level, so a wide building stays one flat wall and only its outer corners are boxed. The editor's default camera pitch dropped from 52° to 34°, since from overhead a standing facade is edge-on and the 3D view looked identical to the 2D one.

- 3D event picking: `MapEditor3D.eventAt` raycasts against the event cubes only — ground and facades are one merged mesh per sheet, so a hit there names a sheet rather than a tile. A pointer-up within 4px of its pointer-down counts as a click and selects; a drag orbits and selects nothing. Double-click calls through to `EventManager.editEvent`, the same entry point the 2D map uses. Coverage in `map-3d-viewport.test.cjs`.

- `Geometry.build`/`uprightRuns` take `isAuthored`, and `AUTO_MAX_FACADE` now judges guesses only. The cap was documented as never overriding an explicit `CLASS_UPRIGHT`, but `isUpright` reaches the builder as a plain boolean, so an authored tile was capped identically to a guessed one. On Moletown, 155 of 397 vertical runs exceeded the 8-tile cap and were dropped to ground — the tileset draws each building as one perspective prop up to 51 tiles tall — which is why a classified city still rendered flat. A run is now kept if any tile in it was authored, since a run is a single object and classifying its base classifies the thing. `MapScene.build` supplies the predicate from `Reactor3D.isClassified`. Coverage in `reactor-3d-geometry.test.cjs`.

- 3D events carry their sprite, name and the interactions the 2D map has. `eventSprite` crops the character frame into its own canvas rather than addressing it by UV, since `$` sheets are a single 3x4 block and normal sheets a 4x2 grid of them; sheets load once each per map (37 sheets for Moletown's 354 events). Sprites and labels billboard on yaw only — pitching them would lay a character flat when you looked from above. Labels rescale with camera distance so they hold their size on screen, and hide past `LABEL_DISTANCE` where they overlap into a band. Picking runs against `pickables`, which excludes labels: they are overlays, and selecting one fought with that rescale. Right-click routes through `onEventContextMenu` to `EventManager.showContextMenu` with the same arguments the 2D canvas passes. Selection is bidirectional via a new `rr-event-selected` announcement from `EventManager.selectEvent` — the single funnel the map, the events panel and the editor already share — so neither side needs to know who made the selection. Coverage in `map-3d-viewport.test.cjs`.

- `src/MapEditor3D.js`: the editor's 3D viewport, toggled by a checkbox beside the A1 toggle and stored as `map3DView`. three.js and `reactor_3d.js` are read off disk from `ProjectManager.getRuntimePath()` and injected as classic scripts at first use — the editor is packaged separately from `runtime/`, so a relative `<script src>` would resolve in the source tree and break in a distributed build, and a viewport with its own copy of the geometry would drift from the runtime the first time either changed. The 2D canvas is hidden, never destroyed: `TilemapManager` owns it and has sized and cropped it for the open map. Events become boxes coloured by trigger, the one thing a view of tile data cannot otherwise show. `MapScene.build` gained `options.flags`/`options.tilesetId` and `$gameMap` reads moved behind `typeof` guards, since in the editor it is not merely null but never declared. A completed stroke dispatches `rr-map-edited`, debounced to one rebuild, so the view follows edits instead of freezing on whatever was open. The toggle reflects what actually happened rather than what was asked: a missing runtime directory leaves the box unticked with the reason in the status bar. Coverage in `map-3d-viewport.test.cjs`.

- Tileset sheets F and G, in the tile-ID band `1024-1535` that RPG Maker leaves unallocated between E (ending at 1023) and A5 (starting at 1536). No MV- or MZ-authored project can contain an ID in that band, so the addition cannot collide with imported data, and `$dataMap.data` is unchanged — map files stay structurally identical. The band needs no new address arithmetic: `setNumber = 5 + Math.floor(tileId / 256)` already resolves it to slots 9 and 10, and that one expression backs both `Tilemap._addNormalTile` and `Sprite_Character.tilesetBitmap`, so tile rendering and event tile-graphics both work unmodified. The stock 8192-entry `flags` array already covers `1024-1535`, so passability, star, ladder, bush, counter, damage and terrain tags need no format change either. Sheets F and G behave exactly like B-E: 16x16 grids stored as two 8-wide halves, non-autotile, default to map layer 3, and participate in auto-layer stacking. Slot 11 is deliberately left unused — the pre-v8 renderer packs sheets four to a 2048px texture with `MAX_GL_TEXTURES = 3` and UltraMode7 reimplements that packing as `setNumber >> 2`, so slots 0-11 stay inside those assumptions while slot 12 would break them. Stored `tilesetNames` arrays are never migrated: an MZ-authored tileset keeps its nine entries and grows only when a slot beyond it is actually assigned, so a project that does not use F/G writes byte-identical `Tilesets.json`. Assignment goes through `RRTilesetSheets.setNameAt`, which fills any skipped slot with `''` rather than leaving a hole, because `JSON.stringify` emits holes as `null` and the runtime hands every entry of that array to `ImageManager.loadTileset`. The slot table, band bounds and sheet-base map live in the new `src/utils/TilesetSheets.js` and replace four duplicated `['A1'..'E']` literals; the map editor's four copy-pasted B/C/D/E tile-ID cases collapse into one case driven by that table. Coverage in `tileset-extended-sheets.test.cjs`, including a sweep asserting no authored map in the bundled Demo already uses the band.

- Collapsible event block structures: `Conditional Branch` (111/412), `Show Choices` (102/404), and `Loop` (112/413) render a fold arrow in the same slot and with the same affordance as the existing plugin-command expander. `findBlockEndIndex` matches the terminator on indent rather than by counting openers, so it agrees with the indent-aware structure parsing the rest of the editor uses, and a nested branch closing first cannot be mistaken for the outer one's end. Collapsed state is held in a `WeakSet` keyed on the opening command **object**, not its index: inserting or deleting commands earlier in the page shifts every later index, so an index key would leave the fold behind on an unrelated command. `collapsedHiddenIndices` scans outermost-first, so an inner block nested inside a folded one keeps its own state for when the outer reopens. A folded row carries a badge with the hidden line count so it is never mistaken for an empty branch, and a malformed structure with no terminator stays expanded rather than swallowing the remainder of the list. Coverage in `event-command-collapse.test.cjs`.
- Persisted event fold state: `EventCollapsePreferences` stores folds in `localStorage` under `rpg-reactor.eventCollapsedBlocks`, scoped by project path, map, event, and page index, so pages do not share state. Only deliberate folds are recorded and a fully expanded page deletes its record rather than storing an empty list, which keeps "everything open" the default for pages that have never been touched. Command objects are rebuilt from JSON on load and cannot be the persisted key, so entries are `index:code:indent`: pinning the code and indent alongside the position makes a stale entry self-correcting, since a page edited since the fold was saved simply fails to match and renders expanded instead of folding whatever unrelated command moved into that slot. Hydration runs once per page rather than per render — the live `WeakSet` remains authoritative within a session, tracking commands through edits, while the positional keys are rewritten from current indices on every toggle. Malformed or non-object storage degrades to fully expanded. `clearProject` drops a deleted project's pages. Coverage in `event-collapse-preferences.test.cjs`.

- Destructive map resize confirmation: `analyzeMapResize` reports the populated tiles falling outside the new bounds and the events that would be deleted, and `saveMapProperties` aborts without touching the map unless the listed loss is confirmed. Events outside the new bounds are now removed rather than left at coordinates the map no longer has, where they were invisible to the editor.
- Anchored map resize: a nine-cell picker in Map Properties chooses which corner or edge existing content keeps, so a map can grow from the top or left. `computeResizeOffset` turns the anchor into a tile offset and `resizeMapData` walks the destination, letting one expression serve every anchor; `top-left` yields a zero offset and is byte-identical to the previous top-left-only behavior, and remains the default on every open. Tiles and events move together, direct-designation Set Event Location commands on the map are shifted, and `System.json` player/boat/ship/airship starts on that map are updated. A non-zero offset also scans every other map file and Common Events for direct-designation Transfer Player (201) and Set Vehicle Location (202) commands targeting the resized map and offers to shift them, listing where they were found and naming any map that could not be read. Variable-designation commands resolve at runtime and are left alone. A live hint under the picker shows the offset and pending loss as the size fields change; `countTilesOutsideResize` short-circuits when the old rectangle lands wholly inside the new one, so the common grow case does not rescan six planes per keystroke.
### Removed

- The unreachable standalone tileset editor. `main.js` carried a `showTilesetEditor`/`closeTilesetEditor` pair that constructed `new TilesetEditor(...)` — a class that does not exist in `editor/src` and is loaded by no script tag, so the call would have thrown `ReferenceError` had anything invoked it. Nothing did: the two callbacks were passed into `DatabaseEditorUI`, which reads `updateStatus`, `getTilemapManager`, `showTypesEditor` and `showTermsEditor` from that bag and never the tileset pair. That dead entry point was the only route into `DatabaseTilesetEditor.initializeUI` and everything below it, so 19 methods — 1,220 lines, roughly 39% of the file — were reachable from nowhere. Among them was `drawFlagOverlays`, whose flag-offset table disagreed with the correct one in `getTileIndexForImage` on **every** sheet (A1 at 0 rather than 2048, A5 at 5888 rather than 1536, B at 6144 rather than 0) and which ignored the 48-slot stride autotile kinds occupy; had that UI ever been revived it would have drawn every passability marker from the wrong tile. A second pass removed `renderTilesetList` and `loadTilesetPreviews`, which survived on paper but queried `#tileset-list`, `.image-preview`, `.image-filename` and `.clear-image-btn` — elements only the deleted UI ever created, so both could only no-op — along with the `#tileset-name-input` fallback that the compact UI's own field had superseded. Reachability was recomputed from the three entry points `DatabaseEditorUI` actually calls (`constructor`, `cleanupListKeyHandler`, `showTilesetEditorDetail`, the last of which reaches `initializeCompactUI` through a nested instance of the same class): 35 methods remain and every one is reachable. Two tests were pinning the removed duplicates — a second tileset-only copy of the 250-row lazy-list batching that `DatabaseEditorUI` already provides generically for every database type, and the second of two terrain-tag readers — and now pin the single surviving implementation.

### Changed

- Bumped the current development version to RPG Reactor 0.96.0.
- Tileset layer list density and the empty-tab affordance. Eleven slots did not fit the left column: rows carried a 32px thumbnail with 6px padding and margin, and the list took a `flex: 1` half share of a column it split with the Flags panel, so it scrolled while the panel below had room to spare. Rows are now ~32px (22px thumbnail, 3px margin) and the list is `flex: 0 0 auto`, sizing to its contents and leaving the remainder to Flags. The "Normal (B-E)" heading became "Normal (B-G)", which the localization source audit correctly rejected as an untranslated phrase; the key and its letter range were updated across all seventeen non-English locales, with the Greek entry's transliterated `Β-Ε` corrected to the Latin `B-G` the tabs themselves are labelled with. An unassigned tab rendered nothing but a line of text, leaving double-clicking a row in the left column as the only way to assign a sheet; it now offers a **Choose Image** button in that dead space, using the existing translated phrase rather than introducing one. B-G are single-slot tabs and get one button; the A tab spans A1-A5 and gets one per sublayer rather than guessing which was meant.
- The tileset image picker uses the shared `rr-modal-*` chrome and the shared searchable browser. It was a hand-rolled overlay with an inline header, a red Close button matching nothing else in the editor, and a file list built one `div` at a time with its own hover handling and no search. It now composes `rr-modal-overlay`/`-header`/`-title`/`-close`/`-body`/`-footer` and delegates the list to `RRPickerIndex.createBrowser`, gaining live name search, Unicode section rails, sticky headers and keyboard selection for free. It opens on the layer's current sheet and scrolls it into view; Escape, the close button, Cancel and a backdrop click all dismiss it, and the document-level key handler is removed on close. Assignment funnels through a single `confirm()` reachable from a row double-click, a preview double-click, or the footer button, which starts disabled and cannot fire without a selection. A pinned **(None)** row sits above the browser rather than inside it, so clearing a slot cannot be filtered out of reach by the search box; because `''` is a legitimate selection meaning "clear this slot", the no-selection sentinel is `null` and the guard is an explicit null check rather than a falsiness test, which would have made (None) silently do nothing. The list column is a flex column holding that pinned row plus the browser, and the browser's own `height: 100%` is cleared before it is given a flex basis — the two rules cannot share a column, and leaving both in place stops the inner list scrolling and takes the section rail with it. The **+** button on each layer row is no longer gated on the slot being empty, since swapping an assigned sheet wants the same affordance. The (None) row's selected state uses `--color-accent-tint-15` and `--color-accent-border-strong` rather than `--color-selection-deep`, which is a fixed `#094771` that does not track the active palette and so read as generic blue against the gold accent text.
- Tileset passability markers are legible over any artwork. They are painted directly onto the tile art, so a pale glyph on pale ground — or the red X over red brickwork — vanished into it. `drawFlagGlyph` strokes each marker in near-black before filling it, with `lineJoin: round` so the corners of X and the star do not spike, and restores `strokeStyle`/`lineWidth`/`lineJoin` because the caller keeps drawing afterwards. All five markers (O, X, star, damage, terrain tag) route through it, and the O and X fills go from 0.6/0.8 alpha to 0.95 now that the outline supplies the separation rather than the wash. The remaining markers are shapes rather than text and get explicit edges through three primitives: `drawFlagDot` rims a filled circle, `drawFlagArrow` traces each chevron dark-and-wide before laying the colour over it, and `drawFlagRect` borders a filled rectangle. That covers the four blocked-direction dots, the four passable-direction arrows, the ladder stile, the bush berry and the counter bar; the ladder rungs and the bush centre stay unoutlined because they sit inside an already-edged shape and rimming them at 10x2px would be noise. A first attempt gave the shapes a soft `shadowBlur` instead, which proved too weak against busy or same-hued tile art — the hard edge is what makes them legible, and dropping the shadow also removes a per-tile canvas state change. `drawFlagArrow` restores `lineCap`/`lineJoin` for the same reason `drawFlagGlyph` restores its stroke state: both run inside a per-tile loop that keeps drawing afterwards. Coverage in `tileset-image-picker.test.cjs`, including a guard that `PickerIndex.js` and `AssetFiles.js` load before this file.
- Tileset edits reach the map and palette without a reload. `TilemapManager.currentTileset` is captured when a map opens, so a sheet assigned afterwards was absent from the array `loadTilesetImages` walks and its texture never loaded. `refreshTilesetImages` diffs the incoming record against the held one, reloads only the slots whose filename changed, drops only the `textureCache`/`a1AnimationCache` entries keyed to those slots, and repaints only when `mapUsesSheets` finds a tile currently on the map drawn from one of them — so assigning a sheet to a previously empty slot, the common case, costs one image load and no repaint. A first attempt called `renderMap()` unconditionally on every tileset save, which froze a 200x200 map; that is what this avoids. `ProjectController.refreshTilesetSurfaces` drives both surfaces and ignores saves against a tileset the open map does not use, and the `rr-tileset-saved` listener is installed once and debounced by 150ms because the tileset editor announces a save on every flag edit as well as on an image assignment. Coverage in `tileset-refresh-on-save.test.cjs`.
- `MapEditor.previewLayer` is a `PIXI.Container` with a `previewGraphics` child, rather than a `Graphics` doing both jobs. It is parented to on every pointer move, and PIXI v8 deprecated parenting to a Graphics, so each `addChild` ran a deprecation path on a node that does not batch like a container. Three sites also allocated a fresh `PIXI.Graphics` per tile — per pointer move, for every tile in a stamp or circle radius — purely to get draw ordering; `previewGraphics` is the layer's first child and already renders behind the sprites added after it, so they draw into it instead. `_resetPreviewLayer` replaces the old `clear()` plus `removeChildren()` pairs, which would now remove the graphics child and silently kill every rectangle drawn afterwards; it clears the vectors, and destroys the discarded sprites rather than detaching them, explicitly preserving their textures since those are shared with the tileset caches. Coverage in `map-preview-layer.test.cjs`.
- Map Properties identifies the map it is editing. The heading composes as `<base> | <id>: <name>` — `Map Properties | 0211: Canite City`, or the New Map heading with the id the map will take, which creation has already reserved. The name is read from the live `map-name-input` rather than the map record, so renaming in General Settings retitles the modal as you type while Cancel still discards the edit and the heading never claims an unsaved rename. It is written through `textContent`, so a project-authored map name containing markup cannot reach the editor's own interface as HTML. The heading is composed after `populateMapPropertiesForm` has filled the field, since composing earlier would read an empty name. The input is static in the document and outlives the modal, so its listener is rebound by stored reference — `removeEventListener` then `addEventListener` — rather than by the clone-and-replace the modal's buttons use: cloning restores the element's value attribute and would discard the name written into it moments earlier, and re-adding blind would stack a handler per open. A map with no id degrades to the name instead of printing a padded `000`. Coverage in `map-properties-title.test.cjs`.
- Map Properties tileset dropdown entries lead with the tileset id, `0211: Canite City`, so an entry can be matched to its numbered row in the database. Padding is four digits to match the Change Tileset command's picker, which already labelled tilesets that way; the two list the same database and would otherwise number the same record differently, and a test pins them together. The option `value` remains the bare id, so the prefix is presentation only and cannot reach the saved `tilesetId`. A tileset with no name now shows its id plus the shared "unnamed" label rather than the old `Tileset 7` fallback, which would have read as `0007: Tileset 7`.
- Map Properties sizing and layout density: the dialog was a fixed `height: 85vh` regardless of content, so short configurations reserved a band of empty space beneath them while expanding BGM, BGS, and Battleback overflowed and forced scrolling. It is now `width: min(96vw, 1400px)` with `max-height: 92vh` and an auto height, so it shrinks to its contents and scrolls only on genuine overflow. Map Name/Display Name and Tileset/Scroll Type each collapse into a two-column row. The hand-built left/right wrappers are gone: the panels are direct children of a `.map-props-columns` container using CSS multi-column (`columns: 340px auto`) with `break-inside: avoid`, so they pack and balance into two columns, dropping to one below roughly 700px. The count is capped at two deliberately: General Settings is a single indivisible tall panel, so a third column cannot balance against it and instead strands a short panel on its own beside a full-height gap. Grid was the wrong primitive here — it aligns rows, so the tall General Settings panel reserved matching dead space beside every shorter one. The Note textarea no longer derives its height from a stretched parent and gained `resize: vertical`. `map-properties-layout.test.cjs` pins each control appearing exactly once, the sizing rules, and the packing behavior.

- Standing geometry in 3D is billboarded rather than fixed. `Reactor3D.Geometry.build` now emits cut-outs into their own groups, flagged `billboard`, whose `positions` hold the object's anchor repeated per vertex and whose `offsets` hold each corner in tiles; `Reactor3D.billboardMaterial` assembles the quad in the vertex shader on the camera's own right and up axes, read back into world space from the first two columns of `viewMatrix`. The up axis is `mix(worldUp, cameraUp, Reactor3D.BILLBOARD_TILT)`, and `BILLBOARD_TILT` is 0 — upright, yaw only, matching what `faceCamera` has always done for the event sprites. Any lean displaces a cut-out's art horizontally from its anchor by `height * |up.xz|`, which swings with the azimuth: at 0.75 and a 45 degree camera, a six-tile object's top sits 3.4 tiles out and moves 4.8 tiles through a quarter turn, which reads as the object drifting off its own cells and as everything sitting above the ground. At 0 that displacement is identically zero. The degenerate overhead case that leaning was meant to solve is handled by clamping `orbit` to 72 degrees instead. `polygonOffset` handles the coplanar case that creates, where a flat-lying cut-out shares a plane with the ground under it. This replaced a fixed south-facing plane plus a second plane crossing it at right angles, which existed only so an object did not vanish edge-on and which showed as a seam through the art at intermediate angles. Hinging at the ★ flag was tried first and does not work: on the Infernis gantry only the top of three rows is starred, since that is the only row that must overlap a character behind it, so ★ is a lower bound on height and not a partition of the prop.

- A3 and the A4 wall rows derive as `CLASS_SCENERY` rather than `CLASS_UPRIGHT`. As cut-outs they were built per column on the run's southern face, so on Map564 (a 79x111 labyrinth, 4,800 A3 cells) the overhead view showed bare floor with slivers where the maze should be, and obliquely a wall block eight cells deep stacked into an eight-unit tower. Raised, the walled area is solid, the corridors are carved from it, and the vertical faces come from the wall pass — which takes `stack[0]`, the wall autotile itself, since a dungeon paints walls on layer 0. The fixed-plane path in `Geometry.build` is kept for a tile an author classifies upright by hand.

- Declared 3D objects in `Tilesets.r3d.json`: an `objects` list per tileset, each `{tile, w, h, roles}`, where `tile` is the top-left of a rectangle on the sheet and `roles` is `w * h` characters of `S` (stands) or `F` (lies flat). `Tileset3DClass` gains `objectAt`, `roleOf`, `defineObject`, `clearObject` and `cycleRole`; `Reactor3D` mirrors `objectAt`/`tileRole` and `uprightPredicate` returns false for an `F` tile, so a role overrides a class. `Geometry.uprightObjects` takes a `declaredAt` lookup and groups declared tiles by instance origin — a cell's map position minus its position within the object — which is exact, before falling back to the adjacency guess for anything undeclared. A synthetic case shows why: tiles 8-9 and 10-11 painted side by side group as one four-wide object by adjacency and as two by declaration. The generator seeds a declaration per solid rectangular block of standing prop art. Shift-click two corners in the 3D Shape editor to declare, shift-click a declared tile to clear, alt-click to flip a role; declared objects draw as a yellow outline with a bar on flat cells. Coverage in `tileset-3d-class.test.cjs` and `reactor-3d-geometry.test.cjs`.

- 3D authoring is tool-based and applied by dragging a rectangle. `tile3dTools`/`tile3dToolbar` render a palette into the key panel, `attachTile3DDrag` owns the pointer on every sheet canvas, and `applyTile3DTool` applies the selected tool across `dragBounds`. `handleCompactCanvasClick` no longer handles `tile3d` at all — a click is a 1x1 drag. Cycling on click left no way to inspect a tile without altering it, and counting corners was the wrong shape for declaring a rectangle. The drag scales the pointer by `canvas.width / rect.width`, since the sheet is laid out at a different size from its backing store. `select` changes nothing, `object` declares, `erase` undeclares, `role` decides once for the whole rectangle rather than toggling per tile so a mixed selection is not scrambled.

- A5 is treated as a picture sheet rather than an autotile throughout: `Tileset3DClass.isPictureTile` and `Reactor3D.isPictureTile` admit 1-2047, `sheetCell`/`tileAtCell` address A5 as an eight-wide grid at `setNumber` 4 with no half-sheet split, `Geometry.samePicture` and `uprightObjects` group it with B-G, and the fixed-plane wall path narrows to `>= 2048`. The old `< 1536` cutoff put A5 with the autotiles, so an A5 wall could not be declared as an object at all.

- `previewArtSource` reads an autotile's cell from `cachedBaseCanvas` — the entry in `imageCache` the tab canvas is composed from — rather than the tab canvas itself, which has the flag overlay drawn onto it; the preview was showing the class chevron as part of the art. `drawTile3DPreview` derives its cell size from the object's extent instead of a fixed 22px.

- `applyTile3DTool` refuses a 1x1 `object` drag over a tile that is already declared, refuses `object`/`erase`/`role` on autotile indices with a reason on the console, and sets `selected3dRect` to the declared object's rectangle when the selection falls inside one — the overlay draws that rather than the clicked cell. Coverage in `tileset-3d-authoring.test.cjs`, including that a class painted on an A tab lands on the kind's base id and reads back from every shape of it.

- An upright object's anchor is the centre of its footprint in x *and* z, not the middle of its southern row. The anchor is the axis the billboard rotates about, so an off-centre one makes the object orbit that point: a six-deep object swung two and a half tiles around its own front edge through a quarter turn. Height stays separate — `base` is still `surfaceAt` at the southern row, the ground the object faces — and conflating the two is what made the southern row look correct for a while. `drawTile3DPreview` had the same bug and the same fix.

- `Geometry.build` computes a `roofLift` pass before `surfaceAt`: connected regions of A3/A4 art that are not themselves raised, and that touch a raised cell, take that cell's height. Walls raise into a mass through `CLASS_SCENERY`, and the roof behind them is ordinary flat terrain, so a building rendered as a block with its roof lying on the ground beside it. Coverage in `reactor-3d-geometry.test.cjs`.

- `drawTile3DPreview` projects a slanted, gridded ground plane that rotates with the drag, and `previewArtSource` reads an autotile's art from the rendered A-tab palette rather than from a sheet — the A tabs compose one cell per kind, so sheet addressing drew nothing. A cut-out's own picture cannot change as the view turns, which is why the ground had to carry the rotation.

- `drawTile3DPreview` no longer foreshortens or column-reverses as it turns. A yaw-billboard presents the same picture from every azimuth, so only the ground plane and the flat-role pieces rotate; treating the object as solid mirrored its columns and produced a broken silhouette from behind.

- `Tileset3DClass.defineObject` rejects tile 0, which is the engine's "no tile". `objectAt` already refused it, so a declaration anchored there could be created and never found again.

- Key rows are canvases drawn by `drawKeyMark`, which reuses `drawFlagDot`/`drawFlagArrow`/`drawFlagRect`/`drawBushMark` — the same helpers the tile overlay calls. The first version used tinted `<span>`s and came out blank for Ladder, Bush and Counter, whose overlays are glyphs rather than tints, and it could have drifted from the overlay without anything noticing. `drawBushMark` replaces the bush's `drawFlagDot`, since a green dot was indistinguishable from the direction dots.

- The 3D class marker draws only on a declared object's top-left cell (`markHere`); the tint still covers every cell. Drawn per cell, a 2x2 mountain read as four separate mountains — exactly what the declaration exists to deny.

- `DatabaseTilesetEditor.flagKey` renders a per-mode key into `#flag-mode-key`, and `refreshTile3DPreview` draws the selected tile or declared object into `#tile3d-preview` — both in the margins the centred sheet was already leaving empty, so nothing the author was looking at moves. The preview is an oblique diagram drawn on a 2D canvas from the tile's own art rather than a real render: showing one tile through the actual renderer means loading three.js, the sheets and a scene, and what needs checking is which parts stand and which lie flat. Both panels are English for now — routing them adds nineteen phrases across seventeen locales and the wording is still settling. Coverage in `tileset-3d-authoring.test.cjs`.

- `DatabaseTilesetEditor.flagIcon` draws an inline SVG for each flag mode, replacing the ASCII prefixes that only three of the eight buttons had. One 24-unit grid, `currentColor` so a selected button's icon follows its label, and each drawn as a few strokes because these render at 16px where anything finer smudges — checked by rasterising at 16 and 96. `tileset-3d-authoring.test.cjs` asserts every mode has one and that each button renders it.

- Cut-outs are grouped into whole objects. `Geometry.uprightObjects` replaces the per-column runs: cells join an object when they are adjacent on the map *and* adjacent the same way on the sheet, which is what makes a stamped picture a picture. Plain 4-connectivity was tried first and merged 408 cells of unrelated props across 44x27 tiles into one anchor on the world map; each object turns about a single axis through the middle of its southern row, so a wide structure holds together instead of fanning into one card per column. The anchor is what the object's position reads as, and both other candidates were visibly wrong: on the southern *edge* a five-wide structure sat over its cells from the south and a tile clear of them from the east, and on the footprint centre its base hung above the ground in front of it at a low camera. Autotile uprights are excluded and keep the fixed-plane path — a wall belongs to a building and has a real facing, and swinging it to the camera would rotate the building.

- B-E props derive as `UPRIGHT` when `standsUp` holds — impassable on all four sides, or starred — instead of requiring the star flag alone. On Map596 that flag test left tile 28 (a lone tree, 147 cells), 40 and 48 (mountains), 112 and 122 (landed ships) and 92 (scrub) lying flat while the canopy beside them stood: ★ is set only where a character walks *behind* something, so it is a lower bound on height and misses every object a character walks in front of. The test runs per block of art via `sheetBlocks`, so one qualifying piece raises the whole picture — the Infernis gantry is three rows tall and only its top row is starred, and per tile that stood its head up and left its legs on the floor. Pits and craters are the known false positive: the art blocks the way like an object and is a hole in the ground, so it wants `Flat` set by hand, once per tileset.

- A fourth 3D class, `CLASS_FOLIAGE`, for terrain that is a repeated object rather than a mass. It stands cut-outs on the cell taken from the terrain's lone variant — `classKey(id) + 46` for an autotile, since shape 46 is what MZ draws for a single isolated cell, and a recorded `standIns` entry for a B-E tile, which the generator guesses as the same-sized block directly above the tiling art on the sheet. Stand-ins are stored as `[topLeftTile, width, height]`: a lone variant is usually drawn over a block, and sampling only its first tile stood up the top-left quarter of each tree. `foliageDensity` cut-outs per cell (1 by default — a cell is one instance of the terrain, so it gets one object; several per cell multiplied the geometry to break up a grid the position hash already breaks up), each scaled 0.75-1.25 and displaced by up to `foliageSpread` from the centre through a position hash, so a wood is scattered but rebuilds identically; `foliageLift` raises the cell 0.3 so the wood has a visible edge; and `foliageFloor` is off, so the tiling art is not laid flat as well — it is the same terrain seen from above, so drawing both draws each cell twice and shows as a mat of canopy at the feet of the trees. Two alternatives were built and rejected against renders: keeping the floor art matched 2D from overhead and produced the mat, and capping the interior of a mass with the tiling art while standing cut-outs only on its border — `Geometry`'s `foliageBorder`, kept — was ideal overhead and made forests into flat terraces with a tree fringe from the ground. The 3D Shape editor cycles Flat → Upright → Scenery → Foliage → Auto. Coverage in `reactor-3d-geometry.test.cjs` and `tileset-3d-class.test.cjs`.

### Fixed

- Billboard cut-outs rendering dark and unfogged. `Reactor3D.billboardMaterial` was a `ShaderMaterial` whose fragment shader wrote the texel straight to `gl_FragColor`, skipping three.js's `colorspace_fragment` conversion from the texture's colour space to the renderer's output — so a tree was visibly darker than the same art lying flat beside it — and skipping the fog chunks, so the editor's `THREE.Fog` never reached it. It is a `MeshBasicMaterial` with an `onBeforeCompile` that replaces `#include <begin_vertex>`, keeping every other chunk the flat tiles use; `customProgramCacheKey` stops three.js compiling a program per material, since it cannot see the injected code.

- Vertical faces in 3D sampling the wrong layer. `Geometry.build` took a cliff face's art from `stack[stack.length - 1]`, the topmost layer of the cell, and above a step that is normally a decoration whose art is transparent everywhere the decoration is not — so the face alpha-tested away to nothing and a raised region stood on a rim of holes. Faces take `stack[0]` now, which is the terrain.

- 3D tile classification derived from the tileset flags standing terrain on end. `Reactor3D.uprightPredicate` treats an explicit `CLASS_UPRIGHT` as authored and `uprightRuns` therefore exempts it from `AUTO_MAX_FACADE` — deliberately, since a tileset may draw a tower fifty tiles tall as one prop — so a `Tilesets.r3d.json` filled in from `flag & 0x10 || (flag & 0x0f) === 0x0f` had no ceiling at all. On Map596 (200x200) that produced 685 facades, 64 of them past the cap and the tallest sixteen tiles, and connected-region analysis put 1,587 of the 2,337 standing cells inside regions of 4x4 or larger — terrain, not props. `build-scripts/derive-tileset-3d-classes.cjs` derives the file from map usage instead. Each B-E placement is judged in a 5x5 neighbourhood: covered ground whose tiles repeat is terrain (`SCENERY`), sparse or non-repeating company is a prop, and a tile reading as terrain in a quarter of its placements is terrain everywhere, since a misjudged prop rises a tile while a misjudged forest becomes a wall. The neighbourhood replaced a connected-region test that let a crater painted at a forest's edge inherit the forest's verdict and rise on a plinth. `0x10` alone decides height for props — impassable covers craters and rubble too, and honouring it put the map on stilts — and autotiles are read off the sheet: A1 flat whatever its flags, A3 and the A4 wall rows upright, blocked A2 and A4 roof rows raised as a mass. Only painted tiles are written. Coverage in `tileset-3d-derivation.test.cjs`.

- A tile selection surviving a palette layer switch. `selectLayer` reassigned `currentLayer` and re-rendered, but never touched `selectedTiles`, so the entries — sheet coordinates tagged with the layer they were taken from — stayed live. The redrawn palette had nothing to highlight, since the selection belonged to a sheet no longer on screen, while a click still resolved those entries against their original layer and painted its tiles. The selection is now cleared when the layer actually changes, the readout is reset to "No tiles selected" to match, and any hover preview built from it is dropped, since nothing rebuilds one until the pointer moves again. Re-asserting the current layer leaves the selection alone, and the clear is guarded on the outgoing value, so it has to run before `currentLayer` is reassigned or every switch would read as a no-op. Coverage in `palette-layer-switch.test.cjs`.

- Tileset sheets F and G erasing instead of painting. `MapEditor` carries two routines that turn a palette position into a tile id — `getTileIdFromPalettePosition` and `getBaseTileIdFromPalettePosition` — and only the first was extended when F/G landed. The second fell through its `switch` default and returned `0`, which is the empty tile, so committing a placement wiped the cell. Everything that merely *displays* a tile resolves its texture separately, which is why the palette and the hover preview both looked correct and only painting failed. Both routines now share one case driven by `RRTilesetSheets.baseTileIdForSheet`, and `tileset-extended-sheets.test.cjs` asserts every sheet key is handled in both, so a future sheet cannot be added to one and forgotten in the other.

- Newly created database records missing fields the runtime reads unconditionally. `getDefaultTemplates` was diffed against the field sets of every record in the bundled RPG Maker-authored projects, and four types were short. **Skills** lacked `requiredWtypeId1`/`requiredWtypeId2`, and `Game_Actor.isSkillWtypeOk` opens with `wtypeId1 === 0 && wtypeId2 === 0` — `undefined === 0` is false and `undefined > 0` is false, so every clause failed and a skill created in Reactor could never be used by any actor. **Weapons** lacked `etypeId`, which `changeEquip` compares against `equipSlots()[slotId]`, so a new weapon never appeared in the equip list and equipping it silently did nothing. **Items** lacked `tpGain`, and `applyItemUserEffect` computes `Math.floor(item.tpGain * tcr)`, so using one set the user's TP to `NaN` for the rest of the battle. **States** lacked `motion` and `overlay`, which `stateMotionIndex`/`stateOverlayIndex` return directly to the battler sprites, so a new state produced no pose and no overlay. The skill template also invented `message3`/`message4`, which no authored skill carries, and omitted `messageType`. This reached existing projects too: `createBlankDatabaseEntry` backs both the Clear action and the repair pass for null slots. The derived field and command shapes are vendored in `tests/helpers/authored-data-shapes.json` (regenerate with `helpers/derive-authored-data-shapes.cjs`) so the check runs in CI without the private project fixtures. Coverage in `database-record-templates.test.cjs`.

- Scroll Map dropping Wait for Completion: `command204` reads `params[3]` to decide `setWaitMode("scroll")`, but `ScrollMapEditor` had no control for it, read only `params[0..2]` when opening a command, and emitted a three-element array. The option could therefore never be turned on, and opening an imported command that had it set and pressing OK silently discarded the flag. The editor now carries `wait` through load and save and offers the same checkbox the Tint Screen, Flash Screen, and Shake Screen editors use. A sweep comparing every command editor's emitted parameter count against the highest `params[n]` the matching `Game_Interpreter.commandNNN` reads found this to be the only editor writing an array the interpreter reads past the end of; that sweep now runs as a test. Coverage in `event-command-parameters.test.cjs`.

- The window title showing the bundled demo's name for every project. `installCompatibilityTitlebar` built its label from the literal string `RPG Reactor | Reactor One` and nothing ever wrote to it again, so the title was frozen at the demo project's name no matter what was open. `updateWindowTitle` composes the correct `app.title | gameTitle` and sets `document.title` and `nw.Window.get().title`, but the compatibility titlebar is a `<div>` — neither of those reaches it. The titlebar is now seeded from the live title at construction and rewritten by `updateWindowTitle`, so it follows project open, close, save and language change like the native one. It reproduces only under frameless compatibility mode (`?rrFrameless=1` / `?rrWineFrame=0`), which is the self-relaunch used for Wine/Proton — a normal framed window keeps the native titlebar and was never affected, which is why this showed up for packaged users and not in development. Coverage in `window-title.test.cjs`.

- Set Event Location's "Exchange with another event" reading and writing the wrong slots. `command203` uses `params[2]` as the **other character's id** — there is no map id in this command form — and `params[4]` as the direction. The editor parsed `params[2]` as a map id and invented an event id from `params[3] || 1`, labelled them "Map ID" and "Event ID", and emitted only four parameters, so the direction slot was truncated away on every save. Both authored instances in the bundled projects are `[charId, 2, otherCharId, 0, direction]`; the editor now loads and rebuilds them byte-identically, and offers a direction control in place of the invented map id.

- A1 waterfall kinds 9 and 11 drawn from the wrong table in map-image export. `drawAutotileToCanvas` gated the waterfall table on `Math.floor(tx / 4) === 1`, which is false for exactly those two kinds — they fell through to the floor table and sampled a different 96px band of the sheet. The engine (`reactor_core.js`) and all three sibling render paths select it unconditionally, and `renderAutotile` in the same file carries a comment saying so. Kind 9 appears **2,149 times across 21 maps** in the bundled projects, kind 11 **74 times across 5**. Affected Save Map as Image and the right-drag map-stamp preview.

- The eraser swallowing the bucket fill. The pointerdown dispatch let `eraserMode` claim the click ahead of the tool branches, so selecting eraser + bucket erased a single cell like the pencil. `eraseFillArea` — 37 lines, fully implemented and correct — was unreachable from the UI. The eraser is now treated as a modifier on the active tool rather than a tool of its own, matching how the rectangle and circle tools already handled it.

- The web editor had no keyboard shortcuts at all. `setupKeyboardShortcuts` opened with `if (typeof nw === 'undefined') return;`, but only the F5, F11 and F12 branches need NW.js — the early return also removed Ctrl+S, Ctrl+Z/Y, Ctrl+C/X/V and Delete. The NW-specific branches now guard individually, matching the `typeof nw === 'undefined' && !window.RPGReactorHost` pattern used elsewhere, and `reloadApplicationIgnoringCache` degrades to `location.reload()`.

- The loop toggle not reaching the playing audio. `toggleLoop` flipped `audioPlayer.loop` and restyled the button, but the flag only reached a media element when a track started, so turning loop off mid-playback did nothing audible. It now sets `loop` on the current channel as well.

- Sidebar divider positions saved and then always discarded. `loadSavedSizes` runs once from `initialize()` — before any project is open — and skips `display: none` sections, which every id-bearing section is at that point. `refresh()`, which runs when sections become visible, never re-applied them. It now does, after `initializeSectionFlexValues` so the restored values win.

- Imported character sheets getting a different cell size per frame. The 3x4 crop loop rounded every cell boundary independently, so any sheet whose dimensions are not divisible by 3x4 produced adjacent cells differing by a pixel — and `RR_CG_drawTemplatePart` right-pads short rows rather than centring them, so the character shifted horizontally on the middle walk frame. The origin is still rounded per cell but the width and height are now shared, which makes every checked import uniform. Where the source image genuinely runs out of pixels on the last row, the normalizer below repairs the residue.

- "Normalize All Templates" reporting success while leaving frame heights unequal. `_padSheetInPlace` computed one sheet-wide `padTop`/`padBottom`, so a frame with fewer rows than the sheet maximum stayed short of the target while widths — which are padded per row — came out correct. Padding is now derived per frame. The 279 shipped part descriptors were already uniform; this only ever bit the user-import path.

- Map window culling parking windows that were still on screen. `_rrWindowDormant` tested only `child.x`/`child.y`, but `Window.prototype.move` assigns those as the **top-left corner** with `width`/`height` extending right and down — so a window wider than the 384px margin still overlapped the viewport while its corner sat outside it. Culled windows are *detached from the display tree*, not merely hidden, so the result is disappearance rather than a paint glitch. The character-sprite branch of the same method, sixty lines above, already carried exactly this correction with a comment explaining it; the window branch never got it. The predicate now culls on the drawn extent. Its opacity test was widened too: `contentsOpacity` only reaches `_contentsSprite.alpha`, while `_contentsBackSprite` and everything added through `addInnerChild` (`Sprite_Gauge`, `Sprite_Name`, `Sprite_StateIcon`) are independent siblings in the client area with their own alpha, so a transparent-framed window drawing through those was judged to contribute nothing and parked. Coverage in `window-culling.test.cjs`, which lifts the shipped predicate rather than restating it.

- A window stepped twice on the frame it was parked. `Spriteset_Base.update` cascades `update()` to every child before `updateOffscreenCulling` runs, and the catch-up loop then re-reached the window the first loop had just appended to the holder. This is one extra tick per park transition, not a sustained rate — a doubled openness step during a fade, and a repeat of any side effect a plugin's `update()` performs. The transition is now recorded and consumed once.

- **Party-wide actor commands collapsed onto Actor 0001.** `Game_Interpreter.iterateActorId` branches on `param === 0` to mean *the entire party*, and nine command editors loaded that slot as `params[1] || 1` — 0 is falsy, so every party-wide command was rewritten to the first actor the moment it was opened, and the edit path writes the loaded value straight back. The actor dropdowns also started at index 1, so the value could not be expressed at all. In the bundled projects this covers **284 of 284** Change HP commands, **376 of 379** Recover All, 94 of 114 Change State and 3 of 7 Change Skill: open the inn's "Recover All → Entire Party" event, press OK, and from then on only Actor 0001 is healed. Fixed across `ChangeHPEditor`, `ChangeMPEditor`, `ChangeTPEditor`, `ChangeEXPEditor`, `ChangeLevelEditor`, `ChangeParameterEditor`, `ChangeStateEditor`, `ChangeSkillEditor` and `RecoverAllEditor`, each of which now loads with `??` and offers an **Entire Party** option translated into all 17 non-English locales.

- Show Text losing the Top window position: `parseCommand` read `command.parameters[3] || 2`, and position `0` is Top. **946 of the 18,520** authored Show Text commands in the bundled projects use it. Opening one and pressing OK moved the message window to the bottom, and the dropdown showed the wrong value while open. Every sibling in the family already guarded this correctly — `ShowChoicesCommandEditor` uses `params[3] !== undefined ? … : 2`, and the adjacent `background` read uses `|| 0` because 0 *is* its default — so this was a single outlier.

- Fadeout BGM/BGS authored in frames while the engine reads seconds. `AudioManager.fadeOutBgm(params[0])` reaches `WebAudio._fadeOut`, which calls `linearRampToValueAtTime(0, currentTime + duration)` — a Web Audio API that takes **seconds**. `AudioCommandEditor` defaulted to `[60]` "= 1 second", divided by 60 on load and multiplied by 60 on save. Inserting the command with its default therefore produced a **60-second** fade, and a 5-second setting produced a five-minute one; conversely an imported MZ command reading `5` displayed as `0.083 s`. Every authored 242/246 in the corpus holds a small seconds count. The conversion is gone from the editor and from the command-list label, which had repeated the same assumption.

- Event graphics taken from the right half of a B–E tileset page saved the wrong tile. `TilesetPaletteViewer` presents those sheets as a 16-wide split and reports `x` in 0..15; `MapEditor.getBaseTileIdFromPalettePosition` folds `x >= 8` into `y += 16`, but `EventManager.convertToTileId` did not. Palette B (8,0) became tile 8 instead of 128 — a different tile entirely, written into `Map###.json` and shown wrongly in both the editor and the game. Right-half tiles are ordinary authoring territory: **132,054** placements against 214,715 left-half across the corpus. Verified by feeding both conversions through the engine's own `sx/sy` frame math.

- Plugin command metadata overwriting plugin parameter defaults. `parsePluginParameters` walked the annotation block without resetting on `@command`/`@arg`, so every `@default` belonging to a plugin **command argument** landed on the last real `@param`. `parsePluginParameterMetadata` has carried that guard all along, making this a two-parser divergence. Adding an affected plugin through the Plugin Manager wrote the wrong default to `js/plugins.js` — `debugMode: "1"` where `false` was declared (neither value the runtime's boolean check accepts), `"Gold Icon Index": "true"` where `163` was declared. Both parsers now agree on every bundled plugin, which the test checks by lifting them out of the source and running them over the real plugin set.

- Six of the eight System 1 → Music rows read and wrote keys the engine never looks at. The table synthesised `` `${type}Bgm` `` for all eight, but System.json only spells Title and Battle that way: Victory, Defeat and Game Over are **ME** entries (`victoryMe`, `defeatMe`, and `gameoverMe` — lowercase), and vehicle music nests as `boat.bgm` / `ship.bgm` / `airship.bgm`. `template/Complex` has a real `gameoverMe` of `Seka - WhatIsLeft` that the editor rendered as "(None)", and picking a file for any of the six had no in-game effect while injecting a junk key into System.json that then travelled into every `Test_System.json`. A `resolveMusicSlot` helper now maps each row to the slot the engine reads, for both display and save.

- A newly created animation left `displayType` unset. `Spriteset_Base.isAnimationForEach` tests `animation.displayType === 0`, so an absent value is false and the animation plays **once at the centroid of the target group** instead of once per target — while the editor's own dropdown, which marks no option selected, displays the first entry and claims per-target. All **648** MZ-format authored animations carry the field. The record-template guard now separates MZ-format animations from MV-format ones, which had been diluting the required field set down to just `name`.

- The Traits and Effects tables in the Skill, Item, Weapon, Armor and State editors never refreshed after an edit. All five called `document.querySelector('.database-detail-panel')` — a class that exists in no HTML or CSS in the repository; the pane is `#database-detail`. The selector returned `null` and the redraw silently did nothing, across all six call sites per file (add / edit / cut / copy / paste / delete). The Enemy editor is the one sibling that was ever repaired, and it still carries the stale class as a fallback. Beyond the stale display this made **Delete destructive past the selection**: the index comes from the un-redrawn DOM, so a second Delete removes a row the user never selected, and trait edits have no undo.

- Event page conditions written with the wrong type. `attachConditionListeners` decided how to store a value from the *element* type — `if (e.target.type === 'number') value = parseInt(value)` — which describes the control, not the field. Two consequences. A `<select>` reports type `select-one`, so **Actor** and **Item** conditions were stored as strings (`"3"`), where all 49,371 authored pages in the bundled projects hold numbers; the stock runtime tolerates this because JS object keys are strings, but any plugin comparing `page.conditions.actorId` strictly sees a mismatch, and the project no longer round-trips. More seriously, **clearing the variable-threshold field** stored `parseInt('')`, i.e. `NaN`, which `JSON.stringify` writes as `null`; the runtime's check is a bare `$gameVariables.value(c.variableId) < c.variableValue`, so `< null` is `< 0` and the page began meeting a condition it never had before. Conversion is now driven by the field: the six numeric conditions are parsed as integers, `selfSwitchCh` stays a string, and an empty or unparseable entry keeps the previous value instead of writing something the engine cannot compare. Zero and negative thresholds are preserved, since both are legitimate. Coverage in `event-page-conditions.test.cjs`, including a check that every field the form exposes and authored data holds as a number is on the numeric list.

- Two editor sources referenced by `index.html` were never committed: `src/EditorInstanceBroker.js` and `src/event/EventCollapsePreferences.js`, both added earlier in this cycle. They loaded on the machine that wrote them and would have 404'd for everyone else. Both are now tracked. `shipped-file-references.test.cjs` resolves every local `src`/`href` in `editor/index.html` and `template/Demo/index.html` and asserts each target both exists and is committed, so a forgotten `git add` on a new source file fails the suite instead of shipping. The scan also asserts it matched a plausible number of scripts, since a regex that quietly matched nothing would make the check vacuous.

- The bundled Demo project could not boot from a clean checkout. `template/Demo/js/reactor_main.js` lists `js/libs/lz-string.js` and `js/reactor_picture_extensions.js` in `scriptUrls`, and both files existed on disk but had never been committed — `.gitignore` re-includes `/template/Demo/**`, so nothing was excluding them; they were simply never added. `template/Demo` is the one project directory the repository tracks and `dist-editor-worker.js` bundles it into the editor, so a fresh clone or a CI build shipped a Demo missing two of its sixteen boot scripts. The failure is total rather than degraded: `onScriptLoad` only advances `loadCount` on success and `onScriptError` merely reports, so `onScriptsLoaded` never fires and the Demo stops on the loading spinner. Both files are now tracked and are byte-identical to their `runtime/` originals. `demo-template-completeness.test.cjs` parses the boot manifest, asserts every entry is present *and* tracked in git, checks the Effekseer wasm the same way, and compares each shipped runtime file against `runtime/` so the templates cannot drift again unnoticed. The private-fixture guard in `release-infrastructure.test.cjs` was narrowed to permit `template/Demo` specifically — it is tracked and available in CI — while still blocking traversal of `template` itself.

- Terrain tags silently not applying on tiles whose flag exceeds 16 bits. `Game_Map.terrainTag` is `flags[tile] >> 12` with no mask, but the tileset editor cleared only bits 12-15 when writing a tag (`oldFlag & ~0xF000`) and masked to `0x0F` when displaying one. On a flag with bits above 15 set the two disagree: setting terrain 3 on `0x02CBDE0F` produced `0x02CB3E0F`, which the editor showed as 3 and the engine read as **11443**. The control appeared to work and changed nothing the game could see. The write now clears everything from bit 12 up (`oldFlag & 0x0FFF`), which preserves every flag the engine defines — passage `0x0f`, star `0x10`, ladder `0x20`, bush `0x40`, counter `0x80`, damage floor `0x100` all live below bit 9 — and the two palette renderers read the tag with the same unmasked shift the engine uses, so a flag the game reads as garbage is no longer displayed as a plausible 0-15. Surfaced by decoding real project data: `Star Shift Rebellion` carries 1,200 such flags in each of five tilesets, written by `Cyclone-Map-Editor-MV`. Coverage in `tileset-flag-semantics.test.cjs`, which lifts the shipped cycle out of the editor and checks it against the engine's own bit reads.

- Web editor asset routing mis-resolving nested paths: the service worker located the project root by taking the **last** `/project/` in the request path, so any asset stored under a folder the game itself names `project` matched the wrong occurrence — `project/img/pictures/project/logo.png` resolved to `logo.png` and either served an unrelated file or fell through to a 404. The routing now anchors on `self.registration.scope`, which is where `WebHost` mounts its `/project` virtual root, and requires the path to start there. That also confines interception to the worker's own origin and scope, where before any URL containing the marker was intercepted. Coverage in `web-service-worker-routing.test.cjs`, which loads the shipped worker against a stub `self` rather than reimplementing the rule.

- Battle Test database copies shipping in deployed games: `BattleTestConfigModal` writes a `Test_`-prefixed copy of all fourteen database files into `data/` and never removes them — they are regenerated on the next battle test and are only read in `btest` mode. Neither `build.js` nor `build-worker.js` excluded them, so every release carried a full duplicate of the database: **15 MB in `template/Complex`, 13 MB in `Star Shift Rebellion`**, plus `Test_System.json` exposing the developer's saved test party. Both staging paths now share an `isStagingExcluded` predicate that drops `data/Test_*.json` alongside the existing `Backup`/`save`/`Screenshots` rules. The rule is anchored to `data/` and to `.json`, so a plugin or asset legitimately named `Test_…` is unaffected. Coverage in `deployment-staging-exclusions.test.cjs`, which lifts the shipped predicate out of both scripts rather than reimplementing it, and pins the two copies against drift.

- Non-atomic writes to two critical project files. `FsAtomic.js` exists because a truncate-in-place `writeFileSync` destroys the previous good file along with the new one if the process dies mid-write, and its header names `System.json` and the database JSON specifically — but two call sites still bypassed it. `DatabaseTilesetEditor.saveTilesets` wrote `data/Tilesets.json` directly; it is the largest database file, since each tileset carries an 8192-entry flags array, so it has the widest exposure window of any of them. `BattleTestConfigModal.launch` wrote `data/System.json` and the `Test_` copies directly; `System.json` holds terms, starting party, vehicles, and the boot map, so a partial write leaves the project unopenable. Both now use the same `_writeFileAtomic` wrapper as `DatabaseManager`, `ProjectController`, `ProjectManager`, `PluginManager`, and `TilemapManager`, degrading to a plain write only where `renameSync` is unavailable (test mocks, the web host shim). A sweep now walks every `writeFileSync` in `editor/src`, resolves the target path through nearby assignments, and fails on any that names a critical project file without going through the wrapper. Coverage in `atomic-project-writes.test.cjs`.

- Plugin commands losing their display label on re-edit: `params[2]` of a `357` holds the command's human-readable name — the string from the plugin's `@text` annotation, which is what the event list shows. `PluginCommandEditor` never read it and wrote a hard-coded `''` with the comment "params[2] is unused in MZ", so opening any authored plugin command and pressing OK erased the label in the saved project. In the corpus every authored `357` carries one (`['PSYCHRONIC_PTBS', 'PTBS_StartBattle', 'Start PTBS Battle']`). The editor already parsed `@text` into the command dropdown, so the label is now taken from the annotation when the selected command supplies one and from the loaded command otherwise — the latter covering plugins whose source file is missing or unannotated. Switching plugin or command clears the stale value rather than carrying it across. The event list prefers the label over the internal command name, falling back to it for commands written before this change and for MV's `356`, which has no such slot. Found by sweeping every command editor for parameter slots it emits but never loads, which is the same shape as the Scroll Map wait flag. Coverage in `plugin-command-label.test.cjs`.

- `When [Choice]` branch markers written without the choice text: `ShowChoicesCommandEditor` emitted `parameters: [index]`, while all 2,570 authored `402` commands in the bundled projects carry `[index, text]` — as do Reactor's own Common Event and Troop page templates, making the choices editor the outlier. Only the index drives branching, so gameplay was unaffected, but the command list had nothing to label the branch with and fell back to a bare `Choice 1`, and the project no longer round-tripped through tooling that reads the name. The marker now stores the text and the list renders it, keeping the ordinal as a fallback for markers written before this change.

- Unescaped project-authored text in the event header: `EventEditor` interpolated `${event.name}` into a `value="…"` attribute and `${event.note}` into a `<textarea>` body inside `innerHTML` templates, with no escaping. A quote truncated the attribute and a `</textarea>` closed the element early, so the mangled value was read back from the DOM and saved — degrading the project data itself — and in an NW.js window with Node enabled, markup injected from an imported project executes with full filesystem and process access. Both now use `rrEscapeHtml`. This was the class 0.95.0 swept for project names, notes, and messages; the event header was missed. A new sweep parses every template literal assigned to `innerHTML` across `editor/src`, extracts interpolations reading user-authored record fields, and fails on any that do not pass through an escape helper — the two remaining hits are developer-authored schema text and are allowlisted explicitly. Coverage in `markup-escaping.test.cjs`.

- Audio commands inserted at indent 0 inside a branch body: every event command editor builds its command at `indent: 0` and relies on the insertion path to rebase it onto `EventCommandList.insertionIndent(list, insertIndex)`. The audio branch (codes 241, 242, 245, 246, 249, 250, 251) spliced `editedCommand` directly with no rebase — the single insertion site of 102 that neither rebased nor set `indent: baseIndent` inline. The consequence is not cosmetic: `Game_Interpreter.skipBranch` is `while (this._list[this._index + 1].indent > this._indent) this._index++`, so a body command left at the branch's own indent halts the skip, and it plus every later command in that body execute even when the condition is false. The edit path was already safe because `AudioCommandEditor` clones the original command and preserves its indent; only newly inserted commands were affected. Coverage in `event-insert-indent.test.cjs`, including a check that no insertion site establishes a command without setting its indent.

- Animation timing SE volume discarding a deliberate zero: the save handler read `parseInt(...) || 90`, and `0` is falsy, so a timing dragged to silence on a `min="0"` slider was written back at volume 90. Replaced with `DatabaseAnimationEditor.readNumericInput`, which keeps any finite value — including 0 and negatives — and falls back only when the field is missing, empty, or unparseable. A sweep of every `min="0"` control in the editor and Forge against truthy `||` defaults found this to be the only instance; the neighbouring `timing-duration` (`min="1"`) and `timing-se-pitch` (`min="50"`) reads cannot reach 0 and were left unchanged. Coverage in `animation-timing-se-volume.test.cjs`.

- Silent IndexedDB write failures in the web host: `createFileSystem` queued each `put`/`delete` promise into `pending` and attached only `.finally(() => pending.delete(operation))`. `finally` passes a rejection through rather than handling it, so a failed write produced an unhandled rejection **and** removed itself from `pending` before `flush()` awaited the set — `flush()` then resolved successfully with nothing written, while the in-memory `contents` map still held the new data. The project appeared saved for the rest of the session and reverted to its last persisted state on reload; browser quota exhaustion is the realistic trigger. Failures are now recorded per path, `flush()` uses `Promise.allSettled` so one failure cannot discard the writes that succeeded, and it throws an error carrying the failed paths. The report drains, so a later flush does not re-raise a failure already surfaced. `hasPendingWriteFailures()` exposes the state for callers that want to check without flushing. Coverage in `web-storage-write-failures.test.cjs`.

- Undo restoring a wrong-length map array after a resize: `MapEditor.undo` assigned `this.tilemapManager.currentMap.data = previousData` with no size check, and `saveMapProperties` cleared no history. A paint followed by a Map Properties resize therefore left snapshots sized `oldWidth * oldHeight * 6` on the stack while `currentMap.width/height` described the new size; undoing produced a `data` array that disagreed with its own dimensions, which renders as garbage because every index is computed as `layer * (w * h) + y * w + x`, and persists to `Map###.json` on the next save. `applyMapResize` now clears both the paint history and `EventManager`'s (a resize moves events and deletes those the new bounds exclude, so an event undo would restore them at coordinates the map no longer has), and `undo`/`redo` call `dropStaleUndoStates` first so any snapshot whose length no longer matches `width * height * 6` is discarded rather than applied — closing the same hole for any other path that resizes a map. Coverage in `map-undo-resize-safety.test.cjs`.
- Non-atomic shared clipboard write: `ReactorClipboard.writeSharedFile` used a truncate-in-place `writeFileSync` on the file that carries typed payloads between editor instances. A whole-map envelope is large enough to be read mid-write by the receiving instance, which sees a partial document and fails the paste; a crash mid-write leaves the file corrupt until the next successful copy. It now routes through the same `RRWriteFileAtomicSync` rename-over helper as project data, guarded on `renameSync` existing so mocks and the web host still work.

- Sliver tiles misread as transparent: `TilesetPaletteViewer.isSelectionTransparent` sampled `getImageData(12, 12, 24, 24)` — the centre 24x24 of a 48x48 tile — leaving a 12px border unexamined. A B-E or A5 tile whose art is only a few rows at one edge, the continuation of an object in the neighbouring tile, therefore read as fully transparent, and `autoToggleEraseMode` armed the eraser: painting with it erased instead of placing. Selecting it alongside the tile above short-circuited the check on the first opaque tile, which is why the pair worked and the sliver alone did not. The full tile is now sampled; the source rect is drawn 1:1 into a cleared scratch canvas, so there is no neighbouring-tile bleed the crop was guarding against. Genuinely empty tiles still report transparent and still arm the eraser, and autotile layers (A1-A4) remain excluded because their palette grid is a rendered preview rather than sheet cells. Coverage in `tileset-transparency-detection.test.cjs`.

- Collapsed Tileset dropdown on an unresolvable `tilesetId`: `populateMapPropertiesForm` assigned `select.value = mapData.tilesetId || 1` directly, and `populateTilesetDropdown` emits options only for non-null tilesets. Assigning a value that matches no option leaves `selectedIndex` at `-1`, so the control draws no text and shrinks to its padding — the reported "crunched" dropdown, which recovered as soon as any real entry was chosen. Reached by a map whose tileset was cleared from the database, or by a new map defaulting to id 1 in a project whose tilesets begin at a higher id. `selectTilesetOption` now falls back to the first available option when the requested id does not match, `populateTilesetDropdown` appends a `(None)` placeholder when a project has no tilesets at all so the element is never optionless, and the select carries a `min-height` so it cannot collapse visually even with nothing selected. Coverage in `map-tileset-dropdown.test.cjs`.

- Always-visible Battleback picker: `#map-battleback-picker` declared `display` twice in one style attribute (`display: none; … display: grid;`), and the later declaration wins — so the selectors rendered regardless of the Specify Battleback checkbox. The duplicate is removed, and both toggle sites now set `grid` rather than `flex`, which had discarded the element's own `grid-template-columns`. Covered by a test that rejects a second `display` declaration on any collapsible picker.
- Web editor application menu: the word-based File/Database/Plugins/Tools/Forge/Help bar no longer clips its absolutely positioned dropdowns below a web-only `overflow-y: hidden` boundary. The bar uses visible overflow and wraps at narrow widths; the independent icon toolbar remains horizontally scrollable. Source and generated-web-package tests enforce the non-clipping rule.
- Map tree New Map placement: the right-click target now flows through the map-properties modal and anchors insertion. Confirmed maps are placed immediately after that map in its normalized sibling group, preserving child hierarchy; calls without an explicit context target fall back to the currently highlighted map, and invalid anchors still append safely to the root group.
- Auto-layer bucket preservation: flood filling an A-layer floor now writes only its resolved destination plane, leaving existing upper B-E and A-wall content intact. The post-fill autotile pass reconnects only planes actually changed by the operation, preventing a lower fill from silently changing upper wall shape IDs. Decoration fills still replace their resolved second A slot, while explicit-layer and topmost eraser semantics are unchanged.
- Packaged multi-instance startup: modern NW.js ignores the deprecated `single-instance: false` manifest field, so repeated native launches were redirected into the first process. The editor now brokers subsequent Windows, macOS, and Linux launches into detached processes with distinct atomically leased `--user-data-dir` profile slots. Live slots are never shared, stale leases recover conservatively, slot preferences persist, and project-level locking remains independent.
- Windows developer package finalization: the worker now resolves and requires `resedit` only inside the `releaseBuild` metadata branch. Interactive normal/SDK packages skip PE metadata editing and no longer fail after downloading and extracting NW.js; release candidates still require and verify app-owned Windows metadata before signing.
- Top-level database Clear/Delete consistency: toolbar Delete, keyboard Delete/Backspace, and context-menu Cut now share one blank-template operation. Clearing Tilesets 1-5 and 11-15 leaves all 15 array slots and embedded IDs intact, renders cleared slots as `Unnamed #N`, and survives JSON save/reload without hidden `null` gaps or tail shrinkage. The prior toolbar-only `null` behavior explained the apparent packaged-versus-development discrepancy.
- Null-slot repair scope: `DatabaseManager.deleteEntry` nulls the addressed slot for any `dataKey`, so pre-0.96.0 toolbar deletion could persist hidden gaps in every top-level list, not only Tilesets. `restoreNullDatabaseEntries` now runs once for the opened type ahead of the `openDatabase` switch, repairing Actors, Classes, Skills, Items, Weapons, Armors, Enemies, Troops, States, Animations, Common Events, and Tilesets alike. Index 0 stays reserved, populated records are untouched, and types with no backing array or template no-op.
- Locale completeness: `RR_ADDITIONAL_LOCALES` merges its overrides over `RR_I18N_STRINGS.en`, so `zh-Hant`, `zh-Hans`, `ru`, `pt`, `de`, `fr`, and `el` silently rendered every key added after those tables were written. Between 118 and 144 of 392 keys per locale resolved to English, concentrated in the Effekseer Forge, event-page fields, and toolbar tooltips. All seven tables are completed, and genuine gaps in `es`, `it`, `id`, `vi`, and `tr` are filled. Key-set parity tests could not catch this because the inherited keys do exist; two new tests assert instead that no locale resolves to the English string outside a documented proper-noun and loanword allowlist, and that the allowlist itself contains no entries that have since been translated.
- Instance slot takeover race: `tryReserveSlot` judged a lock stale, deleted it, and re-created it as three unsynchronized steps, so two launches could both judge the same lock stale and the slower delete would destroy the lock the faster one had just created — leaving both processes sharing one `--user-data-dir`, the precise failure the broker exists to prevent, in the post-crash state where stale locks exist. A per-slot `.reclaim` mutex now serializes the re-check, delete, and re-create; losers fail fast to the next slot, and a mutex abandoned mid-reclaim ages out through the existing staleness threshold. Inherited lease cleanup also releases by token alone: it runs in the NW.js window, whose `process.pid` is never the spawned process pid the parent recorded, so the pid comparison could not match and the handler never fired.
- New map rollback: `saveMapProperties` inserted the MapInfos entry and renumbered its siblings before writing anything, then returned `false` on a write failure without undoing either. A failed map or MapInfos write now restores the previous list wholesale, since insertion mutates sibling `order` values that removing the new entry alone would not repair.
- Synchronous per-draw GPU texture upload under PIXI 8: `Bitmap` ends every draw op — `drawText`, `blt`, the fills — with `this._baseTexture.update()`. On v5 that was a dirty flag and the upload happened once at bind time during render; on v8 `GlTextureSystem` subscribes to the source's `update` event and `onSourceUpdate` performs a full synchronous `texImage2D` of the entire canvas. A `Window_VictoryExp` gauge count-up therefore issued 246 `drawText` plus 80 `blt` uploads in a single frame, measured at 106ms of a 128ms frame, while the canvas 2D work behind them totalled 0.4ms — both call sites landed at ~0.32ms each despite doing unrelated work, because the cost was entirely the shared upload tail. `pixi_compat` now queues sources instead and drains the queue from `AbstractRenderer.prototype.render`. Flushing per render rather than per frame is deliberate: render-to-texture passes that run mid-update, such as MVNovaLighting's light map, upload first as well, so no draw can read a stale texture. The queue is swapped before draining so an update raised during the flush defers to the next one, destroyed sources are skipped, and the behavior is gated on v8 — off v8 `update()` stays immediate, since no flush hook is installed there. Coverage in `pixi-texture-upload-batching.test.cjs`.
- ReactorProfiler battle-side attribution: the `windows` phase wraps only `Window_Base.prototype.update`, so a subclass that redraws its contents from its own update — `Window_VictoryExp.updateTicks` calling `drawAllGauges` every frame — contributed nothing to it and landed in the unattributed remainder of `update`. A battle capture could therefore report 108ms of a 110ms frame inside the tick handler with 0.4ms of `windows` and no way to place the rest. Added phases for `Scene_Battle.update`, `Spriteset_Battle.update`, `Window_Base.drawTextEx`, and `Bitmap.drawText`/`getPixel`/`blt`/`gradientFillRect`, so content drawing and the battle spriteset are attributed rather than inferred. `getPixel` is broken out on its own because every text-colour lookup passes through it.
- `ColorManager.textColor` windowskin readback per call: every colour lookup ran `Bitmap.getPixel`, which allocates an `ImageData` for a single pixel and builds its result string from three `toString(16)` conversions. Windows that redraw per frame multiply that quickly — `Window_VictoryExp.drawAllGauges` issues roughly ten lookups per actor per frame, so a six-member party sustained about 3,600 one-pixel reads per second purely to re-derive a fixed palette. The palette is immutable once the skin has loaded, so lookups are now cached per windowskin instance, keyed on the bitmap itself: swapping or reloading a skin replaces the instance and drops the cache, `loadWindowskin` clears it explicitly, and an unready bitmap bypasses the cache so a placeholder colour never becomes permanent. `readTextColor` retains the original uncached behavior. Coverage in `color-manager-cache.test.cjs`.
- MZ actor animation auto-mirroring applied to MV games: `Spriteset_Base.createAnimationSprite` runs `if (this.animationShouldMirror(targets[0])) mirror = !mirror`, and MZ's `animationShouldMirror` is `target.isActor()` — so every animation played on a party member is flipped horizontally. MV's `rpg_sprites.js` defines no such function, so MV content is authored for unmirrored playback and each cell's own `cell[5]` flag is the only intended flip. The reversal affected all actor-targeted animations but was only legible on ones containing text: Star Shift Rebellion's Counter animation (id 370, `img/animations/counter.png`, `mirror` passed as `false`, every cell's `cell[5]` zero) rendered its lettering backwards. `installAnimationMirrorCompatibility` overrides the rule to return false, in Tier 2 so MZ-authored projects are untouched. Coverage in `mv-animation-mirror.test.cjs`, which pins the stock MZ rule as the baseline before asserting the override.
- Null message sub-windows on unassociated `Window_Message` instances: `installMessageSubWindowsCompatibility` guarded only the MV field names (`_choiceWindow`, `_numberWindow`, `_itemWindow`), but MZ's own message flow reads the MZ names directly — `startInput()` calls `this._choiceListWindow.start()` with no null check. Only the scene's message window receives these through `associateWindows`, while plugins construct extra instances for measurement and backlog and custom battle scenes never associate at all; those instances still run the normal update loop, so the first pending choice threw `Cannot read properties of null (reading 'start')` and ended the session. The MZ names now resolve through the same fallback chain as the MV aliases — own window, then the scene's window, then the shared inert stand-in — so the choice starts on a real window and stays answerable rather than merely not crashing. The stand-in gained `start`, which MZ calls on the choice, number, and item windows unguarded. Regression coverage in `mv-message-subwindows.test.cjs` drives the real installer against stub windows.
- Stale MV interpreter character mirror: `installInterpreterCompatibility` mirrors the wait target onto `this._character` for commands 205/212/213 and lets `updateWaitMode` prefer it, but nothing ever cleared it. The map interpreter is a single reused instance whose `setup()` calls `clear()`, and MZ's `clear()` resets `_characterId` while knowing nothing about the mirror, so the previous event's character answered the next event's wait check. Having long since stopped moving, it reported "not waiting" on the first tick and dissolved any wait armed through the MZ contract alone — the same dissolving-wait failure the mirror was added to prevent, arriving from the opposite direction. The mirror is now dropped both in `clear()` and as each wait completes, by assignment rather than redefinition so the property stays non-enumerable and interpreters remain serializable. Regression coverage runs the real installer against a stub interpreter in `mv-interpreter-compat.test.cjs`.
- Bundled Demo runtime drift: `template/Demo/js` had fallen behind `runtime/` — 115 lines in `reactor_core.js`, 96 in `reactor_mv_compat.js`, 46 in `reactor_objects.js`, 15 in `libs/pixi_compat.js`, plus `reactor_windows.js` and `reactor_main.js` — and lacked `reactor_picture_extensions.js` and `libs/lz-string.js` altogether. Because `validateProjectRuntime` throws on any missing required file, Deploy Game rejected the Demo outright; `installRuntime` is a manual menu action and project creation overlays `runtime/` only for newly created projects, so nothing refreshed the Demo opened in place. All three templates are resynced with their own `reactor_plugins.js` preserved, and two tests now assert the tracked Demo is byte-identical to `runtime/` and satisfies both the deployment required-file list and its own `reactor_main.js` loader manifest.

## [0.95.0] - 2026-07-20

Cycle overview: [RPG Reactor 0.95.0: A More Complete Editor](../docs/devlogs/2026-07-18-rpg-reactor-0.95.0.md).

0.94.9 was an internal development version and was never published; its changes ship in 0.95.0.

### Bundled content

- Psychronic and Looseleaf are bundled Character Generator styles, with Psychronic as the default. Project JavaScript and PNG styles remain supported and load automatically.
- Portal is available in the Animation Generator and as a baked Effekseer Energy recipe. The release registries contain exactly 76 Animation Generator recipes and 106 Effekseer recipes across nine categories, including 15 Energy recipes.

### Added

- Localization: `I18nDeepTranslations.js` supplies a generated 2,411-phrase catalog for every non-English locale and loads before `I18nManager`. Static source inventory detects `tText` calls plus consumed array/object schemas, while tests enforce identical locale key sets, placeholder preservation, translated Terms labels, and Arabic RTL direction. Broad editor routing now covers database and event-command editors, Forge tools, map/project controls, deployment UI, and the browser host while leaving project-authored data untouched.
- Control Variables Game Data: the operand row now opens a nested transactional selector modeled after RPG Maker. Item/Weapon/Armor/Actor choices use padded database IDs and names, Actor/Enemy/Character properties use stock parameter lists, Character choices include current-map events, and Party/Other/Last Action Data expose their canonical operands. The parent row displays a readable summary while preserving the exact stock `[type, param1, param2]` tuple. Last Action Data and Troop Lower/Upper Layer labels have explicit translations and no-English-fallback coverage in all 17 non-English locales.
- Release engineering: `release-editor.cjs` packages the tracked Reactor One Demo starter, rejects dirty publish sources, disables unverified bundled-runtime reuse, and validates pinned NW.js archives. Per-target manifests bind version, target, signing state, source commit, size, and SHA-256. GitHub publication accepts only a complete matching Linux/Windows/macOS/Web candidate set and uploads those exact bytes without rebuilding; Windows uses verified Authenticode metadata/signing, macOS uses hardened-runtime signing/notarization/stapling, and optional itch publication uses a pinned hash-verified Butler binary.
- Security and lifecycle: privileged HTML surfaces escape project-authored names, notes, messages, traits, JSON, and Forge values, and Character Generator folder opening uses argument-array execution. Project creation rejects traversal/reserved/non-empty destinations, same-project locks use exclusive no-follow token ownership, stale map loads cannot replace current state, and critical writes use exclusive random temporary files plus file/directory flushing.
- Database workspace: the modal now occupies nearly the full viewport with fixed shell chrome and independently scrolling navigation, list, and detail panes. Web layouts retain the same scroll ownership and reflow the specialized Types and Terms workspaces at narrow breakpoints.
- Cross-instance authored-data clipboard: `ReactorClipboard` carries versioned typed JSON over the NW.js/system clipboard with shared-file and localStorage fallbacks. Top-level database lists publish backward-compatible versioned `databaseEntry` payloads containing one or more ID-free records; Ctrl/Cmd- and Shift-selection, Ctrl/Cmd+A, batch Cut/Copy/Paste, and the context menu work across Actors, Classes, Skills, Items, Weapons, Armors, Enemies, Troops, States, Animations, Common Events, and Tilesets. Batch paste overwrites consecutive slots while retaining destination IDs, one Undo snapshot covers the batch, and lazy-list refresh restores selection, rendered depth, and scroll position. Whole maps retain their existing map/MapInfos/tileset transport. Individual trait/effect rows publish dedicated typed payloads; project-defined element, state, skill, Skill/Weapon/Armor/Equipment Type, and Common Event IDs resolve by a unique exact name in the target project, while missing or duplicate references reject the paste. Sentinel, enum, parameter, and flag IDs remain unchanged. Map, Common, and Troop event command lists support structural multi-selection and external-first typed payloads; block expansion protects branches/continuations, destination indentation is rebased, and clipboard publication completes before Cut deletes. Async operations abort if the project, category, focus, selection, destination data, row owner, event, page, troop, or route changes while clipboard access is pending. Incompatible categories/types are rejected; other referenced assets and numeric references are copied as authored.
- Types: five simultaneous dense panels preserve reserved ID 0 and every later numeric reference. Toggle/range selection, arrows, Enter/F2, Ctrl/Cmd+A/X/C/V, Delete/Backspace, toolbar actions, and a custom context menu support multi-row editing. Clipboard text preserves trailing blank rows, paste can safely grow the target array, async generation guards prevent stale clipboard completion, interior clear never renumbers IDs, and destructive maximum reduction requires confirmation.
- Terms: Basic Statuses and Parameters use paired compact grids, Commands uses a four-column grid, and all keyed Messages occupy a grouped Type/Text table with independent scrolling. Every text field has Cut/Copy/Paste/Select All context actions with delayed-clipboard guards.
- Conditional Branch: complete MZ code-111 support for condition types 0-13 and their subtypes, including variable RHS, Actor predicates, Enemy state/appeared checks, Character direction, equipment inclusion flags, Pressed/Triggered/Repeated buttons, Script, and all vehicles. No-change edits preserve original parameter arrays exactly; new configurations emit canonical arrays and command summaries cover every form.
- Advanced event commands: Control Variables adds a structured Advanced Expression operand covering arithmetic, power, min/max, `atan2`, random, bitwise operations, absolute value, square root, and degree-based sine/cosine. Loop uses direct stock Loop/Repeat Above insertion while preserving existing nested bodies; previously generated finite-loop structures remain byte-identical if encountered. Common Event keeps direct calls as stock code 117 and adds variable Common Event/current-map page calls. Conditional Branch adds extended keyboard pressed/triggered/repeated/released states, independent left/middle/right mouse states, wheel directions, and pointer X/Y comparisons. The same editors and indent-aware insertion behavior are available in Map, Common Event, and Troop command lists.
- Portable generated-command codec: `ReactorEventCommandCodec` emits strict canonical versioned tags only inside stock Script-bearing command layouts (111, 122, and 355). Generated JavaScript is syntax-checked before authoring, exact tag/body agreement is required when reopening, and malformed, edited, or unknown-version tags remain ordinary Script commands. Generated dynamic picture commands include direct stock MV/MZ fallback bodies rather than requiring Reactor to interpret custom parameter fields.
- Picture command extensions: Show/Move/Erase support variable picture IDs; Move supports variable duration; Erase supports One, Range, and All with direct or variable endpoints. Show and Move add initial/fixed/tweened angle, custom anchors, sine-wave offsets, and Overlay blend state. Negative scale uses `-2000..2000` and Quick Setting previews it directly instead of adding a separate flip control. The isolated `reactor_picture_extensions.js` module validates old-save defaults and runtime payloads; Overlay uses the advanced blend only when back-buffer support is active and otherwise degrades to Normal without warnings.
- Picture Quick Setting and movement preview: code 231's Quick Setting button now shares the Image row and opens a responsive surface using `System.advanced.screenWidth/screenHeight`, with MZ defaults for older projects. The screen grid occupies the center of a workspace extending one full screen beyond every edge; pointer capture continues updating X/Y beyond the visible workspace up to the command's ±9,999 range, so off-screen entrances are authorable instead of clamping at the screen edge. The fixed 1,000px dialog ceiling is gone: each layout pass measures wrapped controls and shell chrome, fits the exact project-aspect workspace against both available viewport dimensions, and recalculates on normal window and fullscreen resize. Origin, X/Y, independent width/height scale, and opacity are editable in the modal and remain transactional until Apply. Code 232 exposes the same controls plus live Duration frame entry and replayable easing animation: changing Duration affects the next Preview immediately, while Apply synchronizes it back to the main command. Event and common-event callers provide the current command list/index, the editor scans preceding Show/Move/Erase commands for that picture ID, preserves the latest image and endpoint, and animates from it using RPG Maker's four easing curves. No preceding visible Show produces an intentionally empty preview.
- Map-region sampling: `MapEditor` owns a dedicated six-plane stamp instead of forcing sampled map data through palette-layer/autotile placement rules. Right-drag captures a normalized, map-clipped rectangle from z0-z3, shadow z4, and region z5; the immutable stamp preserves exact autotile shapes and zero values. A side-effect-free canvas composite renders normal tiles, autotiles, higher tiles, shadows, and visible region tint into the hover preview, with an outlined footprint fallback for oversized selections. Pencil left-click/drag writes all planes directly, crops destination overflow, sends changed z0-z4 cells through one `updateTiles` batch, refreshes changed regions separately, and uses the existing begin/commit snapshot so a stroke creates one Undo entry while sampling creates none. Palette/region/tool/map/Event-mode changes clear the stamp. Event interaction teardown now unregisters only its named handlers instead of stripping map painting, sampling, and middle/Shift panning listeners from their shared PIXI container.
- Shift autotile placement: holding Shift while painting A1-A4 with Pencil, Rectangle, or Circle stores the selected shape directly without reconnecting painted tiles or reshaping their neighbors. Other layers and tools retain Shift-drag panning.
- A1 animation preference: `rr-settings.animateAutotiles` defaults on and is exposed as a checkbox in File → Options plus a compact synchronized `A1` checkbox beside the map zoom and coordinates. `TilemapManager.setA1AnimationEnabled` adds/removes the PIXI ticker without rebuilding the map; disabling resets both water sequences to frame zero and refreshes tracked A1 textures. The preference is global, survives sessions and project switches, and affects only the editor preview.
- Tests: added focused cross-instance clipboard, MV save, MV plugin visual, System 2 Magic Skills, event-branch structure, Event-mode double-click, database workspace, preview cleanup, localization source-audit, Unicode asset/index, project-open retry, package preflight, and PIXI pre-init game-loop coverage.
- Recursive assets: new shared `AssetFiles.js` performs deterministic recursive discovery without following symlinks, deduplicates extensionless names by declared format priority, normalizes stored separators to `/`, and resolves encoded desktop/Web preview URLs. It now backs BGM/BGS/ME/SE players and selectors, animation sheets/effects/timing SE, character/face/battler/tileset/battleback/parallax/title pickers, movement-route SE, and plugin `@type file` parameters. Core RPG Maker pickers expose only lowercase `.ogg`/`.png` files that the runtime can reconstruct; `$`/`!$` classification reads the final filename inside a nested path across every character preview surface.
- Picker navigation: new shared `PickerIndex.js` provides Audio-compatible Unicode grapheme sections, deterministic locale/numeric ordering, accent-insensitive Unicode case folding, and full-relative-path search. Its reusable browser adds a search field, clickable section rail, sticky headers, active-section tracking, and keyboard-selectable listbox options to `DatabaseEditorUI.showImagePicker`, `CharacterGraphicPicker`, and the Show Text face picker. Reopening an event character restores its exact file/index/pattern/direction, while a generation guard prevents slow stale image loads from replacing a newer selection.
- Picker search styling: search fields use a high-contrast theme-aware deep fill, accent border, and visible focus ring. A Reactor-styled accent clear button replaces Chromium's native blue search-cancel icon.
- Picker section navigation: clicking an earlier Unicode section after jumping downward now scrolls the picker list back up correctly. Navigation calculates the header's normal-flow position from preceding row heights and assigns the list's `scrollTop` directly, avoiding both `scrollIntoView` and Chromium's sticky-header offset reporting.
- System 1 start locations: Player, Boat, Ship, and Airship rows retain manual Map/X/Y inputs and add a visual Browse action backed by the Transfer Player map tree/canvas. Selection state is local until OK, each triplet commits together, nested expansion state survives tree rebuilds, search reaches nested maps, overlapping async renders cannot replace the latest preview, and the live `TilemapManager` is supplied through the database project proxy. The Title Screen picker now uses `RRPickerIndex.createBrowser` for searchable Unicode sections beside its image preview.
- Plugin search and actions: the load-order pane filters loaded plugins live by name, description, or author without losing their source indexes, while Add Plugin filters unused plugin filenames. Remove Plugin and Save Changes occupy a persistent shell-level footer in that order, so Save remains available with empty, single, or multiple selection. Source-derived `@help` documentation has separate literal case-insensitive highlighting, active/total match status, clear and wraparound previous/next controls, Enter/Shift+Enter and F3/Shift+F3 navigation, and a Plugin Manager-scoped Ctrl/Cmd+F shortcut. Highlight rendering uses text nodes rather than HTML, reloads clear stale help state, nested parameter/add dialogs suspend manager shortcuts, and controls expose accessible labels/live status without changing plugin manifests.
- Complex Plugin Parameters: struct parsing now terminates at each actual `*/`, so later schemas such as `YEP_OptionsCore`'s nested `Options` definition are available instead of producing “Could not find struct definition.” Schema-guided one-layer decoding preserves JSON-looking note/string leaves and simple-array strings; nested editors receive the source plugin's complete definition set, clone drafts for Cancel, support struct and scalar arrays, and synchronize Text edits before returning to Structure. Named rows replace JSON spew and add Edit/Delete/up/down controls, double-click editing, and full-row drag-and-drop ordering with theme-accent insertion feedback. Alternating list surfaces, stronger table framing, and neutral group headers derive entirely from active theme tokens; element editors use OK rather than an ambiguous Close action. New entries use schema defaults, notes use multiline editors, and stable grids align labels and fields. Saving recursively restores RPG Maker's array-of-JSON-strings format. A real nested `YEP_OptionsCore` fixture decodes and re-encodes byte-for-byte.

### Changed

- Bumped current development version to RPG Reactor 0.95.0.
- System 2 `magicSkills` now renders as ordered Skill Type dropdown rows with a trailing fill row. Choosing None removes a row, duplicates are removed, missing IDs remain visible until edited, values stay numeric, and simply opening System 2 does not initialize a missing field.
- Troops and Animations now use dense, viewport-bounded database layouts. Troops uses a two-column upper workspace: the Battle Preview fills the left side while Battle Test, Members, Battleback, and Note stack in a 240-300px right sidebar; narrow web layouts collapse to one column, and Battle Events stays full-width below. Its preview reproduces runtime box centering, MZ/MV vertical offsets, bottom-anchored enemy sprites, side/front battler priority, and battleback sizing from Screen/UI Area dimensions; drag coordinates invert that transform before saving. Battle Test equipment filters by actor-plus-class weapon/armor permissions and sealed slots, completely omits slots with no valid/current item, retains actionable stale items visibly without counting them in stats, and leaves room between dropdowns and the scrollbar. Battle Test Configuration and Conditions use distinct theme-aware headers, scrollable bodies, and separated footers; Conditions also uses a stable label/control grid with alternating rows. Animations removes the fixed 470px/600px preview requirements, places its two sprite properties side by side, shortens the sheet strip, narrows frame/effect controls, and keeps long effect/preview controls locally scrollable. Animation canvas backing sizes stay unchanged, and CSS-to-backing coordinate conversion keeps sprite drops, hit tests, and dragging accurate at every visual scale.
- Troop Members replaces its add dropdown and one-off replacement browser with one `RRPickerIndex` modal. Enemy names lead the Unicode section/search labels, database IDs remain visible/searchable, and the right pane previews the resolved front/side-view battler with stats. Add opens the picker directly; a selected row replaces through double-click, Enter, the swap button, or its context menu. The focusable list and battle canvas share local Ctrl/Cmd+C/X/V and Delete handling, paste inserts an offset deep clone, and the context menu mirrors Add/Replace/Cut/Copy/Paste/Delete without leaking Delete to the database sidebar.

### Fixed

- Interactive editor deployment now uses developer-build runtime policy instead of immutable release policy. Latest Stable and user-selected NW.js versions may download with a clear unauthenticated-development warning, while release CLI builds still require pinned hashes. Cross-platform Windows packages skip ESM-only `resedit` metadata editing inside NW.js workers, fixing Linux-hosted Windows SDK builds; release candidates still apply and verify app-owned metadata under standalone Node before signing.
- Options localization: seven palette names and descriptions no longer inherit English in Traditional/Simplified Chinese, Russian, Portuguese, German, French, and Greek. All new A1 preference labels and toolbar help are translated in every supported locale, and high-visibility localization tests now reject English palette-description fallback across all 17 non-English languages.
- Troop preview Delete routing: clicking the battle canvas now gives it keyboard focus. Delete removes the selected troop member and stops propagation before the database list handler can clear the entire Troop record; with no member selected, preview-scoped Delete safely does nothing.
- Plugin detail scrolling: the right detail pane now owns vertical scrolling across metadata, expanded help, and Parameters. Help retains a nested scroller with a Notes-style lower-right vertical resize grip, Parameters keeps its natural height instead of shrinking into a near-zero flex child, and help-search navigation scrolls only the documentation viewport rather than every ancestor. Previous/Next/Clear controls use distinct theme button surfaces and visible disabled states; the outer search card uses a neutral border, reserving accent border/focus-ring feedback for the active text field.
- Actor equipment compatibility: shared `EquipSlots.js` treats an explicit class `<Equip Slot: ...>` list as authoritative for both `DatabaseActorEditor` and `BattleTestConfigModal`, without inspecting or special-casing plugins; classes without one use standard engine slots and actor/class item permissions. Slots with neither a compatible actor/class-permitted item nor a relevant stale value are omitted, while actionable stale values remain visible. `DatabaseTraitEditor` also preserves original IDs across blank entries in Weapon Types, Armor Types, and Equipment Types instead of assigning post-filter positions to equip/lock/seal traits.
- MV/YEP save compatibility: `runtime/libs/lz-string.js` implements stock MV's exposed API and byte-compatible Base64/UTF16 writers, loads before `reactor_mv_compat.js`, and is required by project/runtime packaging validation. Synchronous `StorageManager.load/save` and async `zipToJson` fallbacks decode legacy saves; local mode supports `localFileDirectoryPath`/`localFilePath`, browser mode supports `webStorageKey`, and custom save directories are created from the resolved path. A real large MV save fixture decompresses to valid JSON before YEP Save Core calls `JsonEx.parse`.
- Actor damage-popup compatibility: MOG BattleHud's actor-only `setupDamagePopup` captured Reactor's basic implementation before VE Damage Popup replaced the battler method, so enemies used Victor's complete HP/MP/TP/state/custom path while actors retained the stale limited path. Final plugin reconciliation now dynamically delegates side-view actors to the current `Sprite_Battler` implementation and keeps MOG's front-view face popup branch intact. The compatibility path is covered by `mv-plugin-visual-compat.test.cjs`.
- MV plugin visuals: `Window_DragonTreasure` moves its full-size contents sprite out of MZ's 168x8 client clip, restoring MOG Treasure Popup's 192x32 label without changing normal window clipping. PIXI 8 `TilingSprite.allowChildren` now supports legacy video-parallax parenting without warning state, and the VideoSource error bridge ignores only teardown events with no configured source while continuing to report actionable media errors.
- Database maximum safety without reduced capacity: `DataLimits.js` centralizes workload-based editor limits. Major databases and Common Events retain 9,999 slots; Animations and Tilesets retain 1,000. Elements rises to 512, Skill Types to 128, Weapon/Armor Types to 256, and Equipment Types to 128. The shared dialog displays each ceiling, while `DatabaseManager.changeMaximum` and `addEntry` enforce it defensively and permit existing oversized imports to be opened and reduced. Generic and Tileset lists remain continuous: the first 250 rows render immediately, and scrolling near the bottom appends the next batch in place. Growth serializes its template once, and 99,999 is rejected without allocation.
- Large-data overhead: the Database Cancel/Apply baseline is now retained as compact JSON rather than a second live clone of every database object. This particularly reduces the cost of Tilesets, whose shared `Tilesets.json` records each carry 8,192 flags even before a detail row is selected. Common Event trigger subsets are cached at map setup instead of filtering up to 9,999 records every idle frame.
- Extended levels and bounded iteration: Actor Initial/Max Level supports 1-999 and keeps Initial at or below Max. Standard level-99 Class parameter curves are linearly extrapolated through level 999 in both runtime and Battle Test without expanding every Class record; blank Class slots keep compact valid level-1 curves until authored, curve readouts use the correct indexes, the EXP preview uses the runtime formula through level 999, and three-digit levels fit the status window. Skill/Item Repeats and Attack Times+ support up to 100, with `Game_Action.numRepeats` applying the final runtime cap even to imported/plugin-modified data.
- Freedom for constant-time values: price, MP/TP cost, TP gain, action speed, success rate, and damage variance no longer inherit MZ-only editor clamps because those values do not multiply work. Effekseer scale/speed/rotation and sprite-cell pattern/scale/rotation/opacity remain bounded before live preview APIs receive them because those values reach rendering systems directly.
- Map scale and paste placement: new and pasted maps share a hole-reusing allocator, support 2,000 live maps with IDs through 9,999, and correctly produce `Map1000.json` and later variable-width filenames. The Transfer Player hierarchy now indexes children once instead of rescanning the entire map list for every tree node. Paste captures the currently selected/loaded map before asynchronous clipboard work, inserts the copy directly after it as a same-parent sibling, then normalizes and saves that sibling group; without a valid selection it falls back to the end of the root list.
- Database snapshots: `takeDatabaseSnapshot(true)` refreshes the active viewer's Cancel baseline after Save All, matching Apply and preventing a post-save Cancel from restoring older in-memory data.
- Tileset asset loading: the compact editor no longer depends on a non-null map renderer to retain the project path, and reused project context stays dynamic. All A-E thumbnails, tab previews, canvases, and full previews use the browser asset bridge or `pathToFileURL`, so Unicode, spaces, `#`, and other URL-significant path characters load correctly.
- Audio filename index: section keys now use normalized Unicode graphemes and locale-aware sorting instead of ASCII-only `A-Z`. Initials such as `É`, `Ü`, `Ω`, `Ж`, `不`, `あ`, and `한` appear directly in the left rail; numbers and punctuation continue to use `#`.
- Project opening: a live `.rpgreactor.lock` rejection no longer falls through to the generic invalid-project alert. Metadata and database readers retry short-lived partial/locked reads, strip UTF-8 BOMs, retain the failing filename/reason for diagnostics, and avoid a second independent `MapInfos.json` parse during the same open.
- Event workflow: Event Editor sessions work on isolated drafts. In Event mode, double-clicking an empty in-bounds tile opens a detached default event; Apply or OK inserts it and records one Undo snapshot, while Cancel, X, or backdrop dismissal leaves the map untouched. Existing-event Apply commits once and refreshes the Cancel baseline, OK commits once and closes, and no-op Apply creates no Undo entry. Occupied tiles retain double-click editing, while bounds/occupancy checks and real-drag timing prevent accidental creation.
- Event sidebar parity: `renderEvents` already rendered every truthy map-event entry, but `updateEventsList` skipped array index 0 and declared every one-element array empty. Imported, hand-edited, or third-party-compacted maps could therefore show event sprites while the left sidebar said “No events on this map.” Both surfaces now consume the same truthy entries, including sparse event ID 5000; the list builds before sprite construction and one malformed graphic is logged without suppressing other rows/sprites. `getNextEventId` requires both the numeric ID and destination slot to be free, while deletion resolves the event's actual storage slot, preventing compact arrays from being overwritten or expanded to a mismatched database ID.
- MV playtest package repair: before runtime conversion or desktop playtest, Reactor validates the root `package.json`, repairs missing/non-string/blank `name` and `main` values to a project slug and `index.html`, and preserves unrelated MV/NW.js fields. Malformed JSON or non-object roots produce an actionable package path/error before old runtime files are archived or NW.js is spawned.

- Preview resource leaks closed across editor surfaces (Chromium caps live WebGL contexts at ~16 and AudioContexts at ~6 per page; leaked document/window listeners retain their closed-over canvases and data forever): `AnimationPickerModal` releases its Effekseer context (`effekseer.releaseContext`) and force-loses its WebGL context (`WEBGL_lose_context`) on close; the System 1 audio picker closes its `AudioContext` on every close path; `DatabaseAnimationEditor` gained a per-detail-view cleanup registry (`_registerDetailCleanup`/`_runDetailCleanups`, run on every `showAnimationDetail`) that unhooks its document-level keydown and sheet-drag mouseup/mousemove handlers — the leaked keydown handlers actively applied Ctrl+V/Delete/undo shortcuts to previously viewed animations — and releases the Effekseer preview context pair; its effect-file picker releases its preview context on close; the event editor's `AnimationPicker` force-loses its GL context in `_cleanup`; the actor-detail and event-page character walk previews' 8fps `setInterval`s self-stop when their canvas leaves the DOM (each leaked timer pinned the cached frame canvases and sheet image); and the Effekseer Forge removes its window-level orbit-drag handlers on teardown and force-loses the discarded preview GL context (each remount retained the previous 720×720 GL canvas). Wiring locked down by `tests/preview-resource-cleanup.test.cjs`.
- Audit backlog cleared (all remaining Medium/Low findings from the 2026-07-13 deep audit):
  - `AudioCommandEditor` clones the command on open and hands the clone back on OK — its sliders/track list mutated the live command, so Cancel silently committed. `ShowPictureEditor` uses `!== undefined` guards so opacity-0/scale-0 round-trip (matching `MovePictureEditor`); `MessageCommandEditor.buildCommands` emits through the last non-empty line so interior blank 401s survive; `ShowChoicesCommandEditor` builds an index map when filtering blank choices and remaps `cancelType`/`defaultType` through it (dangling refs → Disallow / Choice 1).
  - `BattleTestConfigModal.launch` writes the picked battlebacks into a Test_System.json clone only; the real System.json keeps its battlebacks (testBattlers/testTroopId still persist). The Types editor blanks interior entries instead of splicing (system types are referenced by index; the trailing entry still splices). `updateActorField` falls back to defaults instead of writing `parseInt('') = NaN → null`. The animation cell-properties modal uses `Number.isFinite` guards. System 2 Advanced creates `system.advanced` on first edit. `DatabaseTilesetEditor.js` exports the right class name.
  - New shared `rrEscapeHtml` (src/utils/HtmlEscape.js) applied to every string-field `value="…"`/`<textarea>` interpolation across the database editors (~35 sites in 17 files); the four div-based `escapeHTML` helpers now delegate to it (`div.textContent → innerHTML` never escaped quotes, so attributes still broke).
  - `TilesetPaletteViewer.isSelectionTransparent` samples the SOURCE sheet through a scratch canvas (the palette canvas composites the checkerboard, so alpha read 255 everywhere and auto-erase never armed); restricted to B–E/A5 whose grids map 1:1 to the sheet. The checkerboard resolves its colors via ThemeColors (Canvas 2D can't parse var()). Merged-A palette drags clamp to the starting sub-layer. `renderAutotilePreviewToContainer` drops the wrong hard-coded A1 kind-4..7 positions so the shared block formula (with waterfall table) applies. `EventManager.createTileSprite` uses the v8 Texture constructor (was v5 `new PIXI.Texture(baseTexture, rect)` — blank thumbnails) and decodes from the loaded image element instead of re-fetching by URL.
  - PluginManager: opening the manager on a manifest-less project no longer writes a file + alert (the manifest is created on first save); plugin metadata (`@plugindesc/@help/@author/@url`) is cached by mtime in `getPluginMetadata`; `serializeReactorPlugins` writes only name/status/description/parameters into `reactor_plugins.js` (full @help text was round-tripped into a manifest parsed by every game boot).
  - `ProjectController.deleteMap` saves MapInfos.json (restoring in-memory state on failure) BEFORE unlinking map files; `renderMapsList` builds a parentId→children index once per render (was a spread-copy + filter of the whole maps array per tree node); `editMap` guards its JSON.parse; the playtest path wraps `repairInvalidSystemMapReferences` in try/catch; `DatabaseManager.saveAllData` passes `skipVersionBump` so System.json is written once per batch (was rewritten with a fresh versionId after every other file); AudioPlayer removes its previous track-list scroll handler before adding a new one.
  - Event editor performance: selection clicks call the new `updateSelectionStyles` (in-place restyle; hover handlers read live selection state) instead of rebuilding the list; the 125-entry command-name table is memoized on the class; face thumbnails share one decoded Image per face sheet; event drag moves the dragged sprite directly per tile step (full `renderEvents()` only on drop), `renderEvents` destroys removed children so PIXI.Text label textures release, and `updateEventsList` preserves the list's scroll position.
  - Forge: Outfit/Hair Forge desktop saves write to `<project>/forge/character_generator/styles/…` with an overwrite confirm (were writing into `process.cwd()/src/...`); EFK desktop export confirms before overwriting `effects/<name>.efkefc`; AG's PNG/GIF save pickers handle the `cancel` event (button un-sticks, hidden inputs removed); CG sheet save builds its output path only when a project exists (was crashing before the web download fallback); `_drawProceduralPreview` caches the composed 12-cell sheet per spec (walk ticks blit from cache); `_forgePartPreviewDescriptor` memoizes by layer+spec (bounded at 200 entries).
  - Runtime: the MV-compat `Game_Battler` animation queue is non-enumerable (excluded from saves — it grew unboundedly since nothing drains it in a pure-MZ battle flow) and capped at 20; Tier-1 `Window_Command.refresh` recreates contents only when `contentsWidth/Height` changed; `DataManager.loadDataFile` carries a per-name generation (superseded XHR arrivals/errors are dropped) and `onprogress` pushes the stall-watchdog deadline; pixi_compat's UM7 renderLayer uses hoisted scratch uniform buffers, and the `Texture.baseTexture` shim is WeakMap-memoized per TextureSource with a live `resource.source` getter. Verified against generated MZ and large MV compatibility fixtures with clean consoles.
- Event editor: editing Show Choices (102) and Conditional Branch (111) rebuilds through an indent-aware structure parser (`EventCommandList.collectBranchStructure`) instead of flat marker scans that stopped at the first 404/412 regardless of depth — editing a branch containing nested Ifs or choices removed the wrong range and orphaned or duplicated markers. Empty branches are preserved as placeholders so branch N's body can never shift into branch N-1, and bodies re-attach keyed by marker kind (choice order, 403 cancel, then, 411 else) instead of by sequence. Editor-returned blocks are rebased to the edited header's indent (they were spliced in at indent 0, corrupting any edit inside a branch body). A truncated structure with a missing end marker is repaired in place without consuming commands past it. The common-events editor shares the same helpers instead of its own copy of the flat scans, and `expandSelection`'s 102/402/403/404 cases are indent-aware so copy/cut/delete of nested choice structures grabs the right range. `ConditionalBranchEditor` gains a "Create Else Branch" checkbox that mirrors the edited If, so saving no longer force-adds an Else to an If authored without one. Covered by round-trip tests over nested fixtures (`tests/event-branch-editing.test.cjs`).

- Editor: whole-map paint bucket fills apply in a fraction of a second instead of 30-40 seconds. `updateTiles` with a 131k-position batch (a full 256×256 fill) spent 39.6s: each position ran a maintenance `filter()` over the whole A1 water-animation tracking list (131k × 2,093 entries ≈ 275M iterations) plus per-cell sprite churn. Batches over 3,000 positions now route through the streaming full re-render — instant viewport, background fill, caches and region overlay handled — and the A1 tracking maintenance is batched into a single filter pass. A large-map fixture dropped from 39.8s to 0.27s of blocking time. `renderMap` gained a `preserveScroll` option so the fallback (and undo/redo, which previously snapped the view back to the map origin) keeps the current scroll position.
- Editor: `renderMap` drops layer texture caches before rebuilding children, so full re-renders don't re-render map-sized cache textures for the rebuild frames. (Investigated with this change: the "paint bucket doesn't undo" reports reproduce on 0.94.5–0.94.7 as undo-during-streaming-fill — stale lazy-fill batches repainted pre-undo tiles over the restored map; the 0.94.8 generation guard already fixed it, and undo bookkeeping itself was always correct.)

### Fixed — deep audit (editor)

- All critical project writes (`project.rpgreactor`, `MapInfos.json`, `Map###.json`, database JSON, `System.json`, plugin manifests) use randomly named, exclusively created no-follow temporary files, preserve destination permissions, flush file contents before rename, flush the parent directory where supported, and clean failed temporary files. A crash, kill, ENOSPC, stale temp name, or symlink collision can no longer truncate or replace the previous good file.
- `BuildManager.startBuild` awaits `projectController.saveAll()` before launching the build worker (deploys read from disk and silently shipped stale data); the editor-dist worker also deletes a pre-existing archive before zipping (zip UPDATES in place, so rebuilds into a reused output dir kept deleted files and the checksum step blessed them).
- Tileset editor: A1–A4 flag edits indexed the palette cell offset directly into `flags[]`, landing on shape slots of kind 0 — passability/ladder/counter/damage/terrain edits on autotiles never took effect (and read back through the same wrong slot, hiding it). Now indexes `base + kind*48` and mirrors the flag across all 48 shape slots like MZ.
- Class editor: parameter-curve generation treated `params` as 0-based; MZ indexes by LEVEL (index 0 is an unread placeholder) — generated curves read one level low and the modal seeded from the wrong slots. Enemy editor: HP/MP condition params (fractional rates) were `parseInt`ed to 0 on every OK. Trait editor: Attack Element options used post-filter positions, storing every element off by one.
- `DatabaseEditorUI` snapshots the database once per viewer session instead of on every nav-category click — the re-baseline made Cancel keep (and the next save persist) edits from before the last category switch; it also deep-copied the entire database per click.
- Event editor: Show Choices/Conditional Branch emitted their 402/403/411 markers at indent 1 (MZ puts them at the header's indent; the interpreter keys branch state by indent — choice bodies never ran and Else ran alongside Then); all 97 command-insertion sites now rebase inserted/pasted blocks to the insertion point's indent (a command inserted inside a branch at indent 0 terminated skipBranch early); deleting an If/Loop/branch-results Battle header expands to its whole block (an orphaned 413 "Repeat Above" is an unconditional backwards jump); Change Gold wrote `[op, operand, operandType]` where the runtime reads `[op, operandType, operand]`; editing a variable-designated Transfer Player preserved designation and variable ids instead of rewriting them as literal coordinates; clicking an event no longer wipes the redo stack (undo state is captured on first actual movement).
- Map editor: `setupMapInteraction`'s blanket `container.off('pointerdown')` stripped TilemapManager's pan handlers (own handlers are now tracked and removed individually), and panning itself was a no-op (`scrollLeft` on an overflow:hidden container) — it now moves the PIXI container like the scrollbars do. Wheel-zoom/resize/scrollbar-drag listeners are deduped per element (they stacked per map load; five loads made one wheel notch zoom ~1.6× and resize the renderer five times). The region overlay recreates itself after map switches (the tilemap container destroy killed it while `enabled` stayed true). The eraser on the Regions tab erases regions (pencil/rect/circle) instead of the tiles hidden under the overlay. `moveMapBefore` never assigned the dragged map's order (drop-above silently didn't reorder).
- Map editor perf: undo snapshots use `data.slice()` + element compare instead of three whole-map JSON serializations per stroke; the flood fill no longer computes a throwaway autotile shape per cell mid-flood (the post-fill reshape pass recomputes everything anyway); the shape-tool preview early-outs when the drag is still over the same tile pair; the checkerboard pattern's RenderTexture is created once (it leaked one GPU framebuffer per full re-render).
- Animation Generator: `_loadConfig` dropped `keyframes`/`activeKeyframe` when reading v2 configs — every tool mount reset all layers to defaults and the debounced autosave made it permanent; Reset Layer wrote a fresh params object, severing the params↔keyframe alias (preview kept the old look and subsequent edits went to an orphan); `detach()` clears the pending debounced save so a project switch can't write the old project's layers into the new project's config.

### Fixed — deep audit (runtime)

- `reactor_mv_compat`: the unconditional (Tier 1) MV battle-field-order shim created `_battleField` before the battlebacks, so battlebacks rendered ON TOP of every battler in MZ-model battles; `createLowerLayer` now restores MZ's sibling order after construction (verified by boot-probe: field index 1 → 4, above both battlebacks, enemies visible). The MV `_character` wait mirror is defined non-enumerable so JsonEx no longer deep-clones a live Game_Event into MZ saves (the loaded clone froze route/animation/balloon waits forever and bloated saves). `Decrypter.hasEncryptedImages/Audio` are live getters (they captured `false` before `Scene_Boot` set the encryption info). MV event-collision (`length > 0`) moved to Tier 2.
- `reactor_sprites`: a second `Spriteset_Map.destroy` definition (cull-holder fold-in) silently replaced the first (balloon cleanup) — `removeAllBalloons()` never ran on teardown, leaving `_balloonPlaying` set and the interpreter's balloon wait soft-locked after a mid-balloon scene change. Merged into one destroy.
- `reactor_objects`: `Game_Event.isCollidedWithEvents` used the MV body (any event blocks); restored MZ's `some(isNormalPriority)` — NPCs can path over below-priority events again in MZ games.
- `reactor_core`: two unbounded v8 texture leaks — `Sprite._refresh`/`TilingSprite` replacement textures and the tilemap's per-layer `_v8TexCache`/sprite pool register on session-lived TextureSources' resize listener lists and were never destroyed (walk cycles and pause-sign blinks leaked continuously; every map transfer stranded a full tile-texture batch). Replaced textures flagged `__rrSpriteOwned` are destroyed on swap; `Tilemap.Layer.destroy`/`setBitmaps` destroy the cache and pool. `WebAudio.destroy` removes the buffer from the load watchdog (destroyed buffers were re-downloaded and re-decoded ~10s after every mid-load stop); both load watchdog sweeps are throttled to 250ms (each `isReady()` poll swept the whole watch list — O(N²) during load gates); `Sprite._refresh` no longer allocates a Rectangle on the unchanged path (it runs for every sprite every frame via `setFrame`).
- `pixi_compat`: the `Geometry.addAttribute` positional→options rewrite is gated to v8 (`PIXI.TextureSource`) — on v5/v6/v7 it fed an options object to an API expecting a Buffer, corrupting vertex data for UltraMode7 and the tilemap.

The cleared audit findings and dispositions are preserved in [../docs/AUDIT-BACKLOG-2026-07-13.md](../docs/AUDIT-BACKLOG-2026-07-13.md).

Current 0.95.0 validation: **350 tests passed, 0 failed**. Coverage includes cross-instance clipboard transport, database batch/reference safety, event-command serialization, Control Variables Game Data, dynamic pictures, database limits, recursive/Unicode assets, plugin parameters, six-plane map sampling, exact Shift autotile placement, transactional event editing, project lifecycle and atomic-write safety, MV/runtime compatibility, localization no-fallback checks, deployment, and release policy/signing gates. The standalone HTML harness remains at `tests/regression-harness.html`.

## [0.94.8] - 2026-07-13

Release overview: [RPG Reactor 0.94.8: Big Maps Without the Wait](../docs/devlogs/2026-07-13-rpg-reactor-0.94.8.md).

### Changed

- Bumped current development version to RPG Reactor 0.94.8.

### Fixed

- Editor: large maps load ~4× faster and stay responsive afterwards. Two compounding problems on a 256×256 map (~131k populated cells, ~500k tile sprites): (1) the lazy off-viewport fill streamed sprites directly into the live stage, so every editor frame re-rendered the ever-growing uncached sprite tree — over 1s per frame near the end — starving the idle-callback batches and stretching a ~1s fill to ~10s; off-viewport tiles now stream into detached per-layer holder containers that attach (and cache) one per frame when their stream completes, keeping frames viewport-sized throughout. (2) A1 water/waterfall tiles animate by in-place texture swaps, which blocked the ground layer from ever being cached as a texture — a water map's ground container (261k sprites on the measured fixture) re-rendered live every frame forever; A1 tiles now render into small dedicated live overlay containers (one per z-slot, drawn directly above their slot, so stacking and shadow order are unchanged) and every static layer caches unconditionally. Measured through the real TilemapManager: full load 9.4s → 2.6s, steady frame 24ms → 13ms, worst load-time hitch 817ms → 452ms, identical 131,580 rendered cells and all 2,093 water tiles still animating. A generation counter also cancels an in-flight fill when another map loads (previously stale batches kept rendering the old map's tiles into the new map's containers), and paints that land while the fill streams are no longer overwritten by stale captured tile values.
- Editor: repainting a shadow stacked a duplicate shadow container on top of the old one (each at 48% alpha, so quadrants darkened a little more with every paint) — shadow containers are now tracked like tile sprites and replaced on update.
- Editor: MV sprite-sheet animation previews play smoothly in all three surfaces (Animations database page player, the Skills/Items/Weapons animation picker modal, and the event editor's Show Animation picker). Playback was driven by `setInterval(1000/15)`, which drifts and fires late whenever the main thread is busy (e.g. the map rendering behind the database), reading as juddery, time-stretched playback. All three now step on a requestAnimationFrame accumulator locked to the MV 15fps cadence: a hiccup skips ahead instead of slowing the animation, and the database player fires the skipped frames' SE cues so audio stays in sync.
- Editor: region overlay rendering is no longer quadratic with painted cells. Every region paint (including each pencil-drag step) rebuilt the entire overlay, creating a fresh `PIXI.Graphics` AND a `PIXI.Text` per painted cell — PIXI.Text rasterizes its own glyph texture, so bucket-filling a region across a 256×256 map froze the editor for ~5 seconds and re-froze on every subsequent paint. Region cells now share one pre-rendered texture per region ID (identical fill/border/number appearance) and paint operations update only the touched cells through the new `RegionManager.updateRegionCells`; full rebuilds happen only on map load / overlay toggle.
- Editor: dragging the rectangle tool on the Regions tab shows the region color over the selection (it only showed a white outline), and the circle tool shows region-colored cells with outlines (it showed nothing at all — both area tools previously fell through to the tile-preview path, which has nothing to draw for regions).

## [0.94.7] - 2026-07-13

Release overview: [RPG Reactor 0.94.7: Map Editing You Can Trust](../docs/devlogs/2026-07-13-rpg-reactor-0.94.7.md). (0.94.6 was an internal development version and was never published; its changes ship here.)

### Changed

- Bumped current development version to RPG Reactor 0.94.7.

### Fixed

- Editor: the rectangle, circle, and paint bucket tools respect the Regions tab. `getLayerIndex('R')` fell through to layer 0, and the area tools' tile-pattern path read the stale tile selection from the previously active palette tab — so area-painting or bucket-filling with regions selected painted tiles onto the ground layer. The area tools now write the selected region ID to the region layer (z5) over their rectangle/circle footprint, and the bucket flood-fills connected cells matching the clicked cell's region value, refreshing the region overlay once per operation.

- Runtime: game boot no longer races the PIXI v8 renderer initialization. `Graphics.initialize` is async under v8, and plugins that alias `SceneManager.run`/`initialize` with non-async wrappers (VisuMZ Core Engine and others) drop that promise, so `SceneManager.run` reached `Graphics.startGameLoop` mid-init — crashing with `this._app.start is not a function` when the pre-init `PIXI.Application` was visible (v8 installs `start`/`stop`/`ticker` on the instance only during `init()`), or sitting on a black screen forever when `Graphics._app` was still null and the start was silently skipped. The app is now published only after `init()` completes, and `startGameLoop`/`stopGameLoop` record the requested loop state so `_createPixiApp` starts the ticker as soon as the renderer is ready.
- Runtime: MV-era plugins that subclass filters ES5-style — `PIXI.Filter.call(this, vertex, fragment, uniforms)` — work under PIXI v8. The compat `PIXI.Filter` wrapper was an ES6 class, which throws when invoked without `new`; it is now a function-based constructor that supports `.call()`/`.apply()` construction, direct `new`, and `class extends` subclassing alike.
- Web editor: database entry lists show their mini preview icons in the browser edition. `applyListIcon` bailed when `nw` was absent, and painted via CSS `background-image: url("file://...")` — the browser host's file:// bridge rewrites `src`/fetch/XHR but not CSS backgrounds, so even without the gate the images could not load. Icon URLs now resolve through `RPGReactorAssetUrl` on the web host (and unchanged `file://` on desktop), and the enemy battler folder probe uses the host's virtual `fs`.
- Web editor: the character sprite, face, SV battler, and icon picker dialogs open in the browser edition — their guards required NW.js even though the browser host shims `fs.readdirSync` over the virtual project and the pickers' previews load through the bridged `img.src` path.
- Runtime: sprites using `multiply` or `screen` blending render correctly under PIXI v8 instead of drawing as opaque quads over the scene — reported as the whole screen going dark when toggling Sang Hendrix's Realtime Parallax Map Builder collision overlay, with a per-frame flood of "Blend filter requires backBuffer" warnings. PIXI v8 handles both modes natively through GL blend functions, but `pixi_compat.js` also registered them as `ExtensionType.BlendMode` extensions, and registration *overrides* the native path: `BlendModePipe` routes any registered mode through `BlendModeFilter`, which needs `useBackBuffer: true` — deliberately off in Reactor because the back-buffer copy discards Effekseer's post-render GL draws — so PIXI skipped the filter each frame and fell back to normal blending. The compat layer now registers only `overlay` (which has no native GL equivalent) and leaves multiply/screen native; MZ's numeric blend modes 0–3 (normal/add/multiply/screen) therefore all render natively. Verified by WebGL pixel readback: red backdrop × green multiply sprite → black, screen → yellow, with zero backBuffer warnings.
- Editor: the paint bucket fills an entire connected autotile region instead of a same-shape subset. The flood fill matched tiles by exact ID, but autotiles store their shape variant in the ID (base + 0–47), so interiors, edges, and corners of one terrain never matched each other — refilling an existing terrain left rings of old tiles (fills over empty ground worked, which made the bug look intermittent). Matching now collapses autotile IDs to their kind; non-autotiles still match exactly. The fill also recomputes autotile shapes for the filled cells and their border afterwards — during the flood pass each cell's shape was computed while unfilled neighbors still held the old terrain, so even correctly-covered fills came out with isolated/edge shapes inside the region. The eraser's fill mode uses the same kind-based matching.
- Editor: manual layer selection (L1–L4) confines all painting strictly to the chosen z-slot. Ground autotiles ignored the layer picker entirely: they always placed via the auto-stacking rules and unconditionally cleared layers z1–z3 at the painted cell, so painting terrain with any manual layer selected silently erased B–E decorations (and A5 single tiles wiped upper layers the same way). In manual mode the pencil, rectangle, circle, and fill tools now write only to the selected layer and never clear the others; `getAutotilePlacementLayer` honors the manual layer so the placement preview matches what painting produces; and fill matches the region on the selected layer rather than the palette-derived one. Auto mode is unchanged and keeps the MZ-style stacking/clearing rules.
- Editor: the playtest and battle test flow runs against current data — the playtest button saves the project (current map, database, MapInfos) before spawning the game process, cancelling the launch with a status-bar message if the save fails. Previously it launched straight from disk, so unsaved map edits were missing from the playtest.
- Runtime: the DevTools Issues tab no longer reports deprecations from the engine — `SceneManager.setupEventHandlers` listens for `pagehide` instead of the deprecated `unload` (same teardown coverage, including window close), and the MZGlobalUpgrade global scan skips `window.sharedStorage`, whose mere property read tripped Chromium's "Shared Storage API is deprecated" issue.

## [0.94.5] - 2026-07-12

Release overview: [RPG Reactor 0.94.5: The Performance Release](../docs/devlogs/2026-07-12-rpg-reactor-0.94.5.md).

### Added

- Runtime: built-in frame profiler on F10. First press starts recording, second press writes `save/reactor-profile.json` and logs a summary: per-phase timings (map/event updates, character sprites, windows, culling, tilemap paint/sort, image decode, GPU render) for every frame over 20ms, between-frame stalls from work outside the game loop, JS heap movement (GC detection), and the map/position where each spike happened. Costs nothing until activated, so it ships in every runtime. Also drivable from the console via `$reactorProfiler.start()` / `.stop()`. Spike records include the live animation-sprite count, and battle scenes get their own phases — `BattleManager.update`, and when LeTBS is present its tactical AI stages (`tbsAi:process`, action-building steps) — so enemy-turn spikes attribute to the responsible stage.
- Runtime: animation diagnostics console helpers. `$reactorAnimStats()` dumps every live animation sprite in the current scene — including MV-style host-based sprites on character, battler, and LeTBS entity hosts — with source, rate, remaining duration, drawn cells, and position. `$reactorAnimWatch(animationId)` arms a watcher that automatically logs every change in how many copies of one animation are alive (host, duration, position per transition); calling it again with no argument stops and dumps the transition log.
- Build menu: "Install Reactor Runtime..." converts the open project to the Reactor engine. The RPG Maker corescript (`main.js`, `rmmz_*.js`/`rpg_*.js`), the old `js/libs`, and the original `index.html` are moved into `rpgmaker-runtime-backup.zip` in the project root (excluded from deploys) so the two runtimes never mix in `js/`; then the Reactor engine files are installed, `reactor_plugins.js` is seeded from the project's `plugins.js`, `index.html` is pointed at the Reactor bootstrap, and a `package.json` is created when missing. Re-running it refreshes the engine files and offers to rebuild the Reactor plugin manifest from `plugins.js`.

### Changed

- Deployment no longer calls the GitHub API. The FFmpeg encoder for OGG optimization downloads from its pinned release URL and is still verified against SHA-256 hashes pinned in the editor; the NW.js proprietary codec downloads from its predictable release URL and is validated structurally (exactly one expected binary with consistent sizes). This removes random build failures from the unauthenticated GitHub API rate limit (HTTP 403). Any remaining GitHub API request honors a `GITHUB_TOKEN`/`GH_TOKEN` environment variable and names the rate limit in its error message.
- The shipped `runtime/reactor_plugins.js` is an empty plugin manifest instead of a development plugin configuration (GALV, UltraMode7, MOG_BattleHud).
- Runtime: games boot with a clean console. The ~18 informational install banners from `pixi_compat.js` and `reactor_mv_compat.js` (settings bridge, blend-mode probes, MZGlobalUpgrade wrap count, MV-detection, and the like) are now gated behind a debug switch — set `window.$reactorDebugLogs = true` before load, `localStorage.setItem("reactorDebugLogs", "1")`, or add `?debuglogs` to the URL to see them again. Warnings about actual failures are not gated. The benign "skipped invalid Pixi renderer system" notice is gated too, and MZ's stock "Save data is too big." warning (a 50KB web-storage guideline) no longer fires on desktop NW.js, where file saves have no such limit.
- Runtime: `pixi_compat.js` converts v5-style positional `PIXI.BlurFilter(strength, quality, resolution, kernelSize)` construction to the v8 options object before PixiJS sees it, so plugins constructing blur filters positionally no longer trigger the per-boot PixiJS deprecation warning.
- Bumped current development version to RPG Reactor 0.94.5.

### Fixed — performance

- Runtime: large maps run at full speed again. Object-heavy maps (hundreds of events, each with a character sprite and plugin overlay windows — 10,000+ display objects) dropped to ~30 FPS because PIXI v8 spends ~1.3µs per frame on every object in the display tree even when invisible. `Spriteset_Map` now detaches far-offscreen character sprites and dormant/offscreen plugin windows (event mini labels) from the display tree, parking them in an off-stage holder: parked windows keep their update logic running so their own show/fade behavior brings them straight back, character sprites re-enter from their game object's position, event/move-route logic (including `<Always Update Movement>`) is untouched, and everything is folded back in before teardown so nothing leaks. Measured on a 364-event map: 33ms → 12ms per frame; a 255-event map: ~33ms → 7.7ms. Set `window.$reactorDisableCulling = true` to disable for debugging. Also added a fast path to the PIXI v8 `updateLocalTransform` compat patch (skips four trig calls for unrotated objects). The window transparency test judges the pause sign by the window's `pause` flag rather than the sign sprite's `visible` (MZ keeps that sprite visible with alpha 0 on every open window, which exempted ~160 invisible on-screen mini labels — each still paying full per-frame window bookkeeping inside the render pass, ~10ms of a 22ms frame on the profiled map; render dropped to ~2ms once parked), and windows with `visible === false` park too. Character sprites are culled on their drawn extent (anchor ± width/2 horizontally, anchor − height upward) rather than the event's anchor point, so large event graphics — parallax-mapping roof pieces spanning many tiles — no longer vanish while still partly on screen.
- Runtime: scrolling no longer causes frame spikes. The PIXI v8 tilemap repaints its full painted region whenever scrolling crosses the region boundary, and previously destroyed and recreated ~2,000 tile sprites (and their textures) in a single frame — a measured 77ms hitch on every boundary crossing. Tile sprites are now parked in a detached pool between repaints and tile textures are cached per frame rect, making repaints nearly allocation-free. Because unpainted sprites leave the display tree the moment a repaint starts, stale tiles can never linger at the painted-region edge — an earlier in-place-reuse draft trimmed leftovers in a step that threw on `Tilemap.CombinedLayer` (which lacks the `Tilemap.Layer` helpers), leaving bands of garbage tiles at the viewport edge; the pool design removes the trim step entirely. Verified over 220 consecutive full repaints with zero paint errors and no frame over 24ms.
- Runtime: LeTBS enemy AI turns hitch far less. Profiling attributed 80–146ms frames to the AI's skill evaluation (`getAoEPossibleMoves`), which iterates every reachable move cell × every action cell, rebuilding the identical AoE scope and running a per-entity `eval()` for each combination. The compat layer replaces it with a behavior-identical version that hoists the per-skill scope data, caches the AoE and its filtered targets per (action cell, approach direction — plus the move cell when the AoE is line-of-sight sensitive), and caches each entity's use-condition verdict for the evaluation pass. Guarded by a source fingerprint so a modified or future LeTBS keeps its own implementation.
- Runtime: LeTBS AI move decisions no longer freeze the frame — `closestWalkableCellTo`/`farthestWalkableCellTo` ran two full A* pathfinds inside their sort comparator (~1,200 pathfinds to pick one destination, a measured 139ms hitch); the compat layer precomputes each candidate cell's path length once and sorts on the cached values. The AI AoE memoization also caches across move cells for line-of-sight AoEs (LoS is computed from the AoE center, so it is position-independent; only `exclude_user`/`cells_reachable`/`select` scopes stay position-keyed).
- Runtime: Ultra Mode 7 runs at full speed on large maps. The v8 compat renderer re-uploaded every layer segment's vertex buffer to the GPU every frame — on a 256×256 map that is ~47 segments × 16k quads ≈ 135MB per frame, a measured ~29ms stall (20 FPS with visible jitter). Mode 7 geometry is static between repaints (scrolling is the projection matrix; tile animation is a shader uniform), so buffers now upload only when the data actually changes: 36.8ms → 4ms median frame on the measured compatibility-project overworld.

### Fixed — Ultra Mode 7 and plugin detection

- Runtime: `Utils.RPGMAKER_NAME` reports `"MZ"` instead of `"Reactor"`. That constant is not branding — multi-engine plugins branch on the exact string to decide which engine's internals to patch, and a custom name sent them all down the MV path or a dead fallback: Blizzard's UltraMode7 silently rendered nothing (its `IS_RMMZ` check failed, so it patched MV's pixi-tilemap classes, which are inert stubs here), and ten-plus other plugins (Cyclone suite, DK Video Player, Mano_InputConfig, ...) took wrong branches. The engine's identity now lives in `Utils.REACTOR_NAME`. The bundled templates' hand-edited UltraMode7 copies (previously patched to detect "Reactor") are restored to stock.
- Runtime: Ultra Mode 7 works with plugin releases older than v2.2.0, including the v2.1.1 compatibility fixture. Blizzard added a `Tilemap.CombinedLayer` bridge in v2.2.0 for MZ core 1.7's layer wrappers; without it the stock `CombinedLayer.addRect` drops UM7's two extra animation-coordinate arguments (every Mode7 vertex gets NaN animation coords — the GL pass draws nothing) and `layer.animationFrame = n` writes a dead property. `pixi_compat` now installs the same bridge when the plugin doesn't provide it. The measured fixture overworld renders in full perspective at ~6ms/frame.
- Runtime: Ultra Mode 7 maps no longer crash the scene ("Cannot read properties of undefined (reading 'x')"). The tilemap's direct `updateTransform` drive (added for the culling work) ran plugin transform chains outside the onRender bridge's protective try/catch — UltraMode7's chain deliberately ends in the legacy no-args `PIXI.Container.updateTransform`, which throws on PIXI v8 *after* its real work is done. The direct drive now matches the bridge's swallow-the-legacy-tail semantics. Also: the MV project-marker probe uses `fs` under NW.js instead of a synchronous XHR, so MZ projects no longer log `net::ERR_FILE_NOT_FOUND` for the missing marker at boot.
- Runtime: Ultra Mode 7 tile seams are gone in pixel-art games — the v8 compat renderer's atlas filtering now follows the plugin's `TILEMAP_PIXELATED` parameter (NEAREST) instead of always LINEAR, whose sampling bled neighboring atlas texels at tile borders.

### Fixed — MV compatibility

- Runtime: the MV compatibility layer (`reactor_mv_compat.js`) is now two-tier instead of all-or-nothing. Tier 1 — MV plugin API support (gap-fills, MV-argument adapters, the MV `pluginCommand` bridge, MV save/data/asset format handling) — installs for **every** game, so MZ projects can freely mix in MV plugins. Tier 2 — MV game semantics (MV window geometry, scene layout, battle flow, damage popups, MV save encoding), which are mutually exclusive with MZ-authored UI — activates only for games authored in RPG Maker MV: via `window.$reactorMvCompat` (stamped into `index.html` by "Install Reactor Runtime" from the project's origin marker) or, absent the flag, an `RPGMV` project marker beside `index.html`. Previously the whole layer installed unconditionally, giving MZ projects MV metrics (squeezed command windows, washed-out window backgrounds); both modes are pixel-verified against stock corescript behavior.
- Runtime: "Set Movement Route" waits work again when an MV plugin overrides the route command (follower-control plugins routinely do, and YEP Move Route Core's script commands like `MOVE TO: x, y` depend on the wait to keep stepping). MV's interpreter stores the watched character on `this._character`; MZ replaced that contract with `this._characterId`, so with an MV-style override the wait looked up an unset id — which resolves to the *running event* — and dissolved the same tick, letting every later route command replace the moving one before it took a single step. The interpreter compat layer now keeps both contracts alive: native route/animation/balloon commands mirror their target onto `_character`, and the wait check prefers it since it is fresh on either path.
- Runtime: looping MV-format animations ("show animation, wait its duration, repeat" — waving flags, smoke stacks) hand off seamlessly between passes instead of blinking out for a frame and jumping position. Two causes fixed: finished MV animation sprites now get one grace tick before removal, matching MV's lazy cleanup (the grace is frame-stamped because `updateAnimations` runs twice per map tick in stock MZ, and it keeps running `updatePosition` so a scrolling map doesn't leave the last frame at a stale position); and a freshly created MV animation sprite draws its first frame the moment it enters the tree when its sheets are already cached, instead of staying invisible until its first update a tick later (visuals only — frame timings/SEs still fire on the first real update). Verified over 500 consecutive ticks: zero blank frames, zero position jumps. The blink was always present but became obvious at high frame rates, where every logic tick is rendered several times.
- Runtime: LeTBS battle animations no longer ghost. LeTBS parks entity animations (state `loop_animation` sprites, spawn effects) on a shared layer but relies on the owning entity sprite to remove them; finished and orphaned sprites accumulated on the layer wearing their last-drawn frame, and a validation battle showed 20 dead animation sprites leaked within a couple of minutes. The compat layer sweeps the animation layer each battle tick: finished sprites, sprites whose target left the scene, and loop sprites no longer tracked by their host are destroyed; live animations are untouched (verified: healthy loop keeps playing, orphan and finished sprites reaped, battle renders normally).
- Runtime: victory triggers immediately when the last enemy falls in MV games (previously the next ready actor's command window opened over the dead troop, and victory only fired after attacking the empty field). MZ's `BattleManager.endAction` nulls `_subject` as soon as a battler's actions run out, while MV leaves it set until `processTurn` advances it — ATB systems (VE_ActiveTimeBattle) gate new actor inputs on `_subject` being clear, so under MZ semantics an input could open during the killing blow's collapse animation, and the battle-scene input gate then blocked the battle-end check indefinitely. The MV compatibility layer now restores MV's `endAction` verbatim; the fix ordering (`endAction → processVictory`, no input) is verified identical against the same game running its genuine MV corescript.
- Runtime: MV window contents are sized MV-verbatim under the MV compatibility layer (`contentsHeight` = height − padding×2, without MZ's one-row smooth-scroll buffer). MV plugins compute layouts from `contents.height` — YEP_PartySystem's party-select cells, for example — so the MZ buffer displaced their sprites ~36px. Verified byte-identical against the same game running its genuine MV corescript.
- Runtime: MV main-menu sizing plugins apply again under the MV compatibility layer. `Scene_Menu`'s window creators are restored MV-verbatim (constructing from `(x, y)` with the width from the window's own `windowWidth()`) so plugins that wrap them — YEP_MainMenuManager sizes the status window as `boxWidth − x` — capture the originals they were written against. Previously they captured MZ's Rectangle-based creators, so the menu status window kept MZ's `boxWidth − 240` width, was repositioned to the plugin's x, and overflowed the right screen edge.
- Runtime: MV plugins that customize actor status drawing (menu gauges, level display, class rows) apply again under the MV compatibility layer. MZ moved the `drawActor*` family from `Window_Base` onto a new `Window_StatusBase` intermediate class, which sat closer in the prototype chain and shadowed every MV plugin's `Window_Base` patch — menus drew MZ's default layout (level shown, 128px sprite gauges) regardless of the game's plugin configuration. For MV-format games the shadowing copies are now removed so lookups fall through to `Window_Base`, where the MV ports and plugin patches live. Also hardened `Scene_Map.stop` against plugins that remove the map name window.
- Runtime: MZ games show their saves again when the project folder carries leftover MV-era `.rpgsave` files beside the real `.rmmzsave` saves (common in projects ported from MV). The save-format resolver preferred `.rpgsave` whenever one existed, so the save menu read a stale MV-era global index and listed nothing. Resolution is now native-format-first per game type: MZ games stick to `.rmmzsave` and only fall back to an `.rpgsave` for slots with no native save; MV games keep `.rpgsave` as before.
- Runtime: MV games no longer freeze when a plugin's promise rejects unhandled (e.g. a video parallax whose source fails to load — `video.play()` rejections were fatal under MZ's `unhandledrejection` handler, stopping the game loop dead). MV never subscribed to that event, so under MV game semantics the rejection is logged and play continues.

### Fixed — rendering, effects, and editor

- Runtime: Effekseer battle animations keep their shape at every screen position. Effects were positioned by moving them off the camera axis inside one shared wide-FOV frustum, and off-axis perspective stretched them radially — a sphere 300px from screen center rendered 50% wider than tall. Each effect now renders on-axis in its own square viewport centered on the target (MZ's own approach, adapted to the overlay recipe), pixel-verified round at center, offset, and corner positions.
- Runtime: "screen center" (`displayType` 2) Effekseer animations position correctly under PIXI v8 — `targetPosition` read `renderer.view.width`, which is a ViewSystem (not the canvas) on v8, so the position computed NaN.
- Runtime: `pixi_compat.js` provides a `PIXI.settings` bridge on PIXI v8, which removed the namespace. Plugins with bundled pixi-filters builds (e.g. Hendrix_Particles_Builder, Hendrix_Post_Processing_Builder) read `PIXI.settings.FILTER_RESOLUTION` at load time and crashed the game on startup; `FILTER_RESOLUTION`, `RESOLUTION`, `SCALE_MODE`, `WRAP_MODE`, `MIPMAP_TEXTURES`, and `TARGET_FPMS` now map to the per-class v8 defaults, and unbridged settings are stored instead of throwing.
- Runtime: window skins render their pattern quadrant again under PIXI v8. `TilingSprite._refresh` mutated `texture.frame`, which on v8 skips `updateUvs()` and leaves `orig` on the old rectangle, so window backgrounds tiled the entire 192×192 skin — cursor, arrows, and the text-color palette checkerboard showed through behind window contents. The texture is now replaced with a correctly framed one, matching `Sprite._refresh`.
- Runtime: the FPS counter (F2) renders with RPG Maker MZ's stock look in every project. MZ ships the counter's CSS in its default `index.html` style block, which Reactor-generated pages never carried — the unstyled counter sat behind the game canvas (and the MV compatibility layer's workaround restyled it tiny). The stock styling is now inlined in the engine's `FPSCounter._createElements`, stacked above the game canvas and Effekseer overlay; the MV-layer workaround is gone.
- Forge Effekseer Generator: an explicit frame count now always caps the exported effect, including continuous-spin recipes. Previously "N frames" on a continuous stack only set the preview loop length while the exported file ran ~10 minutes (36000-frame node lives) — the database preview never ended and battle animations would stall the battle waiting for the effect to die. Blank duration (∞) still exports endless effects for ambience use, and the Forge preview now replays at the same frame the export ends on.
- Deploying an imported RPG Maker MV/MZ project no longer fails with "Project runtime is incomplete". Staging validation now checks which runtime `index.html` actually boots: projects on their original corescript deploy as-is, while projects on the Reactor runtime get a complete-file check whose error names the "Install Reactor Runtime" recovery action.

## [0.94.4] - 2026-07-11

### Added

- Skills, Items, and Weapons now assign animations through a two-column picker modal: a searchable animation list beside a live playing preview. The preview renders MV sprite-sheet animations on a 2D canvas and Effekseer effects through the game's own WebGL runtime, looping short effects with a brief pause. Skills and Items include the Normal Attack entry; selections dispatch through the existing field handlers.
- Database entry lists display a framed mini icon beside each name: IconSet icons for skills, items, weapons, armors, and states; face portraits for actors; and battler thumbnails for enemies (single battler images scale to fit, charset-style battlers show their front idle frame). Icons refresh immediately when changed through the icon picker, and icon-less entries keep a blank spacer so names stay aligned.

### Changed

- Bumped current development version to RPG Reactor 0.94.4.

### Fixed

- Fixed card-filling database forms (`db-fill`) collapsing every row to content width and centering it — Name and Description fields rendered tiny with large side gaps. Flex-column forms now stretch their rows to the card width.
- Effekseer previews in the Animations tab and the animation picker are aspect-correct: the projection's x-axis is scaled by height/width so spheres render round on non-square canvases.
- Buttons with dynamic labels (the animation picker trigger) are no longer reverted to cached text by the interface-translation observer, and field-styled buttons no longer inherit the read-only gray (CSS `:read-only` also matches `<button>` elements).
- Added Web-only responsive layout rules for laptop, narrow, and short viewports. Menus and toolbars scroll instead of clipping; the sidebar becomes fluid and stacks above the workspace on very narrow screens; splash, welcome, map-information, save-banner, and Playtest overlays fit the viewport; and Database, Event Editor, Map Properties, Image Picker, and general modal layouts reflow and remain scrollable. Desktop NW.js sizing is unchanged.
- Fixed Web Character Generator packaging by bundling the Outfit Engine, Hair Engine, and every built-in style-part script before the generator entry point. Browser execution uses those global engines while continuing to load project-specific parts from the virtual project.
- Added browser-host file export support for blobs, typed binary data, text, and data URLs. Active-project writes flush to IndexedDB; projectless single files use the browser save picker or download fallback, while multi-file exports recreate their resource tree through the directory picker.
- Fixed Web Forge output for procedural and Parts PNG character sheets, Animation Generator PNG/GIF exports, Sound Effect Generator WAV exports, generated Outfit/Hair library parts, and Effekseer effects with all baked, procedural, and user-text textures plus generated models.
- Fixed browser Playtest sometimes starting before the project-overlay service worker controlled the page. Web startup now waits for service-worker control and performs one guarded reload when required, so persisted edits are available to Playtest immediately.
- Removed the unsupported Build menu from the Web editor.
- Fixed test-only plugins not detecting packaged Windows playtests when isolated Chromium profiles are enabled. Windows NW.js exposes the `--user-data-dir` switch as `nw.App.argv[0]`, ahead of the separate `test`/`btest` argument; RPG Maker only inspects that first argument. Windows profile paths now carry the mode as an ampersand-delimited option, preserving profile isolation while restoring Sang Hendrix hover docks and other test-gated tools.
- Added regression coverage for Web responsive scoping, browser save destinations and fallbacks, Character Generator asset bundling, service-worker startup control, Forge project paths, and Windows mode-bearing playtest profiles.

## [0.94.3] - 2026-07-10

### Added

- Deploy Editor now offers a provider-neutral **Web** package. It bundles the browser editor and Reactor One into a root-level ZIP, opens Reactor One automatically, stores mutable project data in IndexedDB, supports resetting those edits, and runs Playtest in an in-page frame using the saved browser data.

### Changed

- Bumped current development version to RPG Reactor 0.94.3.
- The optional AppImage control now appears as an indented Linux sub-option in both deployment dialogs only while Linux is selected, rather than occupying a permanent top-level platform row.

### Fixed

- Fixed large NW.js SDK downloads failing when `dl.nwjs.io` remained idle for 30 seconds. Game and editor deployment now allow a three-minute socket-idle window, retry transient archive failures up to three times, and remove incomplete cache files between attempts.
- Game and editor deployment logs now show a live inline progress bar for every downloaded runtime, codec, FFmpeg, or AppImage tool asset. Known-size downloads display percentage and transferred/total MiB; chunked or unknown-size responses display transferred MiB with an animated activity bar, and the same row updates for retries and completion instead of flooding the log.
- Fixed NW.js worker-thread HTTPS requests sometimes opening a valid `dl.nwjs.io` URL without receiving archive bytes. Build workers now prefer the host's native `curl` transport with atomic output, byte-level progress polling, stall detection, and bounded retries; the verified Node HTTPS downloader remains the automatic fallback when curl is unavailable.

## [0.94.2] - 2026-07-10

### Added

- The RPG Maker MV compatibility layer (`reactor_mv_compat.js`) now ships in the runtime folder and loads in every project. Previously it lived only in a local test project, so the 0.94.1 MV compatibility work was not actually included in new projects or the public runtime. It is inert in pure-MZ projects: every API it provides is gap-filled only when missing.
- Outfit Forge **Part / Material / Accent** native dropdowns per slot, with the preset thumbnail list always visible (search still filters). Matches Procedural-tab option discoverability.
- Added a clean-checkout GitHub Actions workflow plus runtime-manifest, generated-project scaffold, save-safety, editor-distribution staging, and web deployment smoke tests.
- Added **Save Project** and **Playtest** to the File menu, visible shortcut indicators beside all four application commands, and global shortcuts for New (`Ctrl+N`), Open (`Ctrl+O`), Save (`Ctrl+S`), and Playtest (`Ctrl+R`).
- Added an `F5` shortcut for an uncached application reload, guarded by a confirmation warning that unsaved changes will be lost.
- Added an `F11` shortcut to toggle the editor's native NW.js fullscreen mode.
- Deploy Game can now retain only selected NW.js runtime locales while always preserving English as a fallback, reducing desktop package size without touching project translation assets.
- Deploy Game now offers optional lossless Oxipng recompression for staged PNG assets and explicit-quality OGG Vorbis re-encoding. Only smaller validated files replace staged copies, loop metadata is preserved, and the pinned FFmpeg tool is SHA-256 verified and separately cached. Per-file progress keeps large optimization passes visible, and settings persist between sessions.
- Deploy Game and Deploy Editor can optionally emit a portable Linux x86_64 AppImage in addition to the existing Linux folder or ZIP. AppImage creation is available on Linux x86_64 build hosts, uses separately cached and SHA-256-pinned `appimagetool` and Type 2 runtime assets, preserves NW.js symlinks and executable modes, embeds desktop metadata/icons, and builds through an atomic temporary artifact.

### Changed

- Bumped current development version to RPG Reactor 0.94.2.
- Save Project and Save All now share one checked persistence path for the current map, database, project metadata, and authoritative map list. Snapshot-based dirty detection covers tile and event edits without per-control instrumentation, and map/project transitions offer save, discard, or cancel choices.
- Generated fallback projects now carry complete runtime-required `System.json` display/font settings and an empty `reactor_plugins.js`, providing a valid baseline when the bundled Demo is unavailable.
- Deployment dialogs now use a themed, searchable exact-version NW.js selector with release-date filtering, independently persistent game/editor output paths, and consistent uppercase **ZIP archive** descriptions across desktop platforms.

### Fixed

- Character Generator **Parts (PNG)** path inconsistency: loads PNGs from both `styles/<style>/parts/` and legacy `forge/character_generator/parts/`. Empty state explains both paths and that procedural/hair/outfit JS parts live on the Procedural tab. Added Open Folder for the active style parts root.
- Forge project-path stickiness: Character / Animation / Sound Effect generators re-resolve `getCurrentProject().path` on save/load; `ForgeManager.onProjectChanged()` clears cached tool paths when projects open, create, or close, so Save GIF / bake dialogs no longer default to a previous project (e.g. Reactor One).
- Hair Forge **Lower Banding** and **Scraggle** response amplified (deeper edge bites, denser band rows, more highlight lanes) so slider steps are clearly visible; UI notes that Eye Zone is front-only.
- Fixed the Windows splash screen's small vertical startup bounce by removing two pairs of deliberate one-pixel `resizeTo()` height nudges whose native-frame and DPI rounding could produce larger visible movement. Packaged frameless Windows builds under Wine/Proton also no longer relaunch into a second already-frameless window.
- Fixed the Demo crashing on **New Game** while taking the title background snapshot. The MV positional `RenderTexture.create(width, height)` wrapper explicitly passed an undefined resolution into PixiJS 8.14, which replaced Pixi's default with `undefined`, generated `NaN` texture dimensions, and produced the `Value is not of type 'long'` extraction error. The wrapper now omits invalid optional values, and `Bitmap.snap` uses the native Pixi 8 options form with a finite resolution.
- Fixed database, map, and project persistence paths that swallowed write failures or reported success after partial failure. `MapInfos.json` now has one authoritative owner and cannot be overwritten by a stale database copy.
- Fixed packaged Animation Generator GIF import/export by declaring `gifuct-js` directly and staging `gif.js`, its worker, and the decoder's minimal CommonJS dependency closure in every editor distribution.
- Editor and game packaging now validate required runtime files, search for bundled NW.js beside either the editor or repository root, and omit development `save` and backup directories from deployments.
- Fixed valid RPG Maker MZ **Skip** commands (`code 109`) appearing as `Unknown (109)` in Common Events and troop events. The issue was data-dependent rather than Linux-specific; command data and runtime behavior were already correct.
- Fixed NW.js deployment acquisition repeatedly downloading runtimes that already existed in a secondary cache or beside a packaged Windows/Linux editor. Game and editor deployment now search every cache before downloading, use atomic archive downloads, cache the official stable-version manifest, and offer latest-stable, same-as-editor, or validated searchable release selection.
- Added an opt-in, exact-version `nwjs-ffmpeg-prebuilt` overlay for H.264/AAC support in game and full editor deployments. Codec release metadata and archives use a separate cache, GitHub-provided SHA-256 digests are verified, archive contents are constrained to the expected platform binary, and macOS installs into the active NW.js framework rather than an ineffective top-level sidecar.
- Linux editor distributions now use `.zip` like Windows and macOS instead of `.tar.gz`, while preserving executable bits and runtime symlinks in the archive.
- Fixed custom Deploy Game and Deploy Editor output directories resetting to installation-relative defaults after restarting the editor.
- Fixed deployed games preventing the same source project from launching in Playtest by assigning every playtest a Reactor-owned, project-specific NW.js profile on Windows, macOS, and Linux.
- Fixed the Effekseer Generator's Layers workspace and timing controls: Layers sit beside the preview when space permits and stack below it on narrow windows; opacity has a live percentage and applies to animated alpha curves; and keyframe selection, add/delete, frame edits, Start Frame, layer movement, and timing windows remain synchronized.
- Fixed Oxipng reporting every staged PNG as unsupported under NW.js worker threads. Deployment now initializes the supported single-thread WASM codec directly instead of allowing browser-worker detection to select an unavailable threaded build.
- Fixed localized About dialogs that still hard-coded `0.94.1`; every available translation now reads the shared application version.

## [0.94.1] - 2026-07-05

### Added

- Effekseer **Interface** category rebuilt as genuine 3D instruments and grown to **21 recipes**. Every interface is now real world-fixed geometry inside a presentation tilt rather than camera-facing sprites, so the orientation gizmo rotates them truthfully. Rebuilt: Radar Sweep, Target Lock, Power Levels (baseline-anchored segmented LED bars), Network Nodes (a volumetric 3D mesh cloud with sphere-model nodes and per-edge packet traffic), Hex Memory Dump, Boot Screen, Vital Signs, LCARS, Tactical Map, Static. New: Orbital Survey (a build-your-own solar system with per-planet size sliders and custom-texture uploads that wrap each planet as a real globe), Star Chart, Reactor Core (a spinning wireframe-torus core), Xenobiology Scan, Starship Analysis (a fully parametric wireframe starship with hull-tracking component glows), Circular Gauge (segmented LED arc with Hold/Sweep/Pulse fill modes), Bar Meter (vertical/horizontal LED bank), Behavior Matrix (a ternary plot with user-named corners and a tracked laser-dot marker), Flight Prediction (probability cone with Path Arc and Branches sliders), Composite Waveform (a living oscilloscope whose signal is a parametric, phase-layered texture reshaped by Intensity/Frequency/Detail/Noise sliders), and Battery (a 3D cylindrical cell of stacked "sausage-slice" LED segments with Fill/Drain/Hold/Short patterns, the last flashing a lightning bolt).
- Effekseer **user-typed text** for interfaces: a **Display Text** param (single-line strip) and **Paragraph Text** (multi-line, scroll-through or blink), rendered to crisp textures and carried through preview and export, so the same interface recipe can read as anything the user types.
- Effekseer **Physical** attack pack - 10 new battle-hit recipes covering the common combat verbs: **Slash** (curved crescent flurries with a Curve slider from hooked to straight), **Bite** (solid curved-fang jaws that clamp and stop at the bite line, with canine glints and gash marks), **Punch** (multi-hit combos with a knuckle imprint and speed lines), **Impale** (decelerate-and-stick spikes with hooking Curve arcs and exit spray), **Claw Rake** (parallel raking crescents), **Crush** (overhead smash with Bounces, ground ring, and thrown debris), **Arrow Hit** (a fast shaft that sticks with fletching), **Parry** (a barrier flash with directional ricochet sparks, the first defensive effect), **Whip Crack** (an S-curve lash cracking at the tip), and **Blood** (Burst/Spray/Drip patterns built from liquid string-ribbons and seeded morphing splat decals, with full color control for red/ichor/ooze).
- Effekseer **Energy** spell recipes: **Energy Boost** (rising energy streams over a rotating sawblade vortex base), **Energy Column** (an Energy-Beam-style layered 3D cylinder with a Beam Width slider and a thin-line teleporter pad), **Binding Circle** (an upright rotating rune cage), and **Hex Forcefield** (a seam-aligned honeycomb sphere shell with an energy sweep and on-surface impact shimmers).
- Effekseer **Christian Cross** variants: a Variant dropdown adds Orthodox (eight-pointed), Greek, and Celtic forms alongside the Latin default, on a reusable per-glyph symbol-variant mechanism.
- Effekseer **parametric textures**: procedural texture generators can bake slider-driven variants (parameters encoded in the texture name so the runtime and export both cache them by path), plus 20+ new generators (interface tech glyphs/strips/circuitry/scanlines/data-rain/EKG, a crisp three-tier ring family, a honeycomb lattice, a composite-waveform signal, Futhark runes, fangs, lightning bolts, seeded morphing blood splats).
- Map editor **MZ-style layer dimming**: selecting a specific tile layer (1–4) renders every other layer at reduced opacity so it's obvious which tiles live on the active layer, with A restoring full opacity, implemented as per-container alpha (free per frame; the previous per-tile-overlay approach was disabled for freezing large maps).

### Changed

- Bumped current development version to RPG Reactor 0.94.1.

- Moved the Effekseer **Explosion** recipe from Elements to the Physical category.
- Sharpened the Effekseer **Magic Circle**: the rune band is now a legible Futhark-style alphabet at higher resolution, and the inner star/tick struts use crisp textures instead of soft glow that blurred them. Ice Shards now use jagged, irregular seeded crystal models instead of a uniform hexagonal bipyramid.

### Fixed

- Fixed Effekseer preview loading in the Database Animations editor and Event animation picker (chrome-extension URLs could not reach project files after the reorg, and NW.js cross-realm `instanceof ArrayBuffer` checks silently dropped loads) by reading effects via `fs`, copying into a page-realm buffer, redirecting textures to data URLs, and adding a stall watchdog.
- Fixed Effekseer Forge rotation: the gizmo/drag no longer jumps to a different angle, and replaying an animation no longer resets the orientation, via exact trackball matrix math; and a category-flip wrapper is now applied innermost so a parameter rebuild no longer flips the tilt's components (an angle would change when an unrelated slider moved).
- Fixed Effekseer beam/column construction: Energy Beam's hollow core is filled with a Y-axis billboard plane, Teleport Column no longer renders as half a circle (its texture now wraps three times around), and Energy Column's base rings are crisp and sized to the beam instead of a soft oversized disc.
- Resource watchdog sweeps now run from the engine's own frame tick (throttled to 1Hz) instead of only from manager `isReady()` methods, plugins replace those methods wholesale (KODERA-style image caches, MV-1.6 audio caches), which silently disabled stall recovery. Bitmap stall recovery also moved to the `Bitmap` level with the same self-registering sweep as audio, and images that genuinely fail to load degrade to a blank canvas with a console error instead of deadlocking scene startup.
- Hardened the resource watchdogs based on live testing against a 565-plugin MV project: silent stalls now retry indefinitely (a genuinely missing file still fires onerror immediately, giving up after N attempts only produced spurious fatal errors for resources that were about to arrive); recovery is parallel across all pending resources instead of serialized behind cache implementations that short-circuit at the first not-ready item; the audio stall clock uses wall time instead of AudioContext time (which freezes while the context is suspended, blinding the detector exactly when loads stall); audio buffers self-register for watchdog protection when anything polls their readiness, which also revives "zombie" buffers that a plugin cache still gates on after other code destroyed them (MZ treats audio buffers as disposable, MV-era audio caches assume they are reusable); and audio files that genuinely fail to load now degrade to silence with a clear console error instead of deadlocking scene startup behind a plugin readiness gate.
- PIXI v8 compatibility: `getBounds()` returns a `Bounds` object in v8 where v5–v7 returned a `Rectangle`; plugins doing hit tests via `getBounds().contains(x, y)` crashed. `Bounds` now carries a `contains()` delegate.
- Extended the silent-stall watchdog to database JSON and image loads: `DataManager.isDatabaseLoaded()`/`isMapLoaded()` and `ImageManager.isReady()` now detect requests that died without firing onload or onerror (hung boot or hung map transfer with zero console errors), retry up to three times, and then surface a real load error. The data-file watchdog is self-sufficient, it derives what is missing from the database file list rather than hooking `loadDataFile`, which plugins (e.g. YEP_X_CoreUpdatesOpt) replace wholesale.
- Fixed a permanent black screen after loading a save when any audio file's load request silently died (no onload, no onerror, e.g., one dropped XHR in the burst of ~20 parallel audio loads a save triggers): the scene never reported ready, never started, and the stage was never attached. WebAudio now detects zero-progress loads from `isReady()` polling (background-timer-throttle-proof, measured on AudioContext time), retries up to three times, and surfaces a real error instead of hanging the game forever.
- **RPG Maker MV runtime compatibility (mix-and-match with MZ):** the PIXI8 runtime boots and plays a large commercial MV project's full plugin stack (Yanfly, Victor Engine, MOG, SRD, LeTBS, and more) through the intro with no errors, and MV-format save files deserialize cleanly. Loading a mid-game save through the game's own load menu now works and renders. Additional MV compatibility-layer fixes verified against that project's battle scene: MV window internal sprite names (`_windowBackSprite` and friends) alias to their MZ counterparts so MV plugins can restyle windows; the battle FIELD is created before battlebacks in MV's order (MZ's reversed order crashed MV battleback plugins and cascaded into most other battle plugins); and fixed-slot battle status layouts tolerate empty member slots instead of crashing on undefined actors. Two regressions from that batch were subsequently found and fixed in live testing: `ImageManager.reserveBitmap` must preserve MV's `if (filename)` guard exactly (plugins feature-detect these APIs, and translation plugins wrap `loadBitmap` with unguarded string matching, an unguarded port crashed the boot preload chain), and the MV message sub-window creation chain must run exactly once per scene (a second creation path produced duplicate name-box/backlog/choice windows that corrupted menus and ate choice input). A static MV-corescript-vs-runtime API scan then drove a batch of ~25 more MV compatibility gap-fills (verified live at runtime): MV's cell-sheet animation engine restored on `Sprite_Animation` for plugins that subclass it (fixes the LeTBS tactical-battle crash on animation setup), functional character balloons and hosted animations, `ToneFilter`, MV `Bitmap` tone/hue manipulation, `Game_Followers.forEach`, MV save-backup, window scroll/sound/layout helpers, and more. Fixed a structural MV/MZ gap where MV battleback plugins (YEP_ImprovedBattlebacks, VE_\*) call `battleback1Bitmap()` on `Spriteset_Battle` (MV's location) while the MZ model keeps that logic on `Sprite_Battleback`, crashing battle load, the MV compatibility layer now restores MV's full `Spriteset_Battle` battleback chain as gap-filled defaults without touching the MZ path.

## [0.94] - 2026-06-27

### Added

- Added an **Effekseer Animation Generator** to the Forge, a recipe-driven generator for native Effekseer particle effects (`.efkefc`) that removes the need for the external Effekseer editor entirely. The tool is built on an in-house binary format engine: a `.efkefc` reader/writer targeting binary version 1500 with tracked generated-format round-trip coverage, and an `.efkmodel` mesh writer supporting both v3 single-frame models and v5 **multi-frame vertex animation**. Exports write the effect, its procedural textures (`effects/Texture/rr_*.png`), and any generated models (`effects/Model/rr_*.efkmodel`) directly into the open project in the exact layout the engine loads.
- Effekseer Animation Generator rendering options: per-shape **Glowing Outline** style (crossed-quad struts with texture flow along edges) or **Solid (textured)** style with seam-correct UV mapping (duplicated seam columns so textures wrap spheres/tori cleanly), ear-clipped glyph fills, spherical-UV solid polytopes (dodecahedron pentagons recovered from the icosahedral dual), normal-blend faithful texture rendering, and untinted custom textures so a planet map wraps a sphere like a globe. Custom PNG/JPEG textures import through an AG-style picker that copies them into the project's `effects/Texture/`.
- Effekseer Animation Generator **keyframe system**: every layer holds keyframes, full parameter states pinned to user-chosen frames (select a KF chip, set its frame in the "@" box), and a compiler turns the differences into native Effekseer curves: colors and ring colors become FCurve tracks resampled through the keyframe positions, size becomes an eased/FCurve wrapper scale, differing spin rates integrate into FCurve rotations, and **differing custom textures compile to a cross-fade** (one instance per keyframe dissolving via alpha envelopes - Earth melts into Mars on a spinning globe). Keyframed windows re-spawn every master cycle so the pattern repeats identically on every loop, structure params stay in lockstep across keyframes (so transitions can always pair the node trees), and Randomize on later keyframes rolls only tweenable values. Geometry-changing parameters hold keyframe 1's value, the same limitation as Effekseer's own editor.
- Effekseer Animation Generator **AG-style workspace**: a LAYERS panel below the preview on a dark inset backdrop (per-layer cards with SVG visibility eye, reorder, duplicate, delete, opacity slider that rescales the layer's compiled colors, and keyframe chips), ＋ add-as-layer buttons on every sidebar row in a fixed right-hand gutter, a master **Frames** duration field in the playback bar (the counter loops against it; burst compositions restart there; continuous ones loop seamlessly), Delay/Duration inputs per layer, export name + button relocated to the bottom bar, and a 720×720 preview that scales to fill the workspace.
- Effekseer Animation Generator solid-surface rendering corrections: equirectangular UV orientation fixed against a real Earth texture (u as parametrized, v flipped), **backface culling on all closed solids**, the MZ-style projection yields constant depth so occlusion must come from culling, not depth writes (Möbius strip stays double-sided), and custom texture uploads are copied under content-hashed filenames because the runtime caches textures by path (re-uploads previously showed stale pixels).
- Effekseer Animation Generator reliability: the sidebar search filter no longer erases row layout when re-showing entries; a status readout after every rebuild shows bytes, visible layers, and compiled keyframe tracks; the class surface and the index.html wiring are both locked by tests; deferred effect release and sync-load handling keep slider rebuilds crash-free.
- Effekseer Animation Generator **Composer**: a new "Custom Effect" recipe where effects are built from LAYERS with keyframe-style timing, each layer (Flash, Glow, Shock Ring, Particle Burst, Tendrils, Smoke Puffs, Motes) has a start/duration window on a shared timeline plus eased from→to motion inside it, edited through per-layer cards with add/duplicate/remove. Layer factories live in a new composition library (`layers.js`) using staged acts, normal/additive blending, fades, eased scale, spark-dust children, and Track-ribbon tendrils. Everything maps onto native Effekseer primitives, so exports are plain `.efkefc` files.
- Effekseer Animation Generator element recipes rebuilt as staged pro-grammar compositions using the layer library: **Fire Burst** (ignition flash → heat rings → dusted ember fountain → flame tendrils → normal-blend body under additive core → smoke and cinders), **Water Splash** (impact → crown rings → dusted droplet fountain → water body → settling ripple and mist), **Ice Shards** (crystallization snap → shard spray → icy sheen → weightless hanging glitter → sinking mist), **Wind Gust** (pressure shimmer → three depth-layered streak waves → curling air tendrils → swept debris), each ~15-20 nodes versus the old 3.
- Effekseer Animation Generator format engine now **reads Effekseer binary versions 1610 and 1710** in addition to 15/1500 (writer still emits the engine-native 1500): steering behavior, triggers, kill rules, LODs, procedural models, the 1600-era renderer-common extension (emissive scaling, 7 texture slots, extra UV channels, alpha cutoff, falloff, soft particles), per-element force-field falloffs, and the 1710 dependency-table INFO chunk.
- Effekseer Animation Generator spinning recipes (Geometric, Object, Symbolic, Hypersphere, Energy Ball/Beam, Aurora, Black Hole) converted from fixed-length seamless loops to **continuous playback**: spin sliders are now honest degrees-per-second (the old whole-turn loop snapping quantized spin so coarsely that at the default loop length any rate under ±90°/s rounded to zero), there is no loop point at all, and the spark/glint texture was redrawn as a per-pixel four-ray glint with soft falloff.
- Effekseer Animation Generator workflow tools: a **🎲 Randomize** button (like the standard Animation Generator's) that rolls every slider on its step grid, picks pleasing random hues, and flips toggles/selects, texture uploads and recipe-excluded params are left alone, and a **preset system**: save the current parameter set under a name, recall or delete presets from a dropdown, stored per project in `forge/effekseer_generator/presets.json`.
- Effekseer Animation Generator preview and controls: live in-memory playback through the same bundled `effekseer.min.js` WebGL runtime the game uses (ArrayBuffer effects + data-URL resources, zero disk writes), a persistent render loop with background rebuild and seamless effect swap on every slider change. Two runtime timing hazards were diagnosed with a headless harness that runs the real WASM in Node and fixed: the load callback fires *synchronously inside* `loadEffect()` whenever the core already caches every referenced resource path (the common slider-rebuild case), the swap then installed a null effect and the preview stuck on "loading effect", and freeing a replaced effect's WASM memory in the same tick its handle was stopped is a use-after-free (instances are destroyed on the *next* update), so replaced effects retire through a deferred-release queue. Seamless looping by default (spin rates snapped to whole turns per loop, texture flow snapped to whole repeats, and a deterministic frame-counted restart that eliminates the blank-frame flicker at the loop point), an orbit camera on right/Shift-drag, and left-drag rotating the effect itself, the same orientation state the 3D rotation gizmo edits, so the widget and direct dragging stay in sync, both applied in realtime and baked into exports. All top-level nodes are promoted to Always-bind, so realtime steering works on every recipe including the burst effects. Picking a custom texture automatically switches the shape to Solid style so the image visibly wraps the surface (the redundant built-in texture dropdown is gone), and the recipe sidebar keeps its scroll position when switching recipes. The preview is crash-proof: runtime errors surface in red status text instead of silently freezing the render loop, and a load watchdog reports effects that never finish loading. Registered as its own Forge line item and menu entry with i18n across all 10 editor languages, and covered by test suites: stock-corpus parse/round-trip, `.efkmodel` round-trips, every recipe validated at defaults, at parameter extremes, with every individual parameter swept across its range (~2,000 builds), invariants that every declared slider is actually used and every recipe steers, and a **headless playback suite** that runs every recipe through the actual Effekseer WASM runtime in Node (stubbed WebGL): load, play, 150 simulation frames, realtime steering, a mid-playback slider hot-swap, and an instance-pool leak check per recipe, plus a bake-pipeline test that renders every baked recipe through the real Animation Generator code.

### Changed

- Bumped current development version to RPG Reactor 0.94.

### Fixed

- Fixed Mini Skirt side-view cleanup by capping leg underfill at the skirt hem and checking the mini-skirt shade before applying body-edge outlines, preventing orphan outline/bridge bands below the hem where bare legs should show through.
- Fixed Outfit Forge Mini Skirt `Knee plates` so it now renders separate knee pads at the anatomical knees instead of being an ignored segmented-pants-only toggle; the Mini Skirt `waistband` toggle also produces visible top-band shading instead of being swallowed by generic edge outlines.
- Fixed Psychronic Mini Skirt placement by rejecting classifier rows above `legs.minY`; without that guard, Psychronic torso/belt rows were accepted by the skirt painter and made the waistband/skirt look far taller than the actual legs-zone geometry.
- Fixed Outfit Forge card number inputs feeling laggy while typing by debouncing typed numeric edits instead of regenerating the preview on every keystroke; spinner buttons still update immediately.

## [0.93.1] - 2026-06-21

### Changed

- Bumped current development version to RPG Reactor 0.93.1.
- Reworked macOS editor distribution packages so the zip contains only a self-contained `RPG Reactor.app` instead of a sibling runtime folder or loose Chromium files.
- Windows editor distribution packages now remove `--enable-logging` from the packaged editor payload to avoid noisy Chromium logging output in final builds.

### Fixed

- Fixed macOS packaged editor startup by placing the editor payload at `Contents/Resources/app.nw`, the standard NW.js macOS bundle location.
- Fixed macOS packaged playtest by embedding a clean internal `playtest-runtime/nwjs.app` inside `RPG Reactor.app` and symlinking it to the bundled NW.js framework to avoid doubling package size.
- Fixed macOS playtest runtime discovery when NW.js reports paths from helper processes instead of the outer app executable.
- Fixed Windows packaged playtest to prefer the clean adjacent `nw.exe` before any stale `nwjs-win` runtime folders and to suppress playtest console flicker.
- Fixed Windows distribution builds producing Chromium `Invalid logging destination` noise by stripping `--enable-logging` from the packaged editor config.
- Fixed erasing existing imported RPG Maker map tiles by making auto eraser mode target the topmost real layer at each cell, including layer 0 base/autotile data.
- Fixed rectangle, circle, and fill eraser tools so they work without selected palette tiles, preserve eraser mode when changing drawing tools, and never show selected-tile placement previews while erasing.
- Fixed eraser state edge cases where focus changes, Region tab selection, or a `null` active draw tool could leave Eraser visually selected but internally unable to erase.
- Fixed Plugin Manager saves for standard RPG Maker MV/MZ projects so `js/plugins.js` stays compatible with RPG Maker's expected `name`/`status`/`description`/`parameters` entries while Reactor-only parsed help, author, and URL metadata remains editor-only.
- Fixed Database > System in the native top menu by exposing separate System 1 and System 2 entries and routing the legacy `system` database type to System 1 instead of showing an unknown-type error.

## [0.93.0] - 2026-06-21

### Changed

- Bumped current development version to RPG Reactor 0.93.
- Audio Player Volume/Pitch/Pan controls now sit in a distinct themed card instead of blending into the modal background.
- Added the Rarely Typical Players Podcast YouTube channel to the Help/About links.
- Updated the editor window title to use `RPG Reactor | <Game Title>` and refresh on project load, close, language changes, and System 1 game-title edits.
- Reworked Windows/Linux platform editor distribution packaging to append the editor payload into the branded executable, leaving the plain NW.js runtime clean for playtesting without shipping a second full runtime copy.
- Windows editor distribution packages now use frameless compatibility mode with RPG Reactor title controls, centered startup, and manual maximize/restore behavior to avoid Proton/Wine native-frame artifacts on Linux.
- Replaced native emoji flags in the Options language picker with SVG flag badges so Windows/Chromium shows actual flags instead of `JP`/`EN`-style regional indicator text.

### Fixed

- Fixed playtest launch from final Windows editor builds by resolving a clean NW.js runtime and launching game projects with the project directory as the child process working directory.
- Fixed final Linux editor playtest launch by avoiding contaminated sibling `nw` runtimes when a loose editor `package.nw` is present.
- Fixed macOS editor distribution builds to keep a separate clean `nwjs-mac/nwjs.app` runtime for playtest instead of reusing the editor `.app` bundle.
- Fixed packaged Windows editor taskbar icons by resolving icon files from packaged app paths and embedding the best-fitting entry from multi-size `.ico` files.
- Fixed Windows builds run through Proton/Wine showing a harsh white native client-area/menu band with vertically offset mouse hit-testing.
- Fixed packaged editor startup placement so the splash screen and editor window open centered on screen.
- Fixed Forge launcher tiles losing their themed title/description styling when the generic localization text pass flattened complex button markup.
- Fixed database list rows not updating live while editing an entry name in the detail panel.
- Fixed actor image preview cards overflowing outside the Images section in the database modal.
- Fixed the actor Traits empty row alignment so it no longer protrudes into the indicator gutter.
- Fixed Forge Character Generator imported body sheets being shifted by procedural body-centering; bulk-imported/custom bodies now preserve their authored cell position. Also fixed normal RPG Maker 12x8 sheet detection.
- Fixed Psychronic female Outfit Forge armor generation with female-specific head/torso/shoulders/arms/hands/gauntlet/belt/legs/boots zone masks, female-safe mask coordinates, normalized Forge gender tags, and Zone Edit reload/export support so male bodies are unaffected.
- Replaced deprecated Pixi `cacheAsBitmap` map-editor cache calls with Pixi v8 `cacheAsTexture` calls.
- Improved procedural Outfit Forge pants and boots shading with pants underfill to prevent skin-colored cracks, plus broader natural shadow/light patches on pants and boots instead of dot-like striping.
- Improved procedural Outfit Forge helmet, torso armor, shoulders, and arms with connected metal volume shading while preserving seams, glow accents, and hard bevel details.
- Refined Psychronic helmet rendering with lower female visor/open-face placement, side respirator grill detail, and reduced isolated bright edge artifacts.
- Refined Outfit Forge pants and armor visuals with tighter front pants upper highlights, added Psychronic side-view helmet/torso panel detail, and stronger outer separation strokes for pauldrons and gauntlets.
- Refined Psychronic torso, arm, and helmet armor with structured panel shading and boundary-only outline strokes.
- Refined Psychronic back torso armor so the center highlight continues upward and paired panel lines arc into the shoulders.
- Updated the Nova Sentinel belt default material/accent pairing to gold/gold.
- Added an initial Hair Forge tab with anchor-based procedural hair generation, shared Forge walk-preview playback, live preview, save-to-library support, and generated hair regression coverage.
- Improved Hair Forge output with layered crown clumps, carved part lines, tapered bangs, side locks, and back-view flow strands instead of a single smooth hair mass.
- Refined Hair Forge internal hair seams to use shaded pixels instead of transparent cuts that created noisy black holes after outlining.
- Refined Hair Forge hair patterns with connected mirrored highlight/shadow lanes and exterior-only outlining for cleaner pixel-art flow.
- Refined Hair Forge long hair with a coherent panel overlay that connects crown shading into bangs, side curtains, and back locks.
- Stabilized Hair Forge side-view animation by anchoring crown/root pixels to the body frame and moving only lower hair tips subtly; side-view long hair now hangs from the back of the head with only short face-side bangs.
- Refined Hair Forge bangs and temple areas with larger polished hair panels, stronger side-lock connectors, and continuity smoothing for less sloppy strand patterns.
- Refined Hair Forge side bangs into shorter tapered clumps and filled small enclosed hair gaps so strands read as connected hair instead of blocky panels with holes.
- Refined Hair Forge silhouettes by trimming blocky side-bang faces and tapering/rounding long back-hair curtains for a more natural hair shape.
- Refined Hair Forge long hair with pixel-fur style finishing: scalloped exterior tuft edges plus connected V-shaped highlight and shadow flows.
- Refined Hair Forge tuft details to stay clipped inside the hair mass and added front-view crown/bang flow lines for less blocky bangs.
- Lowered and softened Hair Forge side-view front hairlines with connected tapered tufts instead of a square forehead edge.
- Reworked Hair Forge side-view bangs into swept overlapping locks and relaxed the forehead carve to avoid exposed bald-looking side hairline gaps.
- Refined Hair Forge side-view silhouettes with a forward-swept forelock, broader light/shadow shapes, and a preserved eye window so side eyes remain visible.
- Lowered Hair Forge side-view hair mass slightly while keeping the side eye-window anchored to the real eye line.
- Refined Hair Forge front-view layered hair with wider wavy side curtains, swept bang clusters, and a cleaner face opening based on imported Psychronic reference-hair flow.
- Fixed Hair Forge side-view hair by replacing the rectangular eye cutout with a tapered slit, filling the rear scalp cap, and removing disconnected lower hair islands.
- Fixed Hair Forge side-view bangs so the Bangs checkbox controls the swept forelock, fills the forward forehead area, and visibly changes side frames.
- Fixed Hair Forge side-view outlines and side locks so late side-only hair additions receive exterior strokes, side locks anchor from the sideburn/temple area, and the Side Locks checkbox visibly changes side frames.
- Fixed Hair Forge frame selection so frame 0 previews correctly, and moved hair color swatches into the color dropdown option rows.
- Stabilized Hair Forge side-view hair horizontally while preserving the intended 1px side walk-frame vertical bob and subtle hair-flow variation.
- Increased Hair Forge side/back walk-frame hair flow and tightened front-view eye-only clearing against visible eye pixels so animated bangs do not cover the eyes without cutting a forehead strip.
- Added an explicit anchor-based front-view eye protection zone for Hair Forge so Psychronic female frame 2 outline spikes do not cover the eye without cutting a rectangular bang hole.
- Added Hair Forge Eye Zone controls for front-view hair protection, with X/Y/width/height adjustment and a lower default Y offset for eye placement.
- Updated the default Hair Forge Eye Zone to X 1, Y 7, Width 3, Height 5 based on visual calibration.
- Added Hair Forge Hair Pattern controls for lower-hair banding and scraggle, with smoother default side-view lower hair and tunable shading variety.
- Strengthened Hair Forge Hair Pattern controls so lower banding and scraggle visibly affect front, side, and back lower hair instead of only subtly changing side strands.
- Added a Short Spiky Hair Forge style with raised crown spikes and a shorter side/front/back silhouette.
- Reworked Short Spiky Hair Forge generation into its own all-around spiky cap/fringe/sideburn style, with length scaling longer spikes instead of falling back to layered-bob locks.
- Fixed Short Spiky front hair so it keeps the central face open and uses short angular sideburn spikes instead of a face-covering lower curtain.
- Made Short Spiky more aggressively spiky all around by breaking up the front brow band, side lower mass, and back lower block into jagged spike teeth.
- Simplified Short Spiky into a head-local spiky style by removing lower tendrils/pattern passes and trimming excess side/back length.
- Simplified Short Spiky further into a compact cap/fringe/sideburn shape, removing the aggressive jagged-teeth experiments that made it visually noisy.
- Refined Short Spiky with style-specific front/side/back spike silhouettes, connected back-view spike roots, side-view spiky bangs, removed horizontal ponytail-like side spikes, and Short Spiky-specific triangular texture controls.
- Added a Center Part Long Hair Forge style with orderly long straight strands, a visible middle part, smooth side-view bangs, an open face-framing front silhouette, rounded long back curtain, and subtle walk-frame hair sway.
- Expanded Hair Forge colors with auburn, platinum, rose, violet, navy, and emerald palettes.
- Shifted right-facing Hair Forge side hair slightly back so rear scalp coverage matches the left-facing side view.
- Recalibrated Psychronic female Outfit Forge side-frame zone masks for the updated horizontal body-frame alignment.
- Added explicit eye-line anchor metadata for generated outfit placement without turning eyes into a paint-blocking clothing zone.

## [0.91] - 2026-06-18

RPG Reactor 0.91 expands the editor from English-only/deep-partial UI coverage into a multilingual release candidate, refreshes the Options palette/language UX, documents the current project state for GitHub, and continues the Pixi v8 cleanup.

### Release Highlights

- **Ten editor languages** - English, Japanese, Spanish, Traditional Chinese, Simplified Chinese, Russian, Portuguese, German, French, and Greek are registered in the browser-global i18n manager.
- **Instant language switching**, users can switch language from `File -> Options` or the top-menu language button without restarting the editor.
- **Deep editor localization pass**, database/editor chrome, event editor surfaces, many fixed event-command forms, About, Forge, Audio Player, and common alerts/status messages now route through i18n while project-authored game data remains untouched.
- **Palette dropdown polish**, the Options palette picker now shows color swatches, can overflow outside the modal, and uses theme tokens so open rows stay high-contrast like the Language dropdown.
- **Pixi runtime packaging cleanup**, the bundled Pixi v8 runtime now uses the canonical `runtime/libs/pixi.js` path in runtime, packaging, and tests.

### Documentation

#### Changed

- Refreshed the Character Generator procgen handoff so it points to `outfit_engine.js`, style adapters, `procgen/outfits/nova_sentinel.js`, generated full-outfit outputs, and current Psychronic validation work instead of the older single-style/agent-analysis phase.
- Added a root `CHANGELOG.md` so GitHub visitors can see release progress without opening the editor subfolder.
- Updated root/editor README version text to RPG Reactor 0.91 and refreshed the localization/theming documentation for the current Options UI.

### Localization

#### Added

- Added `src/I18nManager.js` with English, Japanese, Spanish, Traditional Chinese, Simplified Chinese, Russian, Portuguese, German, French, and Greek dictionaries, persisted `rr-settings.language`, `I18n.t(key)`, `data-i18n`/`data-i18n-title`/`data-i18n-aria` DOM binding, exact-text translation for generated editor chrome, mutation observation, and a `rr-language-changed` event for dynamic UI refreshes.
- Added a Language selector to `File -> Options` and a top-menu language button. Switching language applies immediately without restarting the editor.
- Localized the stable UI layer: splash/loading text, menu bar/submenus, toolbar labels and titles, welcome screen, sidebar shell labels, map info static labels, Options modal, About dialog, Forge launcher/sidebar/welcome tiles, Audio Player shell labels, and common project/map status or alert messages.
- Localized database/editor chrome and fixed event editor surfaces including command picker/list/context menus, event page UI, switch/variable pickers, graphic/animation pickers, Battle Test config, traits/effects, and many fixed event-command form labels/options.
- Added event command/category name maps so all supported languages share the same event-command localization path.
- Added `data-rr-i18n-skip` for complex custom controls whose internal DOM should not be rewritten by the generic exact-text pass.

### Tests

#### Added

- Added a lightweight Markdown link test for project documentation files so stale local doc links fail in `npm test`.
- Added i18n tests for supported-language dictionary completeness, language persistence/apply behavior, missing localized-key references, and high-visibility localized labels that should not fall back to English.

### Options and Theming

#### Changed

- Replaced the native Palette `<select>` with a custom swatch dropdown so every palette row can show representative colors consistently across platforms.
- Moved Palette dropdown styling into `css/theme.css` theme-token classes instead of inline computed colors. The closed trigger now matches native select styling, and the open menu rows use native option-style dark/high-contrast surfaces with gold selected/hover states.
- Allowed the Options modal body to overflow visibly so the Palette dropdown can extend outside the modal instead of clipping.

### Runtime and Packaging

#### Changed

- Renamed bundled Pixi runtime from `runtime/libs/pixi8.js` to canonical `runtime/libs/pixi.js`, and updated runtime script loading, editor distribution packaging, and tests to use `js/libs/pixi.js` in generated projects.

#### Fixed

- Removed direct editor shell script tags that pointed at missing `node_modules` Pixi/GIF files.
- Fixed low-risk Pixi v8 deprecation warnings including texture wrap mode, container labels, and text constructor/stroke style usage.

### Forge - Character Generator

#### Changed

- **ASCII gap fill now closes 2-cell and 2x2 dimples reliably** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`) - `RR_CG_fillAsciiPinholes` now runs three passes instead of one and relaxes the 2-3 cell crack rule so each blank cell only needs solid support on one side (above or below for horizontal cracks, left or right for vertical). Added an explicit 2x2 dimple pass that fills small square holes with 7+ surrounding solid pixels. Filling is now on by default for registered style sheets too, only disabled when callers pass `repairAscii: false`.
- **Letter `Z` is now the character outline, not eyebrow** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`, `src/forge/CharacterGenerator/CharacterGenerator.js`), analyzed body sheets quantize the dark silhouette border to `Z`. The renderer now maps `Z` to a near-black outline color (matching `N`) instead of `brow.deep`, so the entire character silhouette no longer renders in the eyebrow color. Eyebrow material moved to `M`, and the importer brow heuristic now retags `T/D` above eye clusters to `M`.

#### Changed (other)

- **Procedural humanoid base restyle** (`src/forge/CharacterGenerator/parts/body/`), replaced the front-only anatomical mannequin bodies with a shared 144×144 anime/pixel-art humanoid base that renders all 4 walking directions and 3 walk frames. The new proportions keep the larger frame for future hair, hats, weapons, and oversized equipment while using a more RPG-sprite-readable head, eyes, limbs, feet, and ground shadow.
- **Procedural male running-template reset** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`), threw out the first scaled-grid mannequin attempt and restarted from a single hand-authored forward-idle pixel template. The new square-one pass isolates a bald/body-only chibi sprite using explicit 48×48 logical-pixel spans so the base silhouette can be judged before walking frames, side/back views, clothing, or hair are reintroduced.
- **Default procedural preview temporarily isolates the body layer**, the adventurer outfit and shaggy hair parts remain registered, but `_buildActiveParts()` now renders only the active body while the base sprite is being rebuilt from the template.

#### Added

- **Template PNG → ASCII analyzer** (`src/forge/CharacterGenerator/CharacterGenerator.js`), added an `Analyze Template PNG...` utility to the procedural Character Generator. It loads an original sprite PNG/JPEG/WebP, auto-crops the first cell of a 3×4 character sheet when possible, preserves the source cell dimensions when they fit inside the 144×144 frame, classifies pixels into the body template's ASCII palette letters, and shows copyable rows for `human_base_shared.js`. This gives us a real pixel-analysis workflow for rebuilding the procedural base from reference art instead of eyeballing screenshots.
- **Full 3×4 ASCII sheet templates** (`src/forge/CharacterGenerator/CharacterGenerator.js`, `src/forge/CharacterGenerator/parts/body/human_base_shared.js`), the analyzer now emits a paste-ready `RR_CG_BODY_TEMPLATE_SHEET[direction][frame]` table when it detects a full walking sheet, and the procedural body renderer selects the correct ASCII cell by current direction/frame.
- **Safer ASCII sheet paste format** (`src/forge/CharacterGenerator/CharacterGenerator.js`, `src/forge/CharacterGenerator/parts/body/human_base_shared.js`), analyzer output now uses `window.RR_CG_BODY_TEMPLATE_SHEET = [...]` instead of a top-level `const`, avoiding redeclaration mistakes and making the paste location less fragile. The renderer accepts both the older const style and the new window assignment.
- **ASCII templates render at native size** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`), body templates now stay at their source ASCII-pixel dimensions inside the configured frame instead of scaling up to fill the frame. Oversized templates still scale down to fit.
- **Configurable procedural frame size** (`src/forge/CharacterGenerator/CharacterGenerator.js`), the Procedural tab now exposes frame width/height controls and uses the configured size for preview, template analysis fit checks, and 3×4 sheet export. Frame size persists through the shared Character Generator config.
- **ASCII template alignment controls** (`src/forge/CharacterGenerator/CharacterGenerator.js`, `src/forge/CharacterGenerator/parts/body/human_base_shared.js`), the Procedural tab now has Align X (Left/Center/Right) and Align Y (Top/Middle/Bottom) controls for positioning native-size ASCII sprites inside the configured frame. Alignment persists in Character Generator config.
- **Drag/drop layer ordering for PNG parts** (`src/forge/CharacterGenerator/CharacterGenerator.js`) - Parts (PNG) category rows are now draggable layers in addition to the existing up/down buttons. Layer order persists per character style.
- **Procedural walking preview**, the Character Generator preview now has Play Walk / Stop Walk controls plus direct frame buttons, so generated characters can be judged in motion before exporting the 3×4 sheet.

#### Fixed

- **Procedural saved sheets had blank side/back rows** because the male/female body parts returned early for every direction except front. Side, back, and walking-frame previews now render for both base body types.
- **ASCII template alignment controls had no effect** (`src/forge/CharacterGenerator/CharacterRenderer.js`) - `CharacterRenderer.resolveParams()` discarded non-schema params, so internal renderer values like `style`, `alignX`, and `alignY` never reached the body renderer. It now preserves user-supplied extra params while still filling schema defaults.
- **ASCII body interpreter now uses expanded material letters** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`), added A-Z semantic color slots for skin, underwear, iris, eye whites, mouth/blush, shadows, and eyebrow-ready letters instead of reusing the same few symbols for unrelated materials.
- **Template analyzer now emits style-specific sheet JS** (`src/forge/CharacterGenerator/CharacterGenerator.js`), generated full-sheet ASCII targets `window.RR_CG_BODY_TEMPLATE_SHEETS[style][variant]` and uses the expanded material palette for quantizing imports.
- **ASCII analyzer now has a material-paint editor** (`src/forge/CharacterGenerator/CharacterGenerator.js`), analyzer results include a zoomed pixel grid, A-Z material palette, full-sheet direction/frame selectors, and live regenerated ASCII/JS so imported sprites can be corrected by painting material letters directly onto pixels.
- **Eyebrow material now uses the picked eyebrow color directly** (`src/forge/CharacterGenerator/parts/body/human_base_shared.js`), remapped `Z` from a very dark derived brow shade to `brow.base`, making eyebrow pixels visibly follow `eyebrowColor` instead of resembling skin/outline colors.

### Theming system

Major theme expansion building on the token foundation shipped in 0.9.0.

#### Added

- **`File → Options...`** menu and OptionsManager modal (`src/OptionsManager.js`) for editor preferences. First section is Appearance (theme picker); future settings categories plug in here.
- **Multi-theme system** with 7 palettes × Dark/Light modes = 14 themes total. Each palette is a self-contained `:root[data-theme="<name>"]` block in `css/theme.css`:
  - **Default** (Gold), original gold-on-black; Light variant uses yellow `#ffe97a` interactive accents on neutral light gray
  - **Bubblegum**, vivid hot-pink (`#ff6fa8` dark / `#c2185b` light) cute palette on pink-tinted surfaces
  - **Ocean**, cool sky-blue (`#5dade2` dark / `#2e86c1` light) on blue-tinted surfaces
  - **Cascadia** - Pacific NW evergreen (`#51cf66` dark / `#2e7d32` light) on green-tinted surfaces
  - **Underworld**, blood-red crimson (`#e74c3c` dark / `#c62828` light) on red-tinted surfaces
  - **Orange Creamsicle**, bright tangerine (`#ff9f43` dark / `#d35400` light); Light variant uses warm cream-white with orange dropdowns matching the namesake
  - **Royalty**, royal purple primary (`#af7ac5` dark / `#8e44ad` light) with deliberate gold trim, borders/dividers stay gold even on purple, hitting the crown-jewel aesthetic
- **`.rr-dark-surface` utility class** (`css/theme.css`), drop-in CSS island that re-declares the dark-theme tokens for any element subtree. Applied to the main map editor canvas (`<div id="canvas-container">`) and the animation editor preview canvas so they stay dark/cinematic regardless of which light theme the user picks.
- **`--color-bg-toolbar` token**, dedicated value for the top menu bar, submenus, and modal header strips (was previously hardcoded `#111111`). Stays `#111` in dark themes, light grays in light themes, themable per palette.
- **Themed textarea resize handle** - `::-webkit-resizer` styled with a gold-on-dark-gold diagonal stripe on `.database-field-value` textareas. No more harsh white system square in the bottom-right corner.

#### Changed

- **Light theme palette refined**, surfaces are now neutral light grays (`#e0e0e0` page, `#ededed` panels, `#ffffff` content), with the brand accent color appearing only on interactive elements (inputs, dropdowns, buttons, hover/selected rows). Visual hierarchy reads gray-chrome → white-content → colored-accent.
- **Notes textareas** (database actor/weapon/armor/item/skill notes) - `resize: vertical` + `box-sizing: border-box; max-width: 100%` so they can't be stretched past the parent card. `white-space: pre; overflow-x: auto` on the note field specifically so plugin notetags stay aligned and scroll horizontally instead of wrapping. Description/profile fields keep normal word-wrap (narrative text).
- **Top menu bar / submenus / modal header strips** migrated from hardcoded `#111111` to `var(--color-bg-toolbar)`. Theme-switchable while staying near-black in dark mode.
- **Map Tree / Quick Access tabs** migrated initial inline styling from hardcoded `#1e1e1e`/`#252526` to theme tokens.
- **`select` element global rule** (`css/theme.css`) now uses `--color-bg-input` instead of `--color-bg-panel` semantically (a select IS an input). Side benefit: dropdowns now have more contrast against panels in dark mode (`#2d2d2d` vs `#111`).
- **Tool button `.active` state** got a stronger visual treatment in light themes (deeper accent fill + accent-deep border + inset shadow) so toggled toolbar buttons like Event Manager read unmistakably as "on". Dark mode behavior unchanged.
- **Theme picker UX**, switched from a flat radio list to Palette dropdown + Mode toggle. Picker stays compact regardless of how many palettes get added; new palettes only need a `THEME_PALETTES` registry entry in `src/OptionsManager.js`.

#### Fixed

- **SelectThemingShim popup couldn't open twice in succession** (`src/utils/SelectThemingShim.js`) - `closeOpenPopup()` removed the popup DOM but left document-level `closeOnOutside`/`escClose`/`scrollClose` listeners attached. After picking an option, the next mousedown event triggered the stale listener which then immediately closed the newly-opened popup. Fix: store the cleanup function on a module-level `openPopupCleanup` ref and call it from `closeOpenPopup()` so listeners always tear down whether the popup closes via option-click, outside-click, escape, or scroll.
- **Hue slider rainbow gradient disappeared in dark mode**, the new global `input[type="range"]` styling for the parameter-curve sliders overrode `.hue-slider` due to higher selector specificity (`input[type="range"]::-webkit-slider-runnable-track` = 0,1,2 vs `.hue-slider::-webkit-…` = 0,1,1). Re-scoped the hue slider rules under `input[type="range"].hue-slider` so they match specificity and source-order win, and explicitly styled `::-webkit-slider-runnable-track` and `::-moz-range-track` with the rainbow gradient.
- **Top menu bar / modal headers were darker** in dark mode after the inline `#111111` migration changed to `--color-bg-menubar` (`#2d2d30`). New `--color-bg-toolbar` token preserves the original `#111` in dark while still being themable for light themes.
- **Toolbar button active state** had no visual difference from hover in light mode (both used bg-deep white + dark border). Light themes now apply a deeper accent fill, accent-deep border, and inset shadow only under `:root[data-theme="<palette>-light"]` scope.
- **Various hardcoded color regressions in About dialog and Audio Player** - `color: #ccc` text was unreadable on light theme bg. Migrated all `color: #ccc;`, `color: #999;`, `color: #aaaaaa`, `color: #cccccc`, `background-color: #1e1e1e/#252526/#3c3c3c/#3e3e42/#2d2d30/#111111`, `border: 1px solid #555/#3e3e42/#2a2a2a` instances in `index.html` to theme tokens.

#### Notes

- **Pattern:** dark-theme defaults stay in the base `:root` block untouched. Every light-mode tweak (sidebar header strip, database section header gold, tool button active treatment, OS popup color-scheme) lives under `:root[data-theme="<palette>-light"]` multi-selectors. Adding a new light palette is: (a) copy a theme block in `theme.css`, (b) append palette to `THEME_PALETTES` registry in `OptionsManager.js`, (c) add the `<palette>-light` name to the four multi-selector lists. See the in-file comments for the canonical pattern.

### Forge - Animation Generator

Major expansion of the in-editor Animation Generator: per-layer keyframes, animated GIF + MP4 textures as input, animated GIF export, three new animations, and a sweep of texture-correctness bugs across the existing 3D shapes.

#### Added

- **Keyframe system per layer** (`src/forge/AnimationGenerator/AnimationGenerator.js`), every animation layer now keeps a `keyframes[]` array with per-frame snapshots of all slider/color/texture params, and `layer.params` is a live reference to the active keyframe. Linear interpolation runs over sliders and RGB colors between keyframes; the params object stays the same shape so existing animation renderers don't need changes. Add / remove / select via the layer panel; legacy single-params layers auto-migrate on load.
- **Smooth texture cross-fade between keyframes**, when two adjacent keyframes use different texture files, the renderer dual-renders the segment (once with the "from" texture, once with the "to" texture on an offscreen) and alpha-blends with `u` = segment progress. Pure linear cross-fade across the keyframe interval.
- **Per-keyframe layer opacity**, the layer-panel opacity slider now writes to the active keyframe instead of the layer as a whole, so opacity can animate alongside the rest of the params. Linear interpolation, same shape as slider params.
- **Layer duplication**, clone the active layer (including its keyframes) via the layer panel.
- **Animated GIF textures** (`src/forge/AnimationGenerator/helpers/texture.js`), texture picker now accepts `.gif` files. Loader parses via `gifuct-js`, composites every frame (handling disposal types 1/2/3) into per-frame canvases, and decorates each canvas with `complete`/`naturalWidth`/`naturalHeight` so existing animation renderers treat each frame as a regular image. `gifFrameAt(gif, tFrac)` picks the frame that should be visible at the current loop fraction; any animation that consumes `_textureImage` animates the texture automatically without per-animation changes.
- **Video textures (MP4/WebM/MOV/M4V/OGV/OGG)**, texture picker now accepts video files too. `_loadVideoTexture` seeks a hidden `<video>` frame-by-frame at 24 FPS (capped at 240 frames / 10 seconds) and captures each frame to its own canvas, returning the same shape as the GIF object. New `animatedFrameAt(tex, tFrac)` is a generic frame picker that handles either format. Decode is async, the cache holds the stub immediately so subsequent lookups don't re-trigger decode; preview/sheet redraw once frames are ready.
- **Save preview as animated GIF**, new "Save GIF" button in the footer renders every preview frame via `gif.js`, writes through an NW.js Save As dialog (defaults to `img/animations/`), and shows encoding progress on the button label. Background transparency preserved via chroma-keying: pixels with alpha < 128 stamp to magenta (`0xFE00FE`), and the encoder is told to mark that exact palette entry transparent.
- **Bullet animation** (`src/forge/AnimationGenerator/animations/Bullet.js`, category Object), proper 14-sided cylindrical body capped by an 8-ring ogive nose (`r(t) = R·√(1 − t²)`). Brass default color, full 3D rotation + texturing.
- **Effect animation category** (`src/forge/AnimationGenerator/registry.js`), new category added to `RR_CATEGORY_NAMES` alongside Geometric / Energy / Object.
- **Hypnotize animation** (`src/forge/AnimationGenerator/animations/Hypnotize.js`, category Effect), per-pixel Archimedean spiral `u = (r/R)·stripeCount − (θ/τ)·twist`. `twist` is consolidated from a separate arm-count + spirality pair into a single integer-step-2 parameter, the seam-safety constraint (twist must be even for the spiral to wrap cleanly at θ = ±π) is now enforced by the param schema so the user can't accidentally produce a mismatched stripe at the discontinuity.
- **Acid Trip animation** (`src/forge/AnimationGenerator/animations/AcidTrip.js`, category Effect) - N-fold mirror-symmetry kaleidoscope with three-harmonic sinusoidal noise for organic flow and HSL colour cycling for psychedelic palette. Integer cycle counts keep the loop seamless. `rotationSpeed` parameter lets the mandala spin.

#### Fixed

- **GIF texture animation didn't pick up keyframe frame index** (`_renderParamsFor`), when a texture param resolves to an animated GIF, the renderer now passes the current `frameIdx` to `gifFrameAt(tex, fIdx / frameCount)` so the GIF's playback timeline tracks the animation's loop fraction. Same path now handles video textures via `animatedFrameAt`.
- **Hypnotize cyan blob in the centre** (`Hypnotize.js`), local variables for the parsed RGB components of `color2` were named `r2`/`g2`/`b2`, shadowing the `r2 = dx² + dy²` squared-distance accumulator used later in the same loop. The shadow turned the spiral colour expression into garbage near the origin, producing a cyan splat. Renamed to `cr1/cg1/cb1` and `cr2/cg2/cb2`.
- **Bullet texture rendering** (`animations/Bullet.js`), three bugs at once. Side quad winding (`[a, b, c, d]`) and tip-fan winding (`[a, b, tip]`) were CW from outside, so backface culling kept the rear of the bullet and dropped the front. Side UVs were `[[0,0],[1,0],[1,1],[0,1]]` on every quad, every segment stretched the entire texture instead of a `1/SIDES` slice. Now `[a, d, c, b]` / `[a, tip, b]`, per-segment U around the cylinder, V along the full bullet length, and the base cap uses radial disc projection.
- **Backface-cull threshold conflated face area with angle** (`src/forge/AnimationGenerator/helpers/shape3D.js`), the cull compared the *unnormalised* cross-product `n_z` against `0.01`. Tiny faces (e.g. the bullet's tip-apex triangles, where `n_z ≈ 0.003` even when facing the camera dead-on) got culled even at proper outward orientation. Now normalises before comparison: `n_z / |n| <= 0.01` is a true ≈cos(89.4°) angular cutoff that ignores face size. Fixes the tip-cap rendering as a side effect.
- **Egg "inverted poles" texture bug** (`animations/Egg.js`), middle-band quad winding `[rs+nxt, rs+lon, rn+lon, rn+nxt]` was CW from outside, so the front half got culled and the rear half rendered (with the UVs reading in the reflected direction). The poles' caps wound correctly, so the eye saw "front-facing poles plus a mirrored body", the visual signature of "inverted poles". Flipped to `[rn+lon, rs+lon, rs+nxt, rn+nxt]`.
- **Egg horizontal texture mirroring** (`animations/Egg.js`), same issue as `Sphere.js`'s `u = 1 - i / meshLon` fix from 0.9.0: the CCW winding wraps U clockwise around the equator, so equirectangular textures (text especially) render reversed. Added `u(lon) = 1 - lon / LON` for all three face groups (top cap, middle, bottom cap).
- **Torus side-quad winding** (`animations/Torus.js`) - `[idx(i, j), idx(ni, j), idx(ni, nj), idx(i, nj)]` was CW from outside. Flipped to `[idx(i, j), idx(i, nj), idx(ni, nj), idx(ni, j)]` so the tube's outward normal points radially out and the camera-facing half stays visible.
- **Coin and Crown side-band winding + UVs** (`animations/Coin.js`, `animations/Crown.js`), both inherited the same CW-from-outside side-quad winding (`[i, SIDES+i, SIDES+n, n]`), and both stamped the full texture on every segment via `stripUV = [[0,0],[1,0],[1,1],[0,1]]`. Now `[i, n, SIDES+n, SIDES+i]` with per-segment `u0..u1 = i/SIDES..(i+1)/SIDES` so a texture wraps once around the rim. Cap UVs upgraded from a flat per-triangle slice to a proper radial disc projection so circular textures (a coin face, a crown gem) render as a disc instead of a tiled wedge fan.
- **Rock middle-ring triangle winding** (`animations/Rock.js`), the two triangles per quad (`[rs+nxt, rs+lon, rn+lon]` and `[rs+nxt, rn+lon, rn+nxt]`) shared the same CW-from-outside winding as the buggy Egg quad, so the rock's front faces were culled and the back rendered. Now fanned CCW from the lower-left vertex: `[rn+lon, rs+lon, rs+nxt]` / `[rn+lon, rs+nxt, rn+nxt]`. (Cap fans were already correct, same convention as the egg's caps.)

#### Notes

- **Animated texture object shape** is now uniform across GIF and video: `{ isGif|isVideo: true, ready: bool, frames: HTMLCanvasElement[], delays: ms[], totalDuration: ms, width, height }`. Frame canvases are decorated with `complete: true`, `naturalWidth`, `naturalHeight` so any animation that checks `tex.complete && tex.naturalWidth > 0` keeps working, the per-frame canvas *is* the texture image for that frame. Frame selection goes through `animatedFrameAt(tex, tFrac)`; the original `gifFrameAt` is preserved for callers that explicitly want the GIF path.
- **Texture-mapping convention for 3D shapes** is documented inline at the top of `helpers/shape3D.js`: face vertex order is CCW from outside; render3DShape builds `n = (vs[1] - vs[0]) × (vs[2] - vs[0])`, normalises, and backface-culls anything with `n_z / |n| <= 0.01`. New shapes should match this. Verified-clean shapes from the audit pass: Cube, Pyramid, Cone, Saw Blade, Dodecahedron (auto-oriented at build time), Mobius / Double Helix / Scythe (`doubleSided: true`, cull bypassed), Sword / Knife / Hammer / Arrow (all composed from the verified `makeBox` / `makeTaperedPrism` / `makeOctahedron` helpers).

## [0.9.0] - 2026-05-31

Editor polish + completion of the PIXI v5→v8 migration. Theme tokens shipped across the entire UI; new Parameter Curve and Terms.Messages editors; actor equipment dynamically filtered by class traits; map editor parallax bleed eliminated via canvas crop; v8 migration functionally complete (Tilemap, UltraMode7, Effekseer, video, cursors).

### Added

- **Theme token system** (`css/theme.css`), single source of truth for colors, spacing, radii, fonts. Every editor and `src/**/*.js` migrated to `var(--token)` references; ~5500 token usages across the codebase. Foundation for future theme switching.
- **`src/utils/ThemeColors.js`** - `ThemeColors.resolve(name, fallback)` helper for canvas 2D contexts (which can't parse `var(--…)`). Used by class parameter curves, animation editor placeholders, icon-picker highlight, troop selection highlight, transfer-player map thumbnails.
- **`src/utils/SelectThemingShim.js`**, global custom-dropdown shim. Wraps every native `<select>` with a div-based gold-themed popup to defeat KDE Plasma's pale-blue option highlight. `MutationObserver` catches dynamically-added selects. Opt-out via `data-no-shim="1"`.
- **Range slider theme rules** (`css/theme.css`), dark track + gold-accent thumb across `-webkit-` and `-moz-` variants. Applied globally to `input[type="range"]`.
- **Parameter Curve editor** (`src/database/DatabaseClassEditor.js`), click any of the 8 parameter mini-curves in the class editor → modal with Lv1/Lv99 sliders + curve shape exponent (0.30 fast → 3.00 slow) + live preview canvas + reverse-fit so slider opens at the existing curve's shape. Apply writes to `classEntry.params[paramIdx]` and refreshes both the mini-curve and the Lv1→Lv99 readout.
- **Terms → Messages editor** (`src/DatabaseEditorUI.js`), fourth Terms category covering all 53 MZ message strings (Options menu, Audio Volume, Save/Load, Battle Flow, Battle Damage, Battle Effects), each with a per-field placeholder hint explaining what each `%N` becomes in plain English (e.g. Actor Damage shows `%1 = actor name, %2 = damage amount`).
- **Animation editor polish** (`src/database/DatabaseAnimationEditor.js`), frame multi-select + clipboard + Delete; SE & Flash Timings selection/clipboard with max-height; custom gold dropdowns for Position/Background/Target; hue slider with real-time sprite-sheet preview; cell properties modal restyle; Display Type dropdown with correct MZ semantics.
- **Database editor polish** - Types and Terms editor complete redesign (`src/DatabaseEditorUI.js`); Items general section tight grid layout (`src/database/DatabaseItemEditor.js`); System2 Game ID editable; "No effects/No traits" rows now align with table headers in Items, Skills, Weapons, Armor, Enemies, States.
- **Actor equipment slot filtering** (`src/database/DatabaseActorEditor.js`), equipment slots and dropdown options now derived from `TRAIT_EQUIP_WTYPE` (51), `TRAIT_EQUIP_ATYPE` (52), and `TRAIT_EQUIP_SEAL` (54) on the actor + class union. Unnamed `equipTypes` slots are hidden; stale equipped items surface as `(incompatible)`; section header shows the source class.
- **Chip-style button utility classes** (`css/theme.css`) - `.rr-btn-chip` (black with gray text, gold hover) and `.rr-btn-chip-danger` (red hover) for the safe-vs-destructive convention used across the app.

### Fixed

- **Effekseer animation 3D rotation sphere not rendering** (`src/database/DatabaseAnimationEditor.js`) - Canvas 2D's `fillStyle`/`strokeStyle`/`addColorStop` silently reject `var(--token)` strings. The bulk theme migration had replaced hex literals with `var(--…)` even inside canvas calls. Sphere now resolves tokens via `ThemeColors.resolve(...)`. Same bug fixed in 7 other files (animation preview placeholders, class graph fill+stroke, troop+icon-picker highlight, transfer-player map thumbnails, event command list face placeholder).
- **Class editor parameter curves only rendering 3 of 8** (`src/database/DatabaseClassEditor.js`) - Defense's entry in `paramColors` used `'var(--color-accent-bright)'`. `gradient.addColorStop(0, color + '80')` threw `SYNTAX_ERR`, the `forEach` aborted, and curves 4-7 never drew. Replaced with hex literal; all 8 curves render.
- **Map editor parallax bleed around small maps** (`src/TilemapManager.js`), when the map was smaller than `#canvas-container` on either axis, the parallax sprite filled the leftover area. New `applyViewportCrop()` resizes the PIXI renderer to `min(containerSize, mapScaledSize)` after every scale change (load, wheel zoom, resize). Scale floor stays at contain-fit so zoom-out still reaches whole-map view.
- **Map editor zoom didn't reset on map switch** (`src/TilemapManager.js`), loading a new map created a fresh container at scale 1.0 but no fit-to-viewport logic kicked in, leaving small maps with empty space around them. Added `applyMinScaleClamp()` to `loadMap` (and reused by the resize handler).
- **MOG_TreasurePopup pickup icons invisible on PIXI v8** (`runtime/libs/pixi_compat.js`), plugin uses `this._cx/_sx/_cy/_sy` as screen-coordinate instance data, colliding with v8's internal cos/sin transform cache. Override of `Container.prototype.updateLocalTransform` computes cos/sin fresh from rotation+skew per call instead of reading the cache fields. Same root cause as the `_position` MOG_BattleCursor bug.
- **`Function.name` was "wrapped" for every MZ class** (`runtime/libs/pixi_compat.js`) - `MZGlobalUpgrade`'s wrapper function was named `wrapped`, and the property-copy loop explicitly skipped `name`. Plugins doing `instance.constructor.name === "Scene_Map"` got false. Now `Object.defineProperty(wrapped, "name", {value: orig.name, configurable: true})` after the `__origName` tag.
- **"Will be implemented" alert for 11 no-args event commands** (`src/event/EventCommandList.js`) - `editCommand`'s fall-through case fired for Exit Event, Get on/off Vehicle, Erase Event, Gather Followers, Save BGM, Replay BGM, Abort Battle, Open Menu Screen, Open Save Screen, Game Over, Return to Title Screen. New `NO_PARAM_EVENT_CODES` set short-circuits with a silent return, these commands have no editable parameters.
- **MZ animation additive blending rendered as normal blend** (`reactor_core.js`), see "PIXI v8 visual-fidelity fixes" section below.

### PIXI v8 visual-fidelity fixes (2026-05-30 session 5)

Three v8 regressions uncovered during gameplay testing, all with different root causes:

#### Fixed

- **RaveLighting hole-punching with positive (white) tint** (`PSYCHRONIC_RaveLighting.js`): when tint is full-white (no darkening), lights would cut light-shaped windows in the wall of white, exposing the scene through them. Logically wrong: hole-punching only makes sense as a darkness-piercing effect. Now gated on `hasNegativeChannel || hasGrayDarkening`, overlay still draws for positive tints (color tint visible) but the `destination-out` punch-out is skipped. Lights still brighten via their own ADD-blend sprites.
- **MZ animation additive blending rendered as normal blend** (`reactor_core.js` Sprite.blendMode setter): MZ data files (animations, plugins) store v5-style numeric blend modes (0=normal, 1=add, 2=multiply, 3=screen). v8 expects strings ('add', 'multiply', 'screen', 'normal'). Our setter forwarded numbers directly to v8's blendMode setter, which silently falls back to normal blending, killing the additive-glow look (visually reads as "no transparency" because additive blends naturally fade-edge into the scene whereas normal blends look solid). Added a `_MZ_BLEND_NUM_TO_STR` mapping in the setter so numbers are converted to strings before forwarding.
- **Window backgrounds rendering near-transparent** (`reactor_core.js` `_createBackSprite`): earlier migration changed `_backSprite` from `new Sprite()` to `new PIXI.Container()` to dodge v8's "Sprite can't have children" deprecation. But Container has no texture rendering of its own, so the windowskin's solid 95×95 corner-piece background was invisible, leaving only the (mostly transparent) TilingSprite tile pattern showing. Reverted to `new Sprite()` now that the pixi_compat `Sprite.allowChildren=true` shim makes Sprite-as-parent render its children correctly.
- **Windows didn't clip overflowing content (KEY FIX)** (`reactor_core.js` Window/Tilemap/TilingSprite `updateTransform`): the actual root cause was a **silent throw**. v8 repurposed `PIXI.Container.prototype.updateTransform` from a per-frame transform-cascade method into a property-setter that takes an `opts` object, and reads `opts.x` etc. unconditionally, so calling it with no args throws `TypeError: Cannot read properties of undefined (reading 'x')`. MZ corescript's `Window.prototype.updateTransform` does `PIXI.Container.prototype.updateTransform.call(this)` near the end, RIGHT BEFORE `this._updateFilterArea()`. On v8 the super call throws, the throw bubbles up through the onRender wrapper's try/catch, and `_updateFilterArea` is never reached, meaning the window's filterArea is never sized to the visible client rect, and no clipping happens. Fix: skip the super updateTransform on v8 (v8's render pipeline computes worldTransform automatically). Same pattern fixed in `Tilemap.prototype.updateTransform` and `TilingSprite.prototype.updateTransform`.

#### Note

The window-clipping bug is the worst kind: v8 silently changed the SEMANTICS of a method that still exists on the prototype. Stack traces don't surface it because our `onRender` bridge catches the throw. The fix took several wrong turns (filterArea coord-space, boundsArea, Sprite/Graphics mask experiments) before diagnostic logging proved that `_updateFilterArea` was never running.

### PIXI v8 runtime compatibility (2026-05-30 session 4)

Deprecation cleanup + several v8 robustness issues uncovered through gameplay testing.

#### Fixed, deprecation warnings (real fixes, not suppression)

- **`PIXI.SCALE_MODES` / `PIXI.WRAP_MODES` / `PIXI.DRAW_MODES` deprecation spam**, v8 ships these as `Proxy` objects that log on every read (and the message text is itself buggy: v8's SCALE_MODES proxy says "DRAW_MODES.X is deprecated"). Shim now force-overrides them with plain objects holding the same string values on v8. Not just suppression, when v9 removes the proxies, our shim continues providing the constants so corescript and plugins keep working.
- **`Graphics#beginFill` / `Graphics#drawRect` deprecation in `ScreenSprite.setColor`** (reactor_core.js:3796) - Switched to v8's chained `graphics.rect(...).fill({color, alpha})` API with polymorphic fallback to the v5/v6/v7 imperative form.
- **`renderer.render(stage, renderTexture)` second-arg deprecation in `Bitmap.snap`** (reactor_core.js:1457) - Switched to v8's `renderer.render({container, target})` options-object with polymorphic fallback.
- **`WindowLayer.render` throwing `renderer.framebuffer.forceStencil is not a function`** (reactor_core.js:4636), v8 removed `forceStencil` (stencil buffer is always allocated now via `stencil: true` in ContextSystem init) and removed the global `renderer.batch` flushable batcher (replaced by per-render-pipe deferred batchers). Raw GL stencil ops interleaved with v8's deferred pipeline don't honor draw order. On v8 the method now short-circuits and lets v8's render pipe iterate children naturally; the only thing lost is window-occlusion stenciling (lower-window pixels under a higher window get drawn but are immediately overdrawn, no visible regression for normal MZ usage). v5/v6/v7 keep the stock stencil-occlusion behavior.

#### Added - `template/*/js/libs/pixi_compat.js`

- **`PIXI.Sprite.prototype.destroy` idempotency patch**, v8 Sprite.destroy has a bug: `super.destroy(options)` short-circuits on already-destroyed but the code BELOW super still runs `this._texture.destroy(...)`, which throws because `_texture` was nulled on the first destroy. This crashes cascading destroys whenever a sprite was already destroyed by a plugin (e.g. `PSYCHRONIC_GifAnimationMZ.stopGifAnimation`) but somehow remained in a parent's children array. Shim early-returns if `this.destroyed` is true.
- **`PIXI.VideoSource.prototype.isValid` null-safe patch**, v8 VideoSource.load() has a race: `const source = this.resource; ...; this.alphaMode = await detectVideoAlphaMode(); this._load = new Promise((r) => { if (this.isValid) ... })`. If `Scene_Map.terminate -> VideoOverlayManager.clearAll` destroys the source during the await, `this.resource` is null when isValid resumes, and `this.resource.videoWidth` throws an uncaught Promise rejection that the MZ SceneManager surfaces as fatal. Shim makes isValid return false when resource is null.
- **`SpritePipe.{addRenderable, updateRenderable, validateRenderable}` destroyed-sprite guards**, when a destroyed Sprite (`_gpuData == null`) is still referenced by some parent's children array, v8 crashes at `_getGpuSprite` reading `null[uid]`. Shim catches and skips, logging once per offending class with parent-chain trace ("class=Sprite, parents=Tilemap < Container < Spriteset_Map < Scene_Map").
- **`Container._getGlobalBoundsRecursive` destroyed-display guard**, same pattern hit via FilterSystem._calculateFilterArea → getFastGlobalBounds, where it reads `this.effects.length` on a destroyed container (effects is null after destroy). Shim returns early.
- **`MZGlobalUpgrade` now tags wrapped classes with `__origName`** so the destroyed-sprite warnings can identify which MZ class an instance came from (instead of every wrapped instance showing as `wrapped` in `constructor.name`).

#### Fixed - `UltraMode7V8Compat` composite Sprite cascade-destroy

- **Mode7 → Mode7 scene transition left the new map without its tileset layer.** When the previous Spriteset_Map's tilemap is destroyed (with `{children: true}` cascading), our UM7V8 composite Sprite (living as `tilemap.children[0]`) is destroyed too. The lazy init only ran once per session, so on the new tilemap `ensureCompositeSprite` re-attached the already-destroyed Sprite instead of creating a new one, leading to no Mode7 ground rendering and per-frame destroyed-sprite warnings. Shim now detects `compositeSprite.destroyed` / `pixiTexture.destroyed` and re-creates both.

### PIXI v8 runtime compatibility (2026-05-30 session 3)

Fixed `PSYCHRONIC_VideoOverlay` and `PSYCHRONIC_VideoParallaxMZ` not playing video on v8. Both plugins use `PIXI.Texture.from(htmlVideoElement)` and apply post-construction property writes (`.scaleMode`, `.mipmap`, `.autoUpdate`, `.wrapMode`) on `.baseTexture`, the read-only proxy from session 1 silently dropped those writes, and v8's Sprite never re-evaluated `width`/`height` when the video metadata arrived (texture orig 1×1 → 1280×720).

#### Added - `template/*/js/libs/pixi_compat.js`

- **`PIXI.MIPMAP_MODES` shim**, maps v5 numeric enum to v8 boolean (`OFF: false`, `POW2/ON/ON_MANUAL: true`). Pairs with the new mipmap setter on the baseTexture proxy so legacy `videoTexture.baseTexture.mipmap = PIXI.MIPMAP_MODES.OFF` actually disables `source.autoGenerateMipmaps`.
- **Setters on the `baseTexture` proxy**, added `set scaleMode`, `set mipmap` (→ `autoGenerateMipmaps`), `get/set wrapMode`, `get/set autoUpdate` that forward to the underlying v8 `TextureSource`. Previously the proxy was read-only, so plugin lines like `videoTexture.baseTexture.scaleMode = PIXI.SCALE_MODES.NEAREST` and `videoTexture.baseTexture.autoUpdate = false` landed on a disposable object and were lost, the underlying VideoSource kept defaults.
- **`PIXI.Texture.from(HTMLVideoElement)` override**, intercepts HTMLVideoElement specifically and constructs `VideoSource` explicitly with `autoLoad: true, autoPlay: false, updateFPS: 0`, then wraps in `new Texture({source: videoSource, dynamic: true})`. Two reasons:
  - **`autoPlay: false`**, prevents VideoSource from racing the plugin's own `video.play()` call (default `autoPlay: true` produced "play() interrupted by load()" patterns).
  - **`dynamic: true`**, without this flag, v8's Sprite `set texture` does NOT attach an `"update"` listener on the texture, so the sprite never reacts to texture changes (including the eventual video resize). Set explicitly because `Texture.from`'s default path leaves `dynamic: false`.
- **Sprite dynamic-texture resize re-apply (KEY FIX)**, wraps `Sprite.prototype.set texture` to attach a per-sprite `"update"` listener that re-runs `_setWidth(_width, texture.orig.width)` / `_setHeight(_height, texture.orig.height)` whenever the underlying source resizes.
  - v8 Sprite's stock texture setter re-applies `_width`/`_height` ONCE at assignment time. When the texture object stays the same but its `orig.width` later changes (VideoSource learning `videoWidth`/`videoHeight` from metadata), the stock setter does nothing, only invalidates bounds.
  - Plugins that do `const sprite = new Sprite(videoTexture); sprite.width = Graphics.width;` bake `scale.x = 816/1 = 816` while the video texture is still 1×1. When VideoSource later resizes to 1280×720, the sprite renders at `816 × 1280 = ~1,000,000` px wide, essentially invisible (one corner pixel fills the screen, looks like a solid black/blank fill).
  - Shim attaches a tracked listener only on `dynamic: true` textures, so static image sprites pay nothing. Listener detaches cleanly when the texture is replaced.

### Fixed

- **`PSYCHRONIC_VideoOverlay` videos invisible on v8**, video element loads and plays correctly, but sprite scale was baked to 1×1 texture causing the rendered video to extend far off-screen. Sprite re-apply patch + dynamic texture flag resolves it.
- **`PSYCHRONIC_VideoParallaxMZ` videos invisible on v8**, same root cause.

### PIXI v8 runtime compatibility (2026-05-30 session 2)

Continued v8 work after the Effekseer + cursor fixes earlier in the day: fixed UltraMode7 perspective rendering, fixed tile-edge seams on regular maps.

#### Added - `template/*/js/libs/pixi_compat.js`

- **`UltraMode7V8Compat` module (~330 lines)**, full v8 compat for UltraMode7's Mode7 perspective tilemap rendering. v8 removed the renderer-plugin system that UM7 depended on; this module sidesteps that by mirroring the Effekseer overlay pattern (offscreen `<canvas>` with its own WebGL1 context):
  - Lazy-initializes when first Mode7 layer renders. Creates a hidden offscreen canvas matching game canvas size, gets `webgl` context with `{premultipliedAlpha: false, alpha: true}`.
  - Compiles UM7's vertex + fragment shaders (`Tilemap.ULTRA_MODE_7_VERTEX_SHADER` + `UltraMode7.generateFragmentShader(...)`) against that context. Prepends `precision mediump float; precision mediump int;` to satisfy WebGL1's mandatory precision declaration (PIXI used to auto-prefix).
  - Creates 4 × 2048×2048 GL atlas textures with `LINEAR` filtering (matches UM7's `TILEMAP_PIXELATED=false` default). Tile-edge bleed prevented by UM7's fragment shader `clamp` against `vFrame`.
  - Enables `OES_element_index_uint` extension. Required because Mode7 maps with `LOOP_MAPS_EXTEND_TILES` commonly have 40k+ tiles (160k+ vertices), without 32-bit indices, indices wrap at 65535 and `drawElements` renders garbage.
  - Replicates UM7's per-segment vertex/index buffer pipeline; raw GL `bufferData` / `vertexAttribPointer` / `drawElements`.
  - Hooks `Tilemap.Layer.prototype.render` after UM7 installs its own override: when `UltraMode7.isActive()` is true, queues the layer for offscreen rendering. Otherwise falls through to base render (the per-tile-Sprite path).
  - Patches `Tilemap.Layer.prototype.initialize` to install per-instance `onRender` callbacks. `MZGlobalUpgrade`'s window-scan misses `Tilemap.Layer` because it's a sub-property of `Tilemap`, without an `onRender`, v8 never calls `render()` on Layer instances per-frame and our hook never fires.
  - `findTilemap(layer)` walks up the parent chain to find the actual `Tilemap` (skipping intermediate `Tilemap.CombinedLayer` etc.) so the composite Sprite attaches to the right level, adding to an intermediate container caused parent-transform distortion.
  - Composite Sprite (wrapping the offscreen canvas) added as `tilemap.children[0]`. Set to `scale.y = -1, position.y = canvas.height` to flip the WebGL1-bottom-left-origin perspective right-side-up.
  - Per-frame flush clears the offscreen canvas to transparent, drains the layer queue, calls `pixiTexture.source.update()` to invalidate v8's GPU upload cache.
  - Debug surface at `window.__UM7V8` (getCanvas, getGL, getAtlases, getPendingLayers, getCompositeSprite, snapshot, isInitialized) for diagnostic work.

#### Changed - `template/*/js/reactor_core.js`

- **`Tilemap.Layer._addV8Tile` tile source `scaleMode: "nearest"`**, previously defaulted to LINEAR, causing visible vertical seam artifacts between adjacent tiles on certain tilesets (sub-texel interpolation across tileset image boundaries pulled in adjacent tile pixels). Original MZ `Tilemap.Renderer._createInternalTextures` hardcoded NEAREST for the same reason.

### Fixed

- **UltraMode7 perspective tilemap not rendering on v8**, fully resolved via `UltraMode7V8Compat`. Mode7 maps now render correctly with proper perspective, characters layer on top, atlas textures sample correctly, no distortion.
- **Visible vertical seams between tiles on regular (non-Mode7) maps**, caused by LINEAR sampling on tile textures. Switched to NEAREST.

### PIXI v8 runtime compatibility (2026-05-30 session)

Continued v7→v8 migration: fixed Effekseer animations not rendering in battle, and fixed MOG_BattleCursor not displaying. Both came down to v8 internals that legacy code paths quietly corrupted in ways that produced no errors but no pixels either.

#### Added - `template/*/js/libs/pixi_compat.js`

- **`PIXI.Container.prototype._position` accessor (KEY FIX)**, v8 stores its internal position `ObservablePoint` in `this._position` (pixi.js line 7129). Some MZ plugins (notably MOG_BattleCursor's `BattleCursorSprite` constructor: `this._position = {}; this._position.x = 0; ...`) use `_position` as a custom data-struct field name, REPLACING v8's observable with a plain object. After that, `sprite.x = N` writes to a plain field instead of the ObservablePoint, so v8's transform-dirty notification never fires and the sprite's `localTransform` stays `NaN`. v8's renderer culls anything with NaN transforms, so the entire subtree silently disappears.
  - Shim installs an accessor on `Container.prototype._position`. The setter captures the `ObservablePoint` when v8's ctor assigns one (stashed as `__pixiPositionObservable`). When a plain object is later assigned, x/y values are copied into the saved observable and `_onUpdate` is triggered. Subsequent reads return the observable. Plugin-side `this._position.xOffset = 5` etc. still works, extra keys are added as own properties on the observable.
  - Backwards-compatible with any plugin using the same `_position` clobber pattern, not just MOG_BattleCursor. **No plugin file edits required.**
- **`PIXI.Sprite.prototype.allowChildren` getter**, always returns `true`, swallows the constructor's `this.allowChildren = false` assignment. Suppresses v8's "addChild on a Sprite" deprecation warning spam. (v8 still iterates Sprite children in `collectRenderablesSimple` regardless of `allowChildren`, so this is purely a warning fix.)

#### Changed - `template/*/js/reactor_core.js` (Effekseer overlay canvas)

- **Dedicated Effekseer overlay canvas with its own WebGL1 context.** Effekseer's `drawHandle` was silently producing zero pixels on v8's WebGL2 context, verified empirically via full-canvas `gl.readPixels` diff (zero pixels changed across 921600 samples) with no GL errors. The proven-good editor pattern (`src/event/AnimationPicker.js`) creates a fresh canvas with `getContext('webgl', { premultipliedAlpha: false, alpha: true/false })` and inits Effekseer against THAT context.
  - `Graphics._createCanvas` now creates an `effekseerOverlay` `<canvas>` alongside `gameCanvas`. Transparent, `pointer-events: none`, z-index 2 (above game canvas at z-index 1).
  - `Graphics._updateCanvas` keeps both canvases sized and centered identically via `_centerElement`.
  - `Graphics._createEffekseerContext` obtains a WebGL1 context on the overlay (`premultipliedAlpha: false, alpha: true`) and inits Effekseer against it. `setRestorationOfStatesFlag(false)` - Effekseer owns the overlay context, no state to restore.
  - The browser compositor naturally layers the overlay over the game canvas. No PIXI integration needed.

#### Changed - `template/*/js/reactor_sprites.js` (Sprite_Animation positioning)

- **`Sprite_Animation.setProjectionMatrix`** rewritten to use AnimationPicker's recipe (`p = -1.2`) instead of MZ's `p = -(viewportSize / canvas.height) = -5.69`. The MZ original combined a 4096×4096 offset viewport with the steep perspective to position effects on screen, that approach silently produced zero pixels through Effekseer's WebGL1 wrapper on the overlay context.
- **`Sprite_Animation.setViewport` / `resetViewport`** use the full canvas viewport now (instead of a 4096×4096 viewport offset by target position).
- **`Sprite_Animation.updateEffectGeometry`** positions effects via `handle.setLocation(wx, wy, 0)` using a canvas-pixel-to-world conversion (`wFactor = 13` = `1 - 10 × p`). Mirror flag flips `wx` sign.
- **`Sprite_Animation` 180° x-axis flip** - `rx = (180 - rotation.x) * π/180` compensates for the projection's y-flip (matches AnimationPicker). Without it, particles designed to fly "up" appeared to fly "down" on screen.
- **`Sprite_Animation.targetSpritePosition`** anchors unconditionally to sprite ORIGIN (`point.y = 0`) instead of center (`-h/2`). MZ vanilla center-anchor put effects approximately `h/2` pixels too high through the AnimationPicker projection.
- **`Sprite_Animation.renderActive`** clears the Effekseer overlay canvas EVERY frame (transparent black) regardless of whether animations are queued. Otherwise leftover Effekseer pixels persist between animations and block view of game-canvas content underneath (e.g., MOG_BattleCursor).

### Fixed

- **Effekseer battle animations not rendering**, fully resolved via the overlay canvas + AnimationPicker recipe described above. Animations now appear at the target battler, fire in the correct direction, and don't bleed into UI between plays.
- **MOG_BattleCursor target arrow + battler name not displaying**, root cause was the `_position` clobber documented above. Cursors are now visible during target selection.
- **Massive console log spam from PSYCHRONIC_* plugins**, silenced per-action repeaters (~109 lines across `PSYCHRONIC_BattleEngineMZ.js`, `PSYCHRONIC_MegaOptionsMZ.js`, `PSYCHRONIC_CoreCustomizerMZ.js`, `PSYCHRONIC_MegaEquipMZ.js`, `PSYCHRONIC_SubclassMZ.js`, `PSYCHRONIC_MenuManagerMZ.js`, `PSYCHRONIC_EventCustomizerMZ.js`). All silenced lines marked with `// [silenced]` prefix for easy restoration. Per-action logs that remain useful for debugging (`🎬 UnifiedAnimationHandler`, `✅ Playing animation`, `🎯 execute: Invoking action.`) were preserved.

### PIXI v8 runtime compatibility (2026-05-25 session)

Follow-up runtime work on the v7→v8 migration: with the bundle and corescript already converted, the focus was making actual gameplay (battles, menus, lighting plugin, in-game scenes) render correctly against v8.

#### Added - `template/*/js/libs/pixi_compat.js`

- **`baseTexture` getter on `PIXI.Texture.prototype`**, force-overrides v8's deprecation getter (which returned the v8 `TextureSource` directly, making `texture.baseTexture.resource.source` resolve to `undefined`). Returns a v5/v6/v7-shaped shim with `.resource.source` pointing at the raw canvas/image.
- **`valid` getter on `PIXI.Texture.prototype`**, v8 removed `Texture.valid`. Plugins like `PSYCHRONIC_RaveLighting` use it as the "did my texture build correctly?" gate; without it every legitimate texture was silently swapped for `PIXI.Texture.WHITE`.
- **Container `name` deprecation accessor deletion**, v8's `set name(value)` on `Container.prototype` was intercepting MZ corescript prototype assignments (`Sprite_Name.prototype.name = function() {...}`) and rerouting them into `label`, silently losing the method. Deleted the accessor before corescript loads.
- **`PIXI.Buffer` constructor wrapper**, v8 switched from positional `(data, isStatic, isIndex)` to `{data, size, usage, ...}` options object. UltraMode7's `Tilemap.Layer._createVao` calls `new PIXI.Buffer(null, true, true)` and crashed on `destructure of null`. Wrapper detects legacy positional signature and converts to v8 options with `BufferUsage.INDEX|VERTEX|COPY_DST|STATIC` flags.
- **`PIXI.Geometry.addIndex/addAttribute` chain wrappers**, v8 dropped `return this` from both methods, breaking the `geometry.addIndex(idx).addAttribute(...).addAttribute(...)` chain pattern. Wrappers restore chaining. `addAttribute` also accepts the v5/v6/v7 8-positional form `(name, buffer, size, normalized, type, stride, start, instance)` and converts to v8's options object with size+type → format-string mapping (e.g. `FLOAT × 4 → "float32x4"`).
- **`window.installLegacyRendererStubs(renderer)`**, augments v8's `WebGLRenderer` instance (called from `reactor_core.js` after `await app.init()`) with the legacy `renderer.batch / .geometry / .state / .shader / .framebuffer / .projection / .view` subsystems as no-op stubs. `batch.flush()` is the exception, it bridges to v8's `renderTarget.finishRenderPass()` so pending v8 batches are committed before plugins like Sprite_Animation start raw GL drawing.
- **MZGlobalUpgrade label-shadow delete**, after `Reflect.construct(pixiBase, [], wrapped)`, walks the v8 instance's own keys and deletes any whose name corresponds to a prototype method on the MZ subclass. v8's `Sprite` ctor hardcodes `this.label = "Sprite"` as an own data property, which was shadowing `Sprite_Gauge.prototype.label()` and breaking battle status windows with `this.label is not a function`.
- **MZGlobalUpgrade onRender bridge**, for any wrapped MZ class whose prototype defines its own `_render` or `render` (and the latter differs from the PIXI base), installs a per-instance `onRender(renderer)` callback that v8 invokes per-frame. Bridges the legacy v5/v6/v7 dispatch pattern to v8's render pipe. Errors are logged once-per-class instead of silently swallowed.
- **Advanced blend mode registration**, registered `multiply`, `screen`, and `overlay` via `PIXI.BlendModeFilter` + `PIXI.extensions.add` so MZ plugins that set `sprite.blendMode = PIXI.BLEND_MODES.MULTIPLY` don't silently fall through to `'normal'`. Uses the official PIXI v8 advanced-blend-modes formulas. (Eventually deprecated in favor of the alpha-composited tone overlay, see below.)

#### Added - `template/*/js/reactor_core.js`

- **Post-`_app.render()` Effekseer flush hook**, calls `Sprite_Animation.renderActive(renderer)` right after `_app.render()` returns, so legacy Effekseer draws happen on top of v8's already-rendered scene instead of being clobbered by v8's batched output.

#### Added - `template/*/js/reactor_sprites.js`

- **`Sprite_Animation._pendingRenders` queue**, on v8, `_render` queues the instance instead of drawing inline. `Sprite_Animation.renderActive(renderer)` drains the queue after v8 finishes its render pass.

### Fixed

- **`PSYCHRONIC_RaveLighting` lighting plugin**, completely reworked for v8:
  - Tone overlay switched from `MULTIPLY` blend (incompatible with v8's advanced blend mode filter chain inside the spriteset's color filter) to plain alpha-composited overlay. Tint color and alpha are now derived directly from `$gameScreen.tone()` magnitude: `[0,0,0,0]` → transparent, `[-255,-255,-255,0]` → opaque black, `[255,0,0,0]` → opaque red, etc.
  - `updateBaseFilters` now zeroes the spriteset's `_baseColorFilter` (`setColorTone([0,0,0,0])`) on every frame so the spriteset filter doesn't double-tint on top of the new overlay.
  - Texture-cache self-heal: `_isCachedTextureAlive(texture)` validates the cached radial/beam/flashlight texture's source is still a real `HTMLCanvasElement` before reusing it. After v8 destroys a `TextureSource`, its `.resource` becomes `null` and `drawImage` throws, the cache now silently rebuilds stale entries on next access.
  - `Spriteset_Map.destroy` clears `PsychronicRaveLighting.textureCache` so cross-scene destroys don't leak stale Texture references into the next map.
- **Battle scene crash** - `this.name is not a function` in `Sprite_Name.updateBitmap` and the follow-up `this.label is not a function` in `Sprite_Gauge.drawLabel`. Two distinct v8 prototype-pollution bugs (setter interception during prototype definition vs. instance own-property shadowing), see the two corresponding compat-layer items above.
- **UltraMode7 scene crash on map load** - `Cannot destructure property 'data' of 'options' as it is null` in `new Buffer(null, true, true)`, then `Cannot read properties of undefined (reading 'addAttribute')` from the broken `addIndex().addAttribute()` chain. Both fixed by the new PIXI.Buffer and PIXI.Geometry compat wrappers.
- **`Sprite_Name` `this.name is not a function`** in load-game menus, v8's `Container.prototype.name` setter was intercepting `Sprite_Name.prototype.name = function() {...}` and routing the function into `label` instead.
- **Window-layer GL state corruption**, removing the early `framebuffer.forceStencil` stub lets `WindowLayer.render` naturally throw early (caught by the onRender bridge) before its raw GL stencil-test calls corrupt v8's batched state. Window backgrounds, ATB bars, and other layered windows render correctly again.
- **`Texture.baseTexture.resource.source` returning `undefined`** in `RaveLighting`'s flashlight/beam draw paths, fixed by the new force-override `baseTexture` getter.

### Known issues

- **Parallax tile-edge fringing in some intro scenes**, separate UV/scaleMode issue.

## [0.8.0] - 2026-05-24

### Added
- **Multi-instance workflow**: The Linux launcher now starts each RPG Reactor window with its own NW.js user-data profile, allowing multiple projects to be open at the same time while a per-project `.rpgreactor.lock` prevents opening the same project twice.
- **Cross-instance clipboard**: Added shared typed clipboard support for events, event pages, event command selections, maps, and plugin JSON/settings so copy/paste works across RPG Reactor windows.
- **Map copy/paste**: Map context menu now supports Copy Map and Paste Map, creating a new `Map###.json`, updating `MapInfos.json`, and importing the source tileset database entry when possible.
- **Map deletion**: Map tree and quick-access selections can now be deleted from the context menu or Delete key, including child-map handling, `Map###.json` removal, `MapInfos.json` updates, current-map fallback loading, and protection against deleting the final remaining map.
- **Save Map As Image**: Map context menu can export a full-size PNG render of a map, including tiles, parallax, shadows, and A1 autotile overlap handling while omitting editor-only event marker boxes.
- **Map audio preview controls**: Map Properties autoplay BGM/BGS sections now include Play, Pause, Stop, and status controls powered by the shared AudioPlayer backend, honoring volume, pitch, pan, and loop settings.
- **Actor sheet cell selection**: Actor Character Sprite and Face Graphic pickers now use clickable highlighted sheet cells instead of prompting for numeric indexes.
- **Tileset database copy/paste**: Tilesets now support Ctrl+C/Ctrl+V in the Tilesets database list, pasting into the selected slot while preserving the slot ID.
- **Plugin Manager multi-select**: Plugins now support Shift-click range selection and Ctrl/Cmd-click toggle selection, with group copy, cut, paste, duplicate, and remove actions.
- **Project manager tests**: Added a `node:test` test harness covering version metadata sync, template metadata, new project creation, and RPG Maker import metadata.
- **Trait action buttons**: All trait editors (Actors, Classes, Weapons, Armors, States, Enemies) now have visible Add, Edit, Copy, Paste, and Delete buttons below the traits table, no longer relies solely on right-click context menu discovery
- **Trait keyboard shortcuts**: When a trait row is selected, Ctrl+C (copy), Ctrl+X (cut), Ctrl+V (paste), Delete (remove), and Enter (edit) operate on the selected trait instead of the parent database entry
- **Image picker "Open in Folder" button**: When selecting face graphics, character sprites, or SV battlers, a new "Open in Folder" button next to "Select This Image" opens the file in the system file manager (e.g. Dolphin) for quick access to external editing tools
- **Editor Distribution Builder** (`Build → Package Editor for Distribution...`): New in-editor tool for packaging RPG Reactor itself for release on itch.io / GitHub Releases. Uses the same worker_threads architecture as the game build system.
  - **3 package types**:
    - *Platform-Specific*: One archive per OS with bundled NW.js runtime (Linux → `.tar.gz`, Windows/macOS → `.zip`)
    - *Universal*: Single `.zip` with all 3 platform runtimes included
    - *Minimal*: Editor only, bootstrap launchers auto-download NW.js on first run
  - **NW.js edition selection**: Normal or SDK (includes DevTools)
  - **3-tier runtime acquisition**: Checks bundled local dirs (`nwjs-linux/`, `nwjs-win/`, `nwjs-mac/`) → `.nw-cache/` → downloads from `dl.nwjs.io`
  - **Partial download protection**: Uses `.part` suffix during download to prevent corrupt cached files
  - **SHA256SUMS.txt** generated for all output archives
  - **Whitelist-based staging**: Only includes editor files needed for distribution (`src/`, `css/`, `images/`, `libs/`, `build-scripts/`, player-facing `template/Demo/`, launcher scripts, docs, bundled `runtime/libs/pixi.js`); excludes dev artifacts (`.git/`, `node_modules/`, `save/`, template dev dirs like `REACTOR_CORE_DUMP_MIDDEV/`, `RMMZ_Corescript/`, `PIXI5/`, `PIXI8/`, `Backup/`, `Screenshots/`)
  - **Bootstrap launchers** (minimal packages): Platform-specific scripts that download + extract NW.js on first run (bash for Linux/macOS, batch + PowerShell for Windows)
  - Worker: `build-scripts/dist-editor-worker.js`, Manager: `src/DistEditorManager.js`
- **Custom icons for built executables**: Game and editor builds now embed custom icons instead of the default NW.js compass icon
  - **Windows**: Embeds icon into `.exe` via PE resource section editing; uses project's `icon/icon.ico` if available, otherwise generates ICO from `icon/icon.png`
  - **macOS**: Replaces `app.icns` in `.app` bundle with ICNS generated from PNG
  - **Linux**: No change needed (runtime `window.icon` already handles this)
  - Editor distribution builds use `images/icon.png` / `images/icon.ico`
  - Graceful fallback: builds succeed even if icon files are missing or replacement fails
  - Helper module: `build-scripts/icon-helpers.js` (pure CommonJS, no npm dependencies)
- **Enemy editor: Hue slider with live preview**: Battler Hue field is now a slider + number input combo; dragging the slider applies a real-time `hue-rotate` CSS filter to the battler preview image

### Changed
- **Version metadata source**: Project creation and RPG Maker import now use root `package.json` as the single source of truth for RPG Reactor engine version metadata.
- **Project templates**: New project creation now copies the player-facing `template/Demo` directly instead of copying the whole `template/` directory; Barebones and Complex remain development/testing templates.
- **Event audio command editor**: Play BGM/BGS/ME/SE command editing now defaults empty commands from the current loaded/playing AudioPlayer channel and uses a richer inline preview UI.
- **Map tileset editing**: Editing map dimensions or tileset from Map Properties now saves the map file and reloads the map when needed, with tileset dropdowns using actual tileset IDs.
- **Map edit undo transactions**: Tile editing now records undo entries only when map data actually changes, keeping undo/redo history cleaner and more reliable across pencil, shape, fill, eraser, shadow, and region operations.
- **Actor editor layout**: Redesigned to use space more efficiently - General Settings and Images display side-by-side in the top row, Traits and Equipment side-by-side in the middle row, with Note full-width at the bottom
- **Enemy editor layout**: Traits and Note sections now display side-by-side in a row (matching the actor editor layout) instead of stacked full-width; Note textarea stretches to fill available height

### Fixed
- **Database section shortcut isolation**: Database section switching now clears stale search bars, button bars, Change Maximum buttons, and keyboard handlers so functionality from one section no longer bleeds into another.
- **Global shortcut bleed**: Map/event global shortcuts no longer fire while database and editor modals are open, preventing database Ctrl+V from triggering event/map paste actions.
- **Tilesets Change Maximum**: Tilesets now has its own Change Maximum behavior instead of reusing a stale handler from Actors or the previously visited database section.
- **Enemy battler selection refresh**: Changing an enemy battler now refreshes the real database detail panel and highlights the current battler in the picker.
- **System 1 sound effect selection**: Sound rows now select reliably by clicking the whole row, support `(None)`, and double-click confirms immediately.
- **Control Self Switch display**: Event command list now displays self-switch command values correctly (`0 = ON`, `1 = OFF`).
- **Map rendering after tileset changes**: Tilemap loading clears stale tileset textures and falls back to the first available tileset when a referenced tileset is missing.
- **Map data persistence**: Map property edits now write existing map changes to `Map###.json` and save `MapInfos.json`.
- **Map editor tool state**: Returning from event mode now updates the real active tool to pencil instead of only changing the toolbar highlight, preventing the Single Tile button from accidentally continuing to use Fill.
- **Map editor input handling**: Right-click, middle-click, Shift-pan, and out-of-bounds clicks no longer create map edits or stale undo entries; fill operations also cleanly reset drawing state and resume lazy loading.
- **Current-map parallax refresh**: Saving Map Properties with a changed parallax now immediately clears/replaces the visible parallax layer on the loaded map.
- **New-game map references after deletion**: Deleting maps now repairs invalid player and vehicle start-map references in `System.json`, and playtest validates/repairs those references before launch.
- **Tilesets list refresh**: Saving a Tilesets database name now immediately refreshes and reselects the visible Tilesets list entry.
- **Stale plugin entries**: Plugin Manager now detects and removes stale missing-plugin entries, including old `A1_AutotileMapper` references.

## [0.7.0] - 2026-02-15

### Added
- **Database list clipboard operations**: Right-click context menu on list entries with Copy, Cut, Paste, and Duplicate, matches RPG Maker behavior (cut blanks slot, paste overwrites selected entry)
- **Database list keyboard shortcuts**: Ctrl+C (copy), Ctrl+X (cut), Ctrl+V (paste), Ctrl+D (duplicate), Delete (blank entry), Ctrl+Z (undo)
- **Database list undo system**: Ctrl+Z reverts destructive list operations (cut, paste, duplicate, delete, new, remove) with full snapshot/restore
- **Troop editor: Replace Enemy picker**: Visual modal with search bar, scrollable list with battler thumbnails, large preview panel with stats, and double-click to confirm
- **Troop editor: Visibility toggle**: Replaced hidden checkbox with eyeball icon button (open/slashed eye) for clearer member visibility control
- **Enemy editor: Battler preview**: Visual preview of the enemy battler image in the General section, with automatic charset frame extraction for `!`/`$` prefix filenames (standard 12x8 and big character 3x4 spritesheets)
- **Enemy editor: Multi-directory battler selection**: Battler image picker now searches `enemies`, `sv_enemies`, and `characters` directories

### Changed
- **Enemy editor layout**: General, Parameters, and Drop Items sections now display in a 3-column row for better use of horizontal space; Action Patterns, Traits, and Note remain full-width below

## [0.6.0] - 2026-02-14

### Added
- **Common Events editor**: Full editor with name, trigger (None/Autorun/Parallel), switch selection, and interactive command list with EventCommandPicker integration
- **Effects editor**: New DatabaseEffectEditor modal for RMMZ effects (Recovery, State, Buff/Debuff, Special) used by Skills and Items editors
- **Add/Delete entries**: New/Delete buttons in the database list panel for all data types with per-type default templates

### Changed
- **Skills editor**: Full rewrite, all fields now editable including skill type, scope, occasion, MP/TP costs, speed, success rate, repeats, hit type, animation, messages, damage formula section (type, element, formula, variance, critical), and interactive effects CRUD with context menu
- **Items editor**: Full rewrite, all fields now editable including item type, price, consumable toggle, scope, occasion, invocation parameters, damage formula section, and interactive effects CRUD with context menu
- **Enemies editor**: Full rewrite, editable general stats (name, battler image picker, hue, EXP, gold), 8 editable parameter inputs, 3 drop item slots with dynamic Kind/DataId/Denominator controls, action pattern table with add/edit/delete modal, and full trait CRUD via context menu
- **Armors editor**: Full rewrite to WeaponEditor parity, armor type and equip type dropdowns from system data, editable price, 8 editable parameter inputs, and full trait CRUD with context menu (Add/Edit/Cut/Copy/Paste/Delete)

## [0.5.0] - 2026-02-14

### Added
- 53 new event command editor UIs, bringing total from 29 to 82 supported commands
- **Flow Control & Messages**: Input Number (103), Select Item (104), Show Scrolling Text (105), Common Event (117), Label (118), Jump to Label (119), Play Movie (261), Name Input Processing (303)
- **Toggle Commands**: Change Save Access (134), Change Menu Access (135), Change Encounter (136), Change Formation Access (137), Change Player Followers (216), Change Map Name Display (281), all use shared ToggleCommandEditor
- **Actor Commands**: Change HP (311), Change MP (312), Change TP (326), Change State (313), Recover All (314), Change EXP (315), Change Level (316), Change Parameter (317), Change Skill (318), Change Equipment (319), Change Name (320), Change Class (321), Change Nickname (324), Change Profile (325)
- **Screen Effects**: Tint Screen (223), Flash Screen (224), Shake Screen (225), Move Picture (232), Rotate Picture (233), Tint Picture (234), Set Weather Effect (236)
- **System & Map**: Change Battle BGM (132), Change Victory ME (133), Change Window Color (138), Change Defeat ME (139), Change Vehicle BGM (140), Set Vehicle Location (202), Change Tileset (282), Change Battle Background (283), Change Parallax (284), Get Location Info (285)
- **Scene Commands**: Battle Processing (301), Shop Processing (302), Change Actor Images (322), Change Vehicle Image (323)
- **Battle Commands**: Change Enemy HP (331), Change Enemy MP (332), Change Enemy TP (342), Change Enemy State (333), Enemy Recover All (334), Enemy Appear (335), Enemy Transform (336), Show Battle Animation (337), Force Action (339)
- **No-Editor Commands** (direct insert): Exit Event Processing (115), Get on/off Vehicle (206), Erase Event (214), Gather Followers (217), Save BGM (243), Replay BGM (244), Abort Battle (340), Open Menu Screen (351), Open Save Screen (352), Game Over (353), Return to Title Screen (354)
- Display text in event list for all 69 new command codes
- Continuation line support for Show Scrolling Text (405) and Shop Processing (605)

### Fixed
- Corrected command codes 131-140 to match RMMZ standard: Save Access (134), Menu Access (135), Encounter (136), Formation (137), Window Color (138), Defeat ME (139), Vehicle BGM (140)
- Added missing Show Animation (212) to command names map
- Added display text for Show Choices (102), Control Timer (124), Change Transparency (211), Show Animation (212), Show Picture (231), Erase Picture (235)
- Added Battle Processing branch display: If Win (601), If Escape (602), If Lose (603), End (604)

## [0.4.5] - 2026-02-14

### Changed
- Version bumped to 0.4.5 to reflect engine completeness

### Added
- Linux launcher script with desktop integration (`RPGReactor.sh`)
- Desktop entry and icon auto-installation on first run
- Set Movement Route editor: full two-panel editor with 45-command button grid, inline parameter dialogs, multi-select, cut/copy/paste, reorder
- Orphaned 505 (movement route) command repair on load
- Audio player: SVG transport buttons, hover tooltips, yellowjacket theme

### Fixed
- Made NW.js binaries executable for Linux
- Movement route display: human-readable command text instead of raw JSON
- Movement route 505 continuation entries properly managed on insert/edit
- Character ID 0 ("This Event") no longer incorrectly mapped to Player
- End markers no longer displayed as visible 505 entries

## [0.1.0] - 2024-11-18

### Added

#### Core Application
- NW.js-based cross-platform application framework
- Dark theme UI optimized for long editing sessions
- Resizable sidebar panels with size persistence
- Welcome screen with project creation and opening

#### Map Editor
- Pencil, rectangle, circle area, fill bucket, and eraser tools
- Four-layer tile system with auto-layer mode
- Shadow pen mode for depth effects
- Undo/redo system (50 step history)
- Autotile support with animated water and waterfall tiles
- Region painting with 256 distinct regions
- Viewport culling and lazy-loading for performance

#### Event System
- Visual event editor with multi-page support
- Page-based conditional system (switches, variables, items, actors)
- Event commands for game logic
- Event copy/paste/cut operations
- Find/search functionality across project
- Drag-and-drop event repositioning
- Starting position markers

#### Database Editors
- Actor editor with stats, equipment, and traits
- Class editor with parameter curves and skill learning
- Skill editor with damage formulas and effects
- Item editor with consumable effects
- Weapon editor with parameters and traits
- Armor editor with resistances and bonuses
- Enemy editor with AI patterns and drops
- Troop editor for enemy group composition
- State editor for status effects
- Animation editor with Effekseer support
- Tileset editor with passage and terrain configuration
- System editor for game-wide settings

#### Audio System
- Multi-channel audio player (BGM, BGS, ME, SE)
- Volume, pitch, and pan controls
- Seek slider with real-time updates
- Loop configuration

#### Plugin System
- Plugin discovery and management
- Enable/disable toggle
- Load order configuration
- Parameter editing interface

#### Playtest
- One-click game testing
- Debug mode support
- Process management

#### Build System
- Cross-platform game builds (Windows, macOS, Linux, Web)
- NW.js runtime packaging via worker_threads (bundled or downloaded)
- Asset bundling into `package.nw`

#### Templates
- Demo template used for new projects created by players
- Development templates retained for local engine testing
- Project structure compatible with RPG Maker MZ format
